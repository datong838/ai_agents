# M2-2b — 元宝技能：媒体图片描述（替代 Qwen2-VL CPU）

> **阶段**：MVP M2-2（知识网增强层）**v2 定稿方向**  
> **替代**：[M2-2-VLM图片描述-本地化部署与使用.md](M2-2-VLM图片描述-本地化部署与使用.md) §零 已暂停的 **Qwen2-VL-2B GPTQ Int4 CPU** 方案  
> **执行端**：**谛听客户端 · 技能集**（本机已安装 **腾讯元宝桌面客户端**，用户保持登录）  
> **原则**：与 M2-2 相同——**增量 UPDATE** 媒体节点；**不**重跑全量 sync；**禁止**接入 `POST /api/analyze` 实时链路  

---

## 一、需求对齐（我们要的两类输出）

与 [知识库-结构化入库方案.md](知识库-结构化入库方案.md) §6、M2-2 §2.3 一致，每张商品/知识库图片需要补全：

| 输出 | 写入字段 | 用途 |
| --- | --- | --- |
| **图片识别后的场景描述** | `knowledge_media_nodes.vlm_description` | 写入 Chroma `knowledge_media_{hash}` 的「场景描述：…」段，供 BGE 检索 |
| **该图适用的上下文 / 问答场景** | `knowledge_media_nodes.use_cases`（可选 `ocr_text`） | 标注「适合回答哪类用户问题」；图内大字可写入 `ocr_text` |

**Chroma chunk 模板**（入库后形态不变）：

```text
【全局媒体】图片
图片地址：https://yanpanji.com/upload/...
出现于：399白钻逆龄双效王炸套装、…
上下文摘录：
- （sync 时从详情 HTML 摘录的 before/after）
场景描述：{vlm_description}
适用场景：{use_cases}
```

> **边界**：微信聊天截图 OCR 仍走客户端 PaddleOCR；**本文只处理知识网 `media_node`（商城 CDN 图 / 指定本地目录）**。

---

## 二、为何改用元宝技能（相对 2B CPU）

| 对比项 | Qwen2-VL-2B Int4 CPU（已停用） | 元宝技能（本方案） |
| --- | --- | --- |
| 单图耗时 | **15–40 分钟** | 目标 **30 s–3 min**（视网络与模型） |
| 识图质量 | 2B 偏弱，实验噪声大 | 云端多模态，**商品图/包装/成分表**可读性更好 |
| 部署 | 服务端 2GB CVM + 复杂 Python 依赖 | **用户本机已装元宝**，零 GPU |
| 合规 | 图不出域 | 图经元宝云端；**仅批处理、不上 analyze**；需用户知悉 |

原 M2-2 服务端夜间 Cron **不适用**本路径；改为 **客户端技能集手动/半自动批跑**（可排队，不占用 SalesAgent API 进程）。

---

## 三、技能流程（定稿）

```text
┌─────────────────────────────────────────────────────────────────┐
│ 技能集 · 媒体图描述（元宝）                                      │
└─────────────────────────────────────────────────────────────────┘
  ① 准备图片
       ├─ A) 从知识网拉取：category=beauty & vlm_description IS NULL
       │      → HTTP 下载 CDN → 临时目录
       └─ B) 用户指定本地目录（开发/抽检）

  ② 附着元宝桌面客户端（用户须已打开并登录）

  ③ 点击「新建对话」（侧栏 speech+ 图标 /  tooltip「新建对话」）

  ④ 上传本张图片到当前对话（输入框旁「+」/ 粘贴 / 拖拽）

  ⑤ 发送定稿 Prompt（§四），等待元宝生成完毕

  ⑥ 读取回复全文
       ├─ 优先：UI Automation / 无障碍树 / 剪贴板复制
       └─ 兜底：对回复区域 OCR（Windows OCR / PaddleOCR）

  ⑦ 解析结构化结果 → 调用 SalesAgent 入库 API（§五）
       → UPDATE vlm_description / use_cases / ocr_text
       → 单条 BGE embed + Chroma upsert

  ⑧ 删除本次元宝对话（侧栏该会话 → 删除），避免历史堆积

  ⑨ 下一张；或结束并展示统计（成功/失败/跳过）
```

**单张时序（人工辅助 MVP）**：可先实现 ①→⑤ 半自动（用户确认发送），⑥–⑨ 全自动。

---

## 四、Prompt 定稿（发给元宝）

每次 **新建对话、仅一张图**。将下面整段作为用户消息（ `{context_excerpt}` 来自知识网 `appears_in` 边与 HTML 上下文）：

```text
你是美妆电商知识库助手。请根据【图片】和【上下文】输出结构化结论。
要求：
1）只描述图中可见内容，不要编造；
2）必须按下面三个标题输出，便于程序解析；
3）总字数不超过 250 字。

【上下文】
{context_excerpt}

【输出格式】
## 场景描述
（产品/包装/成分表/步骤/对比图等，中文）

## 适用场景
（适合回答哪类用户问题，如功效、成分、用法、规格、对比；多条用顿号分隔）

## 图内可见文字
（OCR 摘要；无则写「无」）
```

**解析规则**（入库前）：

| Markdown 段 | 映射字段 |
| --- | --- |
| `## 场景描述` 下正文 | `vlm_description` |
| `## 适用场景` 下正文 | `use_cases` |
| `## 图内可见文字` 下正文 | `ocr_text`（若为「无」则 NULL） |

---

## 五、入库接口（复用 M2-2 数据层）

SalesAgent 侧 **不重跑 Qwen**，只暴露「写结果」能力（实现时可二选一）：

**方案 A（推荐）**：新增 HTTP API

```http
POST /api/knowledge/media/{media_node_id}/vlm_result
Content-Type: application/json

{
  "category": "beauty",
  "vlm_description": "...",
  "use_cases": "...",
  "ocr_text": "...",
  "source": "yuanbao_skill_v1"
}
```

服务端逻辑复用 `knowledge/vlm_enrich.py` 中「更新 SQLite + 重建 chunk + embed upsert」片段，**跳过**下载与 VLM 推理。

**方案 B**：客户端直连 SQLite（仅本地开发）

- 写 `C:/work/salesagent/data/salesagent.db` 的 `knowledge_media_nodes`  
- 再调现有 CLI：`python -m com.yanpanji.agents.cli vlm_embed_one --media-node-id ...`（待建）

**幂等**：若 `vlm_description` 已非空且非 force，跳过。

---

## 六、图片准备

### 6.1 从知识网拉取（默认）

```sql
SELECT media_node_id, media_url, url_hash
FROM knowledge_media_nodes
WHERE category = 'beauty'
  AND media_type = 'image'
  AND (vlm_description IS NULL OR vlm_description = '')
ORDER BY updated_at ASC
LIMIT :batch_size;
```

- 下载：`User-Agent: SalesAgent-VLM/1.0`，`Referer: https://yanpanji.com/`  
- 临时目录：`{DITING_DATA_ROOT}/_staging/vlm_yuanbao/`  
- 可选：JOIN `appears_in` 取商品标题拼 `context_excerpt`

### 6.2 本地目录（开发抽检）

- 用户选择文件夹；每张图 **不关联** `media_node_id` 时仅本地预览，不入库  
- 关联方式：CSV 两列 `filename,media_node_id` 或 UI 下拉选择待补节点

---

## 七、元宝桌面自动化要点

> 参考 UI：侧栏 **「新建对话」**（speech bubble +）；主区输入 **「和元宝说点什么」**；附件 **「+」** 上传图片。

| 步骤 | 自动化手段 | 备注 |
| --- | --- | --- |
| 附着窗口 | **Windows UIA**（`pywinauto` / FlaUI）或 **Electron CDP**（若桌面版暴露调试端口） | MVP 可先 **坐标 + 图像模板**（PyAutoGUI），脆弱但快 |
| 新建对话 | 点击侧栏新建图标；等待输入框 placeholder 出现 | 见用户截图 tooltip「新建对话」 |
| 上传图片 | 点击 `+` → 系统文件对话框 → 填入临时路径 | 或剪贴板粘贴图片 |
| 发送 Prompt | 焦点在输入框 → 粘贴 §四 文本 → Enter / 点击发送 | |
| 等待完成 | 检测「停止生成」消失 / 回复区高度稳定 N 秒 | 超时 120s 记失败 |
| 读取回复 | **Ctrl+A → Ctrl+C** 于回复区，或 UIA `Document` 控件 | OCR 仅作兜底 |
| 删除对话 | 侧栏当前会话右键删除 / 垃圾桶图标 | **必须**执行，避免污染侧栏 |

**前提**：

- 用户 **手动打开** 元宝桌面客户端并保持登录（与 [产品设计文档](产品设计文档-谛听.md) §5.5.4 主动学习一致：**不**自动帮用户登录）。  
- 技能运行期间 **勿**操作键鼠（或提示「自动化进行中」）。

**与网页版关系**：若桌面版自动化困难，可降级为附着 **Chrome 已打开的 [yuanbao.tencent.com](https://yuanbao.tencent.com)**（见 [07-腾讯混元与元宝网页版.md](环境准备/07-腾讯混元与元宝网页版.md)、`ditingclient/config/active_learning.json#yuanbao_web`）。**优先桌面版**（用户现状）。

---

## 八、技能集产品入口（规划）

| 项 | 说明 |
| --- | --- |
| 路由 | `/skills/media-yuanbao`（谛听 Renderer） |
| 技能集卡片 | `SkillSetPage` 新增 **「媒体图描述（元宝）」**，icon 🟢 |
| 页面能力 | 选择来源（知识网 / 本地目录）、batch 数量、开始/暂停、日志、成功数 |
| 前置检查 | 元宝进程是否存在、是否登录（启发式：主窗口标题含「元宝」） |

**禁止**：

- 接入 `POST /api/analyze`  
- 服务端 SalesAgent 内嵌 Playwright 调元宝（必须在 **用户本机**）

---

## 九、配置草案（`ditingclient/config/yuanbao_media_skill.json`）

```json
{
  "enabled": true,
  "yuanbao_desktop": {
    "window_title_contains": "元宝",
    "new_chat_tooltip": "新建对话",
    "input_placeholder": "和元宝说点什么",
    "reply_stable_sec": 3,
    "generation_timeout_sec": 120
  },
  "image_source": {
    "default_category": "beauty",
    "batch_size": 5,
    "download_referer": "https://yanpanji.com/"
  },
  "salesagent": {
    "api_base": "http://127.0.0.1:8765/api",
    "ingest_endpoint": "/knowledge/media/{media_node_id}/vlm_result"
  },
  "prompt_template": "beauty_product_yuanbao_v1",
  "delete_chat_after_each": true
}
```

`server.json` 中 **`vlm.enabled` / `vlm_enrich.enabled` 保持 `false`**；本技能走 **`source: yuanbao_skill_v1`** 独立开关。

---

## 十、验收标准

| # | 检查 | 期望 |
| --- | --- | --- |
| 1 | 单张 beauty 商品主图 | 元宝返回含「场景描述」；入库后 SQL `vlm_description` 非空 |
| 2 | chunk 文本 | 随机 `knowledge_media_{hash}` document 含「场景描述：」 |
| 3 | 耗时 | 单张 **< 5 min**（P95，含人工等待网络） |
| 4 | 幂等 | 同一 `url_hash` 二次运行 skip |
| 5 | 侧栏 | 每轮结束后对话已删除 |
| 6 | analyze | 图检索问题命中媒体 chunk 时上下文含新描述（`verify_m21` / 技能集检索抽测） |

---

## 十一、实现清单（开发按此拆任务）

```text
ditingclient/
├── config/yuanbao_media_skill.json
├── src/.../skills/yuanbao_media/
│   ├── yuanbao_desktop_driver.py    # UIA/模板点击：新建对话、上传、发送、删会话
│   ├── reply_reader.py              # 复制/ UI A / OCR 兜底
│   ├── prompt_builder.py            # context_excerpt + §四 模板
│   ├── result_parser.py             # 解析 ## 场景描述 等
│   └── knowledge_push.py            # POST vlm_result 或写 SQLite
├── renderer/pages/MediaYuanbaoSkillPage.vue
└── electron/.../yuanbao_media_handler.ts   # IPC

salesagent/
├── api/routes/knowledge.py          # POST .../vlm_result（新增）
└── knowledge/vlm_enrich.py          # 抽出 apply_media_vlm_result() 复用

scripts/
└── verify_m22_yuanbao.py            # 覆盖率 + 抽测 chunk
```

**MVP 最小路径**（1–2 天可演示）：

1. 手动选 1 张图 + 复制 Prompt 到元宝 → 脚本只负责 **解析剪贴板 + 调 API 入库**  
2. 再补全自动点击与删会话  

---

## 十二、与现有文档关系

| 文档 | 关系 |
| --- | --- |
| [M2-2-VLM…](M2-2-VLM图片描述-本地化部署与使用.md) | §零 2B CPU **历史**；数据模型与 chunk 模板 **仍以 M2-2 为准** |
| [知识库-结构化入库方案.md](知识库-结构化入库方案.md) §13.4 | M2-2 实施路径改为 **本文** |
| [07-腾讯混元与元宝网页版.md](环境准备/07-腾讯混元与元宝网页版.md) | 网页附着/CDP **降级方案** |
| [技术方案-谛听客户端.md](技术方案-谛听客户端.md) §6.10 | 元宝驱动思路可复用 `yuanbao_driver` 滚动读全文 |

---

## 十三、变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-06-27 | 初稿：元宝桌面技能替代 Qwen2-VL 2B CPU；定稿 Prompt + 入库 + 验收 |
