# M2-2 — 媒体图片描述：元宝桌面技能方案（v2）

> **阶段**：MVP M2-2（知识网增强层）  
> **状态**：**当前推荐方案**（替代已暂停的 Qwen2-VL-2B CPU Int4，见 [M2-2-VLM…§零](M2-2-VLM图片描述-本地化部署与使用.md)）  
> **载体**：谛听客户端 **技能集** →「元宝识图补描述」  
> **原则**：增量 UPDATE `vlm_description` / `use_cases`；**不**清空重同步知识网  
> **关联**：[知识库-结构化入库方案.md](知识库-结构化入库方案.md) §6.3、§13.4；[技术方案-谛听客户端.md](技术方案-谛听客户端.md) §6.10.2

---

## 一、要解决的问题

知识网中每个 **全局媒体节点**（`knowledge_media_{hash}`）需要两类文本，供 BGE 检索与技能集选图：

| 输出 | 写入字段 | 含义 |
| --- | --- | --- |
| **场景描述** | `knowledge_media_nodes.vlm_description` | 图里有什么：产品/包装/成分表/步骤/可见文字等 |
| **适用场景** | `knowledge_media_nodes.use_cases` | 这张图适合回答哪类用户问题（功效、成分、用法、对比…） |

二者一并写入 Chroma 媒体 chunk 模板（§五），形成「场景描述：… / 适用场景：…」段落。

**为何不用 Qwen2-VL-2B CPU Int4**：单图 15–40 分钟、识图弱，已停用（见旧 Runbook §零）。

**为何用本机元宝桌面版**：用户已安装并登录；识图能力显著强于 2B；单张通常 **数十秒级**（人工可接受）；**商品图仅在本机对话中处理**，不进入 `POST /api/analyze` 实时链。

---

## 二、方案总览

```text
┌─────────────────────────────────────────────────────────────────┐
│  技能集 · 元宝识图补描述（本机 Windows）                          │
├─────────────────────────────────────────────────────────────────┤
│  ① 准备图片  ← 知识网待补节点下载 / 本地目录批量                    │
│  ② 打开元宝  ← 附着已运行桌面客户端（用户保持登录）                  │
│  ③ 新建对话  ← 点击侧栏「新建对话」                                │
│  ④ 上传图片  ← 输入区「+」或粘贴/拖拽                                │
│  ⑤ 发送提示词 ← §四 固定模板 + 上下文摘录                          │
│  ⑥ 等待回复  ← 轮询 DOM / UIA 直至输出稳定                         │
│  ⑦ 读取文字  ← 复制回复区全文（非聊天截图 OCR；必要时 UIA GetText）   │
│  ⑧ 解析入库  ← JSON → SQLite + BGE upsert 单条 chunk               │
│  ⑨ 删除对话  ← 侧栏右键删除本次会话，避免污染历史                    │
└─────────────────────────────────────────────────────────────────┘
```

**与现有边界对齐**（产品定稿）：

| 链路 | 元宝 | Qwen2-VL 服务端 |
| --- | --- | --- |
| `POST /api/analyze` 需求雷达 | ❌ 禁止 | ❌ 禁止（M2-2 范围外） |
| 主动学习（链接+群聊） | ✅ 已有设计 §6.10 | — |
| **M2-2 商品图补描述（本文）** | ✅ **本机技能** | ❌ 已暂停 |

---

## 三、技能入口与输入源

### 3.1 技能集卡片（规划）

| 项 | 值 |
| --- | --- |
| ID | `yuanbao-media-enrich` |
| 名称 | 元宝识图补描述 |
| 图标 | 🟢（元宝绿） |
| 状态 | 开发中 → 可用 |

### 3.2 图片来源（二选一或组合）

**A. 知识网待补队列（推荐）**

```sql
SELECT media_node_id, media_url, category
FROM knowledge_media_nodes
WHERE category = 'beauty'
  AND (vlm_description IS NULL OR vlm_description = '')
ORDER BY updated_at ASC
LIMIT :batch_size;
```

- 客户端经 IPC 调 SalesAgent API（或只读 SQLite）拉列表  
- 按 `media_url` HTTP GET 下载到 `%TEMP%/diting_yuanbao_media/{url_hash}.jpg`  
- Referer：`https://yanpanji.com/`（与 `server.json#niushop.image_cdn_base` 一致）

**B. 本地目录批量**

- 用户选择文件夹；每张图可选绑定已有 `media_node_id`，或仅生成临时预览（不入库模式用于试跑）

### 3.3 附加上下文（自动注入提示词）

对知识网节点，从 `knowledge_edges` 拉 `appears_in`：

- 关联商品/文章 **标题**  
- `context_before` / `context_after`（详情 HTML 摘录，最多 3 条）

无图内信息时仍发送图片，上下文填「（无）」。

---

## 四、提示词模板（定稿 v1）

技能每次 **新建对话** 后，上传 **单张** 图片并发送以下用户消息（占位符由程序填充）：

```text
你是美妆电商知识库助手。请根据【图片】和【页面上下文】输出，且只输出一个 JSON 对象，不要 markdown 代码块，不要其他解释。

要求：
1. vlm_description：中文，描述图中可见内容（产品、包装、成分表、步骤、对比等），200 字以内；不要编造图中没有的信息。
2. use_cases：中文，说明这张图适合回答哪类用户问题（如功效、成分、用法、规格、前后对比），80 字以内。

【页面上下文】
{context_excerpt}

【输出格式】
{"vlm_description":"...","use_cases":"..."}
```

**`context_excerpt` 示例**（程序拼接）：

```text
399白钻逆龄双效王炸套装：…上下文前… …上下文后…；栖月汇小程序文章《…》：…
```

**解析规则**：

- 优先 `json.loads` 整段回复  
- 失败则正则提取 `{...}` 再 parse  
- 仍失败：整段文本写入 `vlm_description`，`use_cases` 留空并标记 `parse_status=needs_review`

---

## 五、入库与 Chroma 更新

与 [知识库方案 §6.3](知识库-结构化入库方案.md) 对齐，**单节点增量**：

### 5.1 SQLite

```sql
UPDATE knowledge_media_nodes
SET vlm_description = ?,
    use_cases = ?,
    updated_at = ?
WHERE media_node_id = ?;
```

幂等：若 `vlm_description` 已非空且用户未勾选「覆盖」，则 skip。

### 5.2 Chroma 媒体 chunk 文本

```text
【全局媒体】图片
图片地址：https://yanpanji.com/upload/...
出现于：399白钻逆龄双效王炸套装、…
上下文摘录：
- …
场景描述：{vlm_description}
适用场景：{use_cases}
```

- `chunk_id`：`knowledge_media_{url_hash}`（不变）  
- **仅 upsert 该 id**：BGE embed → Chroma；**不**改 `knowledge_edges`  
- 可选：写 `knowledge_chunks.preview` 前 200 字

### 5.3 写入 API（规划）

```http
POST /api/knowledge/media/{media_node_id}/vlm
Content-Type: application/json

{
  "category": "beauty",
  "vlm_description": "...",
  "use_cases": "...",
  "source": "yuanbao_desktop_skill_v1"
}
```

服务端：UPDATE SQLite + embed upsert（与旧 `vlm_enrich` 共用 `build_media_chunk_document` 逻辑）。

---

## 六、元宝桌面自动化步骤（UI 操作序）

> 界面以 **腾讯元宝 Windows 桌面版** 为准（侧栏：搜索、分组、聊天列表；主区：输入框「和元宝说点什么」、**+** 上传、发送箭头）。

| 步骤 | 用户操作 | 自动化要点 |
| --- | --- | --- |
| 1 | 确保元宝已启动且已登录 | 附着进程/窗口标题含「元宝」；未登录则技能中止并提示 |
| 2 | 点击 **新建对话**（侧栏气泡+） | UIA `Name=新建对话` 或 Tooltip；**每张图必须新会话**，避免上下文串图 |
| 3 | 点击输入区 **+** → 选择图片文件 | `SetValue` 文件路径或模拟文件对话框；支持 jpg/png/webp |
| 4 | 在输入框粘贴 §四 提示词 | `ValuePattern` / 剪贴板粘贴 |
| 5 | 点击 **发送**（上箭头） | 等待「生成中」结束 |
| 6 | 读取 **助手最后一条回复** 全文 | UIA 遍历回复气泡 / `innerText`；必要时向下滚动至稳定 |
| 7 | 解析 JSON → 调 §5.3 API 入库 | 失败写日志，继续下一张或重试 1 次 |
| 8 | 在侧栏 **删除本次对话** | 右键删除或 hover 删除；**必做**，避免历史列表膨胀与误点 |

**读取文字说明**：优先 **DOM/UIA 取 selectable text**；仅在元宝不可访问文本层时，才对回复区截图做 OCR（Windows OCR / 现有 Paddle 子进程），精度次于直接取文。

**速率建议**：串行 **1 张 / 会话**；批处理间隔 ≥3s，避免触发频控；默认每批 **10–20 张**，用户可停。

---

## 七、配置（规划 `ditingclient/config/yuanbao_media_skill.json`）

```json
{
  "enabled": true,
  "yuanbao_desktop": {
    "window_title_contains": "元宝",
    "new_chat_tooltip": "新建对话",
    "input_placeholder_hint": "和元宝说点什么",
    "reply_stable_ms": 2000,
    "max_wait_reply_sec": 120
  },
  "prompt_template": "beauty_product_json_v1",
  "batch": {
    "default_limit": 10,
    "skip_if_vlm_exists": true,
    "delete_chat_after_each": true
  },
  "download": {
    "referer": "https://yanpanji.com/",
    "timeout_sec": 30
  },
  "salesagent": {
    "api_base": "http://127.0.0.1:8765/api",
    "write_endpoint": "/knowledge/media/{media_node_id}/vlm"
  }
}
```

`server.json` 侧保持：

```json
"vlm": { "enabled": false, "provider": "yuanbao_desktop_skill" },
"knowledge_graph": { "vlm_enrich": { "enabled": false } }
```

Qwen 本地模型块保留但 **不启用**。

---

## 八、实现清单（开发边界）

```text
ditingclient/
├── config/yuanbao_media_skill.json
├── src/.../skills/yuanbao_media/
│   ├── yuanbao_desktop_driver.py    # UIA：新建对话、上传、发送、读回复、删对话
│   ├── media_fetch.py               # 知识网队列 + CDN 下载
│   ├── prompt_builder.py            # §四 模板 + context_excerpt
│   ├── result_parser.py             # JSON 解析与兜底
│   └── enrich_orchestrator.py       # 单张 / 批量主流程
├── renderer/pages/YuanbaoMediaSkillPage.vue
└── electron/.../yuanbao_media_handler.ts   # IPC

salesagent/
├── api/routes/knowledge.py          # POST .../media/{id}/vlm（新增）
└── knowledge/vlm_enrich.py          # 复用 upsert_media_chunk / build_media_chunk_document
```

**复用**：主动学习已有 `yuanbao_driver` 思路（[技术方案 §6.10.2](技术方案-谛听客户端.md)），M2-2 技能 **独立模块**，不混入 analyze / 主动学习队列。

---

## 九、验收标准

| # | 检查 | 期望 |
| --- | --- | --- |
| 1 | 单张知识网图片端到端 | ≤2 min（含元宝生成）；SQLite `vlm_description` 非空 |
| 2 | `use_cases` | 非空或明确 JSON 字段存在 |
| 3 | Chroma | 对应 `knowledge_media_{hash}` document 含「场景描述：」「适用场景：」 |
| 4 | 边表 | `knowledge_edges` 计数 **不变** |
| 5 | 对话清理 | 批处理后侧栏无残留测试会话 |
| 6 | 幂等 | 同 `url_hash` 二次运行 skip（除非强制覆盖） |
| 7 | 技能集检索 | [知识关系图检索](KnowledgeGraphSkillPage) 命中媒体 chunk 时可见新描述 |

**SQL 自查**：

```sql
SELECT COUNT(*) AS total,
       SUM(CASE WHEN vlm_description IS NOT NULL AND vlm_description != '' THEN 1 ELSE 0 END) AS with_vlm
FROM knowledge_media_nodes WHERE category = 'beauty';
```

---

## 十、风险与约束

| 风险 | 缓解 |
| --- | --- |
| 元宝 UI 升级导致选择器失效 | 配置化 tooltip/placeholder；版本号写入日志；人工兜底单张 |
| 回复非 JSON | §四 解析兜底 + `needs_review` 队列 |
| 商品图出本机 | 仅桌面客户端；不上传 analyze；可选企业协议评估 |
| 批量误删对话 | **仅删本技能创建的会话**（记录会话标题前缀 `[M2-2]`） |
| 与主动学习冲突 | 队列隔离；同一时刻仅一个元宝自动化任务 |

---

## 十一、文档索引

| 文档 | 关系 |
| --- | --- |
| [M2-2-VLM…§零](M2-2-VLM图片描述-本地化部署与使用.md) | 旧 Qwen2B 方案（已暂停） |
| [知识库方案 §13.4](知识库-结构化入库方案.md) | 增量增强原则 |
| [07-腾讯混元与元宝](环境准备/07-腾讯混元与元宝网页版.md) | 元宝边界（analyze 禁止） |
| [scripts/dev-local.md](../scripts/dev-local.md) | 本地启动；M2-2 v2 待补命令 |

---

## 十二、变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-06-27 | 初稿：元宝桌面技能替代 Qwen2-VL-2B；提示词、入库、UI 步骤、实现边界 |
