# M3-1 — 元宝文档挂接知识网（批量文档学习技能）

> **阶段**：MVP M3–M5（知识库投喂 + 知识网挂接）  
> **状态**：**首版已开发**（2026-06-29）；知识库页为界面入口，**技能集 `/skills/yuanbao-doc-graph` 为同一能力的实现与进度主界面**。
> **类目**：商业大类（如 `beauty` / 四季换肤·美妆）及未来可扩展类目；**非** `ai_agent` 技术群主动学习（§6.10 / PRD §5.5.4）  
> **前置**：基本盘成网（`appears_in` 可视 + M2-3 存量 `mentions` 等，入库方案 §14.10 L2-a/b）  
> **关联**：[产品设计文档-谛听.md](产品设计文档-谛听.md) §5.7.7；[知识库-结构化入库方案.md](知识库-结构化入库方案.md) §4、§13.4、§十四；[开发计划-总览.md](开发计划-总览.md) §14.4；[技术方案-谛听客户端.md](技术方案-谛听客户端.md) §6.11；[M2-2-媒体图片描述-元宝技能方案.md](M2-2-媒体图片描述-元宝技能方案.md)

---

## 一、要解决的问题

M1.6 知识网已有 Niushop **商品树**（`goods_*`）、**文章树**、**全局媒体节点**与 `contains` / `appears_in` 边。

用户在本机积累的多份资料（PDF / Word / Excel / Markdown 等）需要：

1. **先集中存放**，支持多批次上传，**不立即入库**；
2. 用户确认全部传完后，**一次触发**「开始学习」；
3. 由 **本机元宝** 读懂每份文档，并**显式指出**与已有商品/文章/概念/图片的关联；
4. 服务端据此 **新建 `upload_*` 文档子树**，并 **写入 `related_product` / `mentions` / `references` 等边**，与原有知识网汇合。

本文技能 **就是** M3–M5 上传链路里「用户资料 ↔ 已有知识树挂接」的产品化实现，替代原规划中「服务端零样本 SPO 抽边」（M2-3）在**用户上传场景**下的主路径。

---

## 二、与相关能力边界

| 能力 | 产品入口 | 输入 | 元宝用途 | 服务端 | 类目 |
|------|----------|------|----------|--------|------|
| **M2-2 元宝识图** | 技能集 | 知识网待补 **图片 URL** | 识图 → `vlm_description` | UPDATE 媒体节点 | beauty 等 |
| **PRD §5.7.7 知识库页** | 侧栏「知识库」四 Tab | 见 §二·1 | — | Level 1/2 预览 + job 日志 | beauty + ai_agent |
| **§6.10 / §5.5.4 主动学习** | 知识库 Tab「主动学习」 | 技术群 **链接** + 聊天上下文 | 综合成稿 | ingest → `ai_agent` | **ai_agent** · 方案 [M5-1](M5-1-主动学习链接抓取与元宝综合方案.md) |
| **本文 M3-S1** | **知识库页** `[添加资料]` + `[▶ 开始学习]`；技能集同名卡片 | 用户 **本地文档批次** | 读文档 + **挂接已有商品树** | ingest + **写图边** | **beauty 等商业大类** |
| **M2-3（规划）** | 服务端夜间/批任务 | 已有 Niushop **section** 文本 | 可选 DeepSeek SPO | 自动抽 `concept`/`mentions` | 全库冷启动 · **P1 基本盘** |

**硬隔离（与 M2-2 / §6.10 一致）**：

- ❌ 禁止 `POST /api/analyze`、禁止服务端 DeepSeek 读用户文档正文  
- ✅ 元宝仅在用户本机运行；服务端只做 **BGE 召回校验 + 图写入 + 向量化 ingest**

### 2.1 与 PRD §5.7.7 四 Tab 对应（定稿）

| PRD Tab | M3-S1 在本 Tab 的内容 |
|---------|----------------------|
| **本地资料** | `_pending_learn/` 中 **待学习** 文件 + `raw/` 中 **已学习/失败** 归档；上传区 `[添加资料]` / `[▶ 开始学习]` |
| **知识图谱** | 学习成功后挂接边在图谱中可见（`related_product` / `mentions` 虚线）；`upload_{hash}` 文档子树 |
| **上传记录** | 每次「添加资料」批次、每次「开始学习」job 的日志：文件数、成功/失败、写入 `related_product`/`mentions` 边数 |
| **主动学习** | **不展示 / 置灰**（商业大类无技术群链接学习；见 PRD §5.5.4） |

> 原 PRD「已入库向量条目」Tab 已更名为 **「知识图谱」**；切片明细降为折叠 debug 区（M3-S2）。

**与 `ai_agent` 被动投喂区别**：`ai_agent` 在知识库页用 `[开始上传]` → `POST /api/ingest/upload`（`source=user_upload`），**无** pending 队列、**无**商品树挂接。

---

## 三、用户流程（产品）

### 3.1 阶段 A：累积上传（不处理）

用户在选定 **大类**（如 `beauty`，产品展示名可映射「四季换肤·美妆」）下，于 **知识库页**（或技能集快捷入口）：

1. 在技能页 **选择多个文件**（可多批、多次操作）；
2. 客户端复制到 Level 1 目录，并登记队列：

```text
{data_root}/{category}/raw/_pending_learn/
  batch_20260628_001/
    产品培训手册.pdf
    成分对照表.xlsx
  batch_20260628_002/
    四季换肤SOP.docx
```

3. SQLite / manifest 记录：`file_hash`、`batch_id`、`status=pending`、`uploaded_at`；
4. **此阶段不调元宝、不调 SalesAgent ingest**。

UI 展示（**本地资料 Tab**）：「待学习 N 个文件 · M 个批次」；**上传记录 Tab** 登记每批 `batch_id`。

### 3.2 阶段 B：开始学习（逐文件处理）

用户点击 **「开始学习」** 后，客户端 **串行**（与 M2-2 共用元宝自动化锁）处理每个 `pending` 文件：

```text
对每个 file:
  ① 元宝：新建对话
  ② 元宝：上传该文件（PDF/Word/…）
  ③ 元宝：发送 §五 挂接提示词（附带类目名 + 可选子类标签）
  ④ 等待回复稳定 → 读取全文（UIA / 滚动，复用 yuanbao_driver 能力）
  ⑤ 客户端解析 JSON（失败则标记 failed，保留原文供人工重试）
  ⑥ POST 服务端 §七 ingest + graph_link（文件 + yuanbao_json + category）
  ⑦ 元宝：删除本会话（可选，配置项）
  ⑧ 本地 manifest → status=learned | failed
```

### 3.3 阶段 C：服务端入库与挂边

见 §七。完成后该文件进入 `{category}/raw/` 已学习区，并在知识网中可见 `upload_{hash}` 文档节点及与 `goods_*` 等的边。

**下一迭代（ai_agent · 非本文范围）**：外链 **主动学习**（自抓取 + 元宝双源合并）见 [M5-1 方案](M5-1-主动学习链接抓取与元宝综合方案.md)。

---

## 四、产品入口（规划）

### 4.1 主入口：知识库页（PRD §5.7.7）

| 项 | 值 |
|----|-----|
| 路由 | `/knowledge`（侧栏「知识库」） |
| 上传区 | `[添加资料]` → `_pending_learn`；`[▶ 开始学习]` / `[停止]` |
| Tab | 本地资料 · 已入库向量条目 · 上传记录 · （主动学习 · 仅 ai_agent） |
| 前置 | 元宝桌面版已登录；SalesAgent 已连接；目标类目已有 Niushop 同步（M1.6 知识网非空） |

### 4.2 副入口：技能集（与知识库页共用同一 Service / IPC）

| 项 | 值 |
|----|-----|
| ID | `yuanbao-doc-graph-link` |
| 名称 | 元宝文档挂接知识网 |
| 副标题 | 批量上传资料，学习后挂接商城商品知识树 |
| 图标 | 📎 |
| 路由 | `/skills/yuanbao-doc-graph` |
| 说明 | 复用 §6.11 `DocGraphLearnService`；适合调试与从技能集直达，**不重复实现业务逻辑** |

**控件（知识库页与技能集共享）**：

- 类目选择（默认 `beauty`）
- 子类/标签（可选，写入 metadata，如「四季换肤」）
- 「添加文件 / 添加文件夹」→ 仅入 `_pending_learn`
- 待学习列表（批次、文件名、大小、上传时间）
- **「开始学习」** / **「停止」**
- 进度：当前文件、元宝阶段、挂接商品数、失败原因

---

## 五、元宝解析提示词（定稿）

> **目的**：让元宝输出 **可机器解析的 JSON**，供服务端做 BGE 校验与写边；**不是**给人类阅读的摘要 alone。

### 5.1 System / 角色（发送前固定前缀）

```text
你是「谛听」知识库助手，负责阅读用户上传的行业资料，并将其与已有商城商品知识网建立结构化关联。
你必须只输出一个 JSON 对象，不要 markdown 代码块，不要解释性废话。
若无法确定某商品 ID，用 title_guess + confidence，不要编造 goods_id。
```

### 5.2 User 提示词模板（`knowledge_graph_link_v1`）

变量：`{category_label}`（如「美妆/beauty」）、`{sub_category}`（可选）、`{filename}`；`{layout_hint_line}` 由客户端按**文件名通用关键词**可选注入（无匹配则为空，见 §5.2.1）。

```text
【任务】
我已上传文件「{filename}」。类目：{category_label}{sub_category_line}。
请阅读附件全文（含图片、图表、版式），输出 JSON，用于挂接到已有商城商品知识网。
{layout_hint_line}
【你需要的输出结构】（字段都必须出现，无内容则空数组）
{
  "schema": "knowledge_graph_link_v1",
  "document": {
    "title": "文档标题（你归纳）",
    "doc_type_guess": "training_manual|sop|cs_playbook|faq|slide_deck|flowchart|ingredient_table|marketing|poster|other",
    "language": "zh",
    "one_line_summary": "一句话摘要，≤80字"
  },
  "sections": [
    {
      "section_index": 0,
      "heading": "小节标题或空字符串",
      "text": "该段正文（可压缩但保留关键事实、流程、话术、成分、规格、品牌）",
      "themes": ["按实际内容填写，如话术、跟进、成分、功效、用法、异议、活动"]
    }
  ],
  "concepts": [
    { "name": "概念名", "type": "ingredient|efficacy|brand|usage|script|objection|process|audience|promotion|skin_type|season|other", "evidence": "原文短引" }
  ],
  "linked_products": [
    {
      "match_by": "title|brand_sku|context",
      "title_guess": "商品名或品牌+规格（尽量与商城标题接近）",
      "brand_guess": "",
      "relation": "same_product|accessory|alternative|bundle|mentioned_in_training",
      "confidence": 0.0,
      "evidence": "原文依据，≤120字",
      "section_index": 0
    }
  ],
  "linked_media_urls": [
    {
      "url": "https://yanpanji.com/upload/... 或文内出现的完整图片 URL",
      "role": "product_photo|ingredient_table|usage_step|poster",
      "section_index": 0
    }
  ],
  "linked_articles_guess": [
    { "title_guess": "文章标题", "relation": "cited|same_topic", "confidence": 0.0 }
  ],
  "uncertainties": ["无法匹配的商品描述…"]
}

【规则】
1. 图文并茂：行业资料常「图+文」混排（流程图、幻灯片、海报、截图、表格）。须结合附件中的视觉内容与文字一并理解；勿因纯文本层偏少而省略图中节点、箭头、标注、话术。
2. 先判断 doc_type_guess，再按文档结构分段（3–20 段，每段 text 建议 200–800 字）：
   faq→按主题聚类，保留完整「问+答」；sop/cs_playbook/flowchart→按阶段/节点，写清条件→动作→话术；
   slide_deck/training_manual/marketing→按页或章节主题；ingredient_table→按成分/规格/对照维度；other→按语义主题。
3. 销售话术、客服应答、操作步骤、禁忌提醒宜原文或近原文保留，便于后续检索召回。
4. linked_products：只写有依据的；confidence∈[0,1]；同款不同写法（体验装/正装/规格别名）合并一条；title_guess 尽量贴近商城标题。
5. linked_media_urls：只写文内明确 URL 或你可确定的 yanpanji.com 图片链接；勿臆造域名。
6. concepts：从 sections 抽取 3–15 个高价值概念；themes 按段实际内容填写，勿套空模板。
7. 不要输出 goods_id（客户端/服务端没有给你商品库全表）；用 title_guess 供服务端 BGE 召回对齐。
```

`sub_category_line` 示例：`（子类：四季换肤）` 或空。

### 5.2.1 图文并茂与版式提示（通用，非单文档硬编码）

用户投喂的资料**形态多样**（培训手册、FAQ、路径图、课件 PDF、成分表、营销海报等），共同特征是常 **图文并茂**：可提取纯文字可能很少，但图中仍有流程、话术、表格等关键信息。提示词设计原则：

| 原则 | 说明 |
|------|------|
| **图+文一并读** | 元宝上传附件后应理解版式与图片，不得因 OCR/纯文本层偏少而跳过 |
| **先判类型再分段** | 由模型据全文判断 `doc_type_guess`，再选用对应分段策略（§5.2 规则 2） |
| **话术可检索** | 客服话术、FAQ 问答、操作步骤尽量原文保留 |
| **商品从文中抽** | `linked_products` 只写文档内出现的品名/规格，不写死某一品牌 |
| **文件名仅作提示** | `prompt_builder.infer_layout_hint()` 按通用关键词（如 `百问百答`/`路径`/`课件`）追加一行【版式提示】；无匹配时仅靠通用规则，仍适用于任意新文件 |

实现：`ditingclient/src/com/yanpanji/pcwx/yuanbao_doc_graph/prompt_builder.py`。

### 5.3 解析失败降级

- JSON 解析失败 → 重试 1 次（同一对话补发「仅输出 JSON」）  
- 仍失败 → `status=failed`，保留 `yuanbao_raw_text` 供人工粘贴修复  
- **不阻塞**队列中其余文件

---

## 六、服务端挂接逻辑（规划，M3-1b）

> 无需先实现通用 M2-3 SPO 模块；本接口 **即** 用户上传场景的抽边实现。

### 6.1 API（规划）

```http
POST /api/knowledge/ingest/document_with_links
Content-Type: multipart/form-data

category=beauty
source=user_upload_yuanbao
file=@产品培训手册.pdf
yuanbao_json=@link_result.json
sub_category=四季换肤
client_file_hash=sha256...
```

### 6.2 处理步骤

```text
1. 创建 document：doc_id = upload_{client_file_hash}
2. 解析 yuanbao_json.sections → 500/50 分段 → section chunks + contains 边
3. 对每个 linked_products[]：
     a. BGE 召回 category 下 top-K 商品 _meta / title（K=5~10）
     b. 标题 fuzzy + 向量分数加权 → 解析 goods_id
     c. confidence ≥ 阈值（默认 0.55）→ INSERT related_product(upload_doc → goods_{id})
        否则写入 ingest_log 待人工确认
4. 对每个 concepts[] → UPSERT concept 节点（可选）+ mentions(section → concept)
5. 对每个 linked_media_urls[] → normalize URL → get-or-create media_node
     → appears_in(upload_doc) + references(section → media)
     → 若 URL 已存在于 goods_*：共享同一 media_node（M1.6）
6. BGE embed 全部新 chunk → Chroma upsert
7. 返回：{ doc_id, edges_created: { related_product, mentions, references, appears_in }, chunk_count }
```

### 6.3 与 M2-3 的关系（评估见 §八）

| M2-3 规划能力 | 本技能是否覆盖 |
|---------------|----------------|
| 新 chunk 入库即 BGE embed（inductive） | ✅ 服务端 ingest 必做 |
| LLM 零样本 SPO 抽 `mentions` | ✅ **由元宝在客户端完成**（结构化 JSON） |
| 自动 `related_product`（文章→商品） | ✅ **元宝 title_guess + 服务端 BGE 对齐** |
| 对 **全库已有 Niushop section** 批量补边 | ❌ 不覆盖；仍可用 M2-3 夜间任务 |
| 无用户文档时的冷启动 | ❌ 不覆盖 |

---

## 七、数据流总图

```text
┌──────────────── 知识库页 / 技能集（M3-S1 共用）──────────────────────┐
│ 多批 [添加资料] → raw/_pending_learn/（本地资料 Tab · 待学习）         │
│       ↓ 用户点击 [▶ 开始学习]                                        │
│  每文件：元宝新建对话 → 上传文件 → §五 提示词 → 读 JSON 回复           │
│       ↓（上传记录 Tab 写 job 日志）                                   │
│  multipart: 原文件 + yuanbao_json → SalesAgent                       │
└───────────────────────────────┬──────────────────────────────────────┘
                                ↓
┌──────────────── 服务端 ────────────────────────────────────────────────┐
│  upload_{hash} document + sections (contains)                       │
│  BGE 对齐 linked_products → related_product 边 → goods_*              │
│  concepts → mentions 边；media URL → appears_in / references          │
│  Chroma upsert（source=user_upload_yuanbao）                          │
└──────────────────────────────────────────────────────────────────────┘
                                ↓
        已入库向量条目 Tab 可预览 · analyze / 知识关系图检索可沿边扩散
```

---

## 八、评估：是否「零成本」实现 M2-3？是否必须先做 M2-3？

### 8.1 是否间接零成本实现「LLM/BGE 抽 related_product / mentions 边」？

**结论：在用户上传挂接场景下，等价实现，但不是全库 M2-3 的完整替代。**

| 维度 | 说明 |
|------|------|
| **LLM 成本** | 使用用户本机 **元宝 C 端**，无 DeepSeek / 服务端 LLM 读文档费用 → 相对 M2-3 规划中的服务端 SPO，**边际 API 成本≈0** |
| **BGE 成本** | 服务端仍需：新 section embed、商品召回 top-K、写边 — 与 M2-3 相同，**无法省略** |
| **related_product** | 元宝产出 `linked_products` + 服务端 BGE 对齐 `goods_id` → **效果上等同**「LLM 提议 + 检索验证」 |
| **mentions** | 元宝产出 `concepts` + section 索引 → 写 `mentions` 边 → **效果上等同** M2-3 的 concept 抽取 |
| **覆盖范围** | 仅 **用户主动上传且走完学习流程** 的文档；不会自动给 39 个 Niushop 商品详情批量补 concept 边 |
| **质量风险** | 依赖元宝 JSON 稳定度；需 BGE 阈值 + `uncertainties` 人工复核入口 |

**一句话**：本技能 = **「元宝替代服务端 SPO 读文档 + BGE 替代盲猜 goods_id」** 的产品路径；**不是**零工程成本，但是 **零增量 LLM API 成本**（在已有元宝登录前提下）。

### 8.2 是否必须先实现通用 M2-3，才能做本技能？

**结论：不必。本技能可独立开发；与 M2-3 并行或替代其「用户上传」子集。**

| 依赖 | 必须先有？ |
|------|-----------|
| M1.6 知识网（media_nodes + edges 表） | ✅ 是 |
| M2-1 BGE 检索 / graph API | ✅ 是（召回商品用） |
| M2-2 媒体 VLM | ❌ 否（有则 linked_media 更强） |
| 通用 M2-3 `spo_extractor.py` 夜间任务 | ❌ **否** |
| S19 staging + ingest 基础 | ✅ 是（可随 M3-1 一并实现 `document_with_links`） |
| M2-2 同款 `yuanbao_media` UIA 驱动 | ✅ 复用驱动，扩展「上传文件」步骤 |

**推荐顺序**：

1. 实现 M3-1 技能（客户端 pending 队列 + 元宝 JSON + 服务端 `document_with_links`）  
2. M2-3 通用 SPO **降为 P2**：仅用于 Niushop 存量 section 批量补 `mentions`、无用户文档时的冷启动

### 8.3 本技能的目标与 M2-3 目标对齐声明

> **本技能的产品目标，就是实现文件上传链路里的「LLM 提议 + BGE 验证 → related_product / mentions 边」。**  
> 实现载体是 **元宝结构化 JSON + 服务端 graph_link**，而非服务端 DeepSeek 零样本 SPO。

---

## 九、配置项（规划）

`ditingclient/config/yuanbao_doc_graph_skill.json`：

```json
{
  "enabled": true,
  "pending_dir": "{data_root}/{category}/raw/_pending_learn",
  "learned_dir": "{data_root}/{category}/raw",
  "prompt_template": "knowledge_graph_link_v1",
  "yuanbao_desktop": {
    "window_title_contains": "元宝",
    "new_chat_per_file": true,
    "delete_chat_after_each": true,
    "max_wait_reply_sec": 180
  },
  "batch": {
    "max_files_per_learn_session": 50,
    "inter_file_delay_ms": 5000
  },
  "server": {
    "ingest_path": "/api/knowledge/ingest/document_with_links",
    "link_confidence_threshold": 0.55,
    "product_recall_top_k": 8
  }
}
```

---

## 十、验收标准（规划）

| # | 项 | 标准 |
|---|-----|------|
| 1 | 多批上传 | 3 批 5 个文件进入 pending，未点学习前 SalesAgent chunk 数不变 |
| 2 | 元宝 JSON | 单份 PDF 产出可解析 `knowledge_graph_link_v1` |
| 3 | related_product | 至少 1 条边连到真实 `goods_{id}`（BGE 对齐） |
| 4 | mentions | 至少 3 条 concept → section mentions 边 |
| 5 | 媒体 URL | 文内 yanpanji.com 图与已有 media_node 合并，`appears_in` ≥2 |
| 6 | 检索 | analyze 或知识库预览能命中新 upload 文档 chunk |
| 7 | 隔离 | 全流程无 `POST /api/analyze` |
| 8 | 脚本 | `verify_m31.py`（规划） |

---

## 十一、风险

| 风险 | 缓解 |
|------|------|
| 元宝对 PDF/Excel 上传解析不稳定 | 优先 PDF/Word；Excel 可先导出 PDF；失败可人工重试 |
| JSON 格式漂移 | 严格 schema + 客户端校验 + 重试 |
| 商品 title 对不齐 | BGE top-K + 阈值；低置信进「待确认」列表 |
| 与 M2-2 抢元宝 | 全局 **单实例元宝锁**（已有 yuanbao_media 经验） |
| 大文件超时 | `max_wait_reply_sec` 可配；超大文件拆分 |

---

## 十二、文档修订

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-28 | 初稿：批量 pending → 开始学习 → 元宝 JSON → 服务端挂边；M2-3 关系评估 |
| v1.3 | 2026-06-30 | §5 提示词：图文并茂通用规则、扩展 doc_type、按结构分段；文件名版式提示（非单文档硬编码） |
| v1.2 | 2026-06-29 | 首版开发完成：技能集 `/skills/yuanbao-doc-graph` 与知识库页共用 `DocGraphLearnService`；`verify_m31.py` |
| v1.1 | 2026-06-28 | 与 PRD §5.7.7 对齐：知识库页为主入口、四 Tab 映射、source 枚举 |
