# M3-S2 知识图谱 Tab — 开发纪要（2026-06-28 ~ 2026-06-29）

> **关联方案**：[M3-S2-知识库大类图谱Tab方案.md](M3-S2-知识库大类图谱Tab方案.md)  
> **关联排期**：[开发计划-总览.md §14.4.2](开发计划-总览.md) · §14.3.1 M2-3  
> **状态**：P0 主体已落地；跨文档网与补边路线已定稿

---

## 一、本次沟通背景

知识库页「知识图谱」Tab 由 M3-S2 方案驱动，目标是把「已入库向量条目」升级为 **大类知识森林可视化**。沟通过程中陆续明确：

1. 右侧需支持 **段落正文 / 图片预览**（含 VLM 描述与适用场景）  
2. 左侧不宜长期停留在 **孤立树形列表**，应逐步呈现 **线·点·网** 关系图  
3. 当前数据已是「半张网」，下一步重点是 **补关系 + 画已有跨文档边**，为 M2-3 / M3-S1 与 Skills 千手观音愿景铺路

---

## 二、已完成交付（代码）

### 2.1 服务端

| 文件 | 内容 |
|------|------|
| `salesagent/.../knowledge/graph_forest.py` | `get_graph_forest()`、`get_graph_subtree()`；子树节点带 `preview_text`、`media_url`、`vlm_description`、`use_cases` |
| `salesagent/.../api/routes/knowledge.py` | `GET /api/knowledge/graph/forest`、`GET /api/knowledge/graph/subtree` |

### 2.2 客户端（Electron + Vue）

| 文件 | 内容 |
|------|------|
| `ditingclient/.../electron/preload.ts` | `KnowledgeGraphForestView`、`KnowledgeGraphSubtreeNodeView` 等类型 |
| `ditingclient/.../electron/services/knowledge_service.ts` | IPC 封装 forest / subtree |
| `ditingclient/.../renderer/components/knowledge/KnowledgeForestPanel.vue` | 知识图谱 Tab 主面板：文档索引 + 网络图 + 右侧详情 |
| `ditingclient/.../renderer/components/knowledge/KnowledgeNetworkGraph.vue` | **线·点·网** SVG 组件（新建） |
| `ditingclient/.../renderer/pages/KnowledgePage.vue` | Tab 改名「知识图谱」；切片列表收进折叠区「切片明细（debug）」 |

### 2.3 关联能力（前期会话已落地）

| 能力 | 说明 |
|------|------|
| 知识库地图 §5.5.5 | `map_overview` + `KnowledgeMapPanel` |
| `sub_category` 补写 | 入库链路 + `backfill_sub_category` CLI |

---

## 三、UI / 交互迭代记录

### 3.1 右侧节点详情

| 需求 | 实现 |
|------|------|
| 点击段落 / 图片后在「在关系图检索中打开」下方预览 | section → `preview_text`；media → 图片 URL |
| 图片展示 VLM 补全信息 | `vlm_description`（描述）、`use_cases`（适用场景） |
| 段落预览区太小 | 右侧栏 flex 布局，正文区 `flex-1` 延伸至栏底可滚动 |

**空态文案说明**：

- 「暂无段落内容」：`preview_text` 为空或未从 API 返回  
- 「暂无图片地址」：`media_url` 为空或未从 API 返回  
- 若 SalesAgent 未重启或子树缓存过旧，需「全部收缩」后重新展开文档

### 3.2 左侧：树 → 网

| 阶段 | 形态 |
|------|------|
| 初版 | 子类 → 文档 → 段落/图片 **缩进列表** |
| 当前 | 左窄栏 **文档索引** + 主区 **KnowledgeNetworkGraph**（节点 + 边） |

**网络图组件特性**：

- 节点：**小实心圆点**（蓝=文档、绿=段落、橙=图片）；悬停 `<title>` 浮层保留完整文案  
- 边：`contains` 实线、`references` 橙色虚线；预留 `mentions` / `related_product` / `appears_in` 样式  
- 收拢：文档 / 段落左侧 `▼`/`▶`；顶栏「展开全部 / 收拢全部」  
- 统计：`可见节点/总节点 · 边数`

### 3.3 Bug 修复：段落收拢未隐藏图片

**原因**：图片在库中同时有两条边：

- `document ──contains──▶ media`  
- `section ──references──▶ media`  

旧逻辑收拢段落时只隐藏 `references`，`contains` 仍把图片画出。

**修复**：凡被段落 `references` 的图片，**仅由段落展开状态控制**；无段落引用的图片才走 `document contains` 直连。

---

## 四、beauty 现网数据快照

| 指标 | 约值 |
|------|------|
| 边总数 | 973 |
| 媒体节点 | 303 |
| 文档树 | 48 |
| 子类分组 | 防晒 / 护肤 / 抗老 / 敏感肌 / 彩妆 / 未归类 |

**边类型分布**（`knowledge_edges`，category=beauty）：

| `rel_type` | 数量 | 跨文档 | 当前 UI |
|------------|------|--------|---------|
| `contains` | 444 | 否 | ✅ 已画 |
| `references` | 187 | 否 | ✅ 已画 |
| `appears_in` | 342 | **是** | ❌ 未画 |
| `mentions` | 0 | 待 M2-3 | — |
| `related_product` | 0 | 待 M3-S1 | — |

**子类**：存在 Chroma `sub_category` 元数据，用于森林分组；**尚未作为图节点/边写入 `knowledge_edges`**。

---

## 五、运维备忘：服务端「连接中」

**现象**：顶栏「🟡 服务端连接中」—— 监听（绿）正常，WebSocket 未完成 `auth_ok`。

**根因**：SalesAgent（8765）进程 **僵死**（端口在听，HTTP/WS 无响应）；非客户端配置问题。

**处理**：

```powershell
netstat -ano | findstr :8765
taskkill /F /PID <PID> /T
cd C:\work\projects\wchat\salesagent
$env:PYTHONPATH="src"
python -m uvicorn com.yanpanji.agents.main:app --host 127.0.0.1 --port 8765
Invoke-RestMethod http://127.0.0.1:8765/api/health
```

---

## 六、产品共识：从「森林树」到「知识网」

### 6.1 现状判断

- 数据模型已是 **异构图**（document / section / media / 未来 concept）  
- UI 当前按 **单文档子树** 展示，隔离感强  
- **342 条 `appears_in`**（同 URL 多文档共用媒体）已在库中，是零成本的第一批「网」

### 6.2 实体层级与图的关系

```
子类（metadata）→ 商品/文章/资料（document）→ 段落（section）→ 图片（media）
                                              ↘ references ↗
                        未来：section ──mentions──▶ concept
                        未来：upload_doc ──related_product──▶ goods_*
                        已有：media ──appears_in──▶ 其他 document
```

**架构隐喻的完整定义**见 [知识库-结构化入库方案 §十四](知识库-结构化入库方案.md#十四生物-数字双模态映射模型v13-定稿)。

### 6.3 排期原则（2026-06-29 定稿）

> **先成网（基本盘），后增补（M3-S1）。**

| 类别 | 定义 | 代表工作 |
|------|------|----------|
| **基本盘** | 把 Niushop 已同步存量做成 **可浏览、可扩散的网** | `appears_in` 展示、M2-3 存量补 `mentions`、子类级子图 |
| **知识增补** | 用户新上传资料 → 挂接到已有网 | **M3-S1**（`related_product` / 新 `mentions`） |

**理由**：

- M3-S1 是 **在已有知识网上的补边/挂接**，前提是网本身成立（跨文档边、概念节点、可视化闭环）。  
- 当前 `mentions` / `related_product` 为 0，单文档树之间仍隔离；先做 M3-S1 容易「边加上了、盘却看不清」。  
- M2-3 面向 **39 商品 + 9 文章存量**，是冷启动基本盘；M3-S1 面向 **运营主动上传**，应排在基本盘验收之后。

### 6.4 推荐下一步（按优先级 · 修订版）

| 阶段 | 任务 | 说明 | 属性 |
|------|------|------|------|
| **①** | 画出已有 `appears_in` | subtree/neighborhood API；网络图「关联文档」列 + 开关 | 基本盘 · 无 LLM |
| **②** | M2-3-1 + M2-3-3 | 存量 section SPO → `concept` + `mentions` | 基本盘 |
| **③** | M2-3-2 + M2-3-4 | 文章内链 `related_product`；同 concept 跨商品 | 基本盘 |
| **④** | M3-S2 P2 子类级子图 | `subgraph?sub_category=`；跨树连通可视化 | 基本盘 |
| **⑤** | PPR / 关系图检索增强 | `ann_then_ppr` 沿 mentions 扩散 | 基本盘验收后 |
| **⑥** | **M3-S1 资料挂接** | 用户上传 → 元宝 → 补边 ingest | **知识增补 · 置后** |

**M2-3 步骤对照**（开发计划 §14.3.1）：

| 步骤 | 任务 |
|------|------|
| M2-3-1 | DeepSeek 零样本 SPO → `concept` + `mentions` |
| M2-3-2 | 文章 HTML 内链 → `related_product`（存量规则边，先于 M3-S1） |
| M2-3-3 | Niushop 存量 39 商品详情 section 批量补边 |
| M2-3-4 | 同 concept 跨商品联结，支撑 PPR |
| M2-3-5 | `incremental_by_hash` 跳过未变 section |
| M2-3-6 | 低置信边进 `ingest_log` 待复核 |

**建议排期（修订）**：

1. **近期**：`appears_in` API + UI；M3-S2 方案线框更新（树→网）  
2. **基本盘一期**：M2-3-1 + M2-3-3 夜间补边；图谱可见 `mentions` 虚线  
3. **基本盘二期**：M2-3-2/4 + 子类级子图；关系图检索 / analyze 接 PPR  
4. **基本盘验收后**：**M3-S1**（上传资料挂接）；验收分 **L2-a / L2-b** 两档（入库方案 §14.10）

### 6.5 方案评审修订（2026-06-29 · 已写入入库方案 v1.7）

| 修订项 | 内容 |
|--------|------|
| M2-3 优先级 | P2 可选 → **P1 基本盘** |
| 双边冗余 | `contains` + `references` 并存为设计约定（§4.3） |
| `concept` 规范 | `concept_{slug}` + Chroma chunk，与 `sub_category` 分层 |
| PPR 实现差距 | meta-path 未接入；PPR 暂无向化 → **L2-c** |
| 验收分档 | **L2-a**（appears_in，无 LLM）/ **L2-b**（M2-3 后） |
| 远期保留 | GNN、TransE、`co_viewed`、Graph RAG 均标 **远期可选**，不删除 |

详见 [知识库-结构化入库方案 §14.12](知识库-结构化入库方案.md)。

---

## 七、待办清单（2026-07-02 对齐）

### 已完成 ✅

- [x] forest / subtree API + IPC  
- [x] 知识图谱 Tab + 文档索引  
- [x] 段落 / 图片右侧预览  
- [x] 图片 VLM 描述 + 适用场景  
- [x] 线·点·网组件 + 实心点 + 收拢展开  
- [x] 段落收拢隐藏图片（双边修复）  
- [x] **L2-a**：`appears_in` 跨文档边 + 「显示跨文档关联」开关（M3-S2-7/8）  
- [x] **L2-b**：M2-3 `mentions` / `related_product` 边在图谱可见（M3-S2-9）  
- [x] **L2-c**：认知唤醒推演页（`KnowledgeGraphSkillPage`）+ 联想过程日志 + **思考过程回放**  
- [x] **M3-S1**：商业大类文档被动学习 / 元宝挂接（与图谱 Tab 联动展示 `upload_*` 子树）  

### L2-b 验收（2026-07-02 脚本复验）

| 脚本 | 结果 | 备注 |
|------|------|------|
| `verify_m23.py` | ✅ | mentions=1273；related_product=14；MP1 共享 concept |
| `verify_m21.py` | ✅ | 有向 PPR 2349 边；analyze 202；PPR 扩展 WARN（样本 query 未多出 chunk，可接受） |
| `verify_m31.py` | ✅ | document_with_links + linked_media |

入库方案 §14.10 **B1–B3 主体通过**。

### 待做 ⏳

**文档**

- [ ] 更新 [M3-S2 方案](M3-S2-知识库大类图谱Tab方案.md) §4.1 线框（树 → 网）  

**远期（P2+）**

- [ ] M3-S2-10：子类级子图 / 力导向大图  
- [ ] 图谱 Hub 默认 + 边粗细 ∝ `rel_weight`（入库方案 §14.13）  
- [ ] GNN / TransE / `co_viewed`（入库方案 §14.12）  

### 下一产品迭代（P1 · 非 M3-S2 范围）

- [ ] **M6-1 美妆客服智能体**（渐进式对话 + 认知推演）— [M6-1 方案](M6-1-美妆客服智能体-渐进式对话方案.md)  
- [ ] **M5-1 主动学习**（暂缓，后续重点沟通）— [M5-1 方案](M5-1-主动学习链接抓取与元宝综合方案.md)  
- [ ] M2-3-6：低置信边 `ingest_log` 复核表（仅 logger  today）

---

## 八、关键代码路径

```
KnowledgePage.vue
  └─ KnowledgeForestPanel.vue
       ├─ 左：文档索引（子类分组）
       ├─ 中：KnowledgeNetworkGraph.vue
       └─ 右：节点详情 + 预览
            └─ graphForest / graphSubtree IPC
                 └─ GET /api/knowledge/graph/forest|subtree
                      └─ graph_forest.py → knowledge_edges + knowledge_chunks + knowledge_media_nodes
```

---

## 九、修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-29 | 初稿：汇总 M3-S2 Tab 开发、UI 迭代、补关系路线与排期建议 |
| 2026-06-29 | **排期修订**：先成网（基本盘 M2-3 + appears_in），M3-S1 资料挂接置后 |
| 2026-06-29 | 架构章：入库方案 **§十四** 定稿；评审修订写入 **v1.7**（§6.5） |
| 2026-07-02 | **待办对齐**：L2-a/b/c + M3-S1 + 认知回放 ✅；L2-b 脚本复验；下一项 **M5-1 主动学习** |
