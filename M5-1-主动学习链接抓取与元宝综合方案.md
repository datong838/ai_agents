# M5-1 — 主动学习（链接抓取 + 元宝综合 + 双源入库）

> **阶段**：MVP M5（`ai_agent` 技术知识库 · **主动学习**）  
> **状态**：**方案定稿 · 待开发**（2026-07-02）  
> **前置**：M3-S1 被动学习（商业大类文档挂接）✅；`ai_agent` 被动直传 `uploadPassive` ✅  
> **关联**：[产品设计文档-谛听.md](产品设计文档-谛听.md) §5.5.4；[M3-1-元宝文档挂接知识网技能方案.md](M3-1-元宝文档挂接知识网技能方案.md)；[开发计划-总览.md](开发计划-总览.md) §14.4.4；[知识库-结构化入库方案.md](知识库-结构化入库方案.md) §9

---

## 一、要解决的问题

**被动学习**（已交付两条路径）：

| 路径 | 入口 | 输入 | 元宝 | 入库 |
|------|------|------|------|------|
| **M3-S1** | 知识库页 · 商业大类 | 本地 PDF/Word 批次 | 读文档 + **挂接商品树 JSON** | `document_with_links` → beauty 图 + Chroma |
| **ai_agent 被动** | 知识库页 · `[开始上传]` | 本地 Markdown/文档 | 无（可选后续 VLM） | `POST /api/ingest/upload` |

**主动学习**（本文 · 待开发）补齐第三种模式：

> 用户给出 **一个或多个链接**（或从微信技术群队列采集），客户端 **主动打开并完整抓取** 页面正文与图片；再 **打开本机元宝**，把抓取结果发给元宝 **征求结构化输出意见**；最终将 **「自抓取原文知识」+「元宝综合知识」** 合并为一份稿件，写入知识库。

与 M3-S1 的差异：**输入是 URL/链接卡**，不是本地文件；元宝职责是 **技术/内容综合与观点提炼**，不是商品树挂接（除非未来扩展 beauty 场景）。

---

## 二、与相关能力边界

| 能力 | 触发 | 元宝用途 | 服务端 LLM | 类目 |
|------|------|----------|------------|------|
| M3-S1 被动 | 用户上传文件 + 开始学习 | 读文档 + 挂接 JSON | ❌ DeepSeek | beauty 等 |
| ai_agent 被动 | 选文件上传 | 无 | ❌ | ai_agent |
| **M5-1 主动** | 粘贴链接 / 队列 + 开始学习 | **综合成稿**（滚动读全文） | ❌ **禁止 analyze** | 默认 **ai_agent** |
| M2-2 识图 | 技能集 | 单图描述 | ❌ | beauty 等 |
| 需求雷达 analyze | 微信消息 | ❌ | ✅ DeepSeek | beauty 网关 |

**硬隔离（红线，与 PRD §5.5.4 R1–R5 一致）**：

- ❌ 主动学习全流程 **禁止** `POST /api/analyze`、禁止服务端 DeepSeek 读外链正文  
- ✅ 网页抓取、元宝对话 **仅在本机客户端** 完成  
- ✅ 服务端 **仅** 接收已落盘的 `LearningArtifact` → BGE 向量化 ingest  
- ✅ 用户未点「开始学习」时，**不** 自动开外链、**不** 占用元宝  

---

## 三、用户流程（产品）

### 3.1 入口

| 入口 | 说明 |
|------|------|
| 知识库页 · **主动学习 Tab** | 类目固定或默认 `ai_agent`；粘贴 URL + 可选描述 |
| 微信学习源群（可选 M5-7） | 监听将链接卡 + 上下文写入 `learning_queue` |
| 技能集卡片（可选） | `active-link-learn` · 与 Tab 共用 Service |

### 3.2 阶段 A：登记队列（不处理）

用户添加一条或多条学习条目：

```text
learning_queue (SQLite / manifest)
  entry_id: al_20260702_001
  url: https://mp.weixin.qq.com/s/...
  title: （链接卡标题，可空）
  chat_context: （可选：群聊上下文）
  source_group: AGI技术交流群
  status: pending
  category: ai_agent
  sub_category: video | image | llm_integration | ...
```

**此阶段不调抓取、不调元宝、不调 SalesAgent。**

### 3.3 阶段 B：开始学习（逐条串行）

用户点击 **「▶ 开始学习」** 后，与 M3-S1 共用 **元宝自动化锁**（避免与文档挂接/识图并发）：

```text
对每个 pending entry:

  ① 网页抓取（客户端）
     · Playwright / 受控浏览器打开 entry_url
     · 提取：标题、正文 HTML→Markdown、内嵌图片 URL 列表、发布时间等
     · 下载图片到 staging/{entry_id}/images/
     · 产出 primary_capture.json（自学习原文包）

  ② 本机元宝综合（客户端 · 核心）
     · 新建元宝对话
     · 发送 §五 主动学习提示词 + primary_capture 摘要/全文
     · 可选：上传 staging 中关键截图
     · 滚动读取元宝输出直至稳定（复用 yuanbao_driver / M3-S1 滚动读能力）
     · 产出 yuanbao_synthesis.md（元宝意见与结构化摘要）

  ③ 双源合并（客户端）
     · 组装 LearningArtifact（见 §四）
     · 写入 {data_root}/ai_agent/raw/active/{date}/{entry_id}/
     · status → synthesized

  ④ 上传 ingest（触达服务端）
     · POST /api/ingest/upload
       category=ai_agent
       metadata.source=active_learning
       附件：merged.md + primary_capture.json + yuanbao_synthesis.md + 图片
     · 服务端：解析 → 分段 → BGE → ai_agent/chroma/
     · **不写** analyze 队列

  ⑤ 归档
     · manifest status → learned | failed
     · 上传记录 Tab 展示 job 日志
```

### 3.4 阶段 C：知识库可见

- **向量检索**：`ai_agent` Chroma 可召回合并后的 chunk  
- **知识图谱 Tab**：若后续为 ai_agent 启用图模型，可展示 `learning_{entry_id}` 文档节点（P2）  
- **不参与** beauty 需求雷达类目网关  

---

## 四、LearningArtifact 数据结构（双源合并）

主动学习的核心产物是 **两份知识 + 一份合并稿**：

```json
{
  "entry_id": "al_20260702_001",
  "category": "ai_agent",
  "sub_category": "video",
  "source": "active_learning",
  "url": "https://...",
  "title": "CVPR 2026 | PoseAnything…",
  "chat_context": "群聊：该框架支持任意角色…",
  "captured_at": "2026-07-02T11:00:00+08:00",

  "primary_capture": {
    "scraper": "playwright",
    "page_title": "...",
    "text_markdown": "（网页正文全文）",
    "image_paths": ["staging/.../img_01.png"],
    "word_count": 4200
  },

  "yuanbao_synthesis": {
    "prompt_version": "active_learn_v1",
    "full_text": "（元宝滚动读出的完整回复）",
    "structured": {
      "summary": "一句话摘要",
      "key_points": ["...", "..."],
      "tech_tags": ["diffusion", "pose"],
      "applicability": "对谛听/销售场景的启示（可选）",
      "caveats": "元宝指出的局限或待核实点"
    }
  },

  "merged_document": {
    "format": "markdown",
    "sections": [
      { "heading": "来源与元数据", "body": "URL、群上下文…" },
      { "heading": "原文抓取（自学习）", "body": "primary_capture.text_markdown" },
      { "heading": "元宝综合意见", "body": "yuanbao_synthesis.full_text" },
      { "heading": "结构化摘要（合并）", "body": "去重后的要点列表" }
    ]
  }
}
```

**合并策略（定稿）**：

1. **保留原文**：`primary_capture` 全文入库，保证可追溯、可审计  
2. **保留元宝输出**：`yuanbao_synthesis.full_text` 独立 section，不丢弃  
3. **检索友好层**：对 `merged_document` 做分段向量化（服务端 ingest 常规 pipeline）  
4. **去重**：结构化 `key_points` 与原文标题级去重，避免 chunk 完全重复（客户端或 ingest 层轻量处理）

---

## 五、元宝提示词要点（草案）

与 M3-S1 挂接提示词 **分离**；主动学习侧重 **技术/内容理解**，不要求输出 `related_product`：

```text
你是技术知识整理助手。请基于以下「网页抓取正文」与「聊天上下文」：

1. 用中文给出结构化摘要（背景、核心方法、创新点、局限）
2. 列出 5–10 条可入库的知识点（每条一句话，可独立检索）
3. 推荐 3–5 个 tech_tags（英文或中文）
4. 指出原文中可能过时或需二次核实的信息
5. 输出 Markdown，便于直接归档

--- 聊天上下文 ---
{chat_context}

--- 网页正文（节选或全文）---
{primary_capture.text_markdown}
```

失败策略：元宝 JSON/Markdown 解析失败 → 仍上传 **仅 primary_capture** 的降级稿件，job 标记 `partial`。

---

## 六、技术模块划分（开发任务 · 对应 M5-5～M5-8）

| ID | 模块 | 端 | 说明 | 复用 |
|----|------|-----|------|------|
| M5-5 | `learning_queue` | 客户端 | SQLite 表 + 主动学习 Tab UI：增删改 pending 条目 | M3-S1 pending 模式 |
| M5-5b | `LinkScraperService` | 客户端 | Playwright 抓取 + 图片落地 + `primary_capture.json` | 新建 |
| M5-6 | `ActiveLearnService` | 客户端 | 编排：抓取 → 元宝 → 合并 → upload | `DocGraphLearnService` 锁与 yuanbao 子进程 |
| M5-6b | `LearningArtifact`  writer | 客户端 | 双源合并 Markdown + manifest | 新建 |
| M5-7 | `learning_source_tap` | 客户端 | 可选：微信学习源群链接入队（**不进** beauty 网关） | listener 扩展 |
| M5-8 | 红线验收 AL1–AL4 | 双端 | 全流程无 analyze；ingest 仅 BGE | 见 §八 |

**服务端**：首版 **不新增** API，沿用 `POST /api/ingest/upload` + `metadata.source=active_learning`。  
**beauty 大类扩展**（远期 M5-S2）：若需链接学习后挂商品树，可再增 `document_with_links` 分支，**不在 M5-1 首版范围**。

---

## 七、配置项

`ditingclient/config/active_learning.json`（PRD §5.5.4 已有产品层定义）：

| 键 | 说明 |
|----|------|
| `enabled` | 总开关 |
| `default_category` | 固定 `ai_agent` |
| `source_sessions[]` | 学习源群名列表 |
| `scraper.timeout_seconds` | 单页抓取超时 |
| `scraper.max_images` | 每页最多下载图片数 |
| `yuanbao_web.scroll_pause_ms` | 滚动读元宝输出停顿 |
| `yuanbao_web.prompt_version` | 提示词版本号 |
| `merge.include_full_primary` | 合并稿是否含原文全文（默认 true） |

---

## 八、验收标准

| ID | 项 | 可观测结果 |
|----|-----|-----------|
| AL1 | 链接抓取 | 给定公众号/知乎 URL，生成 `primary_capture.json`，正文非空 |
| AL2 | 元宝综合 | 本机元宝窗口出现请求；滚动读出 `yuanbao_synthesis` 全文 |
| AL3 | 双源入库 | `merged_document` ingest 后，`ai_agent` Chroma 可检索到原文要点 **与** 元宝摘要 |
| AL4 | 红线 | 全流程 **无** `POST /api/analyze`；网络抓包 / 日志可证 |
| AL5 | 与被动并存 | 同一类目可同时存在 `user_upload` 与 `active_learning` 来源 chunk |
| AL6 | 失败降级 | 元宝超时仍保留 primary 入库，job 状态 `partial` |

建议脚本：`salesagent/scripts/verify_m51.py`（首版仅测 ingest 侧 AL3/AL5；AL2 需本机元宝 E2E 人工或半自动）。

---

## 九、排期建议

| 顺序 | 任务 | 工期（估） |
|------|------|-----------|
| 1 | M5-5 Tab UI + `learning_queue` | 2–3 天 |
| 2 | M5-5b LinkScraper（Playwright） | 3–4 天 |
| 3 | M5-6 ActiveLearnService + 元宝提示词 | 3–4 天（与 M3-S1 共用 driver） |
| 4 | M5-6b 双源合并 + upload | 1–2 天 |
| 5 | M5-8 验收 + `verify_m51` | 1 天 |
| 6 | M5-7 微信群 tap（可选） | +3–5 天 |

**总估**：首版可交付 **约 2 周**（不含 M5-7）；M5-7 可与 MVP 后期并行。

---

## 十、修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-02 | 初稿：双源（自抓取 + 元宝）合并入库；对齐 PRD §5.5.4 与 M3-S1 边界；M5-5～M5-8 任务映射 |
