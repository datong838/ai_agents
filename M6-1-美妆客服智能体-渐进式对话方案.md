# M6-1 — 美妆客服智能体（渐进式对话策略）

> **阶段**：MVP M6（智能体 · 技能层）  
> **状态**：**POC 已落地**（2026-07-02）· 架构演进见 **[M6-2](M6-2-通用客服智能体与话术策略知识库方案.md)**  
> **实验类目**：`beauty`（四季护肤 / 美妆 · **第二层 · 知识网络**）  
> **前置**：L2-b 基本盘 ✅；认知唤醒推演（BGE 种子 + PPR 子图 + 联想日志）✅；M3-S1 被动学习 ✅  
> **关联**：[知识库-结构化入库方案 §14.3.3](知识库-结构化入库方案.md)；[开发计划-总览 §14.5](开发计划-总览.md)；[产品设计文档-谛听 §5.5](产品设计文档-谛听.md)；认知推演页 `KnowledgeGraphSkillPage.vue`

---

## 一、要解决的问题

### 1.1 现状：「屠龙刀」缺「剑鞘」

谛听已具备较完整的 **beauty 知识网络**（约 960 条向量、2415 条边、概念/跨品/跨文档联结）与 **认知推演**能力（激活思考 → 子图召回 → 传导日志 → 思考回放）。

但技术侧默认输出的是：

- 一堆 **chunk 文本**、**node_id**、**PPR score**
- 裸 **商品链接**、**文章链接**

若直接甩给终端用户，体验像 **跟说明书对话**——准确但 **冰冷、生硬、无转化感**。

### 1.2 目标：金牌导购，不是图书馆管理员

引入 **「人机交互策略层」**（Human Interaction Strategy Layer），核心逻辑：

> **模拟金牌导购的「察言观色」与「话术设计」**，而不是图书馆管理员的「索引检索」。

本文将该策略命名为 **「渐进式对话策略」**（Progressive Dialogue Strategy，PDS）。

**优化后用户链路**：

```text
用户提问 → 意图理解 → 认知推演召回（L2 子图）→ 策略层编排（L1 方法论）
         → 分步生成「人话」话术（L3 智能客服技能）→ 客服确认/微调 → 发给用户
```

---

## 二、三层认知架构（与现有工程映射）

> 下图中的 **L1 / L2 / L3** 指 **智能体认知分层**（思想 → 知识 → 技能），与入库方案 §14.4 的「解剖 / 传导 / 可塑」生物隐喻 **并行存在、互不替代**。

| 层 | 名称 | 含义（计算机视角） | 本实验落点 | 工程现状 |
|----|------|-------------------|-----------|----------|
| **L1** | **思想方法层** | 解题策略、启发式规则、元学习 | **渐进式对话策略**、角色设定、输出约束、转化节奏 | 📄 本文定稿；代码待建 |
| **L2** | **知识网络层** | 结构化语义图谱 + 向量检索 | **`beauty` 类目**：商品 / 段落 / 概念 / 图片 / 评价 / 跨文档 | ✅ Chroma + `knowledge_edges` + 认知推演 API |
| **L3** | **解题模型层** | 可执行推理模板 / 操作序列（技能库） | **智能客服**（首技能）；远期：写文档、做短视频… | ⏳ 首版仅 **智能客服** 技能 POC |

```text
┌─────────────────────────────────────────────────────────┐
│  L1 思想方法层 · Progressive Dialogue Strategy           │
│  （承接→释疑→场景→转化 四步话术编排）                      │
└───────────────────────────┬─────────────────────────────┘
                            │ 消费
┌───────────────────────────▼─────────────────────────────┐
│  L2 知识网络层 · beauty 知识图谱 + BGE/PPR 召回            │
│  （认知推演：激活思考 → 子图 → chunks / nodes / edges）    │
└───────────────────────────┬─────────────────────────────┘
                            │ 实例化
┌───────────────────────────▼─────────────────────────────┐
│  L3 解题模型层 · 智能体技能（Skill）                       │
│  智能客服 · 写文档 · 做短视频 …（独立大类下的多个 Skill）    │
└─────────────────────────────────────────────────────────┘
```

### 2.1 与入库方案 §14.3.3 契约对齐

认知推演已实现的 **activate → propagate → package** 不变；智能客服在 **package 之后** 增加 **dialogue_compose** 步骤：

```text
activate(query, category=beauty)     → seeds[]              # 已有：BGE ANN
propagate(seeds, ppr_*)              → subgraph + chunks  # 已有：PPR + 子图
package(subgraph, max_tokens)        → prompt_context     # 已有：结构化上下文
dialogue_compose(context, L1_rules)  → staged_script      # 新增：渐进式话术
human_review(optional)               → final_message      # 客服视图：确认发送
```

**原则**：L2 召回逻辑 **不重写**；L1 策略层 **只读** L2 产物并 **改写表达**。

---

## 三、与现有能力边界

| 能力 | 入口 | 输出形态 | 与本文关系 |
|------|------|----------|-----------|
| **认知唤醒推演** | 技能集 · 认知唤醒推演 | 子图 + 召回文本 + 联想日志 | **L2 引擎视图**；工程师调试 |
| **需求雷达 analyze** | 微信消息 → `POST /api/analyze` | DemandEvent + 需求摘要 | **不同场景**：监听挖需，非客服话术 |
| **M3-S1 被动学习** | 知识库 · 添加资料 | 挂接 beauty 图 | **扩充 L2**，非 L3 |
| **M5-1 主动学习** | ai_agent · 链接学习 | 技术文档 ingest | **暂缓**；与 beauty 客服无关 |
| **M6-1 美妆客服** | 智能体 · 智能客服 Skill | **分步人话话术** + 可选链接 | **本文** |

**红线（与 PRD 一致）**：

- 美妆客服 **可以** 调用服务端 LLM 做 **话术润色**（L1/L3 职责）
- 美妆客服 **必须** 基于 L2 召回事实，**禁止幻觉编造** 成分、功效、价格
- 与 analyze **解耦**：客服 Skill 不走 DemandEvent 链路（除非产品后续显式打通）

---

## 四、渐进式对话策略（PDS · L1 核心）

### 4.1 四步框架

| 步骤 | 名称 | 原则 | 图谱数据来源 |
|------|------|------|-------------|
| **Step 1** | **承接与安抚** | 先共情、后科普；建立信任，避免机器感 | 意图词 + 种子 section 痛点词 |
| **Step 2** | **分步释疑** | 子弹头短句；禁止超三行大段 | concept / section 属性 · 成分 · 注意事项 |
| **Step 3** | **场景化关联** | 从「答题」到「生活方式 / 解决方案」 | `related_product` · `appears_in` 文章 · 搭配商品 |
| **Step 4** | **精准推荐** | 帮用户做决定，给出明确下一步 | PPR 高权重商品节点 · 活动信息（若有） |

### 4.2 实验样例（定稿文案）

**用户触发源**：`面膜补水怎么样，敏感肌能用吗`

---

**Step 1 · 承接与安抚**

> 亲，看来您是换季皮肤有点干痒，想找款温和的补水面膜对吧？

*（系统 OS：已从子图确认意图 = 补水 + 敏感肌顾虑，进入共情模式。）*

---

**Step 2 · 分步释疑（知识软化）**

> 这款「断黑王」面膜主要有这几个特点，我帮您捋一下：
>
> - **成分挺安心的**：不含酒精和香精，对咱们敏感肌比较友好。  
>   *← 图谱：concept / section · 成分属性*
> - **补水锁水力不错**：里面有玻尿酸成分，敷完皮肤水润度能维持挺久。  
>   *← 图谱：concept · 功效*
> - **有个小提醒**：部分极敏感肌第一次用，建议先在耳后试一下哦。  
>   *← 图谱：注意事项节点 / 评价摘要*

---

**Step 3 · 场景化关联（软性植入）**

> 既然是在找补水面膜，我这边还有两个小建议给您参考下：
>
> - **搭配建议**：光敷面膜可能不够，您平时有在用保湿精华吗？这款修护精华和那个面膜绝配，很多顾客都是一套买的 👉 [点击查看详情]  
>   *← 图谱：`related_product` / MP3 文章带货*
> - **避坑指南**：其实敏感肌除了补水，更重要的是「修护屏障」，这篇我们主编写的文章您可以看看 👉 [点击阅读文章]  
>   *← 图谱：`appears_in` 跨文档 / MP5*

---

**Step 4 · 精准推荐（临门一脚）**

> 综合您的需求，首推您试试这款「尚雅秀断黑王」，目前正好有活动。  
> 您是想直接了解购买链接，还是需要我再帮您对比一下另一款？

*← 图谱：PPR Top 商品 seed + 高关联 section*

---

### 4.3 输出约束（Constraints · 写入 Prompt）

| # | 规则 | 说明 |
|---|------|------|
| C1 | **段落上限** | 单段不超过 3 行；长知识拆成 bullet |
| C2 | **成分人话化** | `透明质酸钠` → 「深层抓水因子」；保留括号原文供审核 |
| C3 | **行动节奏** | 每解答约 3 个知识点，给出 1 个行动建议（买 / 看 / 试） |
| C4 | **链接克制** | Step 1–2 不出链接；Step 3–4 才给 👉 链接，且每条配一句理由 |
| C5 | **事实锚定** | 每条 bullet 须能映射到 `chunk_id` 或 `graph_node`（可追溯） |
| C6 | **不确定降级** | 子图无足够依据时，明确说「我帮您确认一下」而非编造 |

### 4.4 角色设定（Role Play · 写入 Prompt）

```text
你是一位从业 5 年的资深美妆顾问，性格温柔、专业、话术接地气。
你在微信私域里服务顾客，像朋友一样给建议，而不是念产品说明书。
你熟悉敏感肌、补水、抗老等常见诉求，但不会夸大功效或做医疗承诺。
```

---

## 五、L2 召回：复用认知推演

### 5.1 输入

| 字段 | 来源 | 示例 |
|------|------|------|
| `query` | 用户问题 / 触发源 | 面膜补水怎么样，敏感肌能用吗 |
| `category` | 固定 `beauty` | beauty |
| `ann_top_k` | 神经元种子数 | 20（默认） |
| `ppr_top_n` | 联想广度 | 30 |
| `max_chunks` | 认知产出量 | 20 |
| `ppr_l2c` | L2-c 有向 PPR | true |
| `ppr_concept_bridge` | 概念跨品 | 按场景 |

### 5.2 输出（供 L1 消费）

沿用 `KnowledgeSearchView` / 认知推演结果：

| 产物 | 用途（PDS 映射） |
|------|-----------------|
| `nodes[]` + `edges[]` | Step 3 跨文档 / 搭配可视化依据 |
| `chunks[]`（含 `is_seed`） | Step 2 事实 bullet |
| `retrieval_trace` | 工程师视图 · 话术溯源 |
| `media[]` | 可选：发图话术 |

### 5.3 节点 → 话术槽位映射

| 节点类型 | 营销槽位 | PDS 步骤 |
|----------|----------|----------|
| `section`（商品详情） | 成分、功效、用法、注意事项 | Step 2 |
| `concept` | 人话标签、同概念多品 | Step 2 / 4 |
| `document`（文章） | 阅读场景、避坑指南 | Step 3 |
| `document`（商品） | 主推款、活动 | Step 4 |
| `media` | 效果图、质地展示 | Step 3（可选） |
| `evaluate` | 社交证明 | Step 2（远期 MP4） |

---

## 六、L3 智能体大类与技能规划

### 6.1 产品信息架构（知识库地图扩展）

在现有 **「商业垂直 / 技术智能体」** 二分之外，新增 **「智能体技能」** 维度（产品层，可与 `ai_agent` 类目或独立 Skill 集共存）：

```text
知识库地图
├── [商业垂直]  beauty · drink · …        ← L2 领域知识
├── [技术智能体] ai_agent                  ← 技术栈 L2（M5 暂缓）
└── [智能体技能]  ← 新增（L3 解题模型）
      ├── 智能客服（beauty）    ← M6-1 首技能 · POC
      ├── 写文档               ← 远期
      ├── 做短视频             ← 远期
      └── …
```

> **说明**：L3 技能 **不替代** L2 类目；智能客服 Skill **读取** beauty L2，**输出** 话术模板实例。

### 6.2 技能清单（M6 系列）

| Skill ID | 名称 | 依赖 L2 | 状态 | 说明 |
|----------|------|---------|------|------|
| `beauty-cs-agent` | **美妆智能客服** | `beauty` | **POC** | 本文 · PDS 四步话术 |
| `beauty-doc-writer` | 美妆写文档 | `beauty` | 远期 | 培训稿 / FAQ / 朋友圈文案 |
| `beauty-short-video` | 美妆短视频脚本 | `beauty` + media | 远期 | 口播稿 + 分镜 |
| `generic-cs-agent` | 通用客服（其他垂直） | drink 等 | 远期 | 复用 L1 策略，换 L2 |

### 6.3 L3 技能包结构（建议）

每个 L3 技能在 `{data_root}/agent_skills/{skill_id}/` 维护：

```text
agent_skills/beauty-cs-agent/
├── manifest.json          # 技能元数据、依赖 category、版本
├── role_play.md           # 角色设定
├── constraints.json       # C1–C6 输出约束
├── pds_steps.yaml         # 四步模板 + 槽位定义
├── phrasebook.json        # 成分人话化词典、常见安抚句
└── examples/              # Few-shot 样例（含「面膜+敏感肌」）
```

**首版可硬编码在服务端 Prompt 模板**，待 POC 稳定后再外置为技能包。

---

## 七、图谱节点「营销属性」标签（L2 增强 · 非阻塞）

为使 PDS 精准抓槽，建议在 L2 节点 / chunk metadata 逐步补齐 **营销标签**（ingest 或 M2-3 后处理写入 `props_json` / chunk metadata）：

| 标签键 | 适用节点 | 示例值 | 服务步骤 |
|--------|----------|--------|----------|
| `audience` | section / document | 敏感肌、熟龄肌 | Step 1 共情 |
| `selling_point` | section | 无酒精、玻尿酸补水 | Step 2 |
| `caution` | section | 耳后试用、孕妇慎用 | Step 2 |
| `reading_scene` | document(文章) | 避坑、入门、搭配 | Step 3 |
| `bundle_with` | document(商品) | goods_65 | Step 3 搭配 |
| `cta_type` | document(商品) | 链接 / 对比 / 试用 | Step 4 |

**POC 阶段**：无标签时由 LLM 从 chunk 正文推断，但 **C5 事实锚定** 仍要求回落到 chunk_id。

---

## 八、界面与页面编排（定稿 · 2026-07-02）

### 8.1 方案结论：**新技能 + 同页 Tab 切换 + 工程师路由保留**

| 决策项 | 定稿 |
|--------|------|
| 技能集 | **新增独立卡片** `beauty-cs-agent`（美妆智能客服），**不替换**现有「认知唤醒推演」卡片 |
| 主入口 | `/skills/beauty-cs-agent` · 默认 Tab = **客服话术** |
| 视图切换 | **同一页面顶部 Segmented Control**：`客服话术` \| `认知推演` |
| 工程师深链 | **`/skills/knowledge-graph` 原样保留**，不删不改默认行为 |
| 状态共享 | 两 Tab **共用一次「激活思考」结果**（query + 子图 + chunks），切换 Tab **不重复请求** |
| 话术生成 | 仅在 **客服话术 Tab** 触发 `dialogue/compose`（工程师 Tab 不调 LLM 话术） |

**为何不用两个完全独立页面？**

- 客服与工程师看的是 **同一次认知推演**；拆成两个路由会导致重复点「激活思考」、子图不一致。
- **Tab 切换**零成本看图谱/debug，符合「剑鞘与刀在同一鞘里」的隐喻。

**为何不把工程师视图只放在 Tab、删掉旧路由？**

- 知识库页、图谱 Tab 已链到 `/skills/knowledge-graph`；**保留旧路由 = 零回归**。
- 研发同学习惯直进工程师视图，不必经过客服壳。

---

### 8.2 技能集卡片（SkillSetPage）

| 顺序 | id | 卡片标题 | 路由 | 图标 | 说明 |
|------|-----|----------|------|------|------|
| 1 | `beauty-cs-agent` | **美妆智能客服** | `/skills/beauty-cs-agent` | 💬 | **新增** · PDS 四步话术 · 默认客服 Tab |
| 2 | `knowledge-graph` | **认知唤醒推演** | `/skills/knowledge-graph` | 🕸️ | **保留** · 纯工程师视图（建议同步改卡片文案，与页内标题一致） |
| … | `yuanbao-doc-graph-link` | 好好学习 | … | 📎 | 不变 |

**卡片描述（beauty-cs-agent 建议文案）**：

> 输入顾客问题 → 认知推演召回 beauty 知识网 → **渐进式四步人话话术**；可切换「认知推演」Tab 查看子图与传导日志。

---

### 8.3 路由与深链

| 路由 | 组件 | 默认 Tab | 用途 |
|------|------|----------|------|
| `/skills/beauty-cs-agent` | `BeautyAgentSkillPage.vue` | `service` | 智能客服主入口 |
| `/skills/beauty-cs-agent?tab=engineer` | 同上 | `engineer` | 从客服页切到工程师 Tab 的可分享链接 |
| `/skills/knowledge-graph` | `KnowledgeGraphSkillPage.vue` | — | 独立工程师视图（**现有页面保持**） |
| `/skills/knowledge-graph?q=…` | 同上 | — | 带 query 深链（可选增强） |

**跨路由跳转（可选增强，POC 可后做）**：

- 客服页工程师 Tab 底部：「在独立窗口打开认知推演 →」→ `/skills/knowledge-graph?q=encodeURIComponent(query)`
- 独立工程师页顶栏：「在智能客服中生成话术 →」→ `/skills/beauty-cs-agent?q=…`

---

### 8.4 页面结构：BeautyAgentSkillPage（Shell）

```text
┌─ 美妆智能客服 ──────────────────────────────────────────────────────────┐
│ ← 技能集          [ 客服话术 | 认知推演 ]          beauty · 渐进式四步   │  ← Segmented Control
├─────────────────────────────────────────────────────────────────────────┤
│ 【共用区 · 两 Tab 均可见】                                                │
│ 设定触发源：[ 面膜补水怎么样，敏感肌能用吗     ]  [ 激活思考 ]              │
│ ▼ 检索规模参数（默认折叠，与现页相同）                                      │
├─────────────────────────────────────────────────────────────────────────┤
│ 【Tab 内容区 · v-show / KeepAlive】                                       │
│                                                                          │
│  ══ Tab A：客服话术（默认） ══                                              │
│  ┌─ 话术选项 ─────────────────────────────────────────────────────┐    │
│  │ ☑ 渐进式四步  ☑ 成分人话化  ☐ 附带商品链接   [ 生成话术 ]          │    │
│  └────────────────────────────────────────────────────────────────┘    │
│  ┌─ 左：子图缩略（可折叠） ──┬─ 右：智能话术生成框 ─────────────────────┐  │
│  │ 30 节点 · 43 边          │ Step 1 承接与安抚                        │  │
│  │ [ 展开大图 ]             │ Step 2 分步释疑 …                        │  │
│  └──────────────────────────┴ [ 复制全部 ] [ 逐步复制 ] [ 展开依据 ] ──┘  │
│                                                                          │
│  ══ Tab B：认知推演（工程师 · 与现 KnowledgeGraphSkillPage 同构） ══       │
│  · 激活联想知识网（全高图谱）                                               │
│  · 联想过程日志 + 思考过程回放                                              │
│  · 召回文本 / 相关图片双栏                                                  │
│  （布局与现有页一致，便于对照调试）                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**共用区行为**：

1. 用户点 **「激活思考」** → 调用现有 `knowledge.search` → 结果写入 **`useBeautyAgentSession()`**（composable）
2. 切 Tab **不重新 search**；工程师 Tab 直接读 session 里的 `nodes/edges/chunks/trace`
3. 客服 Tab 点 **「生成话术」** → 在已有 search 结果上调用 `dialogue/compose`（可只传 `query` + 服务端内部复搜，或传 search 结果摘要以减少重复计算——实现期择优）

---

### 8.5 组件拆分（客户端 · 实现指引）

为 **最小改动现有页**，推荐分两步：

| 阶段 | 做法 |
|------|------|
| **POC v1** | 新建 `BeautyAgentSkillPage.vue` Shell + Tab；工程师 Tab **内嵌**现有区块（从 `KnowledgeGraphSkillPage` **复制**一份模板，或 `import` 子组件后两页共用） |
| **POC v2  refactor** | 抽取共用块，避免双份维护 |

**目标文件结构**：

```text
renderer/
├── pages/
│   ├── BeautyAgentSkillPage.vue          # 新 · Shell + Tab
│   └── KnowledgeGraphSkillPage.vue       # 保留 · 独立工程师入口
├── components/skills/beauty-agent/
│   ├── BeautyAgentSharedTrigger.vue      # 触发源 + 三参数 + 激活思考
│   ├── BeautyCsDialoguePanel.vue         # Tab：客服话术
│   ├── BeautyEngineerPanel.vue           # Tab：认知推演（从现页抽取）
│   └── DialogueStepCard.vue              # 单步话术卡片 + 溯源
└── composables/
    └── useBeautyAgentSession.ts          # query / result / composeResult / loading
```

| 组件 | 职责 |
|------|------|
| `BeautyAgentSharedTrigger` | 两 Tab 共用输入；按钮文案统一为 **「激活思考」** |
| `BeautyCsDialoguePanel` | 话术选项 + 左缩略图 + 右四步卡片 + 复制 |
| `BeautyEngineerPanel` | 现 `KnowledgeGraphSkillPage` 主体（图谱 + 日志 + 召回文本） |
| `useBeautyAgentSession` | 单次 session 状态机：`idle → searching → ready → composing → done` |

**KnowledgeGraphSkillPage 处理原则**：

- **不删除、不改变默认 UI**；后续可改为薄包装：`SharedTrigger` + `BeautyEngineerPanel` only
- 知识库页「认知唤醒推演」按钮 **继续指向** `/skills/knowledge-graph`

---

### 8.6 Tab 切换 vs 其他方案（对比记录）

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **A · 同页 Tab（采用）** | 一次激活、两视图；客服/研发切换零成本 | Shell 页略复杂 | ✅ **M6-1 采用** |
| B · 两个独立路由、无 Tab | 实现简单 | 重复激活、状态不同步 | ❌ |
| C · 仅新页面、工程师用外链跳转 | 旧页零改动 | 切换丢上下文、体验割裂 | ❌ 作深链补充即可 |
| D · 合并进 knowledge-graph 加 Tab | 只维护一个路由 | **客服与工程师入口混淆**；改坏现页风险 | ❌ |

---

### 8.7 客服 Tab 线框（细化）

```text
┌─ 话术选项 ────────────────────────────────────────────────────────────┐
│ ☑ 渐进式四步  ☑ 成分人话化  ☐ Step3 起附带链接   [ 生成话术 ]（二次动作）  │
│ 说明：需先「激活思考」成功；生成话术仅调用 LLM，不重跑 PPR（理想路径）      │
└────────────────────────────────────────────────────────────────────────┘
┌─ 左 28% ──────────────────┬─ 右 72%：智能话术 ─────────────────────────┐
│ ▼ 认知子图缩略（默认折叠）   │ ┌ Step 1 · 承接与安抚 ──────────── [复制] ┐ │
│   30 节点 · 43 边           │ │ 亲，看来您是换季…                        │ │
│   [ 展开 → 切工程师 Tab ]   │ └ 依据：intent 推断 · 种子 section       ┘ │
│                             │ ┌ Step 2 · 分步释疑 ────────────── [复制] ┐ │
│                             │ │ • 成分挺安心的…  ⓘ chunk_26_s2         │ │
│                             │ └────────────────────────────────────────┘ │
│                             │ … Step 3 / 4 …                             │
│                             │ [ 复制全部 ]  [ Step1→4 逐步复制 ]          │
└─────────────────────────────┴──────────────────────────────────────────┘
┌─ 溯源条（可折叠）──────────────────────────────────────────────────────┐
│ chunk: niushop_product_26_s2 · concept: 玻尿酸 · goods: 65  [ 展开列表 ] │
└────────────────────────────────────────────────────────────────────────┘
```

**交互要点**：

- 客服 **默认折叠**左栏子图；需要 debug 时一键 **「展开 → 切工程师 Tab」**
- 每条 bullet 旁 **ⓘ** hover 显示 `chunk_id` / 原文片段
- **「生成话术」**与 **「激活思考」**分两个按钮：前者依赖后者成功（disabled 态明确提示）
- 「逐步复制」按 Step 1→4 依次写入剪贴板并 toast 提示（MVP 手工发微信）

---

### 8.8 知识库页入口（已实现）

| 位置 | 行为 |
|------|------|
| 知识库页右上 · `beauty` 大类 | **智能客服** → `/skills/beauty-cs-agent` |
| 原 **「认知唤醒推演」** | 仍 → `/skills/knowledge-graph` |

两入口并存，对应两种角色默认视图。

---

## 九、后台 Prompt 编排（定稿 · 2026-07-02）

### 9.0 实现拍板

| 项 | 定稿 |
|----|------|
| **compose 是否复搜** | **否** — 客户端提交 `search_snapshot`（工程师 Tab 同次激活结果） |
| **LLM 调用** | **单次** DeepSeek → 四步 JSON + evidence |
| **Tab** | `BeautyAgentSkillPage`：`客服话术` \| `认知推演`，共用 `useBeautyAgentSession` |

### 9.1 单次 LLM

| 阶段 | 输入 | 输出 | 模型 |
|------|------|------|------|
| **话术生成** | `search_snapshot` + query + L1 约束 | JSON：四步 `steps` + `evidence` + `full_text` | DeepSeek（服务端） |

~~两阶段抽取+生成~~ — POC 不采用，改为单次调用（`dialogue_compose.py`）。

### 9.2 抽取 JSON Schema（示意）

```json
{
  "intent": "sensitive_skin_hydrating_mask",
  "pain_points": ["换季干痒", "敏感肌顾虑"],
  "facts": [
    {
      "claim": "不含酒精和香精",
      "humanized": "成分挺安心的，对敏感肌比较友好",
      "source_chunk_id": "niushop_product_26_s2",
      "pds_step": 2
    }
  ],
  "recommendations": [
    {
      "type": "product",
      "label": "尚雅秀断黑王",
      "goods_id": "65",
      "reason": "PPR 种子 + 补水概念匹配",
      "pds_step": 4
    }
  ],
  "content_links": [
    {
      "type": "article",
      "title": "敏感肌屏障修护指南",
      "doc_id": "article_12",
      "pds_step": 3
    }
  ]
}
```

### 9.3 API 草案（新增 · 不影响现有 search）

```http
POST /api/knowledge/dialogue/compose
```

**请求体须含 `search_snapshot`**（来自同 session 的 `knowledge/search` 响应摘取，服务端 **不再** 调用 PPR/BGE）：

```json
{
  "query": "面膜补水怎么样，敏感肌能用吗",
  "category": "beauty",
  "skill_id": "beauty-cs-agent",
  "search_snapshot": {
    "chunks": [],
    "nodes": [],
    "edges": [],
    "media": []
  },
  "options": {
    "humanize_ingredients": true,
    "include_links_from_step": 3
  }
}
```

**响应**：

```json
{
  "ok": true,
  "data": {
    "steps": [
      { "step": 1, "title": "承接与安抚", "text": "亲，看来您…" },
      { "step": 2, "title": "分步释疑", "text": "…" }
    ],
    "full_text": "…",
    "evidence": [ { "chunk_id": "…", "used_in_step": 2 } ],
    "subgraph_summary": { "nodes": 38, "edges": 43 },
    "retrieval_trace_ref": "…"
  }
}
```

**实现策略**：`compose_beauty_cs_dialogue()` **仅读** `search_snapshot` + LLM；**不修改** 现有 `knowledge/search`。

---

## 十、M6-1 POC 实验范围

### 10.1 In Scope（首版）

- 类目：**beauty**
- 技能：**智能客服**（PDS 四步）
- 输入：手动输入触发源（与认知推演相同）
- 输出：四步话术 + 依据溯源
- 视图：**BeautyAgentSkillPage** 双 Tab（客服默认）；`/skills/knowledge-graph` **独立保留**

### 10.2 Out of Scope（首版不做）

- 微信自动发送 / 监听触发
- 多轮对话记忆（working memory）
- drink / 其他垂直类目
- 写文档、短视频技能
- 图谱营销标签批量补写（可用 LLM 推断代替）

### 10.3 成功标准

| # | 标准 | 验收方式 |
|---|------|----------|
| S1 | 同一 query，客服视图话术 **明显比 raw chunks 更可读** | 人工评审 ≥4/5 |
| S2 | 四步结构完整，Step 1 无链接、Step 4 有明确 CTA | 规则检查 |
| S3 | 每条核心 claim 可映射 `chunk_id` | 溯源面板 |
| S4 | 与认知推演子图 **同一 query 节点数一致** | 对比测试 |
| S5 | 无医疗承诺、无虚构成分 | 红线清单 |

---

## 十一、开发任务拆分（供排期 · 本次不写代码）

| ID | 任务 | 端 | 依赖 |
|----|------|-----|------|
| M6-1-1 | `dialogue_compose` Prompt 模板 + JSON Schema | 服务端 | knowledge_search |
| M6-1-2 | `POST /api/knowledge/dialogue/compose` | 服务端 | M6-1-1 |
| M6-1-3a | `useBeautyAgentSession` + `BeautyAgentSharedTrigger` | 客户端 | knowledge.search |
| M6-1-3b | `BeautyCsDialoguePanel`（客服 Tab） | 客户端 | M6-1-2 |
| M6-1-3c | `BeautyEngineerPanel`（工程师 Tab · 从现页抽取） | 客户端 | M6-1-3a |
| M6-1-3d | `BeautyAgentSkillPage` Shell + Tab + 路由 | 客户端 | M6-1-3b/c |
| M6-1-4 | 技能集新卡片 `beauty-cs-agent`；`knowledge-graph` 卡片文案同步为「认知唤醒推演」 | 客户端 | M6-1-3d |
| M6-1-5 | 话术溯源 UI（chunk hover / 溯源条） | ✅ | 客服 Tab 溯源区 |
| M6-1-6 | `verify_m61.py` | ✅ | 2026-07-02 自测通过 |
| M6-1-7 | `beauty_cs_phrasebook.json` | ✅ | `salesagent/config/` |
| M6-1-8 | `KnowledgeGraphSkillPage` 薄包装 | ✅ | 共用 Trigger + EngineerPanel |

**估时**：POC **约 1–1.5 周**（不含自动发送）。

---

## 十二、与 M5 主动学习的关系

| 项 | M5-1 主动学习 | M6-1 美妆客服 |
|----|--------------|--------------|
| 优先级 | **暂缓**（用户后续重点沟通） | **当前方向** |
| 类目 | ai_agent | beauty |
| 目标 | 外链知识 ingest | 已有 L2 → 人话话术 |
| 元宝 | 抓取 + 综合 | POC 不需要（服务端 LLM 即可） |

---

## 十三、修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-02 | **§8 页面编排定稿**：新技能卡片 + 同页 Tab（客服\|工程师）+ `/skills/knowledge-graph` 独立保留；组件拆分与 session 共享 |
| 2026-07-02 | **待补项完成**：verify_m61、phrasebook、KnowledgePage 入口、KnowledgeGraphSkillPage 共用 refactor |
| 2026-07-02 | **§8 页面编排定稿** + **§9 实现拍板** |
