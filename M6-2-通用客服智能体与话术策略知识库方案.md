# M6-2 — 通用客服智能体 + 话术策略知识库（技能集知识库）

> **阶段**：MVP M6（智能体 · 技能层 · 二期架构）  
> **状态**：**已落地**（2026-07-02）  
> **前置**：[M6-1 美妆客服 POC](M6-1-美妆客服智能体-渐进式对话方案.md) ✅（beauty 绑定版，效果已验收满意）  
> **关联**：[知识库-结构化入库方案 §14.3.3](知识库-结构化入库方案.md)；[产品设计文档 §5.5.5](产品设计文档-谛听.md)；[开发计划-总览 §14.5](开发计划-总览.md)

---

## 一、要解决的问题

### 1.1 M6-1 的局限（不是效果问题）

M6-1 美妆客服 POC **效果满意**（四步渐进式话术 + 认知子图 + 溯源），但架构上是 **绑死** 的：

```text
beauty 认知子图（L2）  +  PDS 话术规则（L1，硬编码在 dialogue_compose.py）
        ↓
「美妆智能客服」专用 Skill
```

若按同样方式做 **生鲜、家电、服装** 客服，会重复开发：

- 多个 Skill 页面
- 多套 Prompt 硬编码
- 策略与垂直知识耦死在代码里

### 1.2 M6-2 目标

> **一个通用客服智能体引擎** + **话术策略进知识库** + **垂直大类只换认知子图**  
> 输出效果与 M6-1 **100% 同级**（四步、人话、可溯源、不胡编）。

核心拆分：

| 知识类型 | 放哪 | 回答什么问题 |
|----------|------|-------------|
| **垂直业务知识** | **[商业垂直]** L2 | 这个商品/成分/文章 **是什么** |
| **技术栈知识** | **[AI智能]** L2 | 视频生成、大模型对接 **怎么配** |
| **技能方法论** | **[技能集知识库]** L1/L3 | **怎么说 / 怎么写 / 怎么拍** |

---

## 二、知识库地图：三栏并列（产品定稿）

### 2.1 布局演进

**现状（两栏）**：

```text
┌─ 知识库地图 ─────────────────────────────────────────────┐
│  [商业垂直]                    [技术智能体]                │
│  beauty · drink …              ai_agent                    │
└──────────────────────────────────────────────────────────┘
```

**定稿（三栏）**：

```text
┌─ 知识库地图 ───────────────────────────────────────────────────────────────────┐
│  [商业垂直]              [AI智能]              [技能集知识库]                  │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────────────────┐  │
│  │ beauty       │      │ ai_agent     │      │ 💬 智能客服 · 话术策略    │  │
│  │ 960 条       │      │ 0 条         │      │    ├ 渐进式四步 PDS       │  │
│  │ 商城+资料    │      │ 主动/被动    │      │    ├ 美妆导购话术         │  │
│  ├ 防晒 51 …   │      │ ├ 视频 0     │      │    ├ 生鲜客服话术（远期）  │  │
│  └──────────────┘      │ ├ 大模型 0   │      │ 📝 写文章（远期）         │  │
│  drink（未启用）        │ └ …          │      │ 🎬 做视频（远期）         │  │
│  生鲜（远期）           │              │      │ 📺 做直播（远期）         │  │
│  家电（远期）           │              │      └──────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────────────┘
  图例：🟦 被动学习  🟩 主动学习  🟧 技能策略（人工维护+版本）  ⚪ 未启用
```

### 2.2 命名变更

| 原称 | 定稿 | 说明 |
|------|------|------|
| **技术智能体** | **AI智能** | 仅改产品展示名；`category` 代码仍可 `ai_agent` |
| （无） | **技能集知识库** | 新增第三栏；与侧栏「技能集」菜单概念对齐，但是 **可投喂/可检索的知识** |

### 2.3 三栏职责边界

| 栏位 | Chroma / 图模型 | 典型内容 | 是否进需求雷达 |
|------|-----------------|----------|----------------|
| **商业垂直** | `beauty` / `drink` / … | 商品、文章、概念、评价、upload | ✅ beauty 可进 analyze |
| **AI智能** | `ai_agent` | 技术文档、API 笔记、主动学习外链 | ❌ |
| **技能集知识库** | `agent_skills`（建议 code） | PDS 母版、行业话术策略、写稿模板、分镜脚本 | ❌ |

**原则**：技能集知识库管 **「怎么做一件事的方法论」**；商业垂直管 **「这件事涉及哪些事实」**。二者在运行时 **拼装**，不合并存储。

---

## 三、三层认知架构（M6-2 版）

```text
┌─────────────────────────────────────────────────────────────────┐
│  L3 · 通用客服智能体（一个 Skill 引擎）                           │
│  输入：垂直类目 + 用户问题 → 输出：四步话术（与 M6-1 同级）         │
└────────────────────────────┬────────────────────────────────────┘
                             │
          ┌──────────────────┴──────────────────┐
          ▼                                      ▼
┌──────────────────────┐              ┌──────────────────────┐
│ L1 · 技能集知识库     │              │ L2 · 商业垂直知识网     │
│ strategy_id 检索     │              │ category 认知推演       │
│                      │              │                      │
│ · PDS 四步母版        │              │ · beauty 子图         │
│ · 角色/约束/词典      │              │ · 生鲜 子图（远期）     │
│ · 行业话术策略变体    │              │ · 家电 子图（远期）     │
└──────────────────────┘              └──────────────────────┘
```

与 M6-1 对照：

| 层 | M6-1（POC） | M6-2（目标） |
|----|-------------|--------------|
| L1 | `dialogue_compose.py` 内嵌 Prompt | **`agent_skills` 向量库 + 策略包 JSON** |
| L2 | 写死 `beauty` | 运行时 **`domain_category`** 参数 |
| L3 | `beauty-cs-agent` 专用页 | **`通用客服智能体`** + 类目选择 |

---

## 四、技能集知识库：类目与子技能

### 4.1 顶层技能大类（技能集内）

| skill_family | 展示名 | 状态 | 说明 |
|--------------|--------|------|------|
| `cs_dialogue` | **智能客服 · 话术策略** | **M6-2 首期** | 渐进式对话 PDS + 行业变体 |
| `doc_writer` | 智能写作 | 远期 | **方案定稿** → [M6-3](M6-3-智能写作技能-文案脚手架方案.md) |
| `video_script` | 做视频 | 远期 | 口播稿 + 分镜 |
| `live_stream` | 做直播 | 远期 | 直播话术 + 节奏 |

每个 skill_family 在知识库地图 **技能集栏** 占一张卡片；卡片内 `sub_categories` 展示子策略条目数。

### 4.2 智能客服 · 话术策略（首期明细）

| strategy_id | 名称 | 适用垂直 | 说明 |
|-------------|------|----------|------|
| `pds_v1` | **渐进式四步母版** | 全垂直通用 | Step1–4 框架、C1–C6 约束、角色人设 |
| `cs_beauty_v1` | 美妆导购话术 | `beauty` | 成分人话化、敏感肌共情、搭配推荐语气 |
| `cs_fresh_v1` | 生鲜客服话术 | `fresh`（远期） | 保鲜、配送时效、产地话术 |
| `cs_appliance_v1` | 家电客服话术 | `appliance`（远期） | 参数对比、安装售后、能效说明 |

**检索逻辑（运行时）**：

```text
strategy_pack = retrieve(agent_skills,
                        skill_family=cs_dialogue,
                        strategy_id IN [pds_v1, cs_{domain_category}_v1],
                        top_k=3)
```

先取 **母版** `pds_v1`，再取 **行业变体**（有则叠加，无则仅母版）。

---

## 五、通用客服智能体：运行时流程

### 5.1 与 M6-1 对齐的链路

```text
① 用户选择：垂直大类 domain_category（如 beauty）+ 输入顾客问题 query
② 激活思考（L2 · 不变）
   POST /api/knowledge/search  →  search_snapshot（子图，不复搜）
③ 获取话术策略（L1 · 新增）
   GET/POST 策略检索  →  strategy_pack（PDS 母版 + 行业变体）
④ 合成话术（L3 · 引擎复用）
   POST /api/knowledge/dialogue/compose
        domain_category + query + search_snapshot + strategy_pack
⑤ 输出：四步话术 + evidence（证据仍只来自 L2 chunk_id）
```

**与 M6-1 的差异仅在于 ③**：策略从知识库来，不再写死在 Python。

### 5.2 Prompt 拼装（两段式 · 效果等价）

```text
[System]
{strategy_pack.role_play}
{strategy_pack.constraints}
{strategy_pack.pds_steps}

[User]
垂直大类：{domain_category}
顾客问题：{query}

【话术策略补充】
{strategy_pack.industry_hints}

【认知子图召回 · 唯一事实来源】
{search_snapshot 格式化 chunks/nodes/edges}
```

**红线（继承 M6-1）**：

- 事实 **只能** 来自 `search_snapshot`（L2）
- 策略 **只能** 规定语气、结构、节奏，**不能** 编造成分/价格
- 不走 `POST /api/analyze`

### 5.3 界面（技能集）

| 项 | M6-1 | M6-2 |
|----|------|------|
| Skill 名 | 美妆智能客服 | **通用客服智能体** |
| 路由 | `/skills/beauty-cs-agent` | `/skills/cs-agent`（建议） |
| 类目 | 写死 beauty | **下拉：beauty / 生鲜 / 家电…** |
| Tab | 客服话术 \| 认知推演 | **不变** |
| 工程师深链 | `/skills/knowledge-graph` | **不变** |

M6-1 页面可 **重定向** 到通用页 `?domain=beauty`，避免破坏已有书签。

---

## 六、话术策略知识库：数据模型

### 6.1 存储布局

```text
{data_root}/agent_skills/
├── cs_dialogue/                    # skill_family
│   ├── pds_v1/
│   │   ├── manifest.json           # 元数据、版本、适用垂直
│   │   ├── role_play.md
│   │   ├── constraints.json        # C1–C6
│   │   ├── pds_steps.yaml          # 四步定义 + 槽位
│   │   └── examples/               # Few-shot
│   ├── cs_beauty_v1/
│   │   ├── manifest.json
│   │   ├── industry_hints.md
│   │   └── phrasebook.json         # 成分人话化（从 beauty_cs_phrasebook 迁入）
│   └── cs_fresh_v1/                # 远期
├── doc_writer/                     # 远期
├── video_script/
└── live_stream/
```

### 6.2 Chroma 类目 `agent_skills`

| metadata 字段 | 说明 |
|---------------|------|
| `skill_family` | `cs_dialogue` / `doc_writer` / … |
| `strategy_id` | `pds_v1` / `cs_beauty_v1` / … |
| `domain_category` | `*`（通用）或 `beauty` / `fresh` / … |
| `doc_type` | `role_play` / `constraint` / `step_def` / `example` / `phrasebook` |
| `version` | `1.0.0` |
| `source` | `skill_strategy_manual` |

**向量化**：每个 markdown/json 片段一条 chunk；支持 BGE 检索「与当前 query 最相关的策略段落」（如敏感肌场景命中美妆行业 hints）。

### 6.3 manifest.json 示例

```json
{
  "strategy_id": "cs_beauty_v1",
  "skill_family": "cs_dialogue",
  "name": "美妆导购话术",
  "domain_category": "beauty",
  "extends": "pds_v1",
  "version": "1.0.0",
  "enabled": true
}
```

`extends: pds_v1` 表示运行时 **先加载母版，再叠加本策略**。

---

## 七、API 演进（相对 M6-1）

### 7.1 策略检索（新增）

```http
GET /api/knowledge/skills/strategies?skill_family=cs_dialogue&domain_category=beauty
```

或

```http
POST /api/knowledge/skills/retrieve
{
  "skill_family": "cs_dialogue",
  "domain_category": "beauty",
  "query": "面膜补水 敏感肌",
  "top_k": 5
}
```

**响应**：`strategy_pack`（合并后的 role、constraints、steps、hints 文本）。

### 7.2 话术合成（扩展 M6-1）

```http
POST /api/knowledge/dialogue/compose
{
  "query": "...",
  "domain_category": "beauty",
  "skill_id": "universal-cs-agent",
  "strategy_pack": { ... },           // 可选：客户端已检索则传入
  "strategy_ids": ["pds_v1", "cs_beauty_v1"],  // 或只传 id 由服务端检索
  "search_snapshot": { "chunks": [], "nodes": [], "edges": [], "media": [] },
  "options": { "humanize_ingredients": true, "include_links_from_step": 3 }
}
```

**原则**：

- `search_snapshot` **仍由客户端传入，服务端不复搜**（M6-1 拍板不变）
- `strategy_pack` 可由服务端根据 `strategy_ids` 自动加载（推荐）

### 7.3 知识库地图 API（扩展）

```http
GET /api/knowledge/map-overview
```

**响应扩展**：

```json
{
  "commercial": [ ... ],
  "ai_intelligence": [ ... ],
  "skill_set": [ ... ],
  "categories": [ ... ]
}
```

| 字段 | 说明 |
|------|------|
| `commercial` | 原 `commercial` 不变 |
| `ai_intelligence` | 原 `technical` 改名（兼容期可双字段） |
| `skill_set` | **新增**：技能集各大类卡片 |

---

## 八、与侧栏「技能集」菜单的关系

| 概念 | 侧栏 `/skills` | 知识库「技能集知识库」栏 |
|------|----------------|-------------------------|
| 作用 | **执行入口**（跑 Skill、看进度） | **知识沉淀**（策略、模板、版本） |
| 例子 | 通用客服、认知推演、好好学习 | PDS 母版、美妆话术策略、写稿模板 |
| 关系 | 执行时 **读取** 知识库中的策略 | 运营 **维护** 策略文档并 ingest |

```text
技能集菜单（执行）          技能集知识库（智识）
     │                            │
     ├─ 通用客服智能体  ──读取──►  cs_dialogue/*
     ├─ 认知唤醒推演    ──读取──►  （仅用商业垂直 L2）
     ├─ 好好学习        ──读取──►  （商业垂直 L2）
     └─ 写文章（远期）  ──读取──►  doc_writer/*
```

---

## 九、从 M6-1 迁移路径（不丢效果）

| 步骤 | 内容 | 风险 |
|------|------|------|
| M6-2-1 | 将 `dialogue_compose.py` 内 Prompt 抽为 `pds_v1` + `cs_beauty_v1` 入库 | 低：对照 M6-1 输出 diff |
| M6-2-2 | `compose` 改为读 `strategy_pack`；保留无 pack 时走旧逻辑 fallback | 低 |
| M6-2-3 | 地图三栏 UI + `ai_intelligence` 改名 | 纯 UI |
| M6-2-4 | `beauty-cs-agent` → `cs-agent?domain=beauty` 重定向 | 低 |
| M6-2-5 | `verify_m62.py`：同 query 对比 M6-1/M6-2 四步结构与 evidence | 验收 |

**验收标准（与 M6-1 S1–S5 对齐）**：

- 同一 `query` + 同一 `search_snapshot`，M6-2 四步完整、可溯源、无医疗承诺
- 人工评审话术质量 **不低于** M6-1 基线

---

## 十、开发任务拆分

| ID | 任务 | 端 | 依赖 |
|----|------|-----|------|
| M6-2-1 | `agent_skills` 目录 + ingest 管线 | 服务端 | categories 配置 |
| M6-2-2 | 导入 `pds_v1` / `cs_beauty_v1`（从 M6-1 提炼） | 内容 | M6-2-1 |
| M6-2-3 | `GET/POST skills/retrieve` API | 服务端 | M6-2-1 |
| M6-2-4 | `dialogue/compose` 接 `strategy_pack` | 服务端 | M6-2-3 |
| M6-2-5 | 地图 API `skill_set` + UI 三栏 + **AI智能** 改名 | 双端 | map_overview |
| M6-2-6 | `UniversalCsAgentPage` + 类目下拉 | 客户端 | M6-2-4 |
| M6-2-7 | `verify_m62.py` + M6-1 输出对照 | 脚本 | M6-2-4 |
| M6-2-8 | 技能集知识库 Tab（投喂/预览策略文档） | 客户端 | M6-2-1 |

**远期（不写进首期）**：`doc_writer` / `video_script` / `live_stream` 仅保留地图占位卡片。

**估时**：首期 **约 2 周**（含策略入库 + 通用页 + 地图三栏）。

---

## 十一、配置与 categories.json 建议

在 `shared/config/categories.json`（或等价配置）增加维度：

```json
{
  "code": "agent_skills",
  "name": "技能集知识库",
  "icon": "🧩",
  "enabled": true,
  "knowledge_map_column": "skill_set",
  "knowledge_only": true,
  "sub_categories": [
    { "code": "cs_dialogue", "label": "智能客服" },
    { "code": "doc_writer", "label": "写文章", "enabled": false },
    { "code": "video_script", "label": "做视频", "enabled": false },
    { "code": "live_stream", "label": "做直播", "enabled": false }
  ]
}
```

商业垂直新增大类时 **只加 commercial 条目**；技能集新增「写直播话术策略」时 **只加 agent_skills 子类 + 策略文件**，无需新 Skill 工程。

---

## 十二、修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-02 | 初稿：三栏知识库地图；技能集知识库；通用客服智能体；策略与 L2 解耦；M6-1 迁移路径 |
