# M6-3-1 — 智能写作 · 子图事实清单与元宝闭环方案

> **阶段**：M6-3 质量增强（二期）  
> **状态**：**已定稿 · 待开发**（2026-07-03 用户拍板）  
> **前置**：[M6-3 智能写作技能](M6-3-智能写作技能-文案脚手架方案.md) ✅ 首期已落地  
> **触发**：运营勾选「尚雅绣体验套 + 断黑王 4 件套」大类全貌子图后，DeepSeek 生成文案 **漏产品、漏促销、无配图**。

---

## 一、问题复盘（你的案例）

### 1.1 运营意图

| 勾选索引 | 角色 | 期望在文中的位置 |
|----------|------|------------------|
| **重磅主推【尚雅绣钻石光感体验套】** | 小套盒 · **体验引流品** | 开头共情、低门槛尝试、种草 |
| **断黑王钻石光感 4 件套** | 正装大套盒 | 核心功效、成分证据、主 CTA |
| **关联文档**（子图内 `related_product` / `appears_in`） | 399 双套餐 ×2、99 钜惠包 | 结尾促销、组合购、限时福利 |
| **媒体节点**（含 VLM 描述 + 适用场景） | 礼盒、质地、场景图 | 文中 **图文并茂**，非纯文字墙 |

### 1.2 实际输出问题

1. **单产品叙事**：全文围绕体验套，**断黑王 4 件套几乎未出现**。  
2. **促销文档丢失**：399 套餐、99 钜惠等 **关联文档卖点未写入**。  
3. **图片未用**：子图内大量 `media` 节点（描述、场景）**未进入成文**。  
4. **体感**：「子图很丰富，文案很贫瘠」——与运营预期严重不符。

---

## 二、根因分析（对照现有实现）

> 结论：**不是子图没数据，而是「写作前事实清单」不完整 +「合成器」过度简化**。

### 2.1 数据侧：大类全貌子图其实有货

勾选两棵文档树并「展示全部」后，`overviewGraph` 已合并：

- `document` / `section` / `concept` / `media`  
- `appears_in` 跨品、`related_product` 挂接促销文  

点网右侧也能看到图片 **描述 + 适用场景**（元宝识图结果）。

### 2.2 写作链路的三个「漏斗」

```text
overviewGraph（62 节点 · 158 边）
    ↓ buildGraphSnapshot
graph_snapshot（nodes/edges/chunks/media）
    ↓ slot_extractor（服务端）
slot_values（≈15 个占位符，单 product_name）
    ↓ 单次 DeepSeek + 脚手架 prompt
article（四维纯文字）
```

| 漏斗 | 代码/行为 | 丢失什么 |
|------|-----------|----------|
| **F1 快照** | `buildGraphSnapshot` 从节点拼 `chunks`；**关联文档段落**若未挂在勾选树下，可能只在 `related_product` 边可达 | 促销文全文、评价 chunk |
| **F2 抽取** | `slot_extractor._primary_documents` **只取第一个根文档** 作 `product_name` | 断黑王、第二 SKU 叙事 |
| **F2 抽取** | `is_cross_doc` / `is_related_product` 文档 **不参与** primary 列表 | 399/99 促销文档 |
| **F2 抽取** | `_pick_media_slots` **顺序取前 N 张图**，未按 `vlm_description` / `use_cases` 匹配段落 | 场景图、对比图 |
| **F3 合成** | LLM 上下文 = 截断后的 `slot_values` + 子图摘要（节点 ≤40） | 长尾事实、多产品对照、图位编排 |
| **UI** | 「切片明细（debug）」展示的是 **类目 Chroma 向量**，不是 **当前勾选子图** | 运营无法核对「将要喂给写作的事实」 |

### 2.3 与「切片明细」的错位

当前 `KnowledgePage` 底部 **切片明细（debug）**：

- 数据源：`libraryState.vectors`（全库向量预览）  
- **与** 勾选子图 **无关**  

因此它 **不能** 作为写作前的质检面板——这也是你感觉「信息没记录下来」的重要原因之一。

---

## 三、目标与原则

### 3.1 目标

| # | 目标 |
|---|------|
| G1 | 写作前，运营能 **100% 看见** 即将参与成文的事实（节点、边、文本、图链、场景） |
| G2 | 多产品 + 促销关联文档 **强制进入** 叙事结构（体验引流 → 正装转化 → 套餐 CTA） |
| G3 | 成文 **图文并茂**（Markdown 内嵌图片 URL + 图注 + 槽位） |
| G4 | **可选** 走本机元宝生成，**不消耗** 服务端 DeepSeek token |
| G5 | 元宝产出 **可存储、可预览、可溯源**（非一次性剪贴板） |

### 3.2 原则

1. **事实只来自子图**——不编造价格、链接、疗效；缺失则省略或「请咨询」。  
2. **不把 `M6-3.md` 整篇扔给元宝**——编译为 **《写作任务书》**（≤3KB 结构化指令 + 事实附录）。  
3. **DeepSeek compose 逻辑保留但不走主路径**（未来演示可一键启用）。  
4. **先修事实清单，再换模型**——换元宝前，Fact Sheet 必须先对。

---

## 四、方案总览

```text
┌─ 认知图谱 Tab ─────────────────────────────────────────────────────────┐
│ 勾选文档 → 展示全部 → 【子图事实清单】（新，替代 debug 切片明细）          │
│   · 按节点类型分色 Tab（document/section/concept/media/关联文档）          │
│   · 每条：node_id · 文本预览 · 出边/入边 · 图 URL · VLM 描述 · 场景       │
│   · 统计：根文档 N · 促销文档 M · 图片 K · 边 158                         │
├────────────────────────────────────────────────────────────────────────┤
│ ✍️ 生成文案 → 选标题结构 → 【写作任务书预览】→ 发送元宝（主路径）          │
└────────────────────────────────────────────────────────────────────────┘
         │
         ▼
  《写作任务书》+《子图事实附录》
  UIA 粘贴发送 → 元宝生成 → UIA「复制」读剪贴板 → 解析入库
  （article_compose / DeepSeek 保留代码，默认不调用）
         │
         ▼
  解析 → 存 salesagent/data/articles/{category}/{id}.json
         → 客户端「文案预览」图文并茂展示
```

---

## 五、模块 A：子图事实清单（Subgraph Fact Sheet）

> **建议命名**：「子图事实清单」或「写作素材清单」——**不叫**「切片明细」，避免与 Chroma 向量混淆。

### 5.1 位置

| 项 | 定稿 |
|----|------|
| 区域 | 知识图谱 Card **底部**（原 `切片明细（debug）` 位置） |
| 默认 | **展开**（写作场景核心质检区） |
| 数据源 | **当前** `overviewGraph`（或勾选子树合并结果），**不是** 全库 vectors |

### 5.2 UI 风格（对齐图 3「联想过程日志」）

参考 `RetrievalTraceLog.vue`：

```text
┌─ 子图事实清单 ────────────────────────────────────────────────┐
│ 62 节点 · 158 边 · 根文档 2 · 促销文档 3 · 图片 17 · [复制 JSON] │
│ [全部] [document] [section] [concept] [media] [关联文档] [边]   │
├────────────────────────────────────────────────────────────────┤
│ #1 [document/根] goods_xxx · 尚雅绣钻石光感体验套 · 段12 图8    │
│ #2 [document/根] goods_yyy · 断黑王钻石光感4件套 · 段15 图9    │
│ #3 [document/促销] article_zz · 399白钻逆龄双效王炸套装 · …     │
│ #4 [media] niushop_product_59_media_0 · url=https://…           │
│      vlm=银色礼盒内含滴管瓶… · scene=回答包装与实物对比            │
│ #5 [section→concept/mentions] section:…s3 --mentions--> concept:补水 │
│ …                                                              │
└────────────────────────────────────────────────────────────────┘
```

### 5.3 节点类型配色（与点网图例一致）

| node_type | 颜色 | 清单中必展示字段 |
|-----------|------|------------------|
| `document`（根） | 蓝 | label、node_id、section/media 计数、是否引流/正装标签（可推断） |
| `document`（关联/促销） | 紫 | label、挂接边 `related_product` 来源、促销关键词高亮 |
| `section` | 绿 | preview_text（全文，可折叠）、chunk_id |
| `concept` | 青 | label、被哪些 section mentions |
| `media` | 橙 | **url**、vlm_description、use_cases、所属 document |
| `edge` | 灰字 | source --rel--> target |

### 5.4 服务端/客户端分工

**首期建议纯客户端**：从 `overviewGraph` + 已缓存子树 **序列化** 为 `fact_sheet.json`，无需新 API。

可选增强：服务端 `POST /api/knowledge/graph/fact-sheet` 补全 DB 中 section 全文、评价节点（若子图 UI 未展开）。

### 5.5 验收

- 勾选尚雅绣 + 断黑王后，清单中 **≥2 条根 document**、**≥3 条促销/关联 document**（含 399/99）、**≥10 条 media 含 url**。

---

## 六、模块 B：《写作任务书》编译器（给元宝，不是整篇 M6-3）

### 6.1 为什么不能直接扔 `M6-3.md`

| 问题 | 说明 |
|------|------|
| 太长 | 500+ 行方案文档含排期、API、历史——干扰元宝 |
| 非执行态 | 元宝需要 **本次任务** 的结构、禁忌、素材附录 |
| 不可版本化 | 应与 `agent_skills/doc_writer` **同源**，自动编译 |

### 6.2 《写作任务书》结构（建议 ≤3500 字）

```yaml
# 由 compile_writing_brief() 生成，存于内存/临时文件，不提交 git

task:
  category: beauty
  template: beauty_article_v1
  title_formula: "痛点问句 + 产品名 + 利益点"
  root_products:
    - role: trial_pack      # 体验引流
      name: 尚雅绣钻石光感体验套
      node_id: goods_xxx
    - role: main_pack       # 正装主推
      name: 断黑王钻石光感4件套
      node_id: goods_yyy
  promo_docs:               # 见 §6.5 促销/商品识别与深度检索
    - name: 399白钻逆龄双效王炸套装
      node_id: goods_59
      goods_id: 59
    - ...

scaffold:                   # 来自 agent_skills/scaffold_v1 + beauty_article_v1
  sections:
    - role: opening
      template: "…"
    - role: body_rational
    - role: body_scene
    - role: closing
  typography:
    max_line_chars: 20
    emoji: true
  forbidden: [治愈, 根治, 100%有效]

facts_appendix:             # 来自 Fact Sheet 压缩版（见 6.3）
  documents: [...]
  sections: [...]
  media: [...]
  edges_summary: [...]

output_contract:            # 元宝必须遵守的输出格式
  format: markdown
  require_images: true      # 至少 4 处 ![alt](url)
  require_dual_product: true  # 体验套 + 正装均须出现
  require_promo: true       # 399/99 套餐须出现（若有素材）
  evidence:                 # 每段标注 [node_id]
```

### 6.3 事实附录压缩策略

元宝上下文有限，附录采用 **分层**：

1. **必达层**（全量）：2 个根 document 的 meta + 各 Top5 section 原文 + 全部促销 document 标题与关键段 + 全部 media（url + vlm + scene）。  
2. **增强层**（截断）：其余 section 取前 200 字；concept 列表；边关系摘要（按 meta-path 分组）。  
3. **禁止层**：未出现在 fact_sheet 的价格/链接 **不得写**。

### 6.4 编译器实现位置

| 组件 | 路径（建议） |
|------|----------------|
| `fact_sheet_builder.ts` | 客户端：overviewGraph → FactSheet |
| `writing_brief_compiler.py` 或 `.ts` | 合并 `agent_skills` + FactSheet + 标题公式 → 任务书 Markdown |
| `promo_enricher.py` | 服务端：按 goods_id / 索引名补全促销素材（见 6.5） |
| 配置 | 复用 `scaffold_v1` / `beauty_article_v1`，**不**读 `docs/M6-3.md` |

### 6.5 促销 / 商品识别与深度检索（已定稿）

> **用户拍板**：商品节点有 **goods_id**（如 `goods_59` ↔「399白钻逆龄双效王炸套装」）；按 **索引名** 可唯一定位商品信息；是否深度查关联 **取决于写作意图**——仅介绍单品时子图内 `related_product` 可能够用；要写「商品 + 套餐推荐」完整文，则需 **额外检索**。

#### 6.5.1 商品 vs 促销文档 识别规则

| 信号 | 判定 | 示例 |
|------|------|------|
| `node_id` 匹配 `goods_\d+` | **商品节点** | `goods_59` |
| `label` / 索引名与 Chroma `niushop_product_*` 一致 | 商品元数据 | 「399白钻逆龄双效王炸套装」 |
| 子图内 `related_product` 边可达的 `document` | **关联促销/套餐文档** | 99 钜惠包、399 焕白套餐 |
| 标题含 `399` / `99` / `钜惠` / `套餐` / `超值` | 促销文档辅助标签 | 规则兜底，非唯一依据 |

**不以** 标题关键词 **单独** 判定促销——以 **goods_id + 索引名 + 子图边** 为主。

#### 6.5.2 素材覆盖分级（写作任务书编译时选用）

```text
L0 勾选子图 fact_sheet          ← 必做，运营可审计
L1 related_product 边内文档      ← 子图已挂接的促销文
L2 索引名 → goods_id 反查        ← 同名商品 meta / 评价 / 媒体
L3 深度关联扩展（按需）            ← 套餐包全文、跨品 appears_in、评价 chunk
```

| 写作模式 | 启用层级 | 说明 |
|----------|----------|------|
| **单品种草** | L0 + L1 | 体验套 / 正装各写一段即可 |
| **双品转化** | L0 + L1 + L2 | 尚雅绣 + 断黑王 + 子图促销 |
| **完整带货文**（商品 + 套餐推荐 + 评价） | L0 ~ **L3** | 用户案例默认此档 |

#### 6.5.3 L2 / L3 检索接口（草案）

| 方法 | 路径 | 输入 | 输出 |
|------|------|------|------|
| POST | `/api/knowledge/product/resolve` | `label` 或 `goods_id` | 商品 node_id、meta、media 列表 |
| POST | `/api/knowledge/promo/enrich` | `root_doc_ids` + `depth=l3` | 补全 fact_sheet：促销 document / section / media / **评价 chunk** / **跨品 appears_in** |

**深度开关**：生成文案弹窗 **「含套餐推荐（深度检索）」** 默认 **开**（L3）。

#### 6.5.4 L3 深度检索范围（已定稿 · 用户确认）

> **用户确认（2026-07-03）**：L3 **包含评价 chunk、跨品 appears_in**，写作素材要 **足够丰富**，宁可多给、由任务书压缩，不可漏给。

| 数据源 | 检索方式 | 进入 fact_sheet 的字段 |
|--------|----------|------------------------|
| **评价 chunk** | `niushop_evaluate_*` / Chroma `source=niushop_evaluate`，按 `goods_id` 或商品名关联 | `chunk_id`、星级、评价全文、关联 `goods_id` |
| **跨品 appears_in** | 从 L0/L1/L2 节点沿 `appears_in` 边 BFS（深度 ≤2） | 跨品 document label、section 摘要、挂接来源边 |
| **促销套餐全文** | L1 `related_product` + L2 `goods_*` 反查后补 section | 套餐标题、价格卖点段、CTA 句 |
| **媒体** | 上述 document 下全部 `media` 节点 | url、vlm_description、use_cases |
| **概念** | section --mentions--> concept | label、所属段落 |

**原则**：

1. **只增不删**：L3 结果 **合并进** L0 fact_sheet，不去掉子图已有节点。  
2. **去重键**：`node_id` / `chunk_id` / `url` 三选一主键。  
3. **上限（防爆上下文）**：评价 Top **8** 条/商品（按星级+长度）；appears_in 跨品 document Top **6**；超出部分进「增强层」200 字截断（见 6.3）。  
4. **清单可见**：L3 补全条目在「子图事实清单」中以 `[l3/enrich]` 标签区分，运营可核对。

```text
promo_enrich(root_doc_ids, depth=l3):
  seeds ← L0 根文档 + L1 related_product 目标
  for goods in seeds where goods_id:
    += product/resolve(goods_id)           # L2 meta + media
    += chroma.query(niushop_evaluate, goods_id)  # 评价 chunk
  for node in seeds:
    += graph.bfs(node, rel=appears_in, max_hops=2)  # 跨品
  dedupe → merge into fact_sheet
```

#### 6.5.5 写入任务书的契约

- `root_products[]`：用户勾选的 **goods_\*** 根文档，带 `role: trial_pack | main_pack`。  
- `promo_docs[]`：L1 边内 + L2/L3 补全的促销商品/文档，**去重** 后全量进入 `facts_appendix`。  
- `evaluations[]`：L3 评价 chunk，供元宝写「真实口碑」段。  
- `cross_refs[]`：L3 appears_in 跨品摘要，供对比/组合叙事。  
- `output_contract.require_promo`：当 `promo_docs.length > 0` 时为 `true`。  
- `output_contract.require_social_proof`：当 `evaluations.length > 0` 时为 `true`（引用评价，不编造）。

---

## 七、模块 C：元宝写作闭环

### 7.1 流程（你的方案 + 工程细化）

```text
1. 用户点「开始生成」
2. 客户端生成 fact_sheet + writing_brief.md
3. 弹窗：「写作任务书预览」+ [复制] [打开元宝并粘贴发送]
4. 复用 yuanbao_doc_graph.driver.send_document_prompt（已有 UIA 发送能力）
5. 等待元宝回复（两种模式，见 7.2）
6. 回写入库 → 文案预览
```

### 7.2 元宝回复获取（已定稿：UIA 自动读）

**拍板**：复用现有元宝 UIA 能力，**精确定位「复制」按钮 → 读剪贴板**，与文档挂接 Skill 同源。

| 能力 | 现有代码 | 智能写作用途 |
|------|----------|--------------|
| 发送 prompt | `yuanbao_doc_graph.driver.send_document_prompt` | 粘贴《写作任务书》并点发送 |
| 滚动至最新回复 | `_scroll_reply_for_copy` | 长 Markdown 回复底部「复制」可见 |
| 定位复制按钮 | `_locate_latest_copy_button` | 取聊天区最靠下 assistant 旁的「复制」 |
| 读剪贴板 | `_copy_reply_via_copy_button` | 轮询直至剪贴板有完整回复 |
| 等待稳定 | `wait_for_link_reply` 轮询模式 | 改造为 `wait_for_article_reply` |

#### 7.2.1 新增 `wait_for_article_reply`（相对挂接 JSON 的差异）

挂接 Skill 校验 `knowledge_graph_link_v1` JSON；写文案 Skill 校验 **Markdown 文章**：

```python
def _is_complete_article(text: str) -> bool:
    # 1. 长度 ≥ 800（可配置）
    # 2. 含一级标题 # 或 ## 
    # 3. 含 ≥2 处 ![...](http...)  图片语法
    # 4. 不含 knowledge_graph_link_v1（避免误读挂接 JSON）
    # 5. 可选：含任务书要求的 root product 关键词
```

流程：

```text
send_document_prompt(任务书)
  → wait_for_article_reply
       loop: scroll → locate「复制」→ click → read clipboard
       until _is_complete_article(clip) or timeout
  → parse_article_markdown(clip) → ingest API
```

#### 7.2.2 失败降级（不调用 DeepSeek）

| 失败 | 处理 |
|------|------|
| UIA 超时 | 弹窗展示剪贴板 partial + **「手动粘贴入库」** 按钮 |
| 回复缺图 | 提示重试或手动补图后粘贴 |
| 元宝窗口未找到 | 阻塞并引导启动元宝 |

**不** 自动 fallback 到 DeepSeek（代码保留，设置项 `doc_writer.use_deepseek_fallback: false`）。

### 7.3 Token 与成本

| 路径 | DeepSeek 服务端 | 元宝 |
|------|-----------------|------|
| 现状 M6-3 | ✅ 每次 compose 消耗 | — |
| 本方案主路径 | ❌ 不调用（零 token） | ✅ 用户本机额度 + UIA 闭环 |
| 保留 | `article_compose` 代码在库，配置可开 | 未来演示 / A/B |

---

## 八、模块 D：结果存储与图文预览

### 8.1 存储

```text
salesagent/data/articles/
  beauty/
    20260703_尚雅绣断黑王_A1.json
    20260703_尚雅绣断黑王_A1.md
```

**JSON 结构**（与现有 `article_compose` 响应对齐，便于预览组件复用）：

```json
{
  "article_id": "20260703_尚雅绣断黑王_A1",
  "category": "beauty",
  "source": "yuanbao_desktop",
  "title_formula": "A",
  "root_doc_ids": ["goods_xxx", "goods_yyy"],
  "fact_sheet_hash": "sha256:…",
  "title": "…",
  "full_markdown": "…",
  "image_slots": [{ "role": "cover", "url": "…", "caption": "…" }],
  "evidence": [],
  "created_at": "2026-07-03T10:54:00+08:00"
}
```

### 8.2 API（草案）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/knowledge/article/ingest` | 客户端粘贴元宝结果 + metadata 入库 |
| GET | `/api/knowledge/article/{id}` | 预览拉取 |
| GET | `/api/knowledge/article/list?category=beauty` | 历史文案（二期） |

### 8.3 预览增强

- Markdown 渲染 **内嵌图片**（`![caption](url)`）  
- 图注显示 `use_cases`  
- 顶部标签：**元宝生成** / **DeepSeek 降级** / **双品+促销**

---

## 九、对你原方案的采纳与优化

| 你的建议 | 评审 | 优化后定稿 |
|----------|------|------------|
| 切片明细换成节点/边/图全记录 | ✅ 方向正确 | 改名为 **子图事实清单**；数据源改为 **勾选子图** 而非 Chroma |
| UI 像联想日志 | ✅ | 复用 `RetrievalTraceLog` 交互模式 + 节点类型 Tab |
| 点生成后打开元宝 | ✅ | 复用 `send_document_prompt` + **`wait_for_article_reply` UIA 复制** |
| 脚手架给元宝 | ✅ | 编译为 **《写作任务书》**，来自 `agent_skills`，非 `M6-3.md` |
| 结果存服务端再展示 | ✅ | `data/articles/` + `article/ingest` API |
| 不消耗 DeepSeek | ✅ 作主路径 | **DeepSeek 代码保留、默认不走**；UIA 失败时 **手动粘贴**，不自动切 DeepSeek |
| 促销按 goods_id + 索引名 | ✅ | **L0~L3 分级检索**；完整带货文默认 L3 |
| — | ➕ 补充 | **双品叙事契约**（trial + main + promo）写进任务书与验收 |
| — | ➕ 补充 | 先修 `slot_extractor` 多产品逻辑，即使不走元宝也能改善 |

---

## 十、分期与任务

| ID | 任务 | 端 | 依赖 | 估时 |
|----|------|-----|------|------|
| M6-3-1-1 | `fact_sheet_builder` + 子图事实清单 UI | 客户端 | overviewGraph | 3d |
| M6-3-1-2 | `writing_brief_compiler`（agent_skills → 任务书） | 双端 | M6-3-1-1 | 2d |
| M6-3-1-3 | 生成文案弹窗：任务书预览 + UIA 发元宝 + `wait_for_article_reply` | 客户端 | yuanbao driver | 3d |
| M6-3-1-4 | UIA 复制回传 + `article/ingest` API + 手动粘贴兜底 | 双端 | M6-3-1-3 | 2d |
| M6-3-1-8 | `promo_enricher` + `product/resolve` + 深度检索开关 | 服务端 | M6-3-1-1 | 2d |
| M6-3-1-5 | 预览图文并茂 + 历史列表 | 客户端 | M6-3-1-4 | 2d |
| M6-3-1-6 | `slot_extractor` 多产品/促销增强（降级路径） | 服务端 | — | 2d |
| M6-3-1-7 | `verify_m631.py` 尚雅绣+断黑王+399 验收 | 脚本 | 现网数据 | 1d |

**建议迭代顺序**：**1 → 8 → 2 → 3 → 4 → 5 → 7**（M6-3-1-6 slot_extractor 增强 **暂缓**，DeepSeek 默认不走）

首期 **1+8+2** 保证事实清单与促销补全；**3+4+5** 完成元宝 UIA 闭环。

---

## 十一、验收标准（针对你的案例）

| # | 标准 |
|---|------|
| V1 | 事实清单含 **2 根文档 + ≥3 促销文档 + 图片 url** |
| V2 | 成文 **同时出现** 体验套与断黑王，且角色正确（引流 vs 正装） |
| V3 | 成文提及 **399 套餐 / 99 钜惠**（事实清单有则必写，无则不写） |
| V4 | 正文 **≥4 张图** 以 Markdown 图片语法嵌入，且 url 来自子图 media |
| V5 | 人工评审可读性 **≥4/5**（运营主观） |
| V6 | 元宝主路径 **零** `article/compose` DeepSeek 调用 |
| V7 | UIA「复制」成功拉取 Markdown，或手动粘贴兜底可用 |
| V8 | 勾选「含套餐推荐」时，399/99 促销 + **评价 chunk** + **跨品 appears_in** 均进入事实清单与任务书 |

---

## 十二、已定稿决策

| ID | 决策 | 定稿 |
|----|------|------|
| **D1** | 元宝回复回收 | ✅ **UIA 自动读**：`_locate_latest_copy_button` → 剪贴板；新增 `wait_for_article_reply` |
| **D2** | DeepSeek | ✅ **主路径走元宝，DeepSeek 代码保留、默认不调用**；未来演示可配置开启 |
| **D3** | 事实清单位置 | ✅ **图谱 Tab 底部**（替换 debug 切片明细）— 沿用原推荐，用户未异议 |
| **D4** | 促销 / 商品识别 | ✅ **goods_id + 索引名**；L1 子图边；**L3 含评价 chunk + 跨品 appears_in**（用户确认） |

### 12.1 风险（UIA 自动读）

| 风险 | 缓解 |
|------|------|
| 元宝 UI 改版导致「复制」定位失败 | 与挂接 Skill 共用 driver，一改俱改；保留手动粘贴 |
| 长文 Markdown 剪贴板截断 | 轮询 + stable_ms；校验 `_is_complete_article` |
| L3 检索过慢 | 异步进度条；Fact Sheet 先展示 L0，L2/L3 增量合并 |

---

## 十三、修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-03 | 草案：子图事实清单 + 写作任务书 + 元宝闭环；根因对照 slot_extractor / 切片明细错位 |
| 2026-07-03 | **定稿**：D1 UIA 复制闭环；D2 DeepSeek 保留不走；D4 goods_id + L0~L3 促销检索 |
| 2026-07-03 | **L3 范围确认**：含评价 chunk、跨品 appears_in；素材宁丰勿漏 |

---

## 十四、关联文档

- [M6-3 智能写作技能 · 文案脚手架方案](M6-3-智能写作技能-文案脚手架方案.md)  
- [M6-1 美妆客服 · 联想过程日志](M6-1-美妆客服智能体-渐进式对话方案.md)（RetrievalTraceLog 参考）  
- [M3-S2 知识图谱 Tab](M3-S2-知识库大类图谱Tab方案.md)
