# M6-3 — 智能写作技能 · 文案脚手架（知识子图 → 信任状文章）

> **阶段**：MVP M6（智能体 · 技能层 · 三期）  
> **状态**：**M6-3 已落地**（2026-07-02）· M6-3-1~7 ✅  
> **质量增强（二期）**：[M6-3-1 子图事实清单与元宝闭环](M6-3-1-智能写作-子图事实清单与元宝闭环方案.md) · 🚧 首期已编码
> **前置**：[M6-2 通用客服 + 技能集](M6-2-通用客服智能体与话术策略知识库方案.md) ✅ · [M3-S2 知识图谱 Tab](M3-S2-知识库大类图谱Tab方案.md) ✅  
> **关联**：[知识库-结构化入库方案 §十四](知识库-结构化入库方案.md)；[M6-1 美妆客服](M6-1-美妆客服智能体-渐进式对话方案.md)

---

## 一、需求理解（产品定稿）

### 1.1 一句话

> 在 **知识图谱**里勾选若干文档/商品索引，以生成的 **「大类全貌子图」** 为事实基础，由 **智能写作** Skill 输出一篇 **结构完整、可溯源、偏营销心理学** 的图文文章。  
> 写作方法论进 **技能集 · 智能写作** 策略库；事实 **只** 来自子图节点与边，不胡编。

### 1.2 与 M6-1 / M6-2 的关系

| 技能 | 输入 | 策略来源 | 输出 |
|------|------|----------|------|
| **M6-1/2 智能客服** | 用户问题 + beauty 子图 | `cs_dialogue` 话术策略（PDS） | 四步对话话术 |
| **M6-3 智能写作** | 用户勾选子图 + 写作意图 | `doc_writer` 文案脚手架策略 | 标题 + 正文四段 + 配图建议 + 溯源 |

共性：**L2 认知子图复用**（`search` / `category-overview` / 勾选子树），**L1 技能策略进 `agent_skills`**，**单次 LLM 合成、服务端不复搜**（与 M6-1 compose 原则一致）。

### 1.3 角色定位

运营侧不是「写手」，而是 **内容架构师**：

- 在图谱里 **选索引、看全貌**
- 选 **文案模板**（首期：美妆四维脚手架）
- 系统把 **节点/边 → 占位符 → 文章**，人只做审核与微调

---

## 二、核心策略：知识子图 → 信任状

### 2.1 子图里有什么

| 图谱要素 | 典型节点 `node_type` | 典型边 `rel_type` | 文章中的角色 |
|----------|----------------------|-------------------|--------------|
| **产品** | `document`（商品详情） | `contains` | 主角、CTA 对象 |
| **段落/卖点** | `section` | `mentions` | 证据句、场景描写素材 |
| **成分/功效** | `concept` | `mentions` | 理性说服、比喻源 |
| **图片** | `media` | `appears_in` | 封面/细节/场景/对比 |
| **关联文章** | `document` | `related_product` / `appears_in` | 社会证明、延伸阅读 |
| **评价/痛点** | `section` / `concept` | `mentions` | 开头共情、吐槽引用 |

### 2.2 文章四维目标（营销心理学）

```text
吸引眼球（标题）→ 建立信任（痛点+方案）→ 激发欲望（场景+证据）→ 促成行动（CTA）
```

**原则**：用 **图谱数据做骨架**（可溯源 `chunk_id` / `node_id`），用 **脚手架模板做血肉**（语气、结构、修辞），**禁止** 编造成分、价格、疗效承诺。

---

## 三、文案脚手架：「四维立体模板」（美妆首期）

一篇文章固定 **四段 + 配图位**，每段有 **占位符**；运行时从子图快照填充。

### 3.1 标题：悬念或利益点

| 公式 | 结构 | 占位符 |
|------|------|--------|
| **A** | `[痛点] + [解决方案]` | `{pain_point}` `{product_name}` |
| **B** | `[产品名] + [惊人效果]` | `{product_name}` `{key_benefit}` |
| **C** | `[人群] + [专属福利]` | `{audience}` `{offer_hook}` |

**图谱取值**：

- `{pain_point}`：`concept`（干燥/敏感/暗沉）或 `section` 中的问题描述
- `{product_name}`：`document.label` / `goods_name`
- `{key_benefit}`：`concept` 功效 + `section` 卖点句
- `{audience}`：图谱 `适用人群` 类 concept 或 metadata

**输出约束**：≤ 30 字为宜；不得出现医疗承诺（治愈、根治等）。

### 3.2 开头：共情 + 痛点放大

**模板骨架**：

```text
是不是有时候觉得，{empathy_scene}？（共情）
特别是在 {pain_scenario}，{pain_symptom}……（痛点场景化）
别担心，你不是一个人。其实，{transition_to_solution}（引出方案）
```

**图谱取值**：

| 占位符 | 来源 |
|--------|------|
| `{empathy_scene}` | 用户评价类 `section` 或商品 FAQ |
| `{pain_scenario}` | 换季/熬夜/晒后等——来自 `section` 或运营配置的 `scene_tags` |
| `{pain_symptom}` | `concept` + 关联 `section` 原文（可截取，须溯源） |
| `{transition_to_solution}` | 固定话术 + `{product_name}` |

### 3.3 中间：证据链 + 场景化（核心）

分 **两条并行叙事线**，可各 1–2 段：

#### 段落模板 A：成分党（理性说服）

```text
为什么它能解决你的问题？秘密就在它的「配方」里。
它里面添加了 {hero_ingredient}，{ingredient_metaphor}。
同时，还有 {support_ingredient}，{support_metaphor}。
```

| 占位符 | 来源 |
|--------|------|
| `{hero_ingredient}` | `concept`（如透明质酸钠） |
| `{ingredient_metaphor}` | 技能策略 `phrasebook` 人话化（海绵、灭火器等） |
| `{support_ingredient}` | 子图次要 `concept` |
| `{support_metaphor}` | 策略库行业比喻 |

#### 段落模板 B：场景化体验（感性说服）

```text
想象一下，{scene_setup}。（场景）
{sensor_detail}。（感官）
{result_after_use}。（结果）
```

| 占位符 | 来源 |
|--------|------|
| `{scene_setup}` | 睡前/晒后/约会前——`section` 或策略 `scene_bank` |
| `{sensor_detail}` | 质地、清凉感等——`section` 原文改写 |
| `{result_after_use}` | 功效 `concept` + 评价 `section`（不编造周期与疗效） |

### 3.4 结尾：行动号召 + 风险逆转

```text
{summary_one_liner}
现在入手 {product_name}，{offer_detail}，还能 {bonus_value}。
{cta_text}
```

| 占位符 | 来源 |
|--------|------|
| `{offer_detail}` | 商品 `section` / metadata 中的价格、折扣、赠品——**无则不得编造** |
| `{bonus_value}` | 关联文档或固定运营话术（须标注是否来自图谱） |
| `{cta_text}` | 策略库 CTA 模板 + 购买链接占位（链接须来自 `document` metadata） |

---

## 四、图文排版规范（输出结构）

### 4.1 图片选择（从 `media` 节点）

| 槽位 | 数量 | 选取规则 |
|------|------|----------|
| **封面** | 1 | 产品全景、光线明亮、礼盒感 |
| **细节** | 1–2 | 滴管/瓶身/质地特写 |
| **场景** | 1–2 | `appears_in` 关联、氛围强 |
| **对比** | 0–1 | 若有使用前后素材则优先 |

输出 JSON 字段：`image_slots: [{ role, media_node_id, url, caption, evidence }]`

### 4.2 正文排版

- 短句：单行建议 ≤ 20 字（移动端）
- **加粗**：成分、卖点、合规关键词（敏感肌可用、无酒精等）
- 段间空行；适量 emoji（🌸💧✨），密度由策略控制
- 全文须带 `evidence[]`：段落 → `chunk_id` / `node_id`

---

## 五、实战样例（尚雅绣钻石光感体验套）

> 以下为用户提供的 **目标文风样例**；正式实现时须 **从子图实时填充**，下表仅为占位符演示。

| 区块 | 示例文案 | 应对图谱字段 |
|------|----------|--------------|
| **标题（公式 A）** | 敏感肌不敢用美白产品？尚雅绣体验套，让你素颜也敢出门！ | 痛点 concept + `document.label` |
| **开头** | 肤色暗沉、斑点不敢抬头……专为敏感肌设计的「美白小灯泡」 | `section` 评价 + 商品定位 |
| **中间·成分** | 不含酒精香精；植物萃取；光感因子抑制黑色素 | `concept` mentions 边 |
| **中间·场景** | 银色小瓶、质地清爽；坚持 N 天 T 区油光减少 | `media` + `section` |
| **结尾** | 点击链接购买 + 赠护肤指南 | 商城链接 metadata + 关联文章 |

**输入子图**（与用户截图一致）：在「大类全貌」中勾选如 **尚雅绣钻石光感体验套**、**断黑王 4 件套** 等文档树，合并为 **写作子图快照**。

---

## 六、运行时架构（对齐 M6-2）

```text
┌─────────────────────────────────────────────────────────────────┐
│  L3 · 智能写作 Skill（doc-writer-agent）                          │
│  输入：垂直大类 + 勾选子图快照 + 模板_id → 输出：图文文章          │
└────────────────────────────┬────────────────────────────────────┘
                             │
          ┌──────────────────┴──────────────────┐
          ▼                                      ▼
┌──────────────────────┐              ┌──────────────────────┐
│ L1 · 技能集 · 智能写作 │              │ L2 · 商业垂直知识网     │
│ doc_writer 策略包      │              │ 用户勾选的大类全貌子图   │
│ · 四维脚手架母版       │              │ nodes / edges / media  │
│ · beauty 行业变体      │              │ （客户端快照，不复搜）   │
└──────────────────────┘              └──────────────────────┘
```

### 6.1 用户工作流（UI）

```text
① 知识图谱 Tab → 勾选左侧文档索引（1~N 棵子树）
② 点击「大类全貌」或「生成写作子图」→ 得到 search_snapshot 同类结构
③ 进入「智能写作」Skill → 选择模板（美妆四维脚手架 v1）
④ 可选：标题公式 A/B/C、语气、是否加 emoji
⑤ 生成文章 → 预览 / 复制 Markdown / 导出配图清单
⑥ 工程师视图：子图 + evidence 对照（复用认知推演组件）
```

### 6.2 与现有页面的关系

| 现有能力 | M6-3 用法 |
|----------|-----------|
| `KnowledgeForestPanel` 文档多选 | **写作素材选择器**（扩展勾选态） |
| `GET /api/knowledge/graph/category-overview` | 大类全貌子图数据源 |
| `POST /api/knowledge/search` | 可选：按主题 query 扩种子（默认不强制） |
| `compose` 模式 | 新增 `article_compose`，**只吃快照、不复搜** |

### 6.3 认知图谱页 UI 设计（**新增 · 不动现有布局**）

> **页面**：知识库 Tab「知识图谱」· Card 标题 **「心智模型构建-认知图谱」**（`KnowledgePage` → `KnowledgeForestPanel`）  
> **原则**：左侧索引、中间点网、右侧详情 **结构与交互保持不变**；只 **加按钮、加右栏 Tab、加底部抽屉（可选）**。

#### 6.3.1 现有布局（不改）

```text
┌─ 心智模型构建-认知图谱 ─────────────────────────────────────────────────────┐
│  工具栏：跨文档 | 展开/收缩 | [展示全部/●大类全貌] | [补齐关联]  …           │
│  ┌─ 文档索引 200px ─┐  ┌────── 点网图 ──────┐  ┌─ 节点详情 240px ─┐        │
│  │ ☑ 尚雅绣体验套    │  │  document→section  │  │ 大类全貌统计      │        │
│  │ ☑ 断黑王4件套     │  │  →concept→media    │  │ 或段落/图片详情   │        │
│  └──────────────────┘  └────────────────────┘  └───────────────────┘        │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 6.3.2 新增①：工具栏「生成文案」按钮

**位置**：工具栏 **「展示全部 / ●大类全貌」右侧、「补齐关联」左侧**（与现有 `Button variant="outline" size="sm"` 同排）。

| 项 | 定稿 |
|----|------|
| 文案 | **✍️ 生成文案** |
| 启用条件 | 左侧索引 **≥1 份文档已勾选** |
| 推荐流程 | 勾选文档 → **展示全部**（并入大类全貌）→ 再点 **生成文案** |
| 未并入全貌时 | 仍可用勾选文档的 **已缓存子树** 生成；Toast 提示「建议先点展示全部以含共享概念/图片」 |
| 点击后 | 弹出 **轻量选项条**（Popover，非 Modal）：标题公式 A/B/C · 是否 emoji · [开始生成] |

**不改动**：`补齐关联`、跨文档开关、展开/收缩、搜索框逻辑。

#### 6.3.3 新增②：右侧「节点详情」→ 双 Tab（核心展示位）

在右侧 `aside` **顶部**增加 **分段切换**（仅多一行，原有详情内容整块保留在 Tab1）：

```text
┌─ 节点详情 ─────────────────┐
│  [ 节点详情 ]  [ 文案预览 ]   │  ← 新增 segmented control
├──────────────────────────────┤
│  （Tab 内容区，二选一）        │
└──────────────────────────────┘
```

| Tab | 内容 | 与现有关系 |
|-----|------|------------|
| **节点详情** | **现有 aside 全部内容原样**（大类全貌说明 / 文档 meta / 段落 / 图片） | 默认选中，零行为变化 |
| **文案预览** | 生成后的文章（见 6.3.4） | 生成成功 **自动切到此 Tab**；无文章时显示引导空态 |

**为何放右侧**：运营写文案时仍需 **对照子图节点**；右栏 240px 可滚动，不挤压中间点网（用户截图中的主视觉）。

#### 6.3.4 「文案预览」Tab 内容结构

```text
┌─ 文案预览 ──────────────────────────────┐
│ 状态：生成中… / 就绪                       │
│ 模板：美妆四维脚手架 · 标题公式 A          │
│ [复制 Markdown] [复制全文] [在 Skill 页打开]│
├──────────────────────────────────────────┤
│ 子 Tab（文内切换，不增页面）：              │
│  [阅读] [Markdown] [配图] [溯源]          │
├──────────────────────────────────────────┤
│ 【阅读】                                   │
│  # 标题（大字号）                          │
│  开头段…                                   │
│  ✨ 中间·成分…                             │
│  💧 中间·场景…                             │
│  👉 结尾 CTA…                              │
│ 【配图】封面/细节/场景 缩略图 4 格          │
│ 【溯源】段落 ↔ chunk_id / node_id 列表     │
└──────────────────────────────────────────┘
```

| 子 Tab | 说明 |
|--------|------|
| **阅读** | 排版后近似发圈效果（加粗、emoji、短段） |
| **Markdown** | 可编辑 textarea（二期）；首期只读 + 复制 |
| **配图** | `image_slots` 四宫格 + 从子图 `media` 来的 url |
| **溯源** | 与 M6-1 evidence 同风格，便于审核 |

#### 6.3.5 新增③（可选）：底部「文案抽屉」

若右栏 240px 阅读偏窄，生成后可 **同时** 从底部滑出 **可折叠抽屉**（默认关；用户在文案预览 Tab 点「展开大图阅读」打开）：

```text
┌──────────── 点网（高度不变）────────────┐
├─ ▲ 文案预览（40vh，可拖曳高度 / 收起）──┤
│  全文 + 配图                             │
└──────────────────────────────────────────┘
```

**首期建议**：只做 **右栏 Tab**；抽屉列为 **P1**，避免一次改太多。

#### 6.3.6 组件拆分（实现时新增文件，不改旧组件逻辑）

| 文件 | 职责 |
|------|------|
| `KnowledgeForestPanel.vue` | **仅加**：工具栏按钮 + `emit('compose-article', snapshot)` + 右栏 Tab 壳 |
| `KnowledgeArticlePreviewPanel.vue` | **新建**：文案预览 Tab 内容 |
| `KnowledgeArticleComposePopover.vue` | **新建**：公式 A/B/C、emoji 选项 |
| `KnowledgePage.vue` | **可选**：监听 compose 事件，调 API（或交给 ForestPanel 内聚） |

**数据流**：

```text
勾选 docIds + overviewGraph（或 subtreeCache 合并）
  → buildGraphSnapshot()
  → POST article/compose
  → articleResult 传入 KnowledgeArticlePreviewPanel
  → aside activeTab = 'article'
```

#### 6.3.7 技能集深链（第二入口，非图谱页）

| 入口 | 路径 | 场景 |
|------|------|------|
| 图谱页 | 右栏「文案预览」 | **主路径**：对照子图写文 |
| 文案预览内按钮 | `/skills/doc-writer-agent?category=beauty&from=graph` | 大屏编辑、历史记录（二期） |
| 技能集卡片 | 同上 | 无子图时进入空态 Skill 页 |

图谱页 **「在 Skill 页打开」** 通过 `sessionStorage` 或 query 传递 **graph_snapshot**，Skill 页只做 **放大版同一预览**，不重做布局。

#### 6.3.8 线框总览（定稿）

```text
┌─ 心智模型构建-认知图谱 ─────────────────────────────────────────────────────────┐
│ beauty · 158边…  [☑跨文档] [展开][收缩] [●大类全貌] [✍️生成文案] [补齐关联]      │
│ ┌─索引────┐ ┌────────────── 点网 ──────────────┐ ┌─ aside ─────────────┐      │
│ │☑尚雅绣  │ │                                   │ │[节点详情|文案预览]│      │
│ │☑断黑王  │ │                                   │ │ 标题：敏感肌…      │      │
│ └─────────┘ └───────────────────────────────────┘ │ [阅读|MD|图|溯源]  │      │
│                                                    │ …正文…            │      │
│                                                    └───────────────────┘      │
│ （可选 P1）▲ 文案抽屉 40vh ─────────────────────────────────────────────────  │
└───────────────────────────────────────────────────────────────────────────────┘
```

#### 6.3.9 空态与错误（文案预览 Tab）

| 状态 | 展示 |
|------|------|
| 未生成 | 「勾选左侧文档 → 建议点「展示全部」→ 再点「生成文案」」 |
| 未勾选 | 按钮 disabled；hover 提示同上 |
| 生成中 | Tab 内 skeleton + 「正在填充脚手架…」 |
| 失败 | 错误信息 + [重试] |
| 成功 | 自动切 Tab + 顶部绿色「已基于 N 节点 / M 边生成」 |

---

## 七、技能集策略库：`doc_writer`

### 7.1 目录规划（`agent_skills/doc_writer/`）

```text
agent_skills/doc_writer/
├── scaffold_v1/                    # 四维脚手架母版（全垂直）
│   ├── manifest.json
│   ├── role_play.md
│   ├── title_formulas.json         # A/B/C 公式
│   ├── section_templates.yaml      # 开头/中间/结尾骨架
│   ├── typography_rules.json       # 短句、加粗、emoji
│   └── metaphor_phrasebook.json    # 成分比喻库
├── beauty_article_v1/              # 美妆行业变体
│   ├── manifest.json
│   ├── industry_hints.md           # 合规：禁医疗承诺、敏感肌话术
│   ├── scene_bank.json             # 睡前/晒后/换季…
│   └── image_slot_rules.json       # 封面/细节/场景/对比
└── fashion_article_v1/             # 远期
```

### 7.2 占位符 → 子图抽取器（服务端）

| 抽取器 | 输入 | 输出 |
|--------|------|------|
| `extract_products` | nodes `document` | 产品名、链接、SKU |
| `extract_concepts` | nodes `concept` + `mentions` 边 | 成分、功效、人群 |
| `extract_pain_points` | section 文本 + 负面评价关键词 | 痛点列表 |
| `extract_media` | nodes `media` | 按 slot_rules 排序 |
| `extract_offers` | section / metadata | 价格、赠品（无则空） |

抽取结果 → `slot_values` JSON → 填入模板 → **单次 LLM 润色**（保结构、保 evidence）。

---

## 八、API 设计（草案）

### 8.1 子图快照（客户端传入，与 M6-1 对齐）

```http
POST /api/knowledge/article/compose
{
  "domain_category": "beauty",
  "skill_id": "doc-writer-agent",
  "template_id": "beauty_article_v1",
  "title_formula": "A",
  "graph_snapshot": {
    "root_doc_ids": ["upload_xxx", "niushop_goods_23"],
    "nodes": [ ... ],
    "edges": [ ... ],
    "media": [ ... ],
    "chunks": [ ... ]
  },
  "options": {
    "include_emoji": true,
    "max_images": 6,
    "tone": "warm_professional"
  }
}
```

### 8.2 响应

```json
{
  "title": "...",
  "sections": [
    { "role": "opening", "markdown": "...", "evidence": [...] },
    { "role": "body_rational", "markdown": "...", "evidence": [...] },
    { "role": "body_scene", "markdown": "...", "evidence": [...] },
    { "role": "closing", "markdown": "...", "evidence": [...] }
  ],
  "full_markdown": "...",
  "image_slots": [ ... ],
  "strategy_ids": ["scaffold_v1", "beauty_article_v1"],
  "llm_meta": { "degraded": false }
}
```

### 8.3 策略检索

复用 M6-2：`GET /api/knowledge/skills/retrieve?skill_family=doc_writer&domain_category=beauty`

---

## 九、技能集地图与入口

| 项 | 定稿 |
|----|------|
| 技能集大类 | **智能写作**（`doc_writer`） |
| 垂直子类（首期） | **美妆**（`beauty`）；服装/生鲜/家电远期 |
| 侧栏 Skill | `/skills/doc-writer-agent` |
| 知识库联动 | 图谱 Tab 勾选后 **「用所选素材写作」** 深链 |

启用 `skill_families.json` 中 `doc_writer.enabled: true`：**M6-3 验收后已启用**（beauty 子类同步启用）。

---

## 十、开发任务拆分

| ID | 任务 | 端 | 依赖 |
|----|------|-----|------|
| M6-3-1 | `scaffold_v1` + `beauty_article_v1` 策略入库 | 内容/服务端 | M6-2 ingest |
| M6-3-2 | `slot_extractor.py` 子图 → slot_values | 服务端 | 图 schema |
| M6-3-3 | `article_compose.py` + API | 服务端 | M6-3-2 |
| M6-3-4 | 图谱页：工具栏「生成文案」+ 右栏 **文案预览** Tab | 客户端 | ✅ §6.3 UI |
| M6-3-4b | `KnowledgeArticlePreviewPanel` + Compose Popover + 底部抽屉 | 客户端 | ✅ M6-3-4 |
| M6-3-5 | `DocWriterSkillPage` 大屏深链 | 客户端 | ✅ `/skills/doc-writer-agent` |
| M6-3-6 | `verify_m63.py` 尚雅绣样例子图验收 | 脚本 | ✅ |
| M6-3-7 | 技能集地图启用「智能写作」+ 美妆子类 | 双端 | ✅ `skill_families.json` |

**估时**：首期 **约 2.5 周**（含策略撰写与 1 个垂直模板）。

---

## 十一、验收标准

| # | 项 | 标准 |
|---|-----|------|
| A1 | 素材来源 | 文章核心事实 **100%** 可映射到 `graph_snapshot` 的 node/chunk |
| A2 | 结构 | 输出含 **标题 + 开头 + 中间（≥2段）+ 结尾 + image_slots** |
| A3 | 合规 | 无医疗承诺；无编造价格/链接（缺失则省略或标注「请咨询」） |
| A4 | 样例 | 勾选尚雅绣+断黑王相关索引，生成文章 **可读、可发圈**（人工评审 ≥ 3/5） |
| A5 | 性能 | 单次 compose ≤ 1 次 LLM；与 M6-1 同级延迟量级 |
| A6 | 不复搜 | `article/compose` **不** 再调 BGE/PPR |

---

## 十二、远期扩展

| 方向 | 说明 |
|------|------|
| 多垂直模板 | 服装穿搭文、生鲜产地文、家电参数对比文 |
| 多体裁 | 朋友圈短文案 / 公众号长文 / 小红书笔记（不同 `section_templates`） |
| A/B 标题 | 一次生成 3 个标题候选 |
| 与做视频联动 | 文章 → 短剧项目 → [M7-1 导演智能体](M7-1-短视频创作-导演智能体总体方案.md) |

---

## 十三、修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-02 | 初稿：智能写作技能；四维文案脚手架；子图→信任状；对齐 M6-2 架构与知识图谱大类全貌 |
| 2026-07-02 | §6.3：认知图谱页 UI——工具栏「生成文案」+ 右栏「文案预览」Tab（不动现有点网/索引） |
| 2026-07-02 | **M6-3 全量落地**：Skill 页、地图启用、session 深链、底部抽屉 |
