# v0.1 编码实现说明（对照 10 / 10a / 10b）

> **版本**：v1.0 · 2026-07-16  
> **原则**：仅改 `mybuddy-v01` 自有目录；上游只读；契约冻结为 `POST /v1/buddy/ask`

## 本轮交付范围

| 模块 | 路径 | 内容 |
| --- | --- | --- |
| 资产 | `docs/golden-qa.yaml` 等 | ≥10 金牌 + schema 单测；对象雏形；语料清单占位 |
| adapter | `adapter/` | FastAPI `/v1/buddy/ask` + `/healthz`；pytest ≥12 |
| overlay | `openocta-overlay/` | `SearchKnowledgeDoc` 可测纯函数 + Skill 模板；pytest ≥8 |
| desktop | `desktop/` | Tauri2+React 三栏深色壳；vitest ≥10 |
| 脚本 | `scripts/selftest/` | `run-unit` · `run-contract` · golden checklist |

## 非本轮（验收时人工）

- Dify 控制台配真实模型 Key / 上传真实企业语料 / 空聊  
- 三端安装包出齐（可先 Win `tauri build`）  
- OpenOcta 官方二进制正式起服（overlay 契约与 adapter 已可独立验）

## 调用链（试用）

```text
Desktop → BUDDY_ASK_URL (adapter :8090) → Dify chat-messages
可选：OpenOcta 加载 overlay Skill → 同映射逻辑
```
