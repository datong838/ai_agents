# 101 · 通知通道运行时（Webhook 持久化 · Email/SMS 分发）

> **版本**：v1.0 · 2026-07-19  
> **状态**：✅ 已落地 · 自测通过  
> **上游**：[98](98-插件化剩余域收口方案.md) · [100](100-Connector运行时插件分发方案.md) · 20 §3.1  
> **Rules**：通道按 pluginId 分发 · Webhook 禁止仅内存 · SMTP 未配则诚实 501 · 最小更改

---

## 1. 范围

| # | 能力 | 做法 |
| --- | --- | --- |
| **A** | Webhook 持久化 | `meta_aip_kv` 存注册列表；进程重启不丢 |
| **B** | Host 分发 | `POST /v1/channels/{plugin_id}/send` |
| **C** | channel-webhook | send → 对已注册 URL 记录投递（Dev 可 HTTP POST） |
| **D** | channel-email | 有 `AOS_SMTP_*` 则 SMTP；否则 **501 CHANNEL_STUB** |
| **E** | channel-sms | **501** 诚实 |

非目标：邮件模板引擎 · 短信网关厂商 · ACT-10 全量 DLQ。

## 2. 自测

1. 注册 webhook → GET 仍在（KV 持久） ✅  
2. send channel-webhook → 200 + deliveries（dry-run） ✅  
3. send channel-email 无 SMTP → 501 ✅  
4. 未安装 / SMS → 400/501 ✅  

pytest：`test_channel_runtime_101` + `test_pluginization_98` → **9 passed**
