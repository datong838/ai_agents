# 109 · TA.0 分析建模契约骨架方案

> 状态：✅ 已落地（方案先行）  
> 前置：[`73`](./73-产品1.3分析建模下一阶段方案.md) v1.1 · OpenAPI `packages/contracts/openapi/v1.yaml`  
> 索引：[`00`](./00-技术方案索引.md) v1.0.78

---

## 1. 目标（TA.0 DoD）

把已冻结的 OpenAPI **接到 aos-api 可调用面**（诚实 stub，**不起** Jupyter 边车）：

| 方法 | 路径 | TA.0 行为 |
| --- | --- | --- |
| `GET` | `/v1/analytics/health` | 200 · `sidecar=unset` · `notebookUi=notebook7` |
| `POST` | `/v1/notebooks/sessions` | 无 `AOS_ANALYTICS_URL` → **503** |
| `GET` | `/v1/notebooks/sessions` | `{ items: [] }`（或已登记元数据） |
| `GET` | `/v1/notebooks/sessions/{id}` | 404 / 已有元数据 |
| `DELETE` | `/v1/notebooks/sessions/{id}` | 停止元数据会话 |
| `POST` | `/v1/analytics/sql/preview` | **501 `ANALYTICS_STUB`**（实现属 TA.4） |

**禁止：** 在 aos-api 进程内嵌 Jupyter；UI 直连上游；分析员自批写生产。

---

## 2. 环境

| 变量 | 说明 |
| --- | --- |
| `AOS_ANALYTICS_URL` | 空=未装边车；有值时 health 探测 `/health`（边车见 [110](110-TA1-analytics-runtime边车方案.md)；会话票据属 TA.2） |

---

## 3. UI（最小）

`/analytics` 页：展示 health + 诚实文案「契约已立 · 边车自 TA.1」；≠ Workshop 画布。

---

## 4. 自测

- health 200 · sidecar unset  
- create session 503  
- sql preview 501  
- OpenAPI 路径与实现一致  

---

## 5. 明确不做

- ~~analytics-runtime compose（TA.1）~~ → **已落地** [110](110-TA1-analytics-runtime边车方案.md)  
- ~~会话票据（TA.2）~~ → **已落地** [111](111-TA2-Facade会话票据方案.md)  
- 真 SQL / 内核读数（TA.3～4）  
- Draft 写回演示故事（TA.5～7）  
