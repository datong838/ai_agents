# 116 · TA.7 分析→回写演示故事方案

| 字段 | 内容 |
|------|------|
| 状态 | **已落地** |
| 关联 | [73](./73-产品1.3分析建模下一阶段方案.md) **TA.7** · [114](./114-TA5-分析Draft写回方案.md) · [115](./115-TA6-分析治理方案.md) · [72](./72-系统启停与健康检查手册.md) |
| 索引口径 | **v1.0.85** |

## 1. 目标 / 非目标

**目标（DoD）**

- 一镜故事：**读数 → propose Draft → 批准 → 对象变更 + lineage**（复用 TA.4～6 真路径）。
- API：`POST /v1/demo/run-analytics-story`（**演示面**；可含 HITL 一镜内批准，≠ 分析员自批产品路径）。
- 演示编排走 **`POST /v1/demo/run-analytics-story`** + smoke / CUSTOMER-DEMO；**产品页 `/analytics` 不放演示钮**（对齐 75 / 118）。
- `GET /v1/demo/story` 增 **TA.7** 步；`deferred.analyticsNotebook` 改为 MVP 已交付口径。

**非目标**

- nbclient / 真 Jupyter 跑 notebook（仍后置）。
- TA.8 Contour/Quiver/Vertex。
- 把一镜批准钮做成分析员日常主路径（产品写回仍须审批台）。

## 2. 故事步骤（客户可见）

| # | 动作 | API |
|---|------|-----|
| 1 | 种子 | `POST /v1/demo/ensure-seed` |
| 2 | 读数 | `POST /v1/analytics/objects/list`（WorkOrder） |
| 3 | 提案 | `POST /v1/analytics/writeback/propose` |
| 4 | 批准（仅演示一镜） | `POST /v1/aip/drafts/{id}/approve` |
| 5 | 谱系 | `GET /v1/analytics/lineage` |
| 6 | 治理旁证 | public export → 403（可选探测） |

## 3. 工程落点

| 路径 | 说明 |
|------|------|
| `aos_api/demo_story.py` | `run_analytics_story` |
| `aos_api/routers/wave_ext.py` | `/v1/demo/run-analytics-story` |
| `apps/web/.../analytics.tsx` | 「演示一镜」钮 |
| `scripts/demo/run-demo-smoke.*` | TA.7 段 |
| `scripts/demo/CUSTOMER-DEMO.md` | 彩排段 |
| `tests/test_analytics_ta7_116.py` | 单测 |

## 4. 验收

```text
POST /v1/demo/run-analytics-story
# → ok · read.total>0 · draftId · lineageId · productionWritten=true

pytest tests/test_analytics_ta7_116.py -q
# smoke: run-demo-smoke 含 run-analytics-story
```

## 5. 下一刀

**TA.8** ✅ 已落地 · [117](117-TA8-Contour-Quiver-Vertex子集方案.md)；下一为 **产品 1.3 阶段退出收口**。
