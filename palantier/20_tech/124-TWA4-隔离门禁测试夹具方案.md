# 124 · TWA.4 工作区隔离门禁夹具

> **版本**：v1.0.0 · 2026-07-19  
> **状态**：✅ 已编码 · 已自测  
> **对齐**：[20a](20a-多用户与工作区整站隔离方案.md) A1 · [26 §14 TWA.4](26-AOS目标态开发计划.md) · 依赖 TWA.2；与 TWA.3 同批交付

## 目标

OrgA/Prj1 vs OrgA/Prj2（及跨 Org）越权读须 **404**，不泄露存在性；伪造 Header 仍走 TWA.1 **403**。

## 方案（无 PG）

本机 Docker/PG 常不可用，故用内存探针资源，语义与产品读路径一致（按 `org_id`+`project_id` 键控）：

| 项 | 落点 |
| --- | --- |
| Store | `routers/workspaces.py` · `_ITEMS[(org, project, id)]` |
| API | `POST/GET /v1/workspace-items` · `GET .../{id}` |
| 夹具 | `tests/test_twa3_twa4_workspaces.py` · `issue_dev_token(org, project)` |

## 断言

1. Prj1 写入 `secret-1` → Prj2 list 空、get **404**
2. OrgB 同 project_id 串仍 **404**
3. claim≠Header → **AUTH_TENANT_MISMATCH**

## 后续（TWA.5）

把同一谓词盘点到 drafts/modules 等持久表；本夹具保留作门禁回归。

## 自测

- pytest 上述文件 4 cases（含 TWA.3 list）
- 日志：`workspace_item_denied` / `workspace_item_create`（含 org/project，无密钥）
