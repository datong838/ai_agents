# 125 · TWA.5 漏网表/读路径盘点销项

> **版本**：v1.0.0 · 2026-07-19  
> **状态**：✅ 已编码 · 已自测  
> **对齐**：[20a](20a-多用户与工作区整站隔离方案.md) A4/R-ISO-03 · [26 §14 TWA.5](26-AOS目标态开发计划.md) · 依赖 TWA.4 [124](124-TWA4-隔离门禁测试夹具方案.md)

## 目标

业务读/写路径 **双字段谓词**（`org_id` + `project_id`）台账对 **P0** 项清零；缺口有编码修复或显式延期标注（禁止静默漏网）。

## 台账（盘点结果）

| ID | 路径 | 类型 | 双字段 | 等级 | 处置 |
| --- | --- | --- | --- | --- | --- |
| L01 | `drafts.py` list/get/create/reject | HTTP+SQL | ✅ | P0 | **OK** |
| L02 | `runtime_write.py` draft 相关 | HTTP+SQL | ✅ | P0 | **OK** |
| L03 | `workspaces.py` workspace-items | HTTP+mem | ✅ | P0 | **OK**（TWA.4） |
| L04 | `idempotency` | store | ✅ | P0 | **OK** |
| L05 | `modules.py` + `module_store` / `meta_module` | HTTP+SQL | ❌→✅ | P0 | **本刀修补** |
| L06 | `ontology.py` 类型/实例/分支 | HTTP+SQL | 部分 markings | P1 | **延期** 本体元数据共享至 TWA.8/9；实例走 markings/FGA |
| L07 | `actions.py` action types | HTTP | markings | P1 | **延期** 同本体 |
| L08 | `object_sets.py` | HTTP | 对象级 FGA | P1 | **延期** 无独立租户表 |
| L09 | `analytics.py` notebook 会话等 | 内存/混 | 部分写 org | P1 | **延期** 台账跟踪 |
| L10 | `health`/`metrics`/`me` | 系统 | N/A | — | **N/A** |
| L11 | `buddy`/`wave_ext` | 辅助 | 待复核 | P2 | **延期** |

机器可读真源：`aos-api/aos_api/tenant_ledger.json`（测试断言 P0 无 `GAP`）。

## 本刀编码（最小）

1. `meta_module` 增 `org_id`/`project_id`（已有行回填 `dev-org`/`dev-project`）  
2. list/get/update/publish/runtime：**WHERE org+project**；跨区 **404**  
3. create：强制写入 Principal 租户，**忽略**客户端伪造区 ID  
4. `_row_to_mod` 暴露 `orgId`/`projectId`  
5. 单测：过滤纯函数 + ledger P0 清零（无 PG）；有 PG 时可选集成

## 不做（防越界）

- 插件「目录共用」产品语义完整化 → **TWA.6**  
- 本体表全量加列 → TWA.8/9  
- MinIO/向量前缀 → TWA.8  

## 自测门禁

- pytest：`tests/test_twa5_tenant_ledger.py`（4 passed，无 PG）  
- 日志：`module_list` / `module_create` / `module_miss` 含 `org=` `project=`（无密钥）

## 一致性

- 台账 JSON ↔ 本文表 L01～L11  
- P0 无 `GAP`；L05 已由 `meta_module` 双字段销项  
- L06～L09/L11 显式 `DEFERRED`，非静默漏网
