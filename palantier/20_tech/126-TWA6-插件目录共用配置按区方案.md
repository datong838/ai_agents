# 126 · TWA.6 插件目录共用 · 配置按区

> **版本**：v1.0.0 · 2026-07-19  
> **状态**：✅ 已编码 · 已自测  
> **对齐**：[20a](20a-多用户与工作区整站隔离方案.md) §3.4.2 / A9 · [26 §14 TWA.6](26-AOS目标态开发计划.md) · 依赖 TWA.3+TWA.5

## 目标

| 面 | 语义 | API |
| --- | --- | --- |
| **目录（共用）** | Org 内可见的可选能力清单；不按工作区复制 | `GET /v1/plugin-catalog` |
| **已配置实例（按区）** | 大模型等绑定当前 `org_id`+`project_id` | `GET/POST /v1/plugin-configs` · get by id |

## 落点

| 项 | 路径 |
| --- | --- |
| Router | `aos_api/routers/plugins.py` |
| 注册 | `main.py` include `plugins` |
| 测试 | `tests/test_twa6_plugin_catalog.py` |

## 最小实现

- 内存目录种子：`llm.openai` / `llm.local` / `connector.http`（无密钥明文，仅元数据）  
- 配置 store：`_CONFIGS[(org, project, id)]`；跨区 get → **404**  
- create 强制 Principal 租户；**禁止** body 指定他区  
- Module 实例已在 TWA.5 按区；本刀不重复改 `meta_module`

## 不做

- 真密钥 Vault / 桌面钥匙串（TWC.3）  
- 插件包分发/安装  
- 管理 UI 完整页（可后续挂导航；本刀 API+单测优先）

## 自测

- pytest `tests/test_twa6_plugin_catalog.py` · 3 passed（无 PG）  
- 日志：`plugin_catalog_list` / `plugin_config_create` / `plugin_config_denied` 含 org/project
