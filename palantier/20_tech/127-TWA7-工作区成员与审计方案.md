# 127 · TWA.7 工作区成员与审计（P1）

> **版本**：v1.0.0 · 2026-07-19  
> **状态**：✅ 已编码 · 已自测  
> **对齐**：[20a](20a-多用户与工作区整站隔离方案.md) A5 / §5.2 · [26 §14 TWA.7](26-AOS目标态开发计划.md)

## 落点

| 项 | 路径 |
| --- | --- |
| Membership/Audit | `aos_api/membership.py` |
| API | `routers/workspaces.py` members/enter/audit |
| UI | `apps/web/.../WorkspaceMembersPage.tsx` · nav `/workspace/members` |
| 测试 | `tests/test_twa7_membership.py` |

## 自测

- pytest 4 passed（bob 仅见 prj-ops；enter 403；alice 加员+audit）


## 目标

| 能力 | DoD |
| --- | --- |
| Membership | `GET/POST /v1/workspaces/{id}/members`；角色 owner/admin/editor/viewer |
| 列表过滤 | `GET /v1/workspaces` 仅返回当前 subject 为成员的工作区 |
| Audit | 成员变更、进入工作区写入 Audit；`GET /v1/audit` 按当前 org+project 可读 |
| UI | Web「工作区成员」页（最小） |

## 最小实现（无 PG）

- `membership_store` / `audit_store` 内存  
- 种子：`alice` → 测试工作区+运营区 owner；便于单测 bob 非成员拒访  
- POST member 需 caller 为该区 admin/owner  
- 跨区成员 API：workspace id 不在 catalog → 404  

## 不做

- IdP Group 映射（记延期）  
- 持久化 PG（有库后再迁）  
- 完整审计检索 UI（API + 成员页侧栏摘要即可）
