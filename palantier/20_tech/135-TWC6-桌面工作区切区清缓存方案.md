# 135 · TWC.6 桌面工作区切区清缓存

> **版本**：v1.0.0 · 2026-07-19  
> **状态**：✅ 已编码 · 已自测  
> **对齐**：[20c](20c-AOS桌面端详细技术方案.md) UI-05/12 · TWA.3 · [26 §14 TWC.6](26-AOS目标态开发计划.md)

## 落点

- Web `lib/workspaceCache.ts` · `WorkspaceSwitcher` 切区清缓存  
- 桌面监听 `aos-workspace-changed` 双保险  

## 自测

- vitest `workspaceCache.test.ts` · 保留 API Base / 租户 key


## 目标

切工作区时清 **工作区相关本地缓存**（防串区），保留外观/API Base。

## 清项

| 键/前缀 | 动作 |
| --- | --- |
| `aos.mp.draft.*` | sessionStorage 删除 |
| `aos.ontology.recent.v1` / favorites / branch | localStorage 删除 |
| 租户上下文 | 由 TWA.3 `setTenant` 更新（保留） |

## 落点

- Web：`lib/workspaceCache.ts` · `WorkspaceSwitcher` 切区调用  
- 桌面：监听 `aos-workspace-changed` 再清一次（双保险）+ 日志
