# 193m · 组织邀请二维码（不代海报印刷）

> **版本**：v1.1 · 2026-07-20  
> **状态**：✅ **已编码**（M1-W5a）· 自测通过  
> **计划**：[180m](180m-M1后置闭环开发计划.md)  
> **对齐**：[164](164-TWA10-组织工作区创建与邀请审批方案.md) · [182m](182m-成员邮箱手机OTP方案.md)  
> **点名**：用户「继续」→ W5 A  
> **边界**：深链 QR 展示/下载；≠ 印刷海报产线 · ≠ 未登录扫码免登  
> **落点**：`aos_api/invite_qr.py` · `GET /v1/org-invites/{token}/qr` · `OrgMembershipPage` · `tests/test_w5_193_194m.py`

## 已决

| 项 | 行为 |
| --- | --- |
| 数据源 | 既有 `invitePath` + 请求 `origin` → 绝对 `inviteUrl` |
| API | `GET /v1/org-invites/{token}/qr?origin=` → `{ inviteUrl, invitePath, svg }` |
| 生成 | `qrcode` 库 → SVG（无 PIL） |
| 权限 | 须登录；且为该邀请所属 org 的可管理成员 |
| UI | `OrgMembershipPage` 邀请卡片旁嵌入 SVG + 复制链接（已有） |

## 非目标

- 印刷级海报 / PDF  
- 扫码免登接受（仍走既有 `/org/invite/:token`）  
- 改 invite token 语义  

## 自检

- [x] 无 origin → 400  
- [x] 未知 token → 404  
- [x] 合法 → svg 含 `<svg` 且 inviteUrl 含 path  
- [x] 组织页生成邀请后可见 QR  

---

*v1.1 · 193m · 已编码*
