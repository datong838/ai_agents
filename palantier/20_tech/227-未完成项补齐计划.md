# 227 未完成项补齐计划

> 版本：v2.9（2026-08-02 通用平台后续能力复核）  
> 状态：**227 的 W1～W4 是历史收口；其 35 个物理页面已完成 Wave 3A～3C 的真实性治理、实机验收和防回归门，不再按本文历史百分比重开开发**  
> 本次复核代码：`aos-platform` m1 `b78a3bf`（`01da807` 仍作为 227 历史收口时点）  

> 历史依据：
> - [`223-deep-checklist.md`](./223-deep-checklist.md) §1.1（227 收口时的菜单页功能% 总表）
> - [`223-deep-checklist-2.md`](./223-deep-checklist-2.md) v1.1（P1 AIP/模型/本体）
> - [`223-deep-checklist-3.md`](./223-deep-checklist-3.md) v1.1（9 个详情页）
> - [`226-逐页视觉对账计划.md`](./226-逐页视觉对账计划.md)（视觉像素；与本计划互补）
>
> **与旧 `223-plan.md` 关系**：`223-plan.md` 仍描述「16 页完全缺失」等更早的历史目标；本文保留 227 后续功能波次的交付记录，**两者都不再直接作为当前开工排期**。

---

## 2026-08-01 当前代码现状复核

> **交互真实性补充结论**：用户在管道画布实操中发现算子拖入、节点移动、保存与布局均不可用。按同一口径复审 227 的 35 个物理页面后，确认 29 页至少有一项交互真实性/一致性问题，其中 5 页 P0、18 页 P1、6 页仅 P2。因此本文的「波次合入完成」仍是历史事实，但不再能单独证明「页面已可顺畅使用」。

### 复核结论

| 原文内容 | 当前判定 | 更新后口径 |
|----------|----------|--------------|
| W1～W4 全部交付 | **仍有效** | 对应前后端实现、路由和页面专项测试仍存在；`01da807` 是 227 历史收口点 |
| 代码基线 `01da807` | **已过期** | 本次复核 m1 为 `b78a3bf`，且 `01da807` 是当前 m1 的祖先提交 |
| 「五分支同 tip」 | **当前再次成立** | 管道端口拖线收口后，m1 与 4 个 worker 已统一到 `b78a3bf` 且平台工作树干净；这不改变 `01da807` 是 227 历史收口点 |
| §1～§3 中「现%」、「目标%」 | **历史估算，不是当前质量指标** | 保留作为 2026-07-29 排期/收口快照；当前验收以代码、契约和自动化测试证据为准 |
| §6「建议立即开工顺序」 | **已过期** | W1～W4 禁止重开；视觉缺口转 226，结构/安全/契约转 228，新功能缺口应新立方案 |
| §8 跨页主流程人工回归 | **已关闭** | Wave 3A、3B 与 3C 均完成浏览器主任务；Wave 3C 额外覆盖 8 个问题页与 6 个通过页，共 14/14 |

### 当前实现证据与能力边界

| 范围 | 当前代码证据 | 不扩大的边界 |
|------|--------------|----------------|
| W1 搭建链 | `VariablesPage.tsx`、`ModuleInterfacePage.tsx`、`DraftInboxPage.tsx`、`CanvasPage.tsx` 仍有 API 接线和对应测试 | 变量、模块接口、Draft 均保留明示的 MOCK/演示降级；不声称离线降级数据已持久化 |
| W2 AIP/模型 | Analyst、Observability、Capacity、Model Catalog、Studio、Publish 实现和专项测试仍存在 | Analyst/Observability/Capacity/Model Catalog 仍可在 API 失败时降级；Studio 的部分配置是浏览器本地持久化，不等于服务端生产持久化 |
| W3 本体/数据详情 | Wiki 编辑/版本/diff、对象/属性/函数详情、Pipeline 历史、数据源 Schema/预览实现与测试仍存在 | Wiki、Pipeline、数据源仍有演示回落数据；真实运行能力受后端执行器、证据解析器和连接器配置约束 |
| W4 收口 | Link/Action 可视化、Capability 配置/连通测试、Document Intelligence 页面与测试仍存在 | 本地 `mock://` 连通结果只是演示路径；具体电商平台端点不在 227 范围 |
| 后续通用平台 | 228 Wave 0～2 已在 m1 完成迁移、路由、CI、安全扫描、OpenAPI、REST/OAuth、通用电商契约/一致性和 Pipeline 诚实执行基础 | 228 的通用内核不等于某个具体电商平台已接入；未注册真实执行器/证据 resolver 时应 fail-closed |

### 本次复核验证

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 227 相关后端专项回归 | **150 passed** | 覆盖模块变量/接口、Draft、Analyst、Observability、Capacity、Model Catalog、Capability、Wiki、Pipeline 和数据源 |
| 227 相关 Web 页面单测 | **19 files / 565 passed** | 覆盖 W1～W4 主要页面和降级口径 |
| 同一 m1 基线全量门 | **12/12 passed** | Wave 3B 最终门：后端全量通过，Web 94 files/1624，Desktop 40，SDK 6，两端 build、OpenAPI、Helm 和安全扫描均通过 |
| 浏览器跨页人工主流程 | **Wave 3A/3B/3C 均已执行** | P0 主任务、15 个 P1 页及 Wave 3C 的 8 个问题页 + 6 个通过页均已点验；Wave 3C 结果 14/14 |
| Wave 3C 最终门 | **12/12 passed** | 后端 8070 passed / 3 skipped / 2 subtests；Web 101 files/1668；Desktop 40；SDK 6；typecheck、双端 build、OpenAPI、Helm、安全扫描全部通过 |
| AIP Logic Stage A 独立补强 | **已完成** | `/aip/logic` 已由纵向样式列表升级为 canonical 自由 DAG 画布；Web 1711、API 8098、36 页 honesty 门与浏览器保存/强刷/API 重启恢复均通过；dry-run/历史转入新的 `228-` Stage B 方案 |
| AIP Logic Stage B 可信执行 | **已完成** | 10 kind fail-closed dry-run、不可变运行历史、Inputs 显式应用、逐节点状态/错误定位和 Inspector 配置已闭环；API 8155、Web 1759、成功/失败 run、异常拖动坐标防御及 API 重启历史恢复均通过；生产发布/自动化仍禁用 |
| AIP Logic 画布交互收口 | **已完成** | 输出端口可直接拖到目标输入端口，连线带 source→target 箭头；保留 click 连接，复用重复/自环/环路校验。浏览器 3→4 边、专项 16、Web 1761、typecheck/build 与 36 页 honesty 均通过；m1 `186ddd9` |
| Pipeline Builder 方向表达收口 | **已完成** | 管道与 AIP 是独立 SVG 实现；管道正式连线现已补齐 source→target 箭头。专项 22、Web 1762、typecheck/build 与 36 页 honesty 均通过；m1 `da45a30` |
| Pipeline Builder 端口拖线统一 | **已完成** | 管道与 AIP 的主要建连方式已统一为输出端口拖到输入端口，带预览/箭头/取消与重复、自连接拒绝；点击和右键辅助路径保留。浏览器 2→3 边后刷新恢复，专项 25、Web 1765、typecheck/build 与 36 页 honesty 均通过；m1 `b78a3bf` |

> **当前使用规则**：本文§1～§3 是 227 历史排期和交付记录，不再作为当前开工清单。一旦历史百分比与当前代码/测试证据冲突，以当前证据和 228 的 fail-closed 口径为准。AIP Logic Stage A/B 已按两份 `228-` 子方案关闭；Stage C 必须另立方案，不重开 227 百分比任务。

### 2026-08-02 后续通用平台开发清单

以下项目不是 227 历史问题页复开，而是基于真实性闭环后识别出的生产能力补齐；均须另立 `228-` 前缀方案后开发：

| 优先级 | 后续能力 | 当前边界 |
|---|---|---|
| P1 | AIP Logic Stage C 发布治理 | 缺少 canonical 不可变发布、Evals 证据绑定、发布历史与自动化绑定；第一波按 [`228-AIP-Logic-Stage-C发布治理与服务端闭环方案.md`](./228-AIP-Logic-Stage-C发布治理与服务端闭环方案.md) 实施 |
| P1 | Draft / Studio 服务端闭环 | Draft 评论、历史和完整状态流，以及 Studio 创建、发布、工具范围与服务端持久化仍需独立契约 |
| P1 | Wiki / Property / Function 真落库 | 新建 Wiki、版本恢复、属性与函数保存/删除/执行仍存在明确演示或只读边界 |
| P1 | 治理后台契约 | Span 详情、细粒度容量配额、模型组织准入、Action Type UI/Capabilities 写入尚未闭环 |
| P2 | 分析与扩展能力 | Analyst 服务端保存、Wiki HTML、Canvas 低频菜单、Widget 安装、L4/COP 契约后置 |
| 技术债 | 旧接口和进程内状态 | 旧 Model Router/Logic 适配入口在调用归零后删除；仍承担生产语义的内存引擎必须迁移持久层 |

具体电商平台连接器、真实商家凭据和平台专属字段继续后置到电商接入专项，不纳入上述通用平台波次。

---

## 0. 历史目标与口径

### 0.1 目标

在**不破坏已有可用主流程与数据**的前提下，把「未完成」从「有壳/偏 MOCK」抬到 **可用（≥60%）或接近（≥75%）**，并按产品价值分波交付。

### 0.2 完整度口径（与 §1.1 一致）

| 档位 | % | 本计划「完成定义」 |
|------|---|-------------------|
| 骨架 | ≤40 | 本波至少抬到 **可用 ≥55–60** |
| 可用 | 50–70 | 本波优先补 **数据闭环 / 真 API**，再抠视觉 |
| 接近 | 75–90 | 仅做 P2 细项或并入 226 视觉对账 |
| 完成向 | ≥90 | 默认不单开功能波次 |

### 0.3 原则

1. **最小更改**：只改目标页相关模块；禁止顺手大重构。  
2. **先方案后代码**：每波开工前在本文件勾选范围与验收。  
3. **功能优先于像素**：数据写回/API 优先；像素对齐跟 226。  
4. **可降级保留**：MOCK 降级可留，但须标明「演示路径」；主路径尽量真 API。  
5. **自测**：每波交付后对照本文件验收表 + 回归该分区已有主流程。

---

## 1. 2026-07-29 收口快照（非当前实时指标）

> 下表是 227 功能波次的历史估算，不应与 2026-08-01 之后的代码基线、全量测试数或生产可用性直接等同。

| 范围 | 现状 |
|------|------|
| 菜单页 | 中位约 **55–65%**；**无「无路由」菜单页** |
| P1（-2） | 均约 **59%**；弱：Studio / Analyst / Observability / 容量 / 目录 |
| 详情（-3） | 9 页均有路由；弱：Wiki 详情 **35%**、Function **45%**、Wiki diff **40%** |
| 构建链 | W1 后：变量 **65%**、模块接口 **65%**、画布 **70%**、Draft **75%** |

---

## 2. 历史未完成项总池（按缺口类型）

> 本节的「现%」是开工前/当时估算，「目标%」是排期目标；勾选表示当时波次验收并合入，不代表删除了所有演示降级路径。

### A. 数据闭环类（优先）

| ID | 路径 | 现% | **差距(Δ)** | **核心缺失** | 目标% | 波次 |
|----|------|-----|-------------|-------------|-------|------|
| A1 | `/workshop/variables` | 35 | **-30%** | 无真 API、不回写画布 | 65 | **W1** |
| A2 | `/workshop/module-interface` | 45 | **-20%** | 非完整 schema CRUD | 65 | **W1** |
| A3 | `/aip/drafts` | 55 | **-20%** | 默认 MOCK，真审批链路弱 | 75 | **W1** |
| A4 | `/aip/analyst` | 60 | **-15%** | live query API、历史记录 | 75 | **W2 ✅** |
| A5 | `/aip/observability` | 60 | **-15%** | Overview/Traces 真 API | 75 | **W2 ✅** |
| A6 | `/aip/capacity` | 60 | **-15%** | usage/limits 真 API | 75 | **W2 ✅** |
| A7 | `/aip/model-catalog` | 65 | **-15%** | catalog/register 真 API | 80 | **W2 ✅** |
| A8 | `/aip/doc-intelligence` | 55 | **-20%** | 抽取 API、列表展示 | 75 | **W4 ✅** |

### B. 核心体验深度类

| ID | 路径 | 现% | **差距(Δ)** | **核心缺失** | 目标% | 波次 |
|----|------|-----|-------------|-------------|-------|------|
| B1 | `/workshop/canvas` | 55 | **-20%** | 关键功能面板缺失、拖拽操作不完整 | 75 | **W1**（功能面板）+ 226 |
| B2 | `/aip/studio` | 65 | **-20%** | 多 Tab 配置无法保存 | 85 | **W2 ✅** |
| B3 | `/ontology/wiki/:id` | 55 | **-30%** | 编辑保存、版本历史查看 | 85 | **W3 ✅** |
| B4 | `/workshop/publish` | 65 | **-20%** | 环境卡关联 API、发布步骤反馈 | 85 | **W2 ✅** |
| B5 | `/aip/capabilities` | 70 | **-15%** | 配置持久化、连通性测试 | 85 | **W4 ✅** |

### C. 详情页类（checklist-3）

| ID | 路径 | 现% | **差距(Δ)** | **核心缺失** | 目标% | 波次 |
|----|------|-----|-------------|-------------|-------|------|
| C1 | `/ontology/wiki/:wikiId` | 55 | **-30%** | 编辑保存、版本列表 | 85 | **W3 ✅** |
| C2 | `/ontology/object-types/:id` | 80 | **-10%** | 元数据项补齐、SVG 链接图 | 90 | **W3 ✅** |
| C3 | `/ontology/properties/:typeId` | 70 | **-15%** | 列映射 Tab 数据绑定 | 85 | **W3 ✅** |
| C4 | `/ontology/functions` | 65 | **-20%** | 参数表 CRUD、试跑结果展示 | 85 | **W3 ✅** |
| C5 | `/ontology/wiki/:id/diff` | 60 | **-20%** | 两版本文本对比渲染 | 80 | **W3 ✅** |
| C6 | `/data/pipelines/:id` | 70 | **-15%** | 历史运行记录、变换节点展示 | 85 | **W3 ✅** |
| C7 | `/data/sources/:id` | 65 | **-20%** | Schema 树渲染、数据预览 | 85 | **W3 ✅** |
| C8 | Link / Action 详情 | 70/70 | **-15%** | 可视化组件、布局优化 | 85 | **W4 ✅** |

### D. 中位可用、可延后（≥60 且非痛点）

成熟度、Evals、模型路由/供应商、图谱健康、Discover、组件注册表、主题、事件、多数运维/数据连接等 —— **默认进 W4 或并入 226 视觉波**，不占 W1–W2 带宽。

### E. 已知遗漏视觉稿

| ID | 视觉稿 | 现状 | 波次 |
|----|--------|------|------|
| E1 | `pipeline-doc-intel.html` | **并入文档智能**（无独立侧栏） | **W4 ✅** |

---

## 3. 历史波次执行记录

> 本节的 worker 分支、文件所有权和合并顺序仅用于追溯 227 的 2026-07-29 执行过程，已失效，不得直接复用到当前 worker 工作树。

### W1 — 搭建链闭环（最高产品杠杆）

**目标**：变量 ↔ 画布、模块契约、Draft 真审批；画布补关键功能面板（非全像素）。

| 任务 | 文件/区域（预期） | 验收 |
|------|-------------------|------|
| A1 变量 API + 读写 | `VariablesPage.tsx` + API + 画布变量引用 | 列表来自 API；改动能反映到画布预览/绑定 |
| A2 模块接口 schema | `extras.tsx` ModuleInterface + API | 可增删改入参/出参并持久化 |
| A3 Draft 真链路 | `DraftInboxPage.tsx` + drafts API | 无后端时仍可 MOCK；有后端时批准/驳回写真实状态 |
| B1 画布面板缺口（功能） | `workshop` canvas 相关 | 对照 checklist 画布 P0 差距清单勾选 ≥50% 功能项 |

**风险**：画布改动面大 → **只补清单内面板，不做布局大翻修**（翻修归 226）。  
**预估**：8–14 人天。

#### W1 · 四 Worker 并行编排（避冲突 · 2026-07-29）

| Worker | 工作树 | 分支 | 任务 | 差距(Δ) | 目标% | 核心缺失 |
|--------|--------|------|------|---------|-------|----------|
| **W1** | `aos-platform-w1` | `feature/223-worker-1` | **A1** 变量 API 接线 + CRUD | **-30%** | 35→65 | 无真 API、不回写画布 |
| **W2** | `aos-platform-w2` | `feature/223-worker-2` | **A2** 模块接口 schema CRUD | **-20%** | 45→65 | 非完整 schema CRUD |
| **W3** | `aos-platform-w3` | `feature/223-worker-3` | **A3** Draft 真审批链路 | **-20%** | 55→75 | 默认 MOCK，真审批弱 |
| **W4** | `aos-platform-w4` | `feature/223-worker-4` | **B1** 画布功能面板（非全像素） | **-20%** | 55→75 | 关键功能面板缺失 |

**后端已有（优先接线，勿重复造轮子）**

- 变量：`/v1/modules/:id/variables` ← `modules_variables.py` + `module_variables.py`
- 接口：`/v1/modules/:id/interface` ← `modules_interface.py` + `module_interfaces.py`
- Draft：`/v1/aip/drafts` + approve/reject ← `phase3_aip_drafts.py` + `aip_drafts_engine.py`

**文件所有权（写权限 · 严禁越界）**

| Worker | 可写 | 禁止碰 |
|--------|------|--------|
| W1 | `VariablesPage.tsx`(+test)、`module_variables.py`、`routers/modules_variables.py`、`demo/seed_module_variables.py`；CSS 仅追加 `/* W1-A1 */` 段 | `CanvasPage*`、`DraftInbox*`、`extras.tsx`、`styles-di-bridge.css`、`nav.ts` |
| W2 | **新建** `ModuleInterfacePage.tsx`(+test)，从 `extras.tsx` 迁出并留 re-export；`module_interfaces.py`、`routers/modules_interface.py`；CSS 仅 `/* W2-A2 */` | `VariablesPage*`、`DraftInbox*`、`CanvasPage*`、`canvasWidgets*`、`CanvasTabs*` |
| W3 | `DraftInboxPage.tsx`(+test)、`aip_drafts_engine.py`、`routers/phase3_aip_drafts.py`、`styles-di-bridge.css`；勿改主 `styles.css` | `VariablesPage*`、`Canvas*`、`ModuleInterface*`、`extras.tsx` |
| W4 | `CanvasPage.tsx`、`CanvasTabs.tsx`、`canvasWidgets.tsx`、必要时 `ComponentTreeEditor.tsx`；CSS 仅 `/* W4-B1 */` | `VariablesPage*`、`DraftInbox*`、`ModuleInterface*`、`extras.tsx`、变量/Draft 后端 |

**避冲突硬规则**

1. 互不改对方文件；`styles.css` 只在末尾追加自己的标记段。  
2. **W4 不依赖 W1 新建文件**：画布直接调已有 `/v1/modules/:id/variables`（只读展示/绑定 UI）。  
3. **W2 迁出 ModuleInterface**，减少多人改 `extras.tsx`。  
4. 各 Worker **在自己分支提交**（前缀 `w1(A1):` / `w2(A2):` / `w3(A3):` / `w4(B1):`）。  
5. **禁止 push**；禁止改 `m1`；禁止 merge 其他 worker。  
6. 完成后在工作树根写 `Wn_DONE.md`：改动、验收、风险。

**Planner 收口（四 Worker 全完成后）**

1. `m1` 依次 merge worker-1→2→3→4。  
2. 提交 merge 结果。  
3. 四 worker FF/merge `m1`，五分支同 tip。  
4. 回写本文勾选 + §1.1 现%。

**W1 进度板**

| Worker | 状态 | 备注 |
|--------|------|------|
| W1 A1 | ✅ 完成 | `9ad32ce` → 已合入 m1 |
| W2 A2 | ✅ 完成 | `2643124` → 已合入 m1 |
| W3 A3 | ✅ 完成 | `83c0f10` → 已合入 m1 |
| W4 B1 | ✅ 完成 | `4c15c8e` → 已合入 m1 |
| Planner 合并 | ✅ 完成 | 五分支同 tip `ad2241a`（styles.css 保留 W1/W2/W4 三段） |


### W2 — AIP / 模型脱 MOCK

| 任务 | 验收 |
|------|------|
| A4 分析师执行 | NL/SQL 至少一条真查询路径；失败可降级 |
| A5 可观测 | Trace/指标至少接一条真实或采样 API |
| A6 容量 | 使用量/限流读 API 或明确「演示」角标 + 读配置 |
| A7 模型目录 | 目录/已注册来自 API |
| B2 Studio 加深 | 4 Tab 中 ≥2 个可写配置并保存 |
| B4 发布 | 环境卡 + 发布步骤与 API 一致 |
| B5 插件 | 配置保存 + 连通测试可用 |

**预估**：10–16 人天。

#### W2 · 四 Worker 并行编排（避冲突 · 2026-07-29）

| Worker | 工作树 | 分支 | 任务 | 差距(Δ) | 目标% | 核心缺失 |
|--------|--------|------|------|---------|-------|----------|
| **W1** | `aos-platform-w1` | `feature/223-worker-1` | **A4** 分析师真查询 | **-15%** | 60→75 | live query API、历史记录 |
| **W2** | `aos-platform-w2` | `feature/223-worker-2` | **A5** 可观测脱 MOCK | **-15%** | 60→75 | Overview/Traces 真 API |
| **W3** | `aos-platform-w3` | `feature/223-worker-3` | **A6+A7** 容量 + 模型目录 | **-15%** | 60/65→75/80 | usage/limits API、catalog API |
| **W4** | `aos-platform-w4` | `feature/223-worker-4` | **B2+B4** Studio 加深 + 发布 | **-20%** | 65→85 | 多 Tab 配置保存、发布步骤反馈 |

**已有后端（优先接线）**

- 容量：`/v1/aip/capacity/*` ← `routers/model_capacity.py`
- 模型目录：`/v1/aip/model-catalog` ← `routers/model_catalog.py`
- 发布：`POST /v1/modules/:id/publish`（PublishPage 已有部分调用）
- 分析师 / 可观测：若无现成路由，**本 Worker 可在归属文件内新增最小 API**（勿改他人 router）

**文件所有权**

| Worker | 可写 | 禁止碰 |
|--------|------|--------|
| W1 A4 | `AipAnalystPage.tsx`(+test)；新建 `routers/aip_analyst.py` 或扩展现有 analytics；相关 engine/seed | Observability/Capacity/ModelCatalog/Studio/Publish/Capability |
| W2 A5 | `ObservabilityPage.tsx`(+test)；新建 `routers/aip_observability.py` 或 metrics 读接口 | Analyst/Capacity/ModelCatalog/Studio/Publish |
| W3 A6+A7 | `CapacityPage.tsx`(+test)、`ModelCatalogPage.tsx`(+test)、`model_capacity.py`、`model_catalog.py` 及对应 routers | Analyst/Observability/Studio/Publish |
| W4 B2+B4 | `StudioPage.tsx`(+test)、`PublishPage.tsx`(+test)；modules publish/deploy 相关最小后端 | Analyst/Observability/Capacity/ModelCatalog/`CapabilityPage` |

**避冲突硬规则**

1. 互不改对方文件；`styles.css` 仅末尾追加 `/* === W2-A4|A5|A6|B2 === */`。  
2. **禁止**改 `nav.ts` / 大改 `App.tsx` / `routes.tsx`（除非本页 import 断裂且最小修复）。  
3. commit 前缀：`w1(A4):` / `w2(A5):` / `w3(A6A7):` / `w4(B2B4):`。  
4. 禁止 push、改 `m1`、merge 其他 worker。  
5. 完成后写 `W{n}_W2_DONE.md`。

**Planner 收口**：同 W1 — merge w1→w2→w3→w4 入 m1 → 对齐五分支 → 回写 §1.1。

**W2 进度板**

| Worker | 状态 | 备注 |
|--------|------|------|
| W1 A4 | ✅ 完成 | `eb6d7a9` → 已合入 m1 |
| W2 A5 | ✅ 完成 | `2d88f56` → 已合入 m1 |
| W3 A6+A7 | ✅ 完成 | `cee41e7` → 已合入 m1 |
| W4 B2+B4 | ✅ 完成 | `7b8e085` → 已合入 m1 |
| Planner 合并 | ✅ 完成 | 五分支同 tip `b7c8760` |

### W3 — 本体详情 + 数据详情抬升

| 任务 | 验收 |
|------|------|
| C1/B3 Wiki 详情 | 可编辑保存主内容；版本列表可见（不要求完整 Slate 1:1） |
| C5 Wiki diff | 两版本文本 diff 可展示 |
| C2 对象类型详情 | 元数据项补齐 + 链接关系非纯 ASCII |
| C3 属性编辑 | 列映射 Tab 可用 |
| C4 Function | 参数表 + 试跑面板可用 |
| C6 管道详情 | 变换节点或运行历史至少一侧可用 |
| C7 数据源详情 | Schema 树或表预览与连接器一致 |

**预估**：12–20 人天。

#### W3 · 四 Worker 并行编排（避冲突 · 2026-07-29）

| Worker | 工作树 | 分支 | 任务 | 差距(Δ) | 目标% | 核心缺失 |
|--------|--------|------|------|---------|-------|----------|
| **W1** | `aos-platform-w1` | `feature/223-worker-1` | **C1+C5** Wiki 详情 + Wiki 差异 | **-20%** | 55/60→85/80 | 编辑保存、版本历史、diff 渲染 |
| **W2** | `aos-platform-w2` | `feature/223-worker-2` | **C2** 对象类型详情 | **-10%** | 80→90 | 元数据补齐、SVG 链接图 |
| **W3** | `aos-platform-w3` | `feature/223-worker-3` | **C3+C4** 属性编辑 + Function | **-15%** | 70/65→85/85 | 列映射 Tab、参数表 CRUD |
| **W4** | `aos-platform-w4` | `feature/223-worker-4` | **C6+C7** 管道详情 + 数据源详情 | **-17%** | 70/65→85/85 | 历史记录、Schema 树预览 |

**文件所有权**

| Worker | 可写 | 禁止碰 |
|--------|------|--------|
| W1 | `WikiDetailPage.tsx`(+test)、`WikiDiffPage.tsx`(+test)；wiki 相关 API/engine（若有） | ObjectType/Property/Function/pipeline/source |
| W2 | `ObjectTypeDetailPage.tsx`、`objectTypeDetail.tsx`(+test)；OT 相关最小后端 | Wiki*/Property/Function/pipeline/source |
| W3 | `PropertyEditorPage.tsx`(+test)、`FunctionEditorPage.tsx`(+test)；属性/函数最小后端 | Wiki*/ObjectType/pipeline/source |
| W4 | `pipelineCanvas.tsx`(+test)、`sourceDetailPage.tsx`(+test)；管道/源最小后端 | Wiki*/ObjectType/Property/Function |

**避冲突硬规则**

1. 互不改对方文件；`styles.css` 仅末尾追加 `/* === W3-C1|C2|C3|C6 === */`。  
2. 禁止改 `main.py`（优先挂已有 router）；禁止 `nav.ts` 大改。  
3. commit 前缀：`w1(C1C5):` / `w2(C2):` / `w3(C3C4):` / `w4(C6C7):`。  
4. 禁止 push / 改 m1 / merge 他支。  
5. 完成后写 `W{n}_W3_DONE.md`。  
6. **B5 插件本波仍不做**（收口波再排）。

**Planner 收口**：同 W1/W2 — merge w1→w2→w3→w4 入 m1 → 五分支同 tip → 回写 §1.1 / checklist-3。

**W3 进度板**

| Worker | 状态 | 备注 |
|--------|------|------|
| W1 C1+C5 | ✅ 完成 | `37281dc` → 已合入 m1 |
| W2 C2 | ✅ 完成 | `be30035` → 已合入 m1 |
| W3 C3+C4 | ✅ 完成 | `d91c732` → 已合入 m1 |
| W4 C6+C7 | ✅ 完成 | `267dd6c` → 已合入 m1 |
| Planner 合并 | ✅ 完成 | 五分支同 tip `4a8ceb8` |

### W4 — 收口与遗漏

- C8 Link/Action 可视化  
- D 类页面按需点修  
- E1 DocIntel 管道页去留决策并实施或关闭  
- 与 226 剩余 Batch 视觉对账合流  
- **补做** B5 智能体插件（W2 延期）

**预估**：8–12 人天。

#### W4 · 四 Worker 并行编排（避冲突 · 2026-07-29）

| Worker | 工作树 | 分支 | 任务 | 差距(Δ) | 目标% | 核心缺失 |
|--------|--------|------|------|---------|-------|----------|
| **W1** | `aos-platform-w1` | `feature/223-worker-1` | **C8a** 链接类型详情可视化 | **-15%** | 70→85 | 可视化组件、布局优化 |
| **W2** | `aos-platform-w2` | `feature/223-worker-2` | **C8b** Action 详情可视化/布局 | **-15%** | 70→85 | 可视化组件、布局优化 |
| **W3** | `aos-platform-w3` | `feature/223-worker-3` | **B5** 智能体插件配置+连通测试 | **-15%** | 70→85 | 配置持久化、连通性测试 |
| **W4** | `aos-platform-w4` | `feature/223-worker-4` | **E1+A8** 文档智能接线 | **-20%** | 55→75 | 抽取 API、列表展示 |

**文件所有权**

| Worker | 可写 | 禁止碰 |
|--------|------|--------|
| W1 | `LinkTypeEditorPage.tsx`(+test)；link 相关最小后端 | Action*/Capability*/DocumentIntelligence* |
| W2 | `ActionTypeEditorPage.tsx`(+test)；action 相关最小后端 | Link*/Capability*/DocumentIntelligence* |
| W3 | `CapabilityPage.tsx`(+test)；`aip_capabilities*` / `phase3_aip_capabilities.py` | Link*/Action*/DocumentIntelligence* |
| W4 | `DocumentIntelligencePage.tsx`(+test)；doc-intel 最小后端；可在 `nav.ts` **仅**加注释/隐藏项说明；**勿新建侧栏项** | Link*/Action*/Capability* |

**避冲突硬规则**

1. 互不改对方文件；`styles.css` 末尾追加 `/* === W4-C8a|C8b|B5|E1 === */`。  
2. 禁止改 `main.py`（优先已有 router）。  
3. commit 前缀：`w1(C8a):` / `w2(C8b):` / `w3(B5):` / `w4(E1):`。  
4. 禁止 push / 改 m1 / merge 他支。  
5. 完成后写 `W{n}_W4_DONE.md`。  
6. **226 全量截图对账不在本波**（只在 DONE 里列「建议下一批 226 页面」）；本波以功能收口为主。

**Planner 收口**：merge w1→w2→w3→w4 入 m1 → 五分支同 tip → 回写 §1.1 / checklist-3 → 勾 §8 验收总闸可勾项。

**W4 进度板**

| Worker | 状态 | 备注 |
|--------|------|------|
| W1 C8a | ✅ 完成 | `f70b751` → 已合入 m1 |
| W2 C8b | ✅ 完成 | `ddd60bf` → 已合入 m1 |
| W3 B5 | ✅ 完成 | `b9c388e` → 已合入 m1 |
| W4 E1+A8 | ✅ 完成 | `5703c91` → 已合入 m1 |
| Planner 合并 | ✅ 完成 | 五分支同 tip `01da807` |

---

## 4. 与 226 视觉对账的历史分工

| 计划 | 管什么 |
|------|--------|
| **227（本文）** | 功能完整度、API、主流程闭环、详情能力 |
| **226** | 布局/CSS/组件样式与 HTML 视觉稿 1:1 |

同一页面可先 227 后 226；**禁止**在 227 波次大范围复制 Tailwind CDN class（见 226 S12）。

---

## 5. 文档同步约定（历史）

每完成一波：

1. 更新 `223-deep-checklist.md` §1.1 对应 **新%** 与「现状要点」  
2. 同步 `-2` / `-3` 文首总览表中的现%  
3. 在本文对应波次打勾，并记风险/回滚点  

---

## 6. 当前处理入口（替代已过期的开工顺序）

1. **不重开 227 W1～W4，也不重开已关闭的 Wave 3A～3C**；发现回归问题时，对应到具体验收项做最小修复。  
2. 功能与数据闭环的新需求：先新建方案文档，再编码和单测，不使用本节历史 worker 分工。  
3. 视觉/CSS/逐页对账：进入 [`226-逐页视觉对账计划.md`](./226-逐页视觉对账计划.md)。  
4. 结构、安全、契约、CI/交付可信度：进入 [`228-结构化风险修复计划.md`](./228-结构化风险修复计划.md)。  
5. 具体电商平台接入：仍不在 227 范围，保留给后续专项开发。

**2026-08-02 通用平台增量收口**：AIP Logic Stage C C1 已完成并合入 m1 `bc9711a`，包括租户持久化 Evals、指定 revision/hash 的 canonical 真执行评测、不可变 publication、页面发布门禁和双重回读。Web 1781 项和 API 8180 项全量回归通过；浏览器已覆盖 Eval 失败阻断、1/1 成功门控、Dry-Run、发布详情回读、刷新及 API 重启恢复。Automation/生产写回仍 fail-closed。详见 [`228-AIP-Logic-Stage-C发布治理与服务端闭环方案.md`](./228-AIP-Logic-Stage-C发布治理与服务端闭环方案.md) §9。

---

## 7. 非目标（本计划不做）

- 重写导航信息架构（侧栏隐藏「创建应用/模块管理」已完成，保持）  
- 电商行业方案（`电商平台接入/*`）与本 UI 完整度并行，不混排期  
- 全站品牌色强制替换（若做，单开变更）  
- 把 `223-plan.md` 历史「缺失新建」任务重新开单  

---

## 8. 验收总闸（历史收口 + 当前复核）

- [x] `-3` 九页均 ≥50%，Wiki 详情至少 ≥55%  
- [x] 变量可回写画布；Draft 在联调环境可真审批（有 API；无后端演示降级）  
- [x] 主流程回归：管道列表→画布真拖动/折叠/保存/重启重放、Workshop 发布门、Wave 3B 的 15 页主任务，以及 Wave 3C 的 14 页主任务均已通过。  
- [x] §1.1 中 **现% &lt; 50** 的菜单/详情主项已清零或已声明（剩余多为运维静态页等，可进 226）  
- [x] E1：`pipeline-doc-intel` 并入文档智能  

---

## 附录：权威文档索引

| 文档 | 用途 |
|------|------|
| `223-deep-checklist.md` §1.1 | 227 收口时的菜单完整度历史表 |
| `223-deep-checklist.md` §7+ | P0/P2 组件差距详表 |
| `223-deep-checklist-2.md` | P1 差距详表 + 现% |
| `223-deep-checklist-3.md` | 详情页差距详表 + 现% |
| `226-逐页视觉对账计划.md` | 视觉对账 |
| `223-plan.md` | 历史总计划（仅参考已交付记录） |
