# 228-W1-W3 CSS 与导航分层第一刀方案

> 日期：2026-07-31
> Worker：W3 / `aos-platform-w3` / `feature/223-worker-3`
> 基线：`7f6434a`
> 状态：✅ 已完成并合入 `m1`

## 1. 目标

对 11,085 行 `styles.css` 与 708 行 `nav.ts` 做零视觉、零导航业务变更的第一刀机械拆分，保持 Web/Desktop 公共入口兼容和 `packages/ui-kit/tokens.css` 唯一 token 源。

## 2. 固定边界

- `apps/web/src/styles.css` 保留为稳定 facade；导入顺序固定为 token → DI bridge → 六个连续 CSS 模块。
- `styles-di-bridge.css` 保持第二顺位，不修改。
- 子 CSS 不再次导入 token，不替换颜色、不排序 selector、不格式化声明。
- `apps/web/src/nav.ts` 保留公共 facade；`./nav` 与 `@aos-web/nav` 导出完全兼容。
- 74 个 page、8 个 section、4 个 subgroup、3 个诚实 `s2` 状态和原顺序保持不变（page 数以 `7f6434a` 基线静态序列为准，不在本次重构中补造页面）。
- 固定布局业务工作台不改成 Canvas；具体电商页面不开发。

## 3. 文件所有权

W3 可修改：

- `apps/web/src/nav.ts`、`apps/web/src/nav.test.ts`
- 新增 `apps/web/src/navigation/types.ts`、`navigation/items.ts`
- `apps/web/src/styles.css`
- 新增 `apps/web/src/styles/00-shell.css`、`10-data-workshop.css`、`20-aip-ontology.css`、`30-model-data-platform.css`、`40-components-workshop.css`、`50-s2-overrides.css`
- 可新增样式结构测试

总控独占协调：

- `services/aos-api/tests/test_ui_buttons_186m.py`：从直接读取 facade 改为按 import 顺序读取 CSS 模块，禁止在 facade 塞伪 selector。

禁止修改 token 源、DI bridge、页面/路由、Desktop、manifest/lock、CI。

## 4. 两个独立提交

### A. nav

- `types.ts`：类型与 IconName。
- `items.ts`：完整原序 NAV_ITEMS。
- `nav.ts`：兼容 facade 和 helper。
- 增加 id/path 唯一、section/subgroup 精确顺序、状态集合、s2 白名单和最长路径前缀测试。

### B. CSS

仅按原连续完整规则块搬移，建议边界：

- 00：原 4–1448
- 10：1449–3411
- 20：3412–5308
- 30：5309–6472
- 40：6473–8452
- 50：8453–11085

实际边界必须避开未闭合规则、media 和 keyframes。facade 只保留严格有序 imports。

## 5. 验收

- [x] nav 公开导出清单及 `navPages()` 的 id/path/status 序列前后完全相等。
- [x] CSS 构建产物内容/顺序等价；token 仅导入一次。
- [x] Web nav tests、Desktop parity/deepLink、相关 Python CSS 测试通过。
- [x] Web/Desktop typecheck、test、build 通过。
- [x] 10 个关键页面 × 明暗主题共 20 张同 viewport 截图无非预期差异。
- [x] Wave 1 最终 full 回归通过。

## 6. 完成证据

- Worker nav `6e45a24`、CSS `e6d91bf`，合并 `b1c8895`；总控 CSS 测试适配 `70eec32`。
- 74 个 page 的 id/path/status 序列与基线完全相等；六个 CSS 模块按 facade 顺序拼接后与原 11,085 行内容完全相等。
- Web 1557、Desktop 40、SDK 6，两端 typecheck/build 全绿；Python CSS 来源测试 3/3。
- 20 张验收图归档在 `evidence/228-W1-W3-visual/`；概览浅色/深色 SHA-256 分别为 `0f62db2b...`、`2aef521e...`。
- W3 阶段 wave 8/8、最终 `full` 12/12。

## 7. 回滚

nav 与 CSS 两个提交独立回滚。任一导出、顺序、构建 CSS、截图或暗色覆盖变化即回滚对应提交；无数据迁移。
