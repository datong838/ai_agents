# 27 · 本机开发基础设施与工程门禁记录

> **文档性质**：**活文档**（Agent 主驾驶安装 / 自检 / 落档；人仅审结果）  
> **版本**：v1.16 · 2026-07-17  
> **对齐**：[26 §0.1.4](26-AOS目标态开发计划.md) G1～G5 · [24 §4](24-AOS客户侧前置组件安装SOP.md) · [23](23-AOS开源引用与交付军规.md) · [T-UI](T-UI-前端工程与foundry-html落地规范.md) · [T-API](T-API-aos-api稳定契约.md) · [T-CROSS §3.2](T-CROSS-横切能力详细技术方案.md)  
> **工程落点**：`c:\work\projects\wchat\aos-platform\`（自有仓；与 `mybuddy-v01` / Dify 容器隔离）  
> **军规提醒**：`deploy/dev/` 仅本机；**MinIO Server 不进客户包**（AGPL）

---

## 使用的 Rules

| Rule | 应用 |
| --- | --- |
| 中文 | 全文 |
| Agent 主驾驶 | 安装、探针、自测、回写本文；人默认不参与 |
| 先方案后动手 | 口径来自 24/23/26，不另起冲突顺序 |
| 最小更改 | Dev 前置与 Wave-0 脚手架只做门禁必要项 |
| 自测落档 | 每项绿/红必须有命令证据与时间戳 |

---

## 0. 职责分工

| 角色 | 做什么 |
| --- | --- |
| **Agent（主驾驶）** | 装基础设施、建 `aos-platform` 骨架、跑探针与构建、更新本文 §1～§5 |
| **人（可选审阅）** | 只看本文「门禁总览」；需要决策时再介入 |

---

## 1. 门禁总览（结果页）


| ID | 目标 | 状态 | 最近自测 | 证据摘要 |
| --- | --- | --- | --- | --- |
| **G1** | `services/aos-api` + `apps/web` 可 build/启动 | ✅ | 2026-07-17 | API `:8080/v1/health=ok`；`npm run build` 成功 |
| **G2** | `packages/contracts/openapi/v1.yaml` 含 buddy/ask | ✅ | 2026-07-17 | 契约文件含 `/buddy/ask`；运行面 `POST /v1/buddy/ask` 200 |
| **G3** | CI 扫描禁 UI 上游 SDK；违规样例红 | ✅ | 2026-07-17 | `check-no-upstream-sdk.ps1` PASS；`-ExpectFail` 检出 openai fixture |
| **G4** | 统一 Logger + `trace_id` | ✅ | 2026-07-17 | JSON 日志含 `trace_id=gate-test-1`；响应头 `X-Trace-Id` |
| **G5** | PG + 对象存储 Dev 探针绿 | ✅ | 2026-07-17 | PG `:5433` `SELECT 1`；MinIO Put/Get `dev-probe.txt` |


**结论（Agent）：G1～G5 全部 ✅，可按 26 §4 进入 Wave-0 后续任务（T0.3 Auth 占位等）。**

---

## 2. 本机基线快照

> 探测时间：2026-07-17

| 项 | 值 | 备注 |
| --- | --- | --- |
| OS | Windows 10.0.19045 | Docker Engine linux/amd64 |
| Docker | 29.1.3 · Compose 2.40.3 | ✅ |
| Node | v22.22.0 · npm 10.9.8 | ✅ |
| Python | 3.14.6 · fastapi / uvicorn | ✅ |
| 已有容器 | Dify 栈（内部 PG **未映射主机**） | **未复用**；AOS 独立 `aos-dev-*` |
| 主机 6379 | LISTEN | Lite 建议项；本波不阻断 |
| `aos-platform` | 已新建 | `c:\work\projects\wchat\aos-platform` |

---

## 3. G5 · Dev 前置（基础设施）

### 3.1 安装结果

| 组件 | 容器 / 路径 | 主机 | 状态 |
| --- | --- | --- | --- |
| PostgreSQL 16 | `aos-dev-pg` | `127.0.0.1:5433` · db `aos_meta` · user `aos_app` | ✅ healthy |
| MinIO | `aos-dev-minio` | API `9000` · Console `9001` | ✅ healthy · bucket `aos-media` |
| LLM Echo | `aos-dev-llm-echo` / host `:8081` | Dev Provider | ✅ |
| LiteLLM 形边车 | compose / `start-llm-sidecar-host.ps1` `:4000` | T3.9 | ✅ host 模式已证；compose 受 B-WSL-HOSTPORT 影响时用 host |
| Compose | `aos-platform/deploy/dev/docker-compose.yml` | — | ✅ `up -d` |
| Handoff | `deploy/dev/prereq-handoff.local.yaml` | — | ✅ |
| Secrets | `deploy/dev/.secrets.env`（gitignored） | 仅 Dev 占位口令 | ✅ |
| AGE | — | — | ❌ 未装（Ontology 图查询前再开） |

### 3.2 操作与探针日志

| 时间 (UTC+约) | 动作 | 结果 |
| --- | --- | --- |
| 2026-07-17 | `docker compose -f deploy/dev/docker-compose.yml pull && up -d` | pg/minio Created · Started |
| 2026-07-17 | `docker exec aos-dev-pg psql … -c "SELECT 1"` | `ok = 1` |
| 2026-07-17 | `GET http://127.0.0.1:9000/minio/health/live` | HTTP 200 |
| 2026-07-17 | mc Put/Get `local/aos-media/dev-probe.txt` | 内容 `probe` · `PUT_GET_OK` |
| 2026-07-17 | init 日志 | `Bucket created … aos-media` · `minio-init-ok` |

### 3.3 G5 验收勾选

- [x] PG `SELECT 1`  
- [x] 对象仓 Put + Get  
- [x] handoff 存在（`environment: lite` / LOCAL_DEV）  
- [x] 声明不进客户包（README + 本文 + compose 头注释）  

### 3.4 基础设施运维手册（启动 / 关闭 / 在线检测）

> **工作目录（以下命令均先执行）：**  
> `cd c:\work\projects\wchat\aos-platform`  
> Compose 文件：`deploy/dev/docker-compose.yml`  
> 一键检测脚本：`deploy/dev/status.ps1`

#### 3.4.1 全家桶（PG + MinIO）

| 动作 | PowerShell | 期望 |
| --- | --- | --- |
| **启动** | `docker compose -f deploy/dev/docker-compose.yml up -d` | 创建并后台运行 `aos-dev-pg`、`aos-dev-minio`；init 容器跑完退出码 0 |
| **关闭（停容器，保留数据卷）** | `docker compose -f deploy/dev/docker-compose.yml stop` | 端口 5433/9000/9001 不再监听；数据仍在 volume |
| **关闭并删除容器（仍保留卷）** | `docker compose -f deploy/dev/docker-compose.yml down` | 容器删除；`aos_dev_pg_data` / `aos_dev_minio_data` 默认保留 |
| **危险：连数据卷一起删** | `docker compose -f deploy/dev/docker-compose.yml down -v` | **清空** Dev 库与对象；仅重建环境时用 |
| **看状态** | `docker compose -f deploy/dev/docker-compose.yml ps -a` | `aos-dev-pg` / `aos-dev-minio` 为 `Up … (healthy)` |
| **一键在线检测** | `powershell -File deploy\dev\status.ps1` | 末行 `RESULT: Dev prereq ONLINE`；exit 0 |

#### 3.4.2 PostgreSQL（`aos-dev-pg`）

| 动作 | 命令 | 在线判定 |
| --- | --- | --- |
| 只启动 PG | `docker compose -f deploy/dev/docker-compose.yml up -d aos-dev-pg` | — |
| 只停止 PG | `docker stop aos-dev-pg` | — |
| 连通检测 | `docker exec aos-dev-pg pg_isready -U aos_app -d aos_meta` | 输出含 `accepting connections` |
| SQL 探针 | `docker exec aos-dev-pg psql -U aos_app -d aos_meta -c "SELECT 1 AS ok;"` | 一行 `1` |
| 主机端口 | `Test-NetConnection 127.0.0.1 -Port 5433` | `TcpTestSucceeded : True` |

连接串（Dev）：`postgresql://aos_app:***@127.0.0.1:5433/aos_meta`（口令见 `deploy/dev/.secrets.env`）

#### 3.4.3 MinIO（`aos-dev-minio`）

| 动作 | 命令 | 在线判定 |
| --- | --- | --- |
| 只启动 MinIO | `docker compose -f deploy/dev/docker-compose.yml up -d aos-dev-minio` | — |
| 只停止 MinIO | `docker stop aos-dev-minio` | — |
| **API 存活** | `Invoke-WebRequest http://127.0.0.1:9000/minio/health/live -UseBasicParsing` | **HTTP 200** = 在线 |
| **Console** | 浏览器打开 `http://127.0.0.1:9001` 或对 `:9001` 发 GET | 能打开登录页 / HTTP 200 |
| 读写探针 | 见下方 mc 片段 | Put/Get 成功 |
| 重建 bucket（一般不必） | `docker compose -f deploy/dev/docker-compose.yml run --rm aos-dev-minio-init` | 日志含 `minio-init-ok` |

**账号（仅 Dev）：** user `aosdev` · 密码见 `.secrets.env` · bucket `aos-media`  
**Console：** `http://127.0.0.1:9001`

读写探针（容器网络内 mc）：

```powershell
docker run --rm --entrypoint /bin/sh --network aos-dev-prereq_default `
  minio/mc:RELEASE.2025-04-16T18-13-26Z -c `
  "mc alias set local http://aos-dev-minio:9000 aosdev aos_dev_only_change_me && mc ls local/aos-media"
```

#### 3.4.4 LiteLLM 形边车（T3.9）

| 动作 | 命令 | 在线判定 |
| --- | --- | --- |
| Host 模式启动（推荐本机） | `powershell -File deploy\dev\start-llm-sidecar-host.ps1` | `:8081/health` · `:4000/health` |
| Compose | `docker compose -f deploy/dev/docker-compose.yml up -d aos-dev-llm-echo aos-dev-litellm` | 容器 Up；若 Windows `127.0.0.1` 不通见 B-WSL-HOSTPORT |
| aos-api 环境 | `AOS_LITELLM_URL=http://127.0.0.1:4000` · `AOS_LITELLM_FALLBACK=off` · `AOS_LLM_MASTER_KEY=…` · `AOS_LLM_API_KEY_REF=vault:…` | `/v1/aip/chat` → `sidecar=litellm` |
| PG DSN（WSL） | `AOS_DATABASE_URL=postgresql://aos_app:***@<wsl-ip>:5433/aos_meta` | health ok |

方案：[33](33-T3.9-LiteLLM边车去stub方案.md)

#### 3.4.5 判定速查

| 组件 | 在线 | 离线常见现象 |
| --- | --- | --- |
| PG | `pg_isready` 成功 · `:5433` 通 | `No such container` / connection refused |
| MinIO API | `…/minio/health/live` → **200** | 超时 / 连接被拒绝 |
| MinIO Console | `:9001` 可打开 | 页面打不开（API 在、Console 挂极少见） |
| 全家桶 | `status.ps1` → `ONLINE` | 脚本 exit 1 |

#### 3.4.5 自测记录（运维手册）

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-07-17 | `compose ps` + `pg_isready` + MinIO `/minio/health/live` + Console `:9001` | 均为 ONLINE / healthy / HTTP 200 |
| 2026-07-17 | 写入 `deploy/dev/status.ps1` | 供日常一键检测 |

### 3.5 T0.8 Dev 数据面备忘（对照 24 Lite）

| 组件 | 本机状态 | 对照 24 Lite | 缺口 |
| --- | --- | --- | --- |
| PostgreSQL | Docker `aos-dev-pg` `:5433` · db `aos_meta` | 必 | ✅ |
| 对象存储 MinIO | Docker `aos-dev-minio` `:9000` | 必（客户自装；Dev 仅本机） | ✅ |
| AGE 扩展 | 未装 | Ontology 图前必 | ❌ → Wave-2 T2.1 |
| Redis | 主机 `:6379` 已有进程；未纳入 Compose | 建议 | ⚪ |
| OIDC / Keycloak | Dev token `Bearer dev` | Prod 必 | ❌ → TX.3 |
| Vault/密封 | 未 | Lite 必（后置） | ❌ |
| Prometheus/Grafana | 未 | 建议 | ❌ |

---

## 4. G1～G4 · 工程脚手架

### 4.1 目录（已落地）

```text
aos-platform/
├── apps/web/                      # Vite + React 18 + TS · build ✅
├── services/aos-api/              # FastAPI · Logger · /v1/health · /v1/buddy/ask
├── packages/contracts/openapi/v1.yaml
├── scripts/ci/check-no-upstream-sdk.ps1
├── scripts/ci/forbidden-ui-imports.txt
└── deploy/dev/                    # 仅本机 Compose
```

### 4.2 自测明细

| ID | 状态 | 命令 / 结果 |
| --- | --- | --- |
| **G1** | ✅ | `uvicorn aos_api.main:app --port 8080` → health `ok`；`apps/web` `npm run build` → vite built |
| **G2** | ✅ | OpenAPI 含 `/buddy/ask`；`POST /v1/buddy/ask` + Bearer → `echo: ping` + `traceId` |
| **G3** | ✅ | 默认扫描 PASS；`-ExpectFail` 命中 `fixtures/forbidden-import.fixture.ts` |
| **G4** | ✅ | 日志 JSON：`trace_id` / `org_id` / `project_id`；`AOS_LOG_LEVEL=debug`；Audit logger 预留 `aos.audit` |

启动摘录（G4 证据）：

```text
{"level":"info","logger":"aos-api","msg":"request method=POST path=/v1/buddy/ask status=200 …","trace_id":"gate-test-1",…}
```

---

## 5. 变更日志（Agent 追加）

| 时间 | 变更 |
| --- | --- |
| 2026-07-17 | v0.1 建文；基线探测 |
| 2026-07-17 | v0.2：**G1～G5 全绿**；独立 Dev PG/MinIO；aos-platform 脚手架；自测落档 |
| 2026-07-17 | v0.3：§3.4 **启动/关闭/在线检测**（PG·MinIO·全家桶）+ `deploy/dev/status.ps1` |
| 2026-07-17 | v0.4：Wave-0 编码收口（T0.3～T0.7+单测）· T0.8 备忘 · 挂 [28](28-Wave-0全链路集成测试方案.md)；Wave-1 开工（AppShell/Inbox/Selection） |

---

## 6. 障碍 / 需人介入

| ID | 问题 | 影响 | 需要人？ |
| --- | --- | --- | --- |
| N1 | AGE 扩展未装 | 不影响 G5；挡后续图查询 | 否（Agent 后续装） |
| N2 | Redis 未纳入 Compose | Lite 建议项 | 否 |
| N3 | aos-api 当前进程在本机 8080 常驻 | 重启机器后需再拉 | 否（README 有命令） |

---

## 7. 日常运维（Agent / 人可复制）

```powershell
cd c:\work\projects\wchat\aos-platform

# —— 基础设施（详见 §3.4）——
docker compose -f deploy/dev/docker-compose.yml up -d          # 启动
powershell -File deploy\dev\status.ps1                         # 是否在线
docker compose -f deploy/dev/docker-compose.yml stop           # 关闭（保留数据）

# —— aos-api ——
cd services\aos-api
$env:AOS_LOG_LEVEL="debug"
uvicorn aos_api.main:app --host 127.0.0.1 --port 8080

# —— Web ——
cd ..\..\apps\web
npm run dev

# —— G3 ——
powershell -File ..\..\scripts\ci\check-no-upstream-sdk.ps1
powershell -File ..\..\scripts\ci\check-no-upstream-sdk.ps1 -ExpectFail
```

---

## 8. 关联

- [26 开发计划](26-AOS目标态开发计划.md) §0.1.4  
- [24 SOP](24-AOS客户侧前置组件安装SOP.md) §4 / §5  
- [00 索引](00-技术方案索引.md)  
- 工程仓：`aos-platform/README.md`

---

*v0.4 · Agent 主驾驶 · G1～G5 ✅ · Wave-0 收口 · Wave-1 进行中*

### 3.6 Dev Keycloak（2026-07-17 · [50]）

| 项 | 值 |
| --- | --- |
| Compose | deploy/dev/docker-compose.yml · --profile oidc · os-dev-keycloak :8083 |
| Realm | os · client os-api · user lice |
| Probe | scripts/ci/probe-keycloak-oidc.ps1（未起则 SKIP） |
| Env | AOS_OIDC_JWKS_URL / ISSUER / TOKEN_URL 见 .env.example |

### 3.7 T0.9 / T0.10（2026-07-17 · [51]）

| 项 | 命令 / 产物 |
| --- | --- |
| 参考仓盘点 | `scripts/ci/record-aos-deps.ps1 -Strict` → `deploy/dev/aos-deps-inventory.md` |
| SBOM | `scripts/ci/generate-sbom.ps1` → `deploy/dev/sbom-dev.json` |
| 门禁 | `scripts/ci/check-sbom-gate.ps1 -Strict` |
| P0 本机 | PaddleOCR · minio · minio-py ✅ |

### 3.8 syft / trivy 加严（2026-07-17 · [54]）

| 项 | 命令 / 产物 |
| --- | --- |
| 可选扫描 | `scripts/ci/run-syft-trivy.ps1`（PATH 优先；否则 Docker） |
| 门禁挂钩 | `check-sbom-gate.ps1 -WithTools`（`-SkipDockerPull` 可跳过拉镜像） |
| 产物 | `deploy/dev/sbom-syft.json` · `trivy-fs.json` · `syft-trivy-report.md` |
| 本机注意 | `docker.cmd`→WSL 时 bind 用 `/mnt/<drive>/...`；docker trivy 为 license + `aos-trivy-cache` |
| HA Keycloak | 见 §3.11 · [57]；B-TX3-01 Dev 已关 |

### 3.9 Marking 继承 / OpenFGA Facade（2026-07-17 · [55]）

| 项 | 说明 |
| --- | --- |
| 继承 | `graph_edge.rel=inherits_markings_from`；种子 `wo-1003`←`Site/site-east`（restricted） |
| Facade | `POST /v1/authz/check` · `/v1/authz/tuples`；表 `authz_tuple` |
| 挂钩 | 对象 GET 403 / LIST 剔除；有元组才验 `viewer` |
| 可选外呼 | `AOS_OPENFGA_API_URL`（空=本地） |
| 测 | `pytest tests/test_marking_inherit_openfga.py`（与 field marking 共 13 绿） |

### 3.10 Ferry 镜像层（2026-07-17 · [56]）

| 项 | 说明 |
| --- | --- |
| 介质 | `artifacts/images.json` + `images.sig`；可选 `archives/*.tar` |
| 签名 | manifest HMAC；images = cosign 或 **cosign-dev-hmac** |
| 开关 | `AOS_FERRY_IMAGES` · `AOS_FERRY_SKOPEO=1` · `AOS_FERRY_COSIGN_KEY/PUB` |
| status | `mode=mvp-hmac+images` · `skopeo`/`cosign` 探针 |
| 测 | `pytest tests/test_ferry_images.py tests/test_ferry_mvp.py`（10 绿） |

### 3.11 Dev HA Keycloak（2026-07-17 · [57]）

| 项 | 说明 |
| --- | --- |
| Profile | `oidc-ha`（与 `oidc` 互斥，同抢 :8083） |
| 拓扑 | kc-db + keycloak-a/b + nginx LB |
| API | `AOS_OIDC_JWKS_URLS` 故障切换 · `AOS_OIDC_ISSUERS` |
| Probe | `scripts/ci/probe-keycloak-ha.ps1`（未起 SKIP） |
| 测 | `pytest tests/test_jwks_failover.py` + external_jwks（4 绿） |
| B-TX3-01 | **Dev 关闭**；生产 IdP 客户自备 |

### 3.12 OpenFGA 真边车（2026-07-17 · [58]）

| 项 | 说明 |
| --- | --- |
| Profile | `openfga` · 镜像 `openfga/openfga:v1.8.4` · `:8085` |
| Bootstrap | `scripts/ci/bootstrap-openfga.ps1` → `deploy/dev/openfga-store.env` |
| Probe | `scripts/ci/probe-openfga.ps1`（未起 SKIP） |
| API | `GET /v1/authz/status` · Check/Write 可走远程 |
| Env | `AOS_OPENFGA_API_URL` · `STORE_ID` · `STRICT` |
| 测 | `pytest tests/test_openfga_remote.py` + inherit（10 绿） |

### 3.13 Ferry skopeo archive 演练（2026-07-17 · [59]）

| 项 | 说明 |
| --- | --- |
| 开关 | `AOS_FERRY_SKOPEO=1`；默认关 |
| 演练 ref | `AOS_FERRY_SKOPEO_REFS=alpine:latest`（与清单镜像分离） |
| 模式 | PATH skopeo **或** `quay.io/skopeo/stable` + docker.sock |
| Probe | `scripts/ci/probe-ferry-skopeo.ps1`（本机已拷 alpine ~8MB） |
| 测 | `pytest tests/test_ferry_skopeo.py`（与 ferry 共 13 绿） |

### 3.14 生产 IdP 联调（2026-07-17 · [60]）

| 项 | 说明 |
| --- | --- |
| 手册 | [60-生产IdP联调手册](60-生产IdP联调手册.md) · 挂 [24] §3.4 |
| Claim | `orgId`/`organization_id` · `projectId` · `realm_access.roles` |
| Probe | `scripts/ci/probe-prod-idp.ps1`（无参 SKIP；有 Issuer/JWKS 测连通） |
| Env | 生产 `AOS_AUTH_ALLOW_DEV=0` + issuer/JWKS/audience（见 `.env.example`） |
| 测 | `pytest tests/test_idp_claim_aliases.py`（5 绿） |

### 3.15 OpenFGA 生产模型（2026-07-17 · [61]）

| 项 | 说明 |
| --- | --- |
| 模型 | `deploy/dev/openfga/model.json` + `model.fga`（aos-prod-v1） |
| Types | `organization` · `project` · `object`(viewer←editor←owner) · `marking` |
| API | `GET /v1/authz/model` · relation 白名单写校验 |
| Bootstrap | 多种子（viewer/org/project parent/marking bearer） |
| 测 | `pytest tests/test_openfga_model.py` + remote/inherit（17 绿） |

### 3.16 Ferry 大镜像现场打包（2026-07-17 · [62]）

| 项 | 说明 |
| --- | --- |
| 清单 | `deploy/ferry/customer-images.example.json` · `AOS_FERRY_IMAGES_MANIFEST` |
| 现场 | `pack-ferry-images-onsite.ps1` · `probe-ferry-large-images.ps1` |
| 门禁 | `AOS_FERRY_SKOPEO_MAX_MIB`（默认 64；超限不嵌入 API）· `AOS_FERRY_SKOPEO_TIMEOUT` |
| 自测 | alpine archive ~8.7MB；ferry pytest 含 onsite 共 19 绿 |
| SBOM | trivy docker：`--skip-dirs` 须加引号；180s 超时（修挂死） |

### 3.17 OpenFGA ↔ Markings 组合（2026-07-17 · [63]）

| 项 | 说明 |
| --- | --- |
| 公式 | markings = JWT ∪ `marking#bearer`；再 AND object viewer（有元组时） |
| 开关 | `AOS_AUTHZ_MARKING_BEARER`（默认 1） |
| 种子 | `user:bearer-only#bearer@marking:restricted` |
| 测 | `pytest tests/test_marking_fga_combine.py` + inherit（13 绿） |

### 3.18 Ferry 真 cosign 密钥链（2026-07-17 · [64]）

| 项 | 说明 |
| --- | --- |
| 生成 | `scripts/ci/gen-ferry-cosign-keys.ps1` → `deploy/dev/cosign/`（gitignore） |
| 探针 | `scripts/ci/probe-ferry-cosign.ps1` |
| API | PATH 或 Docker cosign；`AOS_FERRY_COSIGN_REQUIRED`；status.`fullChannelDeferred=true` |
| 测 | `pytest tests/test_ferry_cosign.py` + images/mvp（15 绿） |
| 诚实 | Full Spoke/Channel **仍延期** |

### 3.19 字段级 Marking ↔ FGA bearer（2026-07-17 · [65]）

| 项 | 说明 |
| --- | --- |
| 读/写 | `can_see_field` / `ensure_field_writes` 支持 JWT ∪ `marking#bearer` |
| 开关 | 同 [63] `AOS_AUTHZ_MARKING_BEARER`（默认 1） |
| 种子 | `user:field-bearer#bearer@marking:secret` → 可见 `internalCost` |
| 测 | `pytest tests/test_field_marking_fga.py` + field/combine/inherit（25 绿） |

### 3.20 Apollo Channel / Spoke 目录（2026-07-17 · [66]）

| 项 | 说明 |
| --- | --- |
| 表 | `apollo_channel` · `apollo_spoke` |
| API | `/v1/apollo/channels` · promote/recall · `/v1/apollo/spokes` · fleet 读库 |
| 种子 | dev/staging/stable · spoke-local-dev(lite) · spoke-full-stub(full/deferred) |
| 测 | `pytest tests/test_apollo_channels.py`（+ ferry cosign 共 12 绿） |
| 诚实 | `fullSpokeRuntimeDeferred=true`（无 K8s/Helm） |

### 3.21 Apollo Change / Release UI（2026-07-17 · [67]）

| 项 | 说明 |
| --- | --- |
| Release | `/apollo/release` · channels 列表 · Promote/Recall 按钮 |
| Change | Draft + Channel 梯子摘要 · 跳转 Release |
| Spoke | `/v1/apollo/spokes` catalog 列表 |
| 测 | `apps/web` nav test · `pytest tests/test_apollo_channels.py` |
| 诚实 | Full 运行时仍延期 |
