# 27 · 本机开发基础设施与工程门禁记录

> **文档性质**：**活文档**（Agent 主驾驶安装 / 自检 / 落档；人仅审结果）  
> **版本**：v0.2 · 2026-07-17  
> **对齐**：[26 §0.1.4](26-AOS目标态开发计划.md) G1～G5 · [24 §4](24-AOS客户侧前置组件安装SOP.md) · [23](23-AOS开源引用与交付军规.md) · [T-UI](T-UI-前端工程与foundry-html落地规范.md) · [T-API](T-API-aos-api稳定契约.md) · [T-CROSS §3.2](T-CROSS-横切能力详细技术方案.md)  
> **工程落点**：`c:\work\projects\wchat\aos-platform\`（自有仓；与 `mybuddy-v01` / Dify 容器隔离）  
> **军规提醒**：`deploy/dev/` 仅本机；**MinIO Server 不进客户包**（AGPL）

---

## 使用的 Rules

| Rule | 应用 |
| --- | --- |
| 中文 | 全文 |
| Agent 主驾驶 | 安装、探针、自测、回写本文；人默认不参与 |
| 先方案后动手 | 口径来自 24/23/26，不另起冲突顺序 |
| 最小更改 | Dev 前置与 Wave-0 脚手架只做门禁必要项 |
| 自测落档 | 每项绿/红必须有命令证据与时间戳 |

---

## 0. 职责分工

| 角色 | 做什么 |
| --- | --- |
| **Agent（主驾驶）** | 装基础设施、建 `aos-platform` 骨架、跑探针与构建、更新本文 §1～§5 |
| **人（可选审阅）** | 只看本文「门禁总览」；需要决策时再介入 |

---

## 1. 门禁总览（结果页）


| ID | 目标 | 状态 | 最近自测 | 证据摘要 |
| --- | --- | --- | --- | --- |
| **G1** | `services/aos-api` + `apps/web` 可 build/启动 | ✅ | 2026-07-17 | API `:8080/v1/health=ok`；`npm run build` 成功 |
| **G2** | `packages/contracts/openapi/v1.yaml` 含 buddy/ask | ✅ | 2026-07-17 | 契约文件含 `/buddy/ask`；运行面 `POST /v1/buddy/ask` 200 |
| **G3** | CI 扫描禁 UI 上游 SDK；违规样例红 | ✅ | 2026-07-17 | `check-no-upstream-sdk.ps1` PASS；`-ExpectFail` 检出 openai fixture |
| **G4** | 统一 Logger + `trace_id` | ✅ | 2026-07-17 | JSON 日志含 `trace_id=gate-test-1`；响应头 `X-Trace-Id` |
| **G5** | PG + 对象存储 Dev 探针绿 | ✅ | 2026-07-17 | PG `:5433` `SELECT 1`；MinIO Put/Get `dev-probe.txt` |


**结论（Agent）：G1～G5 全部 ✅，可按 26 §4 进入 Wave-0 后续任务（T0.3 Auth 占位等）。**

---

## 2. 本机基线快照

> 探测时间：2026-07-17

| 项 | 值 | 备注 |
| --- | --- | --- |
| OS | Windows 10.0.19045 | Docker Engine linux/amd64 |
| Docker | 29.1.3 · Compose 2.40.3 | ✅ |
| Node | v22.22.0 · npm 10.9.8 | ✅ |
| Python | 3.14.6 · fastapi / uvicorn | ✅ |
| 已有容器 | Dify 栈（内部 PG **未映射主机**） | **未复用**；AOS 独立 `aos-dev-*` |
| 主机 6379 | LISTEN | Lite 建议项；本波不阻断 |
| `aos-platform` | 已新建 | `c:\work\projects\wchat\aos-platform` |

---

## 3. G5 · Dev 前置（基础设施）

### 3.1 安装结果

| 组件 | 容器 / 路径 | 主机 | 状态 |
| --- | --- | --- | --- |
| PostgreSQL 16 | `aos-dev-pg` | `127.0.0.1:5433` · db `aos_meta` · user `aos_app` | ✅ healthy |
| MinIO | `aos-dev-minio` | API `9000` · Console `9001` | ✅ healthy · bucket `aos-media` |
| Compose | `aos-platform/deploy/dev/docker-compose.yml` | — | ✅ `up -d` |
| Handoff | `deploy/dev/prereq-handoff.local.yaml` | — | ✅ |
| Secrets | `deploy/dev/.secrets.env`（gitignored） | 仅 Dev 占位口令 | ✅ |
| AGE | — | — | ❌ 未装（Ontology 图查询前再开） |

### 3.2 操作与探针日志

| 时间 (UTC+约) | 动作 | 结果 |
| --- | --- | --- |
| 2026-07-17 | `docker compose -f deploy/dev/docker-compose.yml pull && up -d` | pg/minio Created · Started |
| 2026-07-17 | `docker exec aos-dev-pg psql … -c "SELECT 1"` | `ok = 1` |
| 2026-07-17 | `GET http://127.0.0.1:9000/minio/health/live` | HTTP 200 |
| 2026-07-17 | mc Put/Get `local/aos-media/dev-probe.txt` | 内容 `probe` · `PUT_GET_OK` |
| 2026-07-17 | init 日志 | `Bucket created … aos-media` · `minio-init-ok` |

### 3.3 G5 验收勾选

- [x] PG `SELECT 1`  
- [x] 对象仓 Put + Get  
- [x] handoff 存在（`environment: lite` / LOCAL_DEV）  
- [x] 声明不进客户包（README + 本文 + compose 头注释）  

---

## 4. G1～G4 · 工程脚手架

### 4.1 目录（已落地）

```text
aos-platform/
├── apps/web/                      # Vite + React 18 + TS · build ✅
├── services/aos-api/              # FastAPI · Logger · /v1/health · /v1/buddy/ask
├── packages/contracts/openapi/v1.yaml
├── scripts/ci/check-no-upstream-sdk.ps1
├── scripts/ci/forbidden-ui-imports.txt
└── deploy/dev/                    # 仅本机 Compose
```

### 4.2 自测明细

| ID | 状态 | 命令 / 结果 |
| --- | --- | --- |
| **G1** | ✅ | `uvicorn aos_api.main:app --port 8080` → health `ok`；`apps/web` `npm run build` → vite built |
| **G2** | ✅ | OpenAPI 含 `/buddy/ask`；`POST /v1/buddy/ask` + Bearer → `echo: ping` + `traceId` |
| **G3** | ✅ | 默认扫描 PASS；`-ExpectFail` 命中 `fixtures/forbidden-import.fixture.ts` |
| **G4** | ✅ | 日志 JSON：`trace_id` / `org_id` / `project_id`；`AOS_LOG_LEVEL=debug`；Audit logger 预留 `aos.audit` |

启动摘录（G4 证据）：

```text
{"level":"info","logger":"aos-api","msg":"request method=POST path=/v1/buddy/ask status=200 …","trace_id":"gate-test-1",…}
```

---

## 5. 变更日志（Agent 追加）

| 时间 | 变更 |
| --- | --- |
| 2026-07-17 | v0.1 建文；基线探测 |
| 2026-07-17 | v0.2：**G1～G5 全绿**；独立 Dev PG/MinIO；aos-platform 脚手架；自测落档 |

---

## 6. 障碍 / 需人介入

| ID | 问题 | 影响 | 需要人？ |
| --- | --- | --- | --- |
| N1 | AGE 扩展未装 | 不影响 G5；挡后续图查询 | 否（Agent 后续装） |
| N2 | Redis 未纳入 Compose | Lite 建议项 | 否 |
| N3 | aos-api 当前进程在本机 8080 常驻 | 重启机器后需再拉 | 否（README 有命令） |

---

## 7. 日常运维（Agent / 人可复制）

```powershell
cd c:\work\projects\wchat\aos-platform

# 前置
docker compose -f deploy/dev/docker-compose.yml up -d

# API
cd services\aos-api
$env:AOS_LOG_LEVEL="debug"
uvicorn aos_api.main:app --host 127.0.0.1 --port 8080

# Web
cd ..\..\apps\web
npm run dev

# G3
powershell -File ..\..\scripts\ci\check-no-upstream-sdk.ps1
powershell -File ..\..\scripts\ci\check-no-upstream-sdk.ps1 -ExpectFail
```

---

## 8. 关联

- [26 开发计划](26-AOS目标态开发计划.md) §0.1.4  
- [24 SOP](24-AOS客户侧前置组件安装SOP.md) §4 / §5  
- [00 索引](00-技术方案索引.md)  
- 工程仓：`aos-platform/README.md`

---

*v0.2 · Agent 主驾驶 · G1～G5 ✅ · 以 §1 总览为准*
