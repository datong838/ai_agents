# 47 · 技术方案全面对齐补缺方案（G-ALIGN-05+）

> **版本**：v1.0 · 2026-07-17  
> **任务**：对照 [T-API](T-API-aos-api稳定契约.md) · [26](26-AOS目标态开发计划.md) §10/§11 · [31](31-波次交付结果台账.md) §9，关闭「有契约 / live 页、实现缺或路径形变」死角  
> **工程**：`aos-platform/services/aos-api` · `apps/web` · `packages/contracts/openapi/v1.yaml`  
> **硬规则**：最小改动；Ferry/Channel 全集仍延期；双注册兼容旧 path

---

## 使用的 Rules

| Rule | 应用 |
| --- | --- |
| 先方案后编码 | 本文通过后再改 |
| 契约优先 | T-API 精确 path 可达 |
| 不影响已有 | 旧 path 保留 alias |

---

## 1. 审计结论（摘要）

| 分类 | 数量/代表 |
| --- | --- |
| G-ALIGN-01～04 | ✅ 已关闭 |
| **本刀必补契约死角** | modules publish/PATCH · evals 契约 path · apollo fleet/spokes/{id} · capabilities invoke · functions/{id}/invoke · TX.4 FORBIDDEN · OpenAPI 补表 |
| 显式延期 | Ferry · channels 全集 · T4.10 · S2 余量 7 Stub |
| 阻塞 B-* | AGE · 真 Keycloak · SBOM · 真 Paddle |

新标：**G-ALIGN-05**（modules publish）· **G-ALIGN-06**（evals/fleet/invoke 形变）· **G-ALIGN-07**（TX.4 Marking）· **G-ALIGN-08**（OpenAPI 漂移）

---

## 2. 本刀落地

| ID | 变更 |
| --- | --- |
| G-ALIGN-05 | `PATCH /v1/modules/{id}` · `POST /v1/modules/{id}/publish`；PublishPage 改打 publish |
| G-ALIGN-06a | `GET/POST /v1/aip/evals`（status/set 保留） |
| G-ALIGN-06b | `GET /v1/apollo/fleet` · `GET /v1/apollo/spokes/{id}`（local 保留） |
| G-ALIGN-06c | `POST /v1/aip/capabilities/{id}/invoke`（同步/job 门面） |
| G-ALIGN-06d | `POST /v1/functions/{id}/invoke` + 保留 `/v1/functions/invoke` |
| G-ALIGN-07 | `require_markings` → 403 `FORBIDDEN`；actions/execute · modules 读 |
| G-ALIGN-08 | OpenAPI `v1.yaml` 补主 path 表（非全量 schema 完美） |

**非本刀：** Ferry · Module 全量落 PG · 真 IdP · Channel CRUD

---

## 3. 自测

- [x] publish / fleet / evals / invoke / functions/{id} 200
- [x] marking 不足 → 403 FORBIDDEN
- [x] 旧 path 仍可用
- [x] pytest `test_align_g05` 8 passed · vitest 15

---

*v1.0*
