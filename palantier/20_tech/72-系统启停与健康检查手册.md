# 72 · 系统启停与健康检查手册

> **版本**：v1.4 · 2026-07-18（§5 冻结快检 · 挂 [110](110-W33-可演示冻结维护Runbook方案.md)）  
> **性质**：本机 **运行手册**（启 / 停 / 健康 / 冒烟）；非蓝图符合性真源  
> **工程根**：`aos-platform/`（仓库内相对路径；Win/mac 本机绝对路径各异）  
> **脚本真源**：`aos-platform/scripts/demo/` · 短文 `scripts/demo/README.md`  
> **对齐**：[70](70-业务平台可演示优先计划.md) TB.0 · [26 §12](26-AOS目标态开发计划.md) · [69](69-与目标态差距台账.md) · [88](88-W12-macOS冒烟与72启停手册对齐方案.md) · [98](98-W21-演示脚本README与72手册对齐方案.md)  
> **区分**：[34](34-系统启动与蓝图符合性检查记录.md)=蓝图/壳符合性检查；**本文**=日常启停与探活

---

## 使用的 Rules

| Rule | 应用 |
| --- | --- |
| 可演示优先 | 默认路径 = 业务平台本地演示，不依赖 Full Spoke / 客户 IdP |
| 诚实边界 | 不宣称生产 HA / 气隙 Ferry 运维包 |
| 最小 | 命令与端口以 `scripts/demo` 为准；改脚本须回写本文 |
| 禁 `/demo` Hub | Web 入口 = `/` 与侧栏真实页 |

---

## 1. 前置条件

| 项 | 要求 |
| --- | --- |
| OS | Windows 10+（PowerShell）或 macOS / Linux（bash） |
| Docker | Desktop 已开；compose 可拉起 `deploy/dev` 核心栈 |
| 运行时 | Python 3.11+ · Node 18+ |
| 工作目录 | 进入本仓 `aos-platform/` |

---

## 2. 一键启动

**Windows：**

```powershell
cd aos-platform
powershell -File scripts\demo\start-local.ps1
```

**macOS / Linux：**

```bash
cd aos-platform
bash scripts/demo/start-local.sh
# Docker Hub 不可达时：
bash scripts/demo/start-local-native.sh
```

成功标志终端出现：**`RESULT: DEMO HEALTH OK`**

| 开关（Win） | 含义 |
| --- | --- |
| `-InfraOnly` | 只起 Docker 前置（PG/MinIO/MySQL/LLM/OCR/analytics 等），不起 API/Web |
| `-SkipInstall` | 跳过 `pip install -e .` / `npm install` |
| `-SkipWeb` | 只起 API，不起 Vite |

**启动后默认进程：**

| 组件 | 地址 | 说明 |
| --- | --- | --- |
| PostgreSQL | `:5433`（容器 `aos-dev-pg`） | 元数据 / 对象 |
| MinIO | `:9000` | 对象存储 |
| aos-api | http://127.0.0.1:8080 | `GET /v1/health` |
| apps/web | http://127.0.0.1:5173 | 演示入口 `/`（侧栏真实页） |
| LiteLLM（边车） | `:4001` | 可选；健康检查 WARN 可不挡 |
| OCR（边车） | `:8082` | 可选；健康检查 WARN 可不挡 |
| analytics-runtime（边车） | `:8084` | TA.1；`AOS_ANALYTICS_URL`；**勿占 8083**（Keycloak） |

**演示鉴权（Dev）：**

```text
Authorization: Bearer dev
X-Org-Id: dev-org
X-Project-Id: dev-project
```

环境：`AOS_AUTH_ALLOW_DEV=1`（启动脚本已设）。

**常用入口：**

| 用途 | URL |
| --- | --- |
| Web 概览 | http://127.0.0.1:5173/ |
| Draft 审批 | http://127.0.0.1:5173/aip/drafts |
| 数据连接 | http://127.0.0.1:5173/data |
| API 探活 | http://127.0.0.1:8080/v1/health |

---

## 3. 停止

**Windows：**

```powershell
cd aos-platform
powershell -File scripts\demo\stop-local.ps1
powershell -File scripts\demo\stop-local.ps1 -AlsoInfra
```

**macOS / Linux：**

```bash
bash scripts/demo/stop-local.sh
```

PID 文件：`deploy/dev/demo-pids/aos-api.pid` · `aos-web.pid`

---

## 4. 健康检查

**Windows：**

```powershell
powershell -File scripts\demo\health-check.ps1
powershell -File scripts\demo\health-check.ps1 -RequireWeb
```

**macOS / Linux：**

```bash
bash scripts/demo/health-check.sh
bash scripts/demo/health-check.sh --require-web
```

| 结果 | 含义 |
| --- | --- |
| `RESULT: DEMO HEALTH OK` | 可继续演示 / 冒烟 |
| `RESULT: DEMO HEALTH FAIL (n)` | 先看 FAIL 行；API 掉线 → `bash scripts/demo/ensure-api.sh` |

**探针对照：**

| 名称 | 检查方式 |
| --- | --- |
| PostgreSQL | `docker exec aos-dev-pg pg_isready -U aos_app -d aos_meta` |
| MinIO | `http://127.0.0.1:9000/minio/health/live` |
| aos-api | `http://127.0.0.1:8080/v1/health` |
| web | `http://127.0.0.1:5173/`（默认非强制） |
| LiteLLM / OCR / Analytics | 可选 WARN |

**分析建模边车（TA.1）：**

```powershell
# compose
docker compose -f deploy/dev/docker-compose.yml up -d aos-dev-analytics
# 或本机
powershell -File scripts/demo/start-analytics-sidecar-host.ps1

Invoke-RestMethod http://127.0.0.1:8084/health
# → status=ok · engine=shaped-dev

# Facade（需 AOS_ANALYTICS_URL=http://127.0.0.1:8084）
Invoke-RestMethod http://127.0.0.1:8080/v1/analytics/health
# → status=ok · sidecar=ok · sessionsCapable

# TA.2 创建会话（Bearer dev）
$h = @{ Authorization = "Bearer dev"; "Content-Type" = "application/json" }
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8080/v1/notebooks/sessions `
  -Headers $h -Body '{"objectType":"WorkOrder","purpose":"explore"}'
# → status=idle · uiUrl 含 ticket
```

---

## 5. 演示冒烟（业务故事）

在健康绿之后：

**Windows：**

```powershell
powershell -File scripts\demo\ensure-seed.ps1
powershell -File scripts\demo\run-demo-smoke.ps1
```

**macOS / Linux：**

```bash
bash scripts/demo/ensure-seed.sh
bash scripts/demo/run-demo-smoke.sh
```

成功标志：**`RESULT: DEMO SMOKE OK`**

覆盖摘要：health · ensure-seed · story · object-sets · run-story · datasets/builds/dlq · **l1-chain** · funnel · buddy · governance · run-capability · modules。

**Agnes 真 LLM（可选）**：`.env` 已填 `AGNES_*` 时执行 `bash scripts/demo/run-agnes-smoke.sh`（断言 `route=agnes` · 见 [94](94-W17-Agnes默认接入与LLM回归方案.md)）。

**TB.8 彩排聚合**：`bash scripts/demo/run-rehearsal-smoke.sh`（demo + Agnes · 见 [95](95-W18-Buddy运行态API数据与彩排脚本方案.md)）。

### 5.1 回归命令矩阵（macOS / Linux）

| 用途 | 命令 | 成功标志 |
| --- | --- | --- |
| **冻结快检** | `bash scripts/demo/run-freeze-check.sh` | `FREEZE CHECK OK`（demo + npm **19**） |
| **冻结快检（Win）** | `powershell -File scripts\demo\run-freeze-check.ps1` | 同上 · 见 [113](113-W36-Windows冻结快检与demo-smoke-l1-chain对齐方案.md) |
| 健康探针 | `bash scripts/demo/health-check.sh` | `DEMO HEALTH OK` |
| 业务冒烟 | `bash scripts/demo/run-demo-smoke.sh` | `DEMO SMOKE OK`（含 **l1-chain**） |
| Agnes LLM | `bash scripts/demo/run-agnes-smoke.sh` | `AGNES SMOKE OK` 或 SKIP（无 `.env`） |
| TB.8 彩排 | `bash scripts/demo/run-rehearsal-smoke.sh` | `REHEARSAL SMOKE OK` |
| API pytest | `bash scripts/ci/run-pytest.sh` | `PYTEST OK` · 180+ passed |
| Web 单测 | `cd apps/web && npm test` | **19** passed |

**Agnes `.env` 路径**：`aos-platform/.env` · 改后 `bash scripts/demo/ensure-api.sh --restart`（见 [94](94-W17-Agnes默认接入与LLM回归方案.md)）。

话术彩排：`scripts/demo/CUSTOMER-DEMO.md`（含 **L1 Source→Sync→Pipeline** 指屏 · [104](104-W27-彩排L1链路与ensure-seed同步Sync方案.md)）  
10 分钟路径短文：`scripts/demo/README.md`

---

## 6. 日志与排障

| 文件 | 用途 |
| --- | --- |
| `deploy/dev/aos-api.out.log` / `aos-api.err.log` | API 标准输出/错误 |
| `deploy/dev/aos-web.out.log` / `aos-web.err.log` | Vite 日志 |
| `deploy/dev/demo-pids/*.pid` | 启停用 PID |

| 现象 | 处理 |
| --- | --- |
| health 红 · PG | `docker compose -f deploy/dev/docker-compose.yml ps`；确认 Desktop/WSL docker |
| API 起不来 · 端口占用 | stop 脚本后重起；查 `:8080` |
| **Web 页 `Failed to fetch`** | **aos-api 掉线**：`bash scripts/demo/ensure-api.sh`（独立 session）；日志见上表；细则 [76](76-蓝图比对与Failed-to-fetch运维自检方案.md) |
| 改 `.env` 后 LLM 仍 mock | `bash scripts/demo/ensure-api.sh --restart` 重载 `AGNES_*`（见 [94](94-W17-Agnes默认接入与LLM回归方案.md)） |
| Agnes smoke 偶发 mock | 脚本已 wait+重试；再跑 `run-agnes-smoke.sh` 或查外网 Agnes |
| ensure-seed 500 | 看 `aos-api.err.log`；确认 PG 可连、先 health 绿 |
| Web 打不开 | 重跑 start-local；或单独 `cd apps/web && npm run dev` |
| 客户问 Jupyter / Full Spoke | 诚实后置（G-ALIGN-09 / Apollo 停车场） |

---

## 7. 与其它文档分工

| 文档 | 管什么 |
| --- | --- |
| **本文 72** | 日常启停、健康、演示冒烟 |
| [34](34-系统启动与蓝图符合性检查记录.md) | 蓝图/壳符合性 · `run-blueprint-check.ps1` |
| [27](27-本机开发基础设施与工程门禁记录.md) | 本机门禁 G1～G7 · 边线运维记录 |
| [70](70-业务平台可演示优先计划.md) | 业务演示波次计划与 DoD |
| [69](69-与目标态差距台账.md) | 与目标态差距（能力/目录） |

---

*v1.4 · docs/palantier/20_tech/72*
