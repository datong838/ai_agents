# 75 · 蓝图深交互分波计划（画布 / Logic / 本体写 / Apollo）

> **版本**：v1.1 · 2026-07-18  
> **状态**：✅ 方案 · **W1～W4 已落地**（每波可独立验收）  
> **对齐**：[74](74-蓝图深页补齐-运营台Action与数据Router方案.md) · foundry/html · [70](70-业务平台可演示优先计划.md)  
> **工程**：`aos-platform/apps/web` · `services/aos-api`（最小契约扩展）  
> **硬规则**：每波 = 可演示升级；不推倒已 live 主路径；不动 `*.ps1`；Win/Mac/Linux 分轨

---

## 使用的 Rules

| Rule | 应用 |
| --- | --- |
| 先方案后编码 | 本文 |
| 迭代升级 | 波次串行；上一波绿再开下一波 |
| 最小更改 | 复用 Module / Logic / Ontology 已有 API |
| Apollo 末波 | 运维面最后加深，不挡业务深交互 |

---

## 0. 总序（强制）

```text
W0 已完成（74）: 运营台 Action · Storage Router · 计划 Cron/PATCH
W1 本波: 画布 Layout 编辑 + Module 持久化 + 预览联动
W2: Logic 三栏可视化 + Debug 面板（dryRun）
W3: 本体写 UI（创建 Object Type · 分支选择进 API）
W4: Apollo 深交互（Hub/Channel/Assets 可操作面，非 Full Ferry）
```

**金句：** *每波交付「用户能点、能存、能回看」的一步；禁止只堆 JSON。*

---

## 1. W1 · 画布 Layout 持久化

| 项 | 内容 |
| --- | --- |
| 蓝图 | `workshop-canvas.html` |
| 页 | `CanvasPage.tsx` |
| API | `GET/PATCH /v1/modules/{id}` · `GET .../runtime`（已有） |
| 做法 | 选 Module → 节点列表增删/上下移（HTML5 拖拽可选）→ widgets 存节点对象数组 → 保存后 runtime 可读 |
| 验收 | 刷新页面 Layout 仍在；改 Filter site 预览变；链到 `/workshop/module-interface` 可见 |

**兼容：** 旧 modules 的 `widgets: string[]` 仍可读；保存后升为节点对象。

---

## 2. W2 · Logic 可视化

| 项 | 内容 |
| --- | --- |
| 蓝图 | `aip-logic.html` |
| 页 | `LogicPage.tsx` |
| API | `POST /v1/aip/logic/run` dryRun（已有） |
| 做法 | 左：节点调色板；中：可点选节点串；右：Debug（proposedEdits / productionWritten=false） |
| 验收 | 点运行 → Debug 有 edits；明确不落库 |

---

## 3. W3 · 本体写 UI

| 项 | 内容 |
| --- | --- |
| 蓝图 | `ontology.html` / Object Type |
| 页 | `OntologyPage.tsx`（+ 可选 branches 深页） |
| API | `POST /v1/ontology/object-types`（已有） |
| 做法 | 表单创建 Object Type；分支下拉值传入 list objects（若后端支持 query 参数则接，否则至少 UI 态可见） |
| 验收 | 新建类型出现在列表；lint 失败有提示 |

---

## 4. W4 · Apollo 深交互（末）

| 项 | 内容 |
| --- | --- |
| 页 | `ApolloPage` + s2 apollo/remainder |
| 做法 | Channel 列表操作、Spoke 详情可读可刷新、Assets 打包结果面板；**不做** Full Ferry/气隙 |
| 验收 | `/apollo` 可完成「看舰队 → 看 Channel → 打一包 Assets」故事 |

---

## 5. 非目标

- html 像素 1:1 / 复杂 React Flow 商业画布（W2 用自研轻量节点即可）  
- 改 Windows 打包 ps1  
- 一次做完四波再验收（禁止）

---

## 6. 完成勾选

| 波 | 状态 | 落点 |
| --- | --- | --- |
| W0 (74) | ✅ | Inbox Action · Router · Schedules |
| W1 画布 | ✅ | `CanvasPage` · `PATCH /v1/modules/{id}` widgets 节点对象 |
| W2 Logic | ✅ | `LogicPage` 三栏 · `POST /v1/aip/logic/run` dryRun |
| W3 本体写 | ✅ | `OntologyPage` 创建表单 · `?branch=` 回显 |
| W4 Apollo | ✅ | `ApolloPage` 舰队→Channel→Assets（非 Full Ferry） |

### 变更日志

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-07-18 | 分波计划 |
| v1.1 | 2026-07-18 | W1～W4 编码完成并勾选 |

---

*75 · 深交互分波 · 一步一绿*
