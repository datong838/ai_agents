# 76 · 蓝图表格/按钮对齐自检

> **版本**：v1.0 · 2026-07-18  
> **状态**：✅ 可编码  
> **原则**：蓝图有的列/按钮/分区 **不得缺**；实现多出的能力 **保留**，但 **视觉风格对齐蓝图**（青主钮 / 青描边次钮 / 青色字链）  
> **真源**：`docs/palantier/foundry/html/data-connection.html` 等

---

## 1. `/data` 定稿对照

| Tab | 蓝图必有 | 现缺口 | 定稿 |
| --- | --- | --- | --- |
| 数据源 | 4 KPI + 六列表 + 筛选 + **+ 新建数据源**（青实心） | 大体已齐 | 主钮改 `btn-primary` |
| 同步 | 标题区 + **+ 新建同步** + **六列**：同步名称·数据源·目标·调度·上次运行·状态 | 列不全；缺新建；链风格弱 | 六列 + 新建；**保留**「计划编辑器 · 管道构建 · 管道→」作附加，青字链 |
| 代理 | 标题 + **打开代理管理 →**（青描边）+ 卡片栅格 | 缺按钮；卡片布局弱 | 接 `/data/agents`；卡片对齐蓝图排版；数据来自 API（无则空态，不造假集群） |
| 导出 | 标题 + **+ 新建导出** + **六列**表 | 仅占位文案 | 六列表壳 + 新建（未接线可 disabled） |

### 按钮风格（CSS）

| 类 | 用途 | 视觉 |
| --- | --- | --- |
| `.btn-primary` | + 新建数据源 / + 新建同步 | 青实心 `#22d3ee` · 深字 |
| `.btn-outline-cyan` | 打开代理管理 → | 青描边 + 浅青底 |
| `.bp-action-link` | 计划编辑器 · 管道构建 · 管道 → | 青色字链，无灰框 |

---

## 2. 全站自检清单（本波必扫）

| 蓝图 HTML | 工程页 | 自检焦点 |
| --- | --- | --- |
| `data-connection.html` | `DataPage.tsx` | §1 |
| `data-connection-agents.html` | `data.tsx` EdgeAgents | + 注册代理按钮风格 |
| `data-schedules` / 计划 | `dataSchedules.tsx` | 主操作钮 |
| pipelines / builds | s2 data 子页 | 表头列齐全 |
| `index.html` | Overview | 已对齐过，本波抽检按钮 |

---

## 3. 验收

| 项 | 通过 |
| --- | --- |
| 同步 Tab | 六列齐全；有 + 新建同步；计划编辑器/管道为青字链 |
| 代理 Tab | 有打开代理管理；卡片头右状态 |
| 导出 Tab | 六列表壳 |
| 无假演示行 | 代理/导出空则空，不填 Magritte 假机房 |

## 4. 自检结果（2026-07-18）

| 页 | 结果 |
| --- | --- |
| `/data` 数据源 | ✅ 六列 + `btn-primary` 新建 |
| `/data` 同步 | ✅ 六列 + 新建同步；附加青链「计划编辑器 · 管道构建 · 管道→」 |
| `/data` 代理 | ✅ 「打开代理管理 →」描边钮；卡片来自 `/v1/edge/agents/local` |
| `/data` 导出 | ✅ 六列表壳 + 新建导出（未接线 disabled） |
| `/data/agents` | ✅「+ 注册代理」描边风格 |
| `/data/schedules` | ✅「+ 新建计划」主钮 |
| Overview / AIP / Ontology | 抽检：主操作仍多为 `.btn`；后续按同套 `btn-primary` / `bp-action-link` 渐进 |

---

*v1.0 · 2026-07-18*
