# 77 · 产品 1.3 分析建模 · refs 克隆清单

> **版本**：v1.1 · 2026-07-18  
> **状态**：✅ 可执行（人跑脚本；**仍非 TA.1 编码**）  
> **对齐**：[73](73-产品1.3分析建模下一阶段方案.md) §5 · [23](23-AOS开源引用与交付军规.md) R-DIR-02 · [22](22-AOS开源产品维护清单.md)  
> **脚本**：[`mybuddy-v01/clone_analytics_refs.ps1`](../../../mybuddy-v01/clone_analytics_refs.ps1)  
> **落点**：`mybuddy-v01/refs/analytics/**`（只读参考；禁止进产品编译 / 交付包）

---

## 1. 你怎么跑

```powershell
cd C:\work\projects\wchat\mybuddy-v01

# P0：已 clone 健康的自动 SKIP，只拉缺失/损坏
.\clone_analytics_refs.ps1 -Tier P0 -UseHttps

# 加厚
.\clone_analytics_refs.ps1 -Tier P1 -UseHttps

# 强制重拉（含已健康）
.\clone_analytics_refs.ps1 -Tier P0 -UseHttps -ForceReclone

# 镜像
.\clone_analytics_refs.ps1 -Tier P0 -UseHttps -MirrorPrefix "https://mirror.ghproxy.com/"
```

**跳过规则：** 目标目录存在且 `git rev-parse HEAD` 成功 → **SKIP**（不必再维护 Done 列表）。

**脚本维护注意（v1.1）：** `clone_analytics_refs.ps1` 目录表旁的 `#` 注释须保持 **ASCII**。PowerShell 5.1 在 UTF-8 脚本里若 `#` 行含 em-dash / 中点等 Unicode，会把**下一行 hashtable 吞进注释**，表现为 `Selected` 少仓（曾漏 `jupyter_server` / `papermill` / `superset`）。`Dir` 用正斜杠路径。

### 本机进度（2026-07-18）

| Tier | 仓 | 状态 |
| --- | --- | --- |
| P0 | jupyter_server · notebook · ipykernel · nbclient · duckdb · echarts | ✅ 齐 · 再跑会全部 SKIP（v1.1 修目录表解析后 Selected=6） |
| P1 | papermill · jupytext · nbconvert · plotly.py · evidence · mlflow | ☐ 按需 |
| P2 | superset ·（lab/heavy/agpl 开关） | ☐ 按需 |

---

## 2. 目录树（拉齐后）

```text
mybuddy-v01/refs/analytics/
├── notebook/   jupyter_server · notebook · ipykernel · nbclient
│               (+ papermill · jupytext · nbconvert · jupyterlab?)
├── sql/        duckdb  (+ polars · arrow 需 -IncludeHeavy)
├── viz/        echarts · plotly.py · evidence · (+ superset P2)
├── ml/         mlflow
└── bi-agpl/    metabase   # 仅 -IncludeAgpl
```

---

## 3. Tier 对照 73

| Tier | 仓 | 用途 |
| --- | --- | --- |
| **P0** | jupyter_server · notebook · ipykernel · nbclient · duckdb · echarts | TA.1～TA.4 / 子集图 |
| **P1** | papermill · jupytext · nbconvert · plotly.py · evidence · mlflow | 批量/导出/轻 BI/实验 |
| **P2** | superset | Contour 交互参考 |
| 开关 | `-IncludeLab` / `-IncludeHeavy` / `-IncludeAgpl` | Lab · polars/arrow · Metabase |

**故意不整仓 clone：** pandas（pip）、JupyterHub / Elyra / Ray / Kubeflow（73 后置）、Grafana（已在 `clone_aos_deps.ps1` P1）。

---

## 4. 军规提醒

| 可 | 不可 |
| --- | --- |
| 读协议 / 交互 / compose 形态 | `aos-platform` Docker `COPY refs/` |
| 边车用官方镜像 + 自有 Facade | UI 直连 Jupyter WebSocket |
| pip 装 MIT/BSD 库进边车 | Metabase/Grafana **服务端**进客户包 |
| 抄交互到自有 `/analytics` 页 | 「基于 JupyterLab/Superset 二次开发」话术 |

---

## 5. 与编码门禁

本清单 **≠** TA.1 开工。评审仍走 [73 §10](73-产品1.3分析建模下一阶段方案.md)；refs 齐只降低「捞和抄」摩擦。

---

*v1.0 · 2026-07-18*
