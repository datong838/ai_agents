# 91 · 本体页对齐蓝图（Discover / Overview / 暗色表单）

> **版本**：v1.7 · 2026-07-19  
> **状态**：✅ 已落地（操作行合并 · 水合三钮同风格）  
> **真源**：[ontology.html](../foundry/html/ontology.html) · [ontology-object.html](../foundry/html/ontology-object.html) · demo.css `--aos-input-bg`  
> **Rules**：功能优先 · UI=蓝图 · 白输入违和必修 · 七 Tab 不丢（进深页）

---

## 1. 蓝图 vs 现状

| 蓝图 | 现状问题 | 本刀 |
| --- | --- | --- |
| Discover 单页：收藏 → 最近 → 表 → 水合 | Discover + 七 Tab + 新建同页堆叠 | 发现区专注浏览；卡/表主路径进 **深页**；内嵌七 Tab 保留为次要 |
| Object 深页：① Metadata + ②–⑦ 卡片网格 | Overview 压成跳转链，层次弱 | 恢复蓝图式编号卡片网格 |
| input：`bg-slate-900` / `--aos-input-bg` | 裸 `<input>` 浏览器白底 | 统一 `.aos-input` / `.ont-form` |

---

## 1.1 Discover 规整补刀（v1.1）

| 要求 | 落地 |
| --- | --- |
| 小区块高度 / 边框色统一 | `.ont-block` + 收藏卡统一 `border: rgba(255,255,255,0.08)`、`min-height: 7.25rem`；水合边框同色（仅标题绿色） |
| 最近查看 = 蓝图 | 左链 / 右时间；无说明行；无类型标签占右栏 |
| Discover / 新建显眼 | 发现标题带 ✦ 图标；新建默认 **展开** + ＋ 图标 |

---

## 1.2 标题层级（v1.2）

| 要求 | 落地 |
| --- | --- |
| 发现在搜索框之上 | `✦ 发现` 紧跟页标题，再是搜索/分支顶栏 |
| 收藏 / 最近查看 / 重要 字号 = 发现 | 共用 `.ont-section-title`（与 `.ont-discover-title` 同字号字重） |

---

## 1.3 顶栏两行对齐（v1.3）

| 要求 | 落地 |
| --- | --- |
| 两行同风格、同控件高度 | 搜索行 / 操作行均用 `.ont-toolbar-row` + 同高 `aos-input` / `btn-nav` |
| 分支标签与下拉同一行 | `.mp-field.ont-toolbar-field { flex-direction: row }`（覆盖 `.mp-field` 纵向） |
| 「刷新」按作用单独放 | 作用=重载 OT/分支/健康列表；放到**搜索行右侧**，文案 `↻ 刷新列表`；不与 OKF 混排 |

---

## 1.4 发现中文 · 去顶栏新建（v1.4）

| 要求 | 落地 |
| --- | --- |
| Discover → 发现 | 标题与 lede 用「发现」 |
| 去掉顶栏「＋ 新建 Object Type」 | 页底新建区默认展开已足够显眼；避免重复入口 |

## 1.5 页标题对齐菜单（v1.5）

| 要求 | 落地 |
| --- | --- |
| 页标题 = 侧栏菜单名 | `Ontology Manager` → **本体管理（数字孪生）**（`nav.ts` 真源） |

## 1.6 控件边框统一加深（v1.6）

| 要求 | 落地 |
| --- | --- |
| 本页按钮/下拉边框统一 | `.ont-page` 内 `btn-nav` / `btn-nav-accent` / `btn` / `aos-input` 同色边框 |
| 边框稍加深 | `rgba(255,255,255,0.08~0.10)` → **`0.22`**；OKF 取消金边，与链行同框（可用轻微底区分） |

## 1.7 操作行合并 · 水合三钮同风格（v1.7）

| 要求 | 落地 |
| --- | --- |
| 图1 两行→一行 | 分支 / 健康 / OKF / 域导航合并进同一 `.ont-toolbar-row`，`flex-wrap` 自动换行 |
| 图2 水合三钮一致 | ①OKF / ②Funnel / ③Workshop 统一 `btn-nav`（取消主钮青底独占） |

```text
本体管理（数字孪生）
✦ 发现
[ 搜索 ……………………………… ] [↻ 刷新列表]
[ 分支 ▾ ] [图谱健康] [OKF] [分支管理] [漏斗] [Wiki] [健康度]  ← 一行，满则换行
…收藏 / 最近 / 重要…
标准水合 · 流程文案 …  [① OKF] [② Funnel] [③ Workshop]  ← 三钮同框
＋ 新建 OT（默认展开）
```

---## 2. Discover 层次（对齐 ontology.html）

```text
标题 本体管理（数字孪生）+ lede（发现 · 收藏 / 最近 / 重要）
✦ 发现
搜索行：搜索(暗) · ↻ 刷新列表
操作行：分支 · 图谱健康 · OKF · 分支管理 · 漏斗 · Wiki · 健康度（一行，满则换行）
① 收藏（字号同 发现）
② 最近查看
③ 重要 Object 表
④ 标准水合（三钮统一 btn-nav）
（可选）内嵌详情：仅表格「内嵌」点开时展开，默认不占主舞台
＋ 新建 OT：默认展开 + 暗色表单（唯一新建入口）
```

卡 CTA / 表「打开」→ `/ontology/object-types/:id`（蓝图 Overview 深页）。

---

## 3. Overview（对齐 ontology-object）

- ① Metadata 块（标签 + PropGrid）  
- ②–④ / Data 卡片网格（点链切 Tab / Funnel）  
- Wiki 入口条（橙描边，对齐蓝图）

---

## 4. 防丢

七 Tab（深页+内嵌）· 创建 OT · 水合链 · OKF · Funnel/Wiki/分支深链

## 5. 文件

`OntologyPage.tsx` · `objectTypeDetail.tsx` · `styles.css` · 本文
