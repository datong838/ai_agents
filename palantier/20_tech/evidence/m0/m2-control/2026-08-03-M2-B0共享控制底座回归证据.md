# M2-B0 共享控制底座回归证据

> 结论：**最终 GREEN，允许进入 M2-B1；不得提前进入 B2/M3**
> 日期：2026-08-03
> 代码仓：`aos-platform`
> 编码前基线：`m1@a6e4d31`
> B0 实现提交：`m1@2648808`
> 实施方案：[228-M2-B 安装控制面与 Canonical API 并行实施方案](../../../电商平台接入/228-M2-B安装控制面与Canonical-API并行实施方案.md)

---

## 1. 使用的 Rules

- 先复习 M0/M2-B 方案，再验证代码；不以文档状态代替运行证据。
- B0 只验证共享错误、role/marking/ETag 策略、Protocol、wiring 和旧 Registry 兼容，不提前实现 B1 Service/Router。
- PostgreSQL、immutable DTO/hash/revision 与持久化 Store 继续作为唯一真源。
- 专项、M1+M2 累计、OpenAPI、聚合、数据库、格式和安全门任一失败均不得进入 B1。
- 用户已有 `AppShell.tsx` 与掘金文档改动全程保留。

## 2. 交付范围核对

`2648808` 的 8 个变更文件与 B0 独占清单逐项一致：新增 `control_policy.py`、`control_protocols.py`、`control_wiring.py` 及两份专项测试；更新稳定错误、旧 Registry factory 委托和错误契约测试。未新增 B1 Service、Router、migration、前端或电商领域逻辑。

五个 worktree 的 HEAD 均为 `2648808`，四个 Worker worktree 干净且相对 `m1` ahead/behind 均为 `0/0`。

## 3. 自动化验证

| 门 | 精确结果 | 结论 |
|---|---:|---|
| B0 control policy/wiring + Registry API 专项 | 150 passed，7 warnings | GREEN |
| M1+M2 `asset_registry` 累计 | 536 passed，7 warnings | GREEN |
| OpenAPI deterministic/current | 10 passed，7 warnings | GREEN |
| Router aggregate | 509 manifest entries，无漂移 | GREEN |
| Alembic heads/current | 均为唯一 `228assetinstall` | GREEN |
| B0 8 个 Python 文件 Ruff | All checks passed | GREEN |
| B0 8 个 Python 文件 format | 8 files already formatted | GREEN |
| 源码安全扫描 | 2426 files；critical=0，warning=133 | GREEN |

7 个 warning 均为既有 Pydantic `schema` 字段遮蔽与 TestClient/httpx 弃用类别，本波没有新增失败或安全豁免。

## 4. 环境说明

原 preflight 记录的 API Python 3.11 虚拟环境已不在磁盘。本次在任务 `work/` 目录使用隔离 Python 3.12.13 环境按 `services/aos-api[dev]` 重新安装锁定范围依赖；项目声明支持 Python `>=3.11`。测试环境没有写入仓库依赖文件，wheel 临时 `build/` 目录已移入系统废纸篓。

Ruff 必须从 `services/aos-api` 目录运行；从仓库根运行会因为一方包识别不同而产生错误 import 分组建议，该结果已丢弃，未修改代码。

## 5. 工作树

B0 验证未产生业务代码修改。代码仓仍只保留用户原有改动：

```text
M  apps/web/src/shell/AppShell.tsx
?? docs/juejin-228-public-series-plan.md
?? docs/juejin-series-228/
```

## 6. 阶段结论

B0 实现、方案、专项、累计、OpenAPI、聚合、数据库、格式和安全门一致，未发现残余 P0/P1。允许从 `2648808` 按文件独占进入 B1：W1 Composition Service、W2 Installation/Revalidation/Evidence、W3 Router/Header。W4 必须等待三路集成后再编写 B2 对抗测试；B1 未 GREEN 前不得进入 B2、M3 或电商 G0～G6。
