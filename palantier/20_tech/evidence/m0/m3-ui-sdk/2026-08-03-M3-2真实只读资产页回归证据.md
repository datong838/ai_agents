# M3-2 真实只读资产页回归证据

> 日期：2026-08-03
> 代码仓库：`/Users/ddt/work/projects/ai_agent/aos-platform`
> 最终提交：`m1@7f6e80a`
> 最终 tree：`7548aba94f0919b543b03ffa8d51f5645483c638`
> 结论：**M3-2 GREEN，允许进入 M3-3 Resolve/Create 与 Diff**

## 1. 范围与不变量

本波只把既有 Canonical Registry 与 Installation 只读能力接入 `/apollo/assets`，没有重新设计架构。保持以下不变量：

- 保留 `/apollo/assets` 路由与 `AssetBundlesPage` named export。
- 不新增 mutation UI；resolve/create/action 仍不可达。
- 不修改通用 `apps/web/src/api/client.ts`、后端、数据库、路由或状态机。
- Registry 不伪装服务端分页；Installation 使用服务端 `state/limit/offset/total`。
- 事件时间线只声明完整事件，不伪装每个历史 revision 的完整快照。
- 生产页面不使用 Mock 兜底，不请求不存在的 `/v1/assets`。

## 2. 交付映射

| 路线 | 提交 | 核心交付 |
|---|---|---|
| 总控前置/W4 | `3fd3c6d`、`7f6e80a` | 只读状态模型、资产页接线、旧页面测试替换 |
| W1 | `0faa72d` | Installation list/detail 运行时失败关闭 parser 与 SDK 集成 |
| W2 | `bf9b285` | Registry/Installation 五个只读 hook、请求乱序保护、stale/error 语义 |
| W3 | `544660c`、`f6e5d6e` | Registry bundle/version、Installation list/detail/history 纯展示组件 |

## 3. 自动化验证

| 验证项 | 结果 |
|---|---|
| M3-2 / Asset Control 前端专项 | 72 passed，零失败 |
| Web 全量 | 125 files，1842 passed，零失败 |
| TypeScript | GREEN |
| Web production build | GREEN，219 modules transformed |
| 后端 Registry/Composition/Installation/OpenAPI | 77 passed，零失败；7 条既有 warning |
| `MOCK_ASSET_BUNDLES` 生产源码扫描 | 零命中 |
| `/v1/assets` 生产源码扫描 | 零命中 |
| `git diff --check` | GREEN |

组件与 hook 测试覆盖 loading、真实空数组、刷新、stale、403、404、网络/500、服务端分页、乱序响应、bundle/version 详情以及 installation current revision/decision/events/evidence/pointers。

## 4. 浏览器验收

本地 Vite 页面 `http://127.0.0.1:4173/apollo/assets` 完成浏览器检查：

- 页面标题和说明明确只读、真实服务端事实与无 Mock 兜底。
- `资产 Registry` 与 `安装管理（只读）` 两个页签可切换。
- 安装页显示服务端分页语义、状态筛选、详情与事件时间线边界。
- API 不可达时页面显示明确错误和重试入口，没有回退演示数据。
- 页面控制台仅记录预期的 aos-api 不可达日志，未发现 React 渲染异常。

宿主机健康检查显示 `aos-api :8080` 在线，但隔离的应用内浏览器无法直连宿主机该端口，因此本次没有把“浏览器取得真实数据库响应”列为已验证项。真实响应契约由 77 个后端测试、运行时 parser、SDK 测试和组件 fixture 共同覆盖；后续具备同网段浏览器环境时可补录端到端截图，但不影响本波只读代码与契约退出门。

## 5. 分支与远程

- `m1` 已推送到 `origin/m1@7f6e80a`。
- 四个 Worker 均以 `--ff-only` 同步最终 `m1`。
- 五分支 HEAD 均为 `7f6e80a`，tree 均为 `7548aba9...`，相互 ahead/behind 为 `0/0`。
- 五个代码工作树均干净。

## 6. 风险与下一门禁

- Registry OpenAPI 仍为动态 object，继续依赖运行时失败关闭 parser；服务端字段合法变化必须显式更新 fixture 与测试。
- M3-3 才可开放 Resolve/Create 与 Diff，仍不得开放 submit/approve/apply/verify/rollback。
- M3-3 开工前必须重新冻结文件所有权，并从 `m1@7f6e80a` 起跑。
- M5 完成前仍禁止电商 G0～G6 编码。
