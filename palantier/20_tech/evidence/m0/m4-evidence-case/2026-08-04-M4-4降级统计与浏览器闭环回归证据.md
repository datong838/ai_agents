# M4-4 降级、统计与浏览器闭环回归证据

> 日期：2026-08-04
> 结论：GREEN
> 代码基线：`aos-platform m1@c9da800`
> tree：`4df2217281907e5c206dfed68e374faa3ccb3eca`

## 1. 本波结论

M4-4 作为证明波未发现生产缺陷，未修改生产端点、表、DTO、Policy 或 Web runtime。新增两组真实 PostgreSQL 对抗测试和一套可精确清理的隔离浏览器支撑。

## 2. Evidence 生命周期证明

- 14 类可信 Evidence 按连续门从 `planned` 推进到 `production_active`。
- latest invalid 降级并记录 `negative_observed`；有效续期恢复；数据库时钟到期记录 `evidence_expired`；revoke 记录 `evidence_revoked`。
- 重复 projector `projected=false` 且 Snapshot/Event/Projection 零写。
- 非连续 revision 失败并证明 Evidence、Snapshot、Event、Projection 整体回滚。

## 3. 统计对抗证明

- marking 授权过滤先于 total、分页和 6 项统计；reference 不进入 current。
- connector/pipeline 跨 Case 去重、dataset row 按唯一 revision 求和、latency 取 max。
- 无测量为 `null`，明确测得零保持 `0`；6 指标使用同一 cutoff。
- limit/offset 不影响 total/stats，逐页与全页及重复读取顺序稳定。

## 4. 真实浏览器闭环

隔离环境使用 PostgreSQL 16、6 个生产 migration、真实 `IntegrationCaseService`/`TrustedEvidenceWriter`/Router、真实 dev JWT、透明 Authorization 代理和真实 Vite Web；代理不修改业务 method/path/query/body/status/headers/body，不存在测试专用生产端点或 Web Mock。

- current 显示 2 个案例，`0` 与 `—` 区分正确。
- reference 显示 1 个脱敏案例，Owner、Installation、Overlay、Lock 均未泄漏。
- 详情显示服务端阶段、8 门、Blocker、受限 Evidence；timeline 显示真实 Stage Event。
- 筛选空态与 scope 切换清筛选/清选择通过。
- 主动停止 API 后页面明确 stale 并保留最后事实；恢复后刷新清除 stale。
- 到期 Case 从 `connection_verified` 降为 `planned`，timeline 出现 `evidence_expired`。
- 断网场景产生 8 条预期 API 控制台错误；恢复后无功能残留故障。

## 5. 累计验证与清理

| 验证面 | 结果 |
|---|---|
| M4 后端累计 | 107 passed，7 个既有 warning，2 subtests |
| Web 全量 | 148 files / 2001 passed |
| TypeScript / production build | GREEN / GREEN |
| Harness shell/Python | 语法 GREEN |
| 环境清理 | stop 幂等；55432/18080/18081/1420 无监听；容器、JWT、seed、pid 状态清零 |
| 五分支 | 本地与远端 `c9da800`，ahead/behind `0/0`，clean |

## 6. 下一门

M4-5 只执行 M1～M4 累计回归、OpenAPI/路由确定性、Web/Desktop/Ontology SDK、最终证据和分支收口，不增加功能。M4 最终 GREEN 前仍不得进入 M5 或具体电商平台。
