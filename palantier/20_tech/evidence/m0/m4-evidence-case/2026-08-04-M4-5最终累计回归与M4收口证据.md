# M4-5 最终累计回归与 M4 收口证据

> 日期：2026-08-04
> 结论：**GREEN**
> 代码基线：`aos-platform m1@8964c4b`
> tree：`236f8a25b96e953e320a3cab38cba2486dc0c4de`

## 1. 本波结论

M4-5 完成 M1～M4 最终累计门、契约生成物收口、安全扫描和五分支同步。M4 现已最终 GREEN，允许进入 M5 最小电商包组合验证；仍不得开始任何具体电商平台实现。

累计门发现并修复两类 M4 交付漂移：

1. M4-2 已注册的 5 个 `/v1/integration-cases` operation 已进入 Router 基线，但尚未刷新已提交 OpenAPI/库存生成物和确定性计数。
2. M4-4 新增浏览器支撑文件存在定向格式问题，隔离测试 DSN 的字面连接串会触发敏感扫描。

修复只更新契约计数/生成物、测试基线和浏览器支撑静态写法，没有改变 Router、DTO、数据库、Service、状态机或页面能力。

## 2. OpenAPI 与 Router 确定性

| 项 | 最终值/结果 |
|---|---|
| Router manifest | 512 entries，生成 aggregate 一致 |
| Runtime routes | 4047 |
| OpenAPI paths | 2281 |
| 唯一 operation pairs | 4024 |
| Inventory rows | 4043 |
| OpenAPI schemas | 1498 |
| OpenAPI SHA-256 | `3bede642b651439308a61dacf4e549511b7a5269aebe3b340bf076218c461ed1` |
| 确定性 | 两次干净进程字节一致；生成物 current |
| 契约测试 | 19 passed，2 subtests passed |

五个新增 operation 为 list/create/detail/snapshot/timeline；没有新增或删除本波 API。

## 3. 累计验证

| 验证面 | 结果 |
|---|---|
| 资产域 M1～M4 | 679 passed，7 个既有 warning，真实 PostgreSQL 无 skip |
| M4 专项累计 | 107 passed，7 个既有 warning，2 subtests |
| 后端全量 | 8878 passed，3 skipped，1 deselected，15 个既有 warning，2 subtests |
| Web 全量 | 148 files / 2001 passed |
| Integration Case SDK | 5 files / 58 passed |
| Asset Control SDK | 9 files / 63 passed |
| Web TypeScript / build | GREEN / 244 modules GREEN |
| Desktop | 9 files / 40 passed；typecheck/build GREEN，264 modules |
| Ontology SDK | 1 file / 6 passed；`tsc --noEmit` GREEN |
| OpenAPI gate | deterministic/current；11 OpenAPI tests GREEN |
| Security gate | scanner tests 9 passed；全仓 critical=0，170 条既有 warning |
| M4 Harness 作用域 | critical=0、warning=0；shell/Python 语法 GREEN；双 stop clean |

后端全量显式把 `test_use_llm_with_agnes` 作为 1 个 deselected：该用例读取本机 `.env` 并访问外部 Agnes，不属于离线累计门；此前实连因外部网络超时，未伪装为通过。默认门显式关闭 Agnes/S3/MySQL live flag，并为 `127.0.0.1/localhost` 设置代理绕过；本地 JWKS 专项在该环境下单独 1 passed。三项 skip 均为既有条件测试。

## 4. 静态门与历史债务

- 本波实际修改的 OpenAPI exporter/test、两个浏览器 Python 脚本和两组 M4-4 PostgreSQL 测试均通过定向 Ruff check/format-check。
- 全资产域宽泛 Ruff 基线仍有 34 个 `I001` 和 11 个待格式化文件；它们不属于 M4 新增逻辑，本波未批量改写。
- `git diff --check` GREEN；五个代码工作树和隔离环境均 clean。

## 5. 代码变更

修改：

- `packages/contracts/openapi/v1.generated.json`
- `packages/contracts/openapi/v1.inventory.json`
- `scripts/export_openapi.py`
- `services/aos-api/tests/test_openapi_contract.py`
- `scripts/m4-browser-harness/bootstrap.py`
- `scripts/m4-browser-harness/jwt_proxy.py`
- `scripts/m4-browser-harness/start.sh`

本波没有新增生产文件、迁移、端点、DTO 或页面。

## 6. 五分支最终状态

`m1` 与四个历史命名 Worker 分支、本地与远端全部为：

- HEAD：`8964c4bf09575ef3df3bba732ddf9482ef65a25a`
- tree：`236f8a25b96e953e320a3cab38cba2486dc0c4de`
- ahead/behind：`0/0`
- 工作树：clean

四个 Worker 先从最终 `m1` fast-forward，再分别推送；未 reset、rebase 或强推。

## 7. 下一门

进入 M5-0 前先冻结《228-M5-最小电商包组合验证实施方案》。M5-0 只允许 strict manifest、空内容目录、最小 SBOM/Evals、synthetic InstanceOverlay Schema/fixture 和边界测试；不得写 Niushop SQL/Connector、真实客户数据或任何平台业务逻辑。
