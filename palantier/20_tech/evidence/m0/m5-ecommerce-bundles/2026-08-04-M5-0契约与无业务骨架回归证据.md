# M5-0 契约与无业务骨架回归证据

> 日期：2026-08-04
> 结果：GREEN
> 代码基线：`aos-platform m1@4fd9c7f9f0e4`
> tree：`48719e7141404fae8909d32d7bd81b1eae4ddac0`

## 1. 本波范围

M5-0 只实现冻结方案中的静态契约和无业务 source 骨架：四个 Bundle manifest、最小 SBOM/Evals、0-byte 空 export、strict synthetic InstanceOverlay Schema/fixture、跨组 DTO 对拍和反偷渡门。没有修改生产 Router、Service、Store、迁移、OpenAPI 或 Web，也没有接入 Niushop、微商城、微信小店、抖音或快手。

## 2. Worker 交付

| 路线 | 提交 | 结果 |
|---|---|---|
| W1 | `e6eb365`、`64205b4` | 四包骨架、依赖、SBOM/Evals、空导出和 strict Manifest/Loader 契约 |
| W2 | `3143923` | InstanceOverlay Schema、synthetic fixture、canonical hash 和负向契约 |
| W3 | `a86d363`、`c205697` | 通用内核反向依赖、客户/秘密/平台实现偷渡及 scanner 门 |
| W4/总控 | `4e2b086`、`4fd9c7f` | 三叶请求/四包依赖图、Overlay hash→Installation DTO 对拍、集成纠偏 |

## 3. 关键事实

- 四包坐标为 `domain.ecommerce.core`、`solution.ecommerce.operations-base`、`solution.ecommerce.growth`、`platform.ecommerce.niushop`。
- 三个叶子包只依赖 core；Composition 请求只显式请求三个叶子。
- Bundle content 仅含 14 个 0-byte `.gitkeep`；没有 SQL、Connector、Pipeline、Action 或真实 mapping。
- Overlay `apiVersion` 为 `aos.dev/v1alpha1`，字段 strict 且只接受 synthetic/opaque 引用。
- fixture canonical hash 为 `sha256:5c1d43e9d41081e43ed25b5e3790abefbe23f2ae7b60dff35397d788be7056e7`，现有 `CreateInstallationRequest.overlayRevision` 可原样承载。
- 仓库没有固定签名、私钥、真实客户引用、连接串或生产 trust root。

## 4. 集成纠偏记录

独立 Worker 测试合并后，跨组门捕获并修正了两类偏差：14 个 `.gitkeep` 含单个换行字节，以及边界测试把合法 `compositionLockRef` UUID/hash 误判为真实实例引用。修正后统一为 0-byte 占位；synthetic marker 只约束 org/project/source/secret/dataset 等实例引用，lock UUID/hash 继续由 Overlay Schema 校验。

## 5. 验证结果

| 验证 | 结果 |
|---|---|
| M5-0 专项 `test_m5_*.py` | 68 passed，7 个既有 warning |
| 资产域累计 `tests/asset_registry` | 747 passed，7 个既有 warning |
| Ruff check / format-check | GREEN / GREEN（4 个 M5 Python 测试文件） |
| Git diff-check | GREEN |
| M5 scoped sensitive scanner | 29 files，critical=0、warning=0 |
| 五分支本地/远端 | 同 `4fd9c7f`，ahead/behind `0/0`，五工作树 clean |

## 6. 未完成与下一门

- 四包仍是 unsigned source，不得宣称 published。
- Overlay 仍是文件契约，不是生产 Store/API 或实例管理能力。
- Niushop 坐标仍是空平台包，不得宣称真实平台已接入。
- 下一波只允许 M5-1：运行时测试签名、真实 PostgreSQL Registry 发布、重启快照回读和 trust/evidence 负向门。
