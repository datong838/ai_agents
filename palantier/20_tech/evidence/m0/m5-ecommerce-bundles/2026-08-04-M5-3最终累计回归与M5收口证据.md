# M5-3 最终累计回归与 M5 收口证据

> 日期：2026-08-04
> 结果：GREEN
> M5 功能检查点：`aos-platform m1@86c3076b79d4eb6d97a3aab2066f4b41e2b12d27`
> 最终五分支基线：`aos-platform m1@4d04e1f68679179207e19db5610eac9386d28a89`
> tree：`f8a257cf99c36d8f9fc82ad91e6e8ec5da032a1d`
> 停止线：已停在 Niushop/微商城、微信小店、抖音、快手等具体平台接入编码之前

## 1. 本波范围

M5-3 只执行 M1～M5 最终累计回归、迁移/契约/安全确定性复验、五分支同步和文档证据收口。没有新增或修改生产代码、测试代码、API、DTO、数据库迁移、页面、Bundle 或平台适配实现。

## 2. 四 Worker 与总控结果

| 路线 | 结果 |
|---|---|
| W1 资产域与 PostgreSQL | 资产域 779 passed；M5 专项 100 passed；7 组真实 PG 用例 26 passed |
| W2 Web 与前端 SDK | Web 148 files / 2001 passed；Integration SDK 58 passed；Asset Control SDK 63 passed；Web typecheck/build GREEN |
| W3 Desktop、Ontology SDK 与边界 | Desktop 40 passed；Ontology SDK 6 passed；M5 boundary 8 passed；Desktop/Ontology typecheck、Desktop build GREEN |
| W4/总控后端与契约 | 后端 8978 passed；Router 48 passed + 2 subtests；OpenAPI gate 11 passed；512 manifest entries |

## 3. 最终累计门

| 验证面 | 最终结果 |
|---|---|
| 后端全量 | 8978 passed，3 skipped，1 个外部 Agnes live 用例显式 deselected，15 warnings，2 subtests passed，零失败 |
| 资产域 / M5 | 779 / 100 passed，7 个既有 Pydantic/Starlette warning |
| 真实 PostgreSQL | 26 passed；覆盖 7 个 `*_pg.py`、真实 JWT/PG 生命周期、snapshot corruption precedence、timestamp/restart |
| Alembic | single head/current=`228assetintegration`；从 `228assetinstall` 到 head 的隔离数据库演练 GREEN |
| 本地开发库 | 迁移到 `228assetintegration` 后 8 张 `integration_*` 表存在且均为空；未更新或删除既有业务数据 |
| Router/OpenAPI | domain aggregates current（512 entries）；Router 48 passed + 2 subtests；双干净进程 OpenAPI 确定性及 11 tests GREEN |
| Web / SDK | Web 2001、Integration SDK 58、Asset Control SDK 63 passed；typecheck/build GREEN |
| Desktop / Ontology SDK | Desktop 40、Ontology SDK 6 passed；typecheck/build GREEN |
| Security | M5 scope 41 files、critical/warning=0/0；source 2606 files、critical=0、170 条既有 warning；built artifacts 184 files、critical=0、34 条既有 warning |
| 五分支 | 同 HEAD=`4d04e1f...`、tree=`f8a257cf...`，五个远端 ahead/behind=`0/0`，五工作树 clean；M5 功能检查点=`86c3076...` |

## 4. 诊断记录

首次后端全量从仓库根目录启动时，`--deselect` 误写为带 `services/aos-api/` 前缀的节点路径，未匹配到已知外部联调用例 `tests/test_logic_engine.py::test_use_llm_with_agnes`。结果为 8978 passed、3 skipped、该外部用例因网络超时 1 failed。其余 8978 项均已通过，错误与代码及数据无关。

随后从 `services/aos-api` 目录使用正确节点路径完整重跑，得到 8978 passed、3 skipped、1 deselected、零失败。最终 GREEN 只采用第二次正确门禁结果；首次结果保留用于说明诊断过程，没有被隐去或计为 GREEN。

## 5. 数据库处置

- 先创建一次性隔离数据库，从 `228assetinstall` 升级到 `228assetintegration`，确认 single head/current 后删除该一次性数据库。
- 再对本地开发库执行迁移。迁移前不存在 `integration_*` 表；迁移后 8 张新表均为空，因此没有覆盖、删除或改写现有业务数据。
- 本波未接触生产数据库、真实商城数据库、客户数据、生产 Secret 或平台写回。

## 6. 代码与文档事实

- M5-3 本身无代码增量，最终检查点为 `86c3076`。
- 主工作树同期出现的用户掘金文档由 `1bdef79`、`b222083` 与 `4d04e1f` 独立提交保存，不属于 M5 功能实现，也不影响运行逻辑。
- 上位 228 路线已纠正旧的“Niushop dry install / M5 与 G0 对接”表述：M5 实际只完成 test-only 无业务四包组合验证。

## 7. 最终裁决与下一门

M1～M5 最终 GREEN，说明 AOS 通用内核已具备资产包注册、解析、安装、UI/SDK、Evidence/Case 和最小领域组合验证能力；它不说明任何具体电商平台已接入。

本轮循环在具体平台编码前结束。下一步建议只做 G0 与首个平台选择评审，冻结真实数据、凭据、只读/写回、限流、合规和验收边界。未取得新的明确授权前，不读取 Niushop/微商城真实 schema，不调用微信小店、抖音或快手 API，不编写任何平台 Connector。
