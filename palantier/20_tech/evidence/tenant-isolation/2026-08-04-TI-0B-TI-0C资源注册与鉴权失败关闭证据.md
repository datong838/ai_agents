# 228 · TI-0B/TI-0C 资源注册与鉴权失败关闭证据

> 日期：2026-08-04  
> 方案：`228-组织与工作区租户隔离全量补强实施方案.md` v1.4  
> 代码基线：`aos-platform m1@570fcc1b96782e0833619653390e638c4418528b`  
> 结论：TI-0B GREEN；TI-0C GREEN；允许进入只读 TI-0D，不允许迁移、回填、删除客户业务数据

## 1. TI-0B 交付

- 新增 `services/aos-api/aos_api/tenant_resources.yaml`：登记 86 张 PostgreSQL 表和对象存储、向量、缓存、离线存储、消息队列、调度、进程内存 7 类非表资源族。
- 新增 `tenant_resource_registry.py`：校验分类、租户列、主键、目标租户列、RLS 目标、迁移波次，并只读扫描 PostgreSQL 实际结构。
- 新增 `scripts/tenant_isolation_audit.py`：输出确定性 JSON coverage；发现未登记表、过期登记或列/主键/状态漂移时失败。
- 新增 4 个注册表专项测试；提交 `e7d07b8`。

实库结果：86/86 表登记，coverage `100%`，未登记 `0`、过期登记 `0`、结构漂移 `0`；`STRONG_PK=31`、`WEAK_PK=27`、`NO_TENANT=28`；RLS enabled/force/policy 均为 `0`。因此只标记“盘点 GREEN”，不标记“隔离 GREEN”。

## 2. TI-0C 交付

- Data OS Phase 5 Pipelines、Phase 6 Agents/Connectors/Documents/Media Sets/Schemas/Sources/Syncs 增加统一 Principal 门。
- Pipeline Builder 与 Module Config/Deployment/Interface/Query/Variable/Widget/Event 子资源增加统一 Principal 门。
- 新增 Router 静态门和 16 个代表性无 Token 请求负向门；全部返回 `401 AUTH_REQUIRED`。
- 更新确定性 OpenAPI 与 route inventory；提交 `570fcc1`。

## 3. 验证结果

| 门禁 | 结果 |
|---|---|
| 资源注册表专项 | 4 passed |
| TI-0C Router/无 Token 门 | 17 passed |
| 文档处理 + Module Events 历史回归 | 13 passed |
| Workshop 历史回归 | 33 passed |
| OpenAPI 确定性 | `PASS OpenAPI and inventory are deterministic and current` |
| Ruff / diff check | GREEN |
| 五分支 | `m1`、W1～W4 均为 `570fcc1`；四 Worker 工作树 clean |

主工作树另有 3 个用户头条文档改动，未纳入本波提交、未覆盖、未清理。

## 4. 未完成与下一门

- 非表资源族已登记，但实际 Bucket/Object、Vector collection、Cache key、Queue、Scheduler job 和进程内 Map 的对象级清单尚未完成。
- 27 张 WEAK_PK、28 张 NO_TENANT 表尚未迁移；全部 tenant-owned 表 RLS 仍未启用。
- TI-0C 仅关闭已识别入口的未登录访问；Store 的 org/project 双过滤、跨租户已知 ID 404、复合键/FK/RLS 属于 TI-1～TI-5。
- 下一波执行 TI-0D 只读 precheck：数据归属、空值、重复键、孤儿、props 标记、非表前缀和栖月汇全量空数据差距，不做任何写操作。
