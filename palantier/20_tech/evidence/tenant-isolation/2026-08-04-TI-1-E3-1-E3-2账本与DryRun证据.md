# TI-1 E3-1 / E3-2 账本与 Dry-run 证据

> 日期：2026-08-04
>
> 代码：`0ab3c5e`、`6ed2f6d`
>
> 结论：E3-1 GREEN；E3-2 GREEN；业务表零回填

## 1. E3-1 账本

- 新增 `tenant_backfill_batch`、`tenant_backfill_batch_event`、`tenant_ownership_decision`、`tenant_quarantine_record`。
- 四张表均以 `org_id/project_id` 开头构成强租户主键，并引用控制面工作区。
- 四张表均有数据库级 UPDATE/DELETE 与 TRUNCATE 阻断触发器。
- 迁移只含 schema expand，无 `INSERT ... SELECT`、历史 UPDATE/DELETE、FK Validate、读切换或 RLS。
- Alembic 升级、降级、再升级通过，最终 single head/current=`228ti1e3ledger`。

## 2. E3-2 Dry-run

| 资源 | 当前待处理 | 决策 |
|---|---:|---|
| `authz_tuple` 未归属 | 6 | QUARANTINE |
| `meta_membership` 断链 | 53 | QUARANTINE |
| `twa_ws_member` 断链 | 0 | 无 |
| `twa_audit` 断链 | 0 | 无 |
| 合计 | 59 | ASSIGN=0，QUARANTINE=59 |

6 条 authz 记录是在 E3-0 后的共享库累计测试中再次产生，仍为两租户列 NULL；这进一步证明固定 ID/共享库测试存在隔离债务。它们没有被默认归属测试组织或栖月汇。

Dry-run 只输出 key/evidence/snapshot hash 和聚合，不输出 user/object/subject、Token、审计正文或业务 payload。同一 snapshot + code commit 生成确定性 batch；第二次持久化返回 replay，账本数量没有增长。

## 3. 验证

- E3-1 专项与租户隔离累计：63 passed，7 个既有 warning。
- E3-2 后租户隔离累计：66 passed，7 个既有 warning。
- Ruff、diff check：GREEN。
- 真实账本：1 batch / 1 planned event / 59 decisions / 59 quarantine。
- 业务历史 DML：0。

## 4. 下一门

E3-3 在由匹配版本 `pg_dump` 生成并恢复的新隔离数据库中执行。为完整记录职责分离与 apply/rollback after hash，将增加 append-only decision event，不修改既有 decision。
