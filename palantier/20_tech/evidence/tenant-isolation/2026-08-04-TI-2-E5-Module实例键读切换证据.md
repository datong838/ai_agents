# 228 · TI-2 E5 Module 实例键读切换证据

> 日期：2026-08-04
> 代码：`aos-platform m1@8aee9f6`
> 结论：GREEN；无 DDL/DML 迁移

## 1. 实现结果

- 对外路径参数与响应继续使用 `moduleId`。
- 内部先由 `(org_id, project_id, module_id)` 计算稳定 UUIDv5，并同时核对新键与 scoped legacy `id/module_id`。
- 单边存在、歧义、NULL 或 UUID 漂移抛 `ModuleIdentityDriftError`，不做旧键降级读取。
- Module 父 get/update/touch 与 Canvas、Deployment、Event、Interface、Query、Variable、Widget Instance 的业务读改删切换为 `module_pk + TenantScope`。
- 两条孤儿 Event 保持数据与 quarantine，但因没有父身份而在新读路径不可见。

## 2. 测试修正

临时测试库会在迁移后重新创建历史样例，因此测试夹具显式把父子恢复到 E3 已完成状态；该逻辑只存在于 tests，不写共享库。旧 E2 用例中无父直接建子资源的前提被纠正为先创建租户内 Module。E3 冲突负向用例改用无子记录的隔离父，避免 E4 validated FK 正确阻止其篡改。

## 3. 验证结果

- E5 专项与关键 Workshop：5 passed。
- E5 + E2-A + Workshop：46 passed。
- Tenant Isolation + Workshop 累计：130 passed，7 个既有 warning。
- 静态门：7 个子 Store 不再出现以 `module_id` 为父身份的业务 WHERE 谓词。
- 外部合约仍返回 `moduleId`，不泄漏 `module_pk`。

## 4. 共享库只读核对

| 项目 | 结果 |
|---|---:|
| 父 Module 影子对读 | 160/160 一致 |
| 子记录身份错配 | 0 |
| orphan Event | 2，保持 NULL/quarantine |
| 栖月汇/非测试 Module | 0 |
| RLS enabled/forced | 0 |
| Alembic | `228ti2e4validate` |

## 5. 分支与下一门

`m1`、w1、w2、w3、w4 本地及远端均为 `8aee9f6`；四 Worker clean。下一门为 E6 RLS。APP-04/05 仍未完成，不以 E5 代替主键 Contract 或卸载能力。
