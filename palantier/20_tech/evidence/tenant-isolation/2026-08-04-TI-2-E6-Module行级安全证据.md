# 228 · TI-2 E6 Module 行级安全证据

> 日期：2026-08-04
> 代码：`aos-platform m1@846b49a`
> 结论：GREEN；最终 Alembic `228ti2e6rls`

## 1. 安全边界

- 原连接角色 `aos_app` 是 owner/superuser/BYPASSRLS，不能直接作为 RLS 运行身份。
- 新增 `aos_runtime`：NOLOGIN、NOSUPERUSER、NOBYPASSRLS、非 table owner。
- scoped transaction 使用 `SET LOCAL ROLE aos_runtime`，随后设置 transaction-local org/project GUC；事务结束角色与 GUC 均复位。
- 11 张 Module 实例表 ENABLE+FORCE RLS；10 表按 org+project，Organization Profile 按 org。每表一个 FOR ALL policy，同时包含 USING/WITH CHECK。

## 2. 权限兼容结论

Theme/Widget 等 Store 与 Module 共用 `connect(scope)`，部分 legacy 表又在迁移后动态创建。为避免降权导致正常功能回归，迁移 owner 对 public 表/sequence 配置普通运行 DML 的现有与默认 grant。运行角色仍无 DDL、TRUNCATE、REFERENCES、TRIGGER、角色或 BYPASSRLS 能力。默认 DML grant 不等于租户隔离，新 tenant 表仍必须进入注册表与后续 RLS 波次。

## 3. 备份与可逆演练

- 备份：`/private/var/tmp/aos-ti2-e6.WHDDpN/aos-meta-before.dump`
- 大小/权限：1,615,403 bytes / mode 600
- SHA-256：`91c573644ce17a6c25f57db4dc697c5e0dd29c482cc38a960cdcfdb9f24ea4a3`
- 多轮 `228ti2e4validate → 228ti2e6rls → downgrade → upgrade` GREEN。
- downgrade 后 Module policy=0、RLS=0，角色保留；最终 11/11 policy、enabled、forced。

## 4. 运行矩阵与数据守恒

| 检查 | 结果 |
|---|---|
| schema lint | ok=true；role safe；11 表/11 policy |
| owner Module 总量 | 160 |
| dev-org/dev-project runtime 可见 | 159 |
| 无 GUC runtime 可见 | 0 |
| 栖月汇 Module | 0 |
| 跨 scope 读 | 不可见 |
| 伪造 scope 写 | WITH CHECK 拒绝 |
| 事务后 role/GUC | 不泄漏 |

## 5. 自动化与分支

- E6 + TenantScope 专项：12 passed。
- Theme/Widget 降权兼容回归：10 passed。
- Tenant Isolation + Workshop 累计：134 passed，7 个既有 warning。
- m1、w1、w2、w3、w4 本地及远端均为 `846b49a`；四 Worker clean。

下一门为 E7 Contract。APP-04 相同业务 ID 双租户共存与 APP-05 组织内卸载仍未完成。
