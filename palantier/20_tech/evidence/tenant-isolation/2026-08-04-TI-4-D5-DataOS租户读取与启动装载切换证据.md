# TI-4 D5 · Data OS 租户读取与启动装载切换证据

> 日期：2026-08-04
> 结论：GREEN
> 代码：`m1@2b81a1b`
> Tree：`25b3c9fbd136466576f55a06be1bd95c59f9e49a`
> Alembic：`228ti4d4validate`（本波无迁移）

## 1. 结果

- `load_all(scope)` 强制 canonical TenantScope；7 表查询全部约束 org/project。
- 启动 hook 只清空 Data OS 缓存，不再执行全库业务 SELECT；首个请求按 scope 懒加载。
- Source/Sync/Dataset/History/Pipeline/Build/Schedule list/detail/mutation 使用严格 `(orgId, projectId)` 可见性；org-only 与无标签兼容已移除。
- 同组织不同工作区互不可见；293 条 NULL/quarantine 历史不进入 Store 返回或 runtime map。
- 启动不物理删除数据，D3 账本与共享业务行保持。

## 2. 验证

- D5/DataOS/Connector：10 passed。
- 相关 Wave/API：35 passed / 22 skipped。
- Tenant Isolation 累计：162 passed / 8 skipped。
- 无参 `load_all()` 静态命中 0；共享库 297 行、293 NULL、293 quarantine、revision `228ti4d4validate`。
- m1 与四 worker 同步至 `2b81a1b` / 同 tree。

## 3. 下一门

D5 已关闭应用读取旁路，但数据库仍依赖每条 SQL 正确带 scope。下一门 D6 必须给 7 张 Data OS 表启用 FORCE RLS，以数据库策略阻断漏 scope、跨 scope 和伪造 scope 写入。
