# TI-4 D6 · Data OS 行级安全证据

> 日期：2026-08-04
> 结论：GREEN
> 代码：`m1@3e9677f`
> Tree：`5bd14b623853061fa346fa63e0b67dbc16ee0df3`
> Alembic：`228ti4d6rls`

## 1. 交付结果

- `meta_source`、`meta_pipeline`、`meta_dataset`、`meta_dataset_history`、`meta_sync`、`meta_schedule`、`phase5_pipeline_graph` 共 7 表全部启用 `ENABLE + FORCE ROW LEVEL SECURITY`。
- 7 个 policy 均只授予既有降权角色 `aos_runtime`，并同时使用 `USING` 与 `WITH CHECK` 精确匹配 transaction-local `aos.org_id/aos.project_id`。
- scope A 只能读取与写入 A；scope B 看不到 A；scope A 伪造 B 写入失败。
- 无 GUC 的 `aos_runtime` 可见行为 0，写入失败关闭；迁移未创建角色、未修改业务行、未给运行角色开放 D3 控制账本或隔离记录。

## 2. 测试与实库验证

- D6 专项：`3 passed`。
- Tenant Isolation 累计：`165 passed, 8 skipped`，零失败。
- 共享开发库执行 `D4 → D6 → D4 → D6`：policy/ENABLE/FORCE 为 `7/7/7 → 0/0/0 → 7/7/7`。
- 7 表业务数据始终 297 行，其中 293 行仍为 NULL 未知归属；D3 batch `44902fd4-7390-50ee-ae86-192ebd456c33` 仍为 `NO_ACTION=4`、`QUARANTINE=293`，隔离记录 293。
- 执行前备份：`/private/var/tmp/aos-ti4-d6.b27d7v/aos-meta-before.dump`，1,861,902 bytes，mode `0600`，SHA-256 `669c8f2efd89016efe6bba8f66c537a1d709421ca84586c2b5af5fa6b079c16c`。

## 3. 分支与风险

- `m1` 与四个 worker 本地/远端全部同步到 `3e9677f`，tree 完全一致。
- 293 条未知归属虽然已被应用读链与 RLS 双重隐藏，但仍在活跃表；scope 列仍可空、业务键仍全局唯一，运行时 `ensure_data_os_schema()` 仍包含建表 DDL。
- 因此 D6 不等于 Data OS Contract 完成；必须继续 D7，将未知行原样迁入不可变维护隔离区并冻结 scoped 主键/外键和迁移真源。

## 4. 下一门

执行《228-TI-4-D7-DataOS复合主键隔离区与迁移真源实施方案》。D7 完成前不得开始具体微商城 Connector。
