# TI-4 D7 · Data OS 复合主键、隔离区与迁移真源证据

> 日期：2026-08-04
> 结论：GREEN
> 代码：`m1@c890a7a`
> Tree：`0387668ad3671ef59028b989fcfa47776f56b33d`
> Alembic：`228ti4d7contract`

## 1. 交付结果

- 7 张 Data OS 活跃表全部冻结 `org_id/project_id NOT NULL` 与 scoped 复合主键；同一业务 ID 可在不同组织/工作区共存。
- Source→Pipeline/Dataset/Sync、Pipeline→Dataset/Schedule、Dataset→History 共 6 条真实父子关系由 scoped FK 固化；Pipeline Graph 保持现有独立 Canvas aggregate 语义，不错误依赖 `meta_pipeline`。
- 293 条 D3 `QUARANTINE` 历史按目标环境匹配的完整 D3 batch 原样迁入 `data_os_orphan_quarantine`；活跃 4 + 隔离 293 = 原总量 297。
- 隔离 payload hash mismatch=0，两个 immutable guard 生效，`aos_runtime` 无 SELECT 权限。
- `ensure_data_os_schema()` 不再执行 DDL，只验证 D7 schema readiness；D1 migration 非破坏性接管 legacy 基表 bootstrap，fresh database 只靠 migration chain 可到达 D7。
- 6 类 persist 与 Graph conflict target 全部映射到 scoped 主键，外部 API ID 不变。

## 2. 验证

- D7 专项：4 passed。
- Tenant Isolation：166 passed / 11 skipped。
- Tenant Isolation + DataOS Store + Connector + Phase5：205 passed / 17 skipped。
- DataOS Store + Phase5 独立回归：40 passed。
- 共享库完成 `D6 → D7 → D6 → D7`；降级恢复 297/293，升级回到活跃 4/隔离 293，最终 schema lint `ok=true`。
- D3 证据 batch 自动匹配为 `44902fd4-7390-50ee-ae86-192ebd456c33`；隔离 293 条全部引用该 batch。
- 执行前备份：`/private/var/tmp/aos-ti4-d7.ygIiWO/aos-meta-before.dump`，1,868,213 bytes，mode `0600`，SHA-256 `faa8ac6b8ff822a2b31782042045b7a672322d7c7435965ffd06efe3c441a8c1`。

## 3. 分支与剩余边界

- `m1` 与四个 worker 本地/远端全部同步到 `c890a7a`，tree 一致；用户 `docs/toutiao-series` 改动未纳入提交。
- TI-4 Data OS D1～D7 最终 GREEN。
- 5 张电商 Connector 真源表已有 scoped 主键且为空，但 workspace FK 仍 NOT VALID、RLS 未启用；Ecom SQLAlchemy/OAuth transaction 尚未统一降权并设置 GUC。

## 4. 下一门

执行《228-TI-4-C3-电商Connector数据库Contract与行级安全实施方案》。具体微商城接入继续暂停。
