# TI-5 C3-A Job 与 DLQ 隔离证据

## 结论

TI-5 C3-A 为 GREEN。代码基线 `b96eaf2` 将实际可达的 capability Job、DLQ、docintel failure 与 demo DLQ 从全局 ID/list 改为完整 TenantScope 键与信封；平台 capability 定义仍保持共享模板语义。

## 实施文件

- `services/aos-api/aos_api/routers/wave_ext.py`
- `services/aos-api/aos_api/data_os_store.py`
- `services/aos-api/tests/tenant_isolation/test_ti5_c3a_job_dlq_scope.py`
- 两个受容器类型变化影响的既有租户测试。

## 验证

- Job：同 ID 双 scope 可共存；status 只按当前 scope 命中，跨 scope 返回 404。
- DLQ：同 ID 双 scope可共存；list/retry 只处理当前 scope；docintel failure 带 `orgId/projectId`。
- Demo：同 ID demo DLQ 分 scope seed；purge 可清理已知 demo 项且不依赖全局 list。
- 专项与直接相关既有回归共 19 项通过。
- Tenant Isolation：222 项收集，全套执行通过，无失败。
- Ruff `F/E9` 与 `git diff --check` 通过。

## 分支证据

`m1` 与 W1-W4 本地/远端均为 `b96eaf2`，tree `724edafa185e7beb95ef5e9125c103c181f9dbe6`；四个 Worker clean，主工作树仅保留 26 个用户 `docs/toutiao-series` 改动。

## 边界与下一门

本波没有配置或验证 Redis、外部队列、生产离线存储，也没有修改 Phase5 Pipeline/Schedule 状态；这些分别属于 C3-C 与 C3-B。下一门只执行 C3-B。
