# TI-5 C3-B Phase5 进程态隔离证据

结论：GREEN。`bb795dc` 将 Pipeline、Node、Edge、Proposal、History、Schedule、ScheduleRun 全部改为 `(org_id,project_id,rid)` key；Engine 方法显式 TenantScope，Schedule Router 补 Principal，dispatch 只复制当前 scope 节点。

验证：新增同 ID 双 scope、ScheduleRun/reset 与跨 scope 负向 3 场景；Phase5 Pipeline、执行真实性和既有 singleton 回归合计 71 passed；Tenant Isolation 全套通过；全量 9,210 collected。五分支同 `bb795dc` / tree `0096d58e...`。

边界：executor/evidence resolver 为平台运行模板，本地 `queue.Queue` 为单次执行局部对象；在 C3-C 分类，不作为租户持久化容器。
