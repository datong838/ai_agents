# TI-6-1 资源登记与实库对账证据

## 结论

TI-6-1 GREEN。代码基线 `81db2f0`，数据库 revision 保持 `228ti5b1models`，本波没有业务 DML 或 schema migration。

## 实库对账

- PostgreSQL public 表：101。
- 注册表 PostgreSQL 表：101。
- coverage：100%。
- unregistered / stale / schema drift：0 / 0 / 0。
- 状态：75 `STRONG_PK`、8 `WEAK_PK`、18 `NO_TENANT`。
- 分类：80 tenant-owned、9 platform template、12 system global。
- 栖月汇 `org-org/dev-project` 业务行：0；必要控制面行：2。

本波校准 27 张已迁移表的 `tenantColumns/primaryKey/currentState`，并登记 6 张 ownership ledger/orphan quarantine 维护表。预检为每张表创建 savepoint；错误表只产生自身 `scanError`，不会使连接保持 aborted 并污染后续扫描。

## 验证

- Registry / migration plan / precheck：14 passed。
- Tenant Isolation：220 passed，8 skipped。
- 全量测试收集：9,213 collected，零 collection error。
- 实库 audit：101/101，drift=0。
- 五分支本地及远端同步至 `81db2f0`；四 Worker clean，主树只保留 26 个用户 `docs/toutiao-series` 改动。

## 保留阻断

重扫仍识别 6 个 tenant-owned 弱主键、7 个复合外键缺口、1 类历史空 scope，以及对象维护前缀/向量 key 仍被旧预检误判。Redis、外部 broker、offline store 未配置，因此严格生产空证明仍为 YELLOW；这些问题进入 TI-6-2，不得据此开始真实微商城 Connector。
