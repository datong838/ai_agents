# TI-6-2C 资产控制面 RLS 证据

## 结论

TI-6-2C GREEN。代码 `84a99cc`，共享开发库 revision `228ti6cassets`。

## Contract

- Composition、Installation、Integration Case 共 15 张表新增 `aos_runtime` 双 GUC policy，并统一 ENABLE/FORCE RLS。
- 五个 PostgreSQL Store/Reader/Projector 在真实 TI6 schema 内使用 canonical `TenantScope` 绑定 transaction-local `aos.org_id/aos.project_id`。
- 到期投影不再允许无 scope 全租户扫描；`org_id/project_id` 必须同时显式提供，否则失败关闭。
- 旧迁移快照的隔离测试 schema 未采用 TI6 policy 时不切换 runtime role，避免测试基础设施被新生产契约反向污染。

## 可逆与守恒

- 共享库升级前创建容器内备份 `/tmp/aos-ti6-2c-before.dump`，大小约 1.9 MiB。
- 15 张目标表升级前均为 0 行；迁移仅创建 policy 和切换 RLS，不执行业务 DML。
- 升级后 public 101 表中 66 表 ENABLE/FORCE RLS；TI6 policy 共 17 个，其中本波资产控制面 15 个、上一波 Theme/Widget 2 个。
- 共享库 Alembic single current=`228ti6cassets`。

## 验证

- 资产控制相关定向回归：56 passed。
- Tenant Isolation：227 passed，8 skipped。
- 全量：9,221 collected。
- Ruff、`git diff --check`：GREEN。
- 五工作树本地/远端同线 `84a99cc`，tree `a86454258d50baad7b400f19c4051a46a998286e`；四个 Worker clean，主树用户头条文档改动未夹带。

## 保留门

`authz_tuple` 8 条空 scope、`meta_membership` 53 条投影断链、3 个未知对象和外部后端未配置仍保持显式阻断。TI-6-2C 只完成资产控制面的物理隔离门，不把本地底座 GREEN 误写成生产空证明 GREEN。
