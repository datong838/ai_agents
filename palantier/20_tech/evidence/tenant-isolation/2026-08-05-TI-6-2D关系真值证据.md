# TI-6-2D 关系真值证据

## 结论

TI-6-2D GREEN。代码 `bff998d`，共享开发库 revision `228ti6drelations`。

## 关系真值

- `meta_workspace`、`twa_workspace` 在机器注册表中显式声明为 `workspaceRoot`；根节点不再被通用预检错误要求反向引用自身。
- `bundle_composition`、`bundle_installation_command`、`integration_case` 新增已验证 `(org_id, project_id) → meta_workspace(org_id, project_id)` FK。
- `workspaceRoot` 仅允许用于 `TENANT_OWNED + STRONG_PK + CONTROL_PLANE` PostgreSQL 根表，不能作为一般业务表绕过 FK 的标签。
- 预检 `TENANT_COMPOSITE_FOREIGN_KEY_MISSING` 从 5 降为 0；authz 与 membership 历史阻断保持不变。

## 可逆与守恒

- 共享库升级前备份：`/tmp/aos-ti6-2d-before.dump`。
- 独立恢复库完成 `228ti6cassets → 228ti6drelations → 228ti6cassets → 228ti6drelations`。
- 三张目标表升级前均为 0 行；迁移只增删三条 FK，无业务 DML。
- 临时演练库已删除；共享库只向前升级。

## 验证

- 注册表/预检：13 passed；迁移关系专项：5 passed。
- Tenant Isolation：230 passed，8 skipped。
- 全量：9,224 collected。
- Ruff、`git diff --check`：GREEN。
- 五工作树本地/远端同线 `bff998d`，tree `9bdbe162ce2d45c41bdb95a0796a5a8d81c3dfd0`；四 Worker clean，主树 26 个用户文档改动未夹带。

## 保留门

当前 migration ledger 只剩：`authz_tuple` 的空 scope、弱键和未归属，以及 `meta_membership` 的 53 条历史 orphan。它们属于已披露的 TI-1 历史风险，不由本波猜测修复。下一门进入栖月汇空实例与模板定制安装/卸载验证。
