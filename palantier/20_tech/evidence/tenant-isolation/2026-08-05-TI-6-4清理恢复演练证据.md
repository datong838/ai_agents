# TI-6-4 清理恢复演练证据

## 结论

TI-6-4 GREEN（本地 synthetic 演练范围）。代码 `44887c1`；共享库 revision 不变，仍为 `228ti6edirectory`。本波没有删除栖月汇、测试组织或任何用户数据。

## 新增能力

- `tenant_cleanup.py` 从 `tenant_resources.yaml` 生成确定性 dry-run impact plan，每个 PostgreSQL 资源使用 savepoint 失败隔离。
- 四类处置策略：48 个 `DELETE_SCOPED_ROWS`、18 个 `RETAIN_IMMUTABLE_HISTORY`、1 个 `RETAIN_BROADER_SCOPE`、7 个 `EXTERNAL_NOT_EXECUTED`。
- Connector 旧 `workspace_id` 仅映射 canonical `project_id`；只有 `org_id` 的配置在工作区清理时保留。
- apply 双重硬门：数据库必须为 `aos_test_*`/`aos_ti6_*`，组织与工作区都必须为 `ti6-synthetic-*`。
- 执行器在事务 savepoint 内 apply/verify，随即 rollback，并校验目标内容摘要恢复、peer scope 不变及两次重试结果一致。
- `tenant_cleanup_impact.py` 仅输出只读 JSON 计划，不提供共享/生产删除入口。

## 共享库只读证据

- 栖月汇 `org-org/dev-project`：PostgreSQL 业务行 `0`，可清理行 `0`。
- impact plan hash：`sha256:b13a103c0b44e7b1d6086da30e7c60dc81b706fe3f60906c95d0b1125e4b69c6`。
- 外部 7 类后端显式为 `EXTERNAL_NOT_EXECUTED`，没有冒充已清理。
- 共享库最终仍为 `228ti6edirectory`、101 张 public 表，栖月汇 `theme=0`。

## 备份恢复与 synthetic 演练

- PostgreSQL 容器备份：`/tmp/aos-ti6-4-before.dump`，1.9 MB，保留在 Git 外。
- 首次恢复故意检查到不含 ACL 时 runtime 无 `theme INSERT` 权限；重新按完整 ACL 恢复后 `aos_runtime` 权限正常。这一问题没有被隐藏。
- 恢复库 `aos_ti6_restore_20260805a`：101/101 注册覆盖、schema drift=0、66 张 tenant-owned RLS/FORCE RLS 表和 66 条 policy 一致。
- synthetic 目标删除 `theme=1`，peer 组织 `theme=1` 不变；回滚后目标 `theme=1` 及内容摘要恢复；同一 plan 第二次结果完全一致。
- 临时恢复库已删除，其中只含备份副本和 synthetic 演练数据；备份文件仍可恢复。

## 验证

- TI-6-4 专项：4 passed。
- Tenant Isolation：239 passed，10 skipped。
- Ruff/check/format：GREEN；只读 CLI JSON 可直接解析。
- 五工作树同线 `44887c1`，tree `c587ab87f306c6444b7e123a2a460d9dcb7da01f`；四个 Worker clean，主树 26 个用户 `docs/toutiao-series` 改动未夹带。

## 保留边界与下一门

TI-6-4 GREEN 只证明本地 PostgreSQL synthetic 可逆演练成立，不等于生产组织物理删除 GREEN。TI-1 E4 历史断点、3 个未知对象和未配置外部后端继续是生产 blocker。下一门 TI-6-5 执行累计对账与微商城本地接入前置收口，然后在真实 Connector 编码前暂停。
