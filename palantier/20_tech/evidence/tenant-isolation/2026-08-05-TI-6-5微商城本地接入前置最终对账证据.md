# TI-6-5 微商城本地接入前置最终对账证据

## 结论

TI-6-5 完成，**AOS 本地微商城 Connector 开发前置 GREEN**。代码基线 `m1@44887c1`，共享库 `228ti6edirectory`。本结论只说明平台通用内核、租户隔离、模板/实例、空组织和 synthetic 可逆演练已满足本地接入开发的起跑条件；不表示生产部署 GREEN，也不表示 Niushop 已接入。

## 累计技术门

| 验证面 | 结果 |
|---|---|
| Tenant Isolation | 239 passed，10 skipped |
| Schema / Resource Registry | 10 passed；101/101 public 表覆盖，unregistered/stale/drift=0 |
| 实库 RLS | 66 张 enabled + FORCE，66 policies，tenant-owned enabled/force=66/66 |
| OpenAPI / Route | 38 passed，2 subtests passed |
| OpenAPI 产物 | 2,284 paths，1,499 schemas，4,047 route rows，4,028 unique operation pairs，二次导出无漂移 |
| 后端全量收集 | 9,235 tests collected |
| TI-6-4 可逆演练 | synthetic 双次 apply/verify/rollback 一致，peer 不变，完整 ACL 备份恢复通过 |

## 栖月汇本地 Readiness

- 目标 scope：`org-org/dev-project`；权威目录与 Meta 目录均为“栖月汇商贸有限公司”，工作区双目录均存在。
- PostgreSQL `BUSINESS_DATA=0`，active installed Module `0`，目标非 PostgreSQL item `0`，控制面必需行 `3`。
- `localDevelopmentReady=true`，`localBlockers=[]`。
- 四个不可变平台 Module 模板可在实例组织安装后独立 Overlay，卸载不删业务事实。
- 本次没有向栖月汇写入 synthetic Module、商品、订单、凭据或 Connector 数据。

## 生产门仍为 YELLOW

`productionDeploymentReady=false`，保留以下 blocker：

- `BLANK_TENANT_VALUES`；
- `UNATTRIBUTED_POSTGRES_ROWS`；
- `TI1_E4_HISTORICAL_QUARANTINE`；
- `UNKNOWN_NON_POSTGRES_PREFIXES`；
- `NON_POSTGRES_PROBES_INCOMPLETE`。

此外，真实 Niushop D0 仍需专用只读账号、真实凭据管理、外部后端 PROBED、源站权限负向测试和平台接入专项评审。这些属于开始真实微商城接入后的 D0/部署门，本次按用户约定在此之前暂停。

## 五分支与数据安全

- `m1`、W1、W2、W3、W4 本地/远端均为 `44887c1`，tree `c587ab87f306c6444b7e123a2a460d9dcb7da01f`，ahead/behind 全为 `0/0`。
- 四 Worker clean；主工作树仅保留 26 个用户 `docs/toutiao-series` 改动，未夹带进任何 AOS 提交。
- 共享库未为最终对账做业务写入；TI-6-4 备份 `/tmp/aos-ti6-4-before.dump` 仍保留在 PostgreSQL 容器内。

## 路线对账与停止点

两份 `00-开发计划*`、`228-电商增长参谋长实施文档索引与分波路线图.md`、微商城 FDE 专项、租户隔离总方案与 `AOS项目开发上下文/` 已统一为：

1. 228-AOS 的 `M3` 仍是 FDE UI/SDK，旧排期 `M3` 仍是“抖音上线”，两者不混用。
2. M1～M5 与 TI-0～TI-6 本地平台前置已收口；G0～G6 和具体平台 Connector 不因此自动开始。
3. 下一次工作应先确认首个平台与 G0/D0 安全边界，再新建微商城实施波方案；本轮不编写真实 Connector、不读取凭据、不连接微商城。
