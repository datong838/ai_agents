# W7-11 多媒体累计、浏览器、安全与故障注入门预检 ADR

> 日期：2026-08-15
> 决策：`ENGINEERING_EIGHT_GATES_GREEN / EXTERNAL_THREE_GATES_BLOCKED / NO_EXTERNAL_EFFECT / NO_RELEASE`
> 前置：W7-01～W7-10 全部实现 GREEN 且 Receipt 与当前 release exact revision/hash 一致

## 1. 当前事实

W7-01～10 均已完成事实预检、目标合同优化与两轮复审，但十项实现都未 GREEN。其测试覆盖通用 Stage/Task/Eval/Artifact/Capacity/Usage/Action、Bundle/目录与 Workshop 壳；这些命令存在大量重叠，不能把数量相加冒充媒体全链。从首个 W7-01 预检提交到当前提交，`services/apps/packages/bundles` 无源码变化，因此各预检证明的是同一通用基础树，而不是逐项实现推进。

目前没有 signed media profile 经 canonical compiler/executor/Provider 产生 Artifact，再经四门、返工、发布、结算和 EffectReview 的完整运行；也没有 Media Studio 正向浏览器、安全 EvidencePack、故障注入、真实 Adapter 或发布 canary。W7-11 不得签发。

## 2. 累计门

必须分别为 GREEN：

1. contract_green；
2. service_green；
3. database_restart_green；
4. tenant_rls_green；
5. browser_positive_green；
6. browser_negative_green；
7. security_green；
8. fault_injection_green；
9. provider_adapter_green；
10. publish_canary_green；
11. operational_ready。

任一栏 false/unknown/stale 或 Receipt 不属于当前 release，整体即 blocked；不使用多数通过、平均分或测试总数替代。

## 3. 必须贯穿的真实旅程

- LITE/STANDARD/FULL 均保留八职责与确定性 signed Stage DAG；
- prepare/freeze/recommend/confirm/compile/approve/start/pause/checkpoint/resume/takeover/cancel 可刷新与重启恢复；
- Artifact family/Master/Variant/supersedes/conflict/selection/Approval 不可变；
- 四门同 Artifact，Issue→ReturnDecision→新 attempt/invalidation 正确；
- Provider timeout/unknown/late/partial/cancel/reconcile 不重复收费请求；
- Capacity/Budget/Usage/Settlement 对齐每 Stage/partial Artifact 和币种差异；
- PublishCandidate/Impact/Approval/Lease/Receipt/Handoff/lineage/effect 独立可追；
- drift/revoke/expiry/conflict/stale/partial/forbidden/unknown 全部失败关闭。

## 4. 浏览器、安全与故障注入

浏览器必须使用 production build + 真实 HTTP，在 1280/1440/1920 验证唯一 H1、语义 tabs、键盘/焦点、缩放/reduced-motion、无页面横向溢出，以及 DOM/network/console/authority 对账；刷新与深链恢复同一 exact context。正向只认 `org-org/dev-project`，`dev-org/dev-project` 只作负向隔离。

安全覆盖 RLS/FORCE RLS、permission/marking/purpose、短时受控 URI、MIME/hash/malware/archive bomb/OCR/EXIF/字幕 prompt injection、egress allowlist、Secret/PII、maker-checker、kill/rate/budget。故障覆盖 lease loss/stale fence/takeover race、crash before/after submit、duplicate/out-of-order/late webhook、Provider request id loss、Checkpoint corruption、依赖漂移、capacity/budget race、Artifact/Eval/publish conflict、数据库/API 重启与网络分区。

## 5. 两轮审查

### 第一轮：覆盖完整性

- 三 Profile、八职责和 W7-01～10 全链均有验收入口；
- 浏览器、安全、故障、租户、重启、Provider 与发布 canary 分栏；
- unknown、争议、partial 和人工 Handoff 不被成功率吞并；
- 真实正向、负向 canary 与 Mock/静态证据边界明确。

结论：`PASS`。

### 第二轮：签发纪律

- 每栏必须有当前 release 的独立 Receipt，不能以计划、通用测试或页面可打开替代；
- 重叠测试数不求和，源码未变不声称十项实现推进；
- W7-01～10 未 GREEN 时不启动有副作用累计旅程；
- 当前保持 target-state/disabled 且不签发 W7 退出门。

结论：`PASS_WITH_CUMULATIVE_GATE_BLOCKED`。

## 6. 最终裁决

W7-11 累计验收合同已收敛，可以作为未来签发清单；当前实现为 `NOT_STARTED`、累计门为 blocked。本轮不改代码、不做迁移、不访问真实租户/浏览器、不调用 Provider 或发布。事实见 `.evidence/workshop/2026-08-15-w7-11-media-cumulative-browser-security-fault-preflight.json`。

## 7. 2026-08-26 串行施工更新（编码前方案）

### 7.1 当前基线与本轮目标

W7-01～W7-10 现已在 `m1` 按各自独立 Receipt 完成代码、合同、专项测试和页面验收；当前 authority 为 `AOS-000266`，W7-10 的 Prime 强一致投影已 CURRENT。本轮不把历史测试数求和冒充累计门，而是新建一个可机器判定的十一栏 GateSet，每栏持有独立状态、reason code、evidence ref 与 cutoff。

本轮只能将当前代码可证明的 contract/service/browser/security/fault 等标记为 GREEN；真实 Provider Adapter、publish canary 和 operational-ready 若没有当前 exact Receipt，必须保持 `blocked/unknown`。因此 W7-11 可以完成“累计判定器与证据包”的工程交付，但不伪造运营准入，不开启 Provider、Action 或发布。

### 7.2 文件级施工清单

1. 新增 `services/aos-api/aos_api/ecommerce_workshop_media_cumulative_contracts.py`：冻结十一栏状态、exact evidence ref、整体裁决与不可执行边界；
2. 新增 `services/aos-api/aos_api/ecommerce_workshop_media_cumulative.py`：只读组装 W7-01～10 当前证据及故障注入结果，对缺失、stale、跨 release 或未授权外部门失败关闭；
3. 最小扩展现有 Media Studio composer/router/contracts，使 v6 GET-only 视图返回累计 GateSet，不新建第二业务 authority；
4. 最小扩展 `apps/web/src/api/ecommerceWorkshop/*` 严格解析与 `MediaStudioPage.tsx`，展示十一栏、整体 blocked 原因、原子 Skill → Logic 编排 → 数字同事绑定 → 工作台贡献，仍保持写入口 0；
5. 新增后端累计/故障注入测试和前端 parser/UI 测试，覆盖 stale fence、duplicate/late/unknown、跨租户、缺 Provider/canary Receipt 及重启可重建性；
6. 执行 W7-01～11 专项与累计回归、OpenAPI/domain manifest、TypeScript/build、sensitive scan，再用内置浏览器完成 1280/1440/1920、键盘/焦点、刷新/深链、DOM/console/network 对账；
7. 交付 evidence、Delivery Receipt 和安全提交，最后串行 CAS 更新 authority、01/06 与 Prime；任一外部运营栏不 GREEN 都保留 `NO_REAL_PROVIDER / NO_EXTERNAL_EFFECT / NO_RELEASE`。

### 7.3 不变式与回滚

- 没有 exact Receipt 的栏不得由布尔配置、fixture、页面按钮或通用测试点亮；
- `org-org/dev-project` 只用本地可控测试证据，`dev-org/dev-project` 只用负向隔离，不修改任何真实业务数据；
- 不 apply 共享迁移，不调用真实 Provider，不执行 Action、reconcile、retry、publish 或 release；
- 回滚只需移除 v6 累计贡献与新测试，v1～v5 读模型及历史 Receipt 保持可读。

## 8. 2026-08-26 实施、复审与验收结论

W7-11 已按第 7 节完成 Media Studio v6 十一栏累计判定器。合同、服务、重启确定性、租户隔离、浏览器正向/负向合同、安全和故障注入八栏绑定交付 revision `AOS-000267` 的 EvidencePack；生产 Provider Adapter、发布 Canary 和 operational-ready 三栏没有独立当前 Receipt，继续逐栏 `blocked`。整体状态因此仍是 `blocked`，且 `externalEffectsAllowed=false / releaseAllowed=false`，没有以 8/11 多数通过冒充运营放行。

验收事实：

- 后端累计、Media Studio、发布、Workshop API 与 OpenAPI 回归 `41 passed / 7 warnings`；Python 编译通过，OpenAPI 保持 `2666 paths`，schema 增至 `2389`；
- Web 定向累计复跑 `3 files / 13 tests`，新增负向租户合同后单文件复跑 `5 tests`；Web 全量 `235 files / 2152 tests`、TypeScript 与 production build `344 modules` 通过；
- 内置浏览器通过 1280/1440/1920 三视口，确认唯一 H1、三个语义 Tab 可实际切换、11 栏、AOS-000267、8 栏 ready、3 栏 blocked、刷新稳定、外部副作用和 Release 均关闭，1920 无页面横向溢出；
- scoped security `16 files / 0 critical / 0 warning`，scanner `9 passed`；全仓既有 `5 critical / 326 warning`，故保持 `REPO_BASELINE_RED`，不宣称全仓 GREEN；
- 未 apply 共享迁移、未写真实租户、未调用 Provider、未运行发布 canary、未执行 Action/重试/reconcile、未发布或 release。

代码提交 `be380d16`，证据提交 `de5d55e3`；证据包为 `.evidence/workshop/2026-08-26-w7-11-media-cumulative-gate.json`，宽屏图为 `.evidence/workshop/2026-08-26-w7-11-browser/media-studio-cumulative-1920.jpg`。退出裁决：`W7_11_MEDIA_CUMULATIVE_EIGHT_ENGINEERING_GATES_GREEN_THREE_EXTERNAL_GATES_BLOCKED_CODE_CONTRACT_BROWSER_GREEN_SECURITY_SCOPED_GREEN_REPO_BASELINE_RED_NO_REAL_PROVIDER_NO_EXTERNAL_EFFECT_NO_RELEASE`。W7 工程开发清单 11/11 闭合；运营外部门保留，下一串行入口为 W8-01 的无外部副作用代码/合同阶段。
