# W3-10 公共编排五层累计门预检 ADR

> 日期：2026-08-15
> 状态：`CUMULATIVE_CODE_BROWSER_GREEN / NEXT_WAVE_ALLOWED / NO_RELEASE`

## 1. 裁决

W3-10 不是新增功能 Task，而是 W3-01～09 的同 release 累计签发门。九项目标合同均已审查整改，但运行状态全部为 NOT_STARTED，因此当前不得运行正式累计验收或勾选 W3-10。

## 2. 五层矩阵

| 层 | 当前 | GREEN 条件 |
|---|---|---|
| Contract | 目标合同已审、未全部 runtime publish | exact schema/operation/error/state 同版发布 |
| Store | 部分 AIP 基础存在 | 所有新增 authority 的 RLS/CAS/幂等/重启回读 |
| API | RED | canonical operations、OpenAPI、strict SDK 全齐 |
| Web | RED | 九公共组件、八 Module 单一接入、九态与 typed intent |
| Browser | RED | 正式 HTTP、三视口、键盘、DOM/network/console/authority 同包 |

EvidencePack 固定 Git/build、schema/OpenAPI、Bundle/installation/Module refs、TenantScope、cutoff 与全部 Receipt。正向只认 `org-org/dev-project`；`dev-org/dev-project` 只作隔离 canary。

## 3. 禁止替代

方案批准、预检 Receipt、通用 AIP 单测、静态视觉稿、Mock、blocked-only 页面、历史跨 commit 测试数和 dev-org 正向都不能关闭 W3-10。任一 upstream 或五层轴为 missing/partial/stale/unknown 即 fail-closed。

## 4. 两轮审查

第一轮补齐五层逐栏与同 release identity，避免“上游看似都做过”替代累计证据。第二轮补齐正负租户、正式 HTTP、键盘/网络/authority 同包和禁止替代清单。结论均为 `PASS_AFTER_REMEDIATION`，但运行门保持 RED。

机器证据：`.evidence/workshop/2026-08-15-w3-10-common-orchestration-cumulative-preflight.json` 与 `.evidence/workshop/2026-08-15-w3-10-common-orchestration-doc-ledger.json`。

## 5. 2026-08-25 串行实施裁决

W3-01～W3-09 已分别完成代码、专项测试、累计回归和 Delivery Receipt，W3-10 进入实际累计验收。累计门使用一个不可混拼的候选 release identity：代码 Git SHA、schema/OpenAPI hash、Web build hash、迁移单头、九个 upstream Delivery Receipt 与浏览器 cutoff 必须在同一 EvidencePack 内精确列出。该 identity 只代表本地隔离验收候选，不代表生产发布、真实租户迁移、Provider 调用或业务动作。

文件级最小清单：

1. `scripts/workshop/w3_10_common_orchestration_gate.py`：纯读取并编译累计 EvidencePack；任一 receipt 缺失、重复、不是当前代码祖先、五层状态非 GREEN、租户口径错误或 identity 不完整时返回 RED。
2. `scripts/workshop/tests/test_w3_10_common_orchestration_gate.py`：覆盖完整 GREEN、缺 Receipt、跨 release、unknown/partial、错误正向租户和非祖先提交等失败关闭路径。
3. `.evidence/workshop/2026-08-25-w3-10-*`：保存同一候选 release 的 Contract、Store、API、Web、Browser 事实以及明确未执行的外部副作用。
4. 本 ADR 与 D-waves 总清单：只在五层累计事实同时闭合后更新，不以既有单波 GREEN 自动推导 W3-10 GREEN。

验收顺序为：判定器专项测试 → W3 后端累计与隔离数据库重启回读 → OpenAPI/SDK 与 schema hash → Web 全量与生产构建 → 正式 HTTP 内置浏览器状态、键盘、网络、控制台和布局验收 → 方案/证据一致性复审。内置浏览器无法控制的视口不得伪造；若不能在同一 cutoff 补齐三视口，EvidencePack 必须保持 RED，并先修复可控的浏览器验收基础设施，而不是跳过门禁。

首次累计回归得到 `100 passed / 5 failed / 2 subtests passed`，失败不得被历史单波 GREEN 覆盖。整改范围冻结如下：

- Workshop 路由清单测试补入已交付的 `ecommerceWorkshopTaskCockpitRunHandoffCompile`，不删除或放宽任何旧操作断言。
- OpenAPI 导出冻结值与生成物吸收 W3-05～W3-08 已存在的十个 canonical operations，并继续执行双 clean process 确定性校验。
- `test_w2d_store` 的冻结合同种子从空 JSON 占位改为可被当前 strict DTO 回读的 exact refs、Schema refs 与责任槽，修复跨文件累计顺序中的 500；不更改生产 Store 的失败关闭解析。
- 迁移测试必须在 `services/aos-api` 根目录运行，避免把调用方 cwd 错误误判为迁移实现失败。

## 6. 2026-08-25 累计验收与一致性复审

候选 identity 固定为 `m1@e3089af5a5eb65d22c0a8c389fa470d0d73bdcd5`、OpenAPI hash `0ea1ed43…5648`、components schema hash `4a47b6ce…41a3`、Web build hash `75fa3f5d…c6be`、migration head `w3_017` 与 cutoff `2026-08-25T02:23:02+08:00`。W3-01～W3-09 的九个代码提交均为候选提交祖先，未跨 release 混拼。

累计事实：判定器与 W2 邻接门 `35 passed`；W3 后端 `105 passed + 2 subtests`；OpenAPI `13 passed` 且 deterministic check PASS；Web `221 files / 2084 tests`、TypeScript 与 production build GREEN；迁移保持单头并仅生成 `w3_016 → w3_017` 离线 SQL，未 live apply。

内置浏览器连接当前 `m1` 正式本地 API，以 `org-org/dev-project` 在 1280×720、1440×900、1920×1000 三档验证：权威只读汇总、unknown 不冒充 0、职责阻断与启动门关闭均可见，键盘焦点可见，三档均无横向溢出；观测 49 个 `/v1` HTTP response 且全部为 200，当前验收窗口 console error 为 0。遗留离线队列未冲刷，任何 command、Action、Handoff、Approval、Provider、AgentRun 与业务写均为 0。

机器判定 `.evidence/workshop/2026-08-25-w3-10-common-orchestration-decision.json` 返回 `GREEN / NEXT_WAVE_ALLOWED`，只授权进入 W3-11 开发，不构成生产发布、真实迁移或 operational GREEN。方案、代码与 163/164 的“原子 Skill → Logic exact ref → 数字同事/assignee → 工作台贡献”链一致，无第二 authority、无能力倒退，风险复审通过。
