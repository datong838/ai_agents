# 228-M2-A1 核心解析与持久化并行实施方案

> 状态：**方案冻结中；M2-A0 累计回归 GREEN 前禁止编码**
> 版本：v1.0 · 2026-08-03
> 上位方案：[228-M2 Resolver、Lock 与 Installation 公共契约冻结方案](228-M2-Resolver-Lock-Installation公共契约冻结方案.md)
> 范围：M2-A1 Core；不包含 Router、控制面状态迁移、真实 Bundle 执行、具体电商平台逻辑或生产数据

---

## 0. 使用的 Rules

1. 先冻结本方案再编码；契约或文件边界变化必须先更新本文件。
2. 只在已分配文件域内开发；共享导出和既有 M1 文件只由总控集成。
3. PostgreSQL 是 snapshot、composition、lock、installation 与幂等记录的唯一真源；不得以内存状态代替。
4. 相同输入必须产生字节一致的 snapshot payload、lock payload 与 hash；顺序变化不得改变结果。
5. 租户、hash、证据、资源预算、依赖、能力或 contribution 冲突存在歧义时失败关闭。
6. 每个 Worker 先跑专项；集成后必须通过 M1+M2 专项、迁移、后端累计回归及既有前端门。
7. 不修改或提交用户现有 `apps/web/src/shell/AppShell.tsx` 工作区改动。

---

## 1. A1 目标与非目标

### 1.1 目标

- 从同一个 PostgreSQL `REPEATABLE READ` 只读事务构造 `RegistrySnapshot`，并复用 M1 发布证据策略。
- 对 snapshot 执行确定性版本解析、依赖图、能力、显式冲突和签名 contribution 冲突检查。
- 生成精确 `permissionDiff`、`migrationPlan`、`contributionDiff` 和不可变 composition lock。
- 实现 composition/lock 与 installation 基础持久化、完整性回读、租户隔离和等价输入幂等。
- 用属性测试、真实 PostgreSQL 测试和对抗测试关闭资源上限、并发快照、篡改与歧义风险。

### 1.2 非目标

- 不新增公开 HTTP API，不修改 Router manifest、生成聚合文件或 `main.py`。
- 不实现 submit/approve/apply/verify/rollback 业务状态机；该部分属于 M2-B。
- 不执行 Bundle SQL、Python、migration、外部调用或生产平台写入。
- 不改 FDE 页面、接入案例页面和具体电商资产包。

---

## 2. A0 前置契约

A1 只使用 A0 已冻结的公共契约和数据库结构：

- `CompositionRequest`、`RegistrySnapshot`、`ResolvedBundle`、`CompositionLockPayload`、`StoredCompositionLock`；
- signed `api/navigation/ui` contribution claims；
- `bundle_composition*` 与 `bundle_installation*` 七张表；
- canonical JSON/SHA-256、九个稳定 M2 错误码和资源预算；
- active/current/previous 指针历史不变量。

除总控外，Worker 不修改 `composition_contracts.py`、`contracts.py`、`errors.py`、包级 `__init__.py` 或 migration。若实现发现契约缺口，先暂停并回写上位方案，不以私有平行 DTO 绕过。

---

## 3. 模块边界与 Protocol

### 3.1 ReleasePolicy

`release_policy.py` 提供无存储副作用的公共判定：给定版本记录、当前时间和可信根状态，验证 published/not-revoked、五类 current release evidence、evidence revision、content hash、签名指纹和 trust root。M1 `RegistryService` 与 M2 snapshot reader 必须复用该判定，禁止跨模块调用下划线私有函数。

### 3.2 RegistrySnapshotReader

`registry_snapshot.py` 只接受服务端注入的连接工厂、clock 和 release policy。它在单个 `REPEATABLE READ READ ONLY` 事务中读取候选、manifest、签名与 evidence，构造 DTO 后由 `RegistrySnapshot.build()` 排序并计算 hash。任一版本在同一快照内信息缺失、重复或不一致时整体失败，不返回部分 snapshot。

### 3.3 Resolver

`resolver.py` 是纯算法入口：

```text
resolve(request, snapshot, baseline_lock | null) -> StoredCompositionLock
```

解析器不得访问数据库、网络、环境变量或当前时间。`checkedAt` 不进入 hash。候选、顶层请求和所有集合先 canonical 排序；选择规则、错误路径和 step budget 固定。

### 3.4 Store

- `composition_store.py`：原子写入 composition + revision 1 lock；等价输入命中同一结果；回读重算全部 JSON/hash 并验证跨列一致性。
- `installation_store.py`：A1 只实现创建 draft 基线、按租户读取/list 和幂等 command 基础原语；不开放 M2-B 状态迁移。
- 所有写操作使用单事务；数据库错误映射为稳定领域错误；不得返回 psycopg 原始文本、SQL 或连接信息。

---

## 4. 确定性解析规则

1. 候选只来自请求指定的 `RegistrySnapshot`；禁止解析中回查当前 Registry。
2. 顶层坐标必须显式 publisher；依赖缺省 publisher 只继承声明者。
3. 合并同一坐标的全部 version range，交集为空返回稳定版本冲突路径。
4. 优先最高 stable；只有约束显式包含 prerelease 时才允许 prerelease。
5. optional dependency 只有被顶层请求或其他 required dependency 激活后才进入图。
6. 每次候选尝试和回溯递增 step；节点、边、深度、候选、claim、payload 和 step 任一达到 `max+1` 立即失败。
7. 图完成后依次检查：cycle、显式 conflict、缺 capability/provider 歧义、API/navigation/UI contribution 冲突。
8. resolved bundles、edges、providers 与 conflict path 均按冻结 sort key 输出。
9. baseline 只从已验证 current active installation 绑定的旧 lock payload 重建；禁止用当前 Registry 反查旧版本。
10. 三类 diff 使用精确集合差：`added=target-baseline`、`removed=baseline-target`、`unchanged=intersection`，migration/contribution 保留 owner coordinate。

---

## 5. 四 Worker 文件所有权

### W1：发布策略与快照

仅修改/新增：

- `services/aos-api/aos_api/asset_registry/release_policy.py`
- `services/aos-api/aos_api/asset_registry/registry_snapshot.py`
- `services/aos-api/aos_api/asset_registry/registry_service.py`（只做总控审查后的最小复用改造）
- `services/aos-api/tests/asset_registry/test_release_policy.py`
- `services/aos-api/tests/asset_registry/test_registry_snapshot.py`

退出：M1 发布门行为不变；并发 revoke/expire 不产生混合快照；真实 PostgreSQL、Ruff 和 M1 Registry 回归通过。

### W2：持久化 Store

仅新增：

- `services/aos-api/aos_api/asset_registry/composition_store.py`
- `services/aos-api/aos_api/asset_registry/installation_store.py`
- `services/aos-api/tests/asset_registry/test_composition_store.py`
- `services/aos-api/tests/asset_registry/test_installation_store.py`

退出：事务原子性、等价输入幂等、双租户隔离、CAS 基础原语、回读 hash 重算、直接 SQL 篡改失败和异常脱敏全部通过。

### W3：确定性算法

仅新增：

- `services/aos-api/aos_api/asset_registry/dependency_graph.py`
- `services/aos-api/aos_api/asset_registry/diff_service.py`
- `services/aos-api/aos_api/asset_registry/resolver.py`
- `services/aos-api/tests/asset_registry/test_dependency_graph.py`
- `services/aos-api/tests/asset_registry/test_diff_service.py`
- `services/aos-api/tests/asset_registry/test_resolver.py`

退出：全部成功/失败图形、三类 diff、至少 100 组 permutation、重复运行 hash 一致和所有预算 `max-1/max/max+1` 通过。

### W4：独立对抗与一致性验证

仅新增：

- `services/aos-api/tests/asset_registry/test_m2_core_adversarial.py`
- `services/aos-api/tests/asset_registry/test_m2_core_integration.py`

W4 不复制实现，不修改生产模块；从公共/模块边界验证 publisher 歧义、prerelease、cycle、capability/contribution 冲突、snapshot/lock 篡改、跨租户、资源预算与 baseline 真源。

### 总控

- 审查所有 Worker 提交并按 W1 → W2 → W3 → W4 顺序集成；有依赖时只做提交级重排，不混合改写历史。
- 仅总控可修改 `asset_registry/__init__.py`、公共导出测试和方案/证据文档。
- 总控负责跨模块适配、完整累计回归、五分支对齐和 M2-A1 GREEN 判定。

---

## 6. 测试矩阵

| 范围 | 必测内容 |
|---|---|
| Release policy | published/revoked/deprecated、五类 evidence、expiry、revision、签名、trust root，M1 行为对照 |
| Snapshot | 空/单/多 publisher、同 id、多版本、事务并发 revoke/expire、重复坐标、hash 回算、候选上限 |
| Resolver | exact/range、prerelease、optional、缺依赖、菱形、无交集、cycle、显式 conflict、capability/provider |
| Contribution | API method/path/operationId、navigation route、UI slot/id；exclusive/shared 规则和 owner 路径 |
| Determinism | ≥100 组请求与候选 permutation、重复进程运行、canonical payload/hash 一致 |
| Diff | permission/migration/contribution 的 added/removed/unchanged 与 owner 保留 |
| Store | 原子写、等价输入、回读重算、篡改、租户、并发、异常脱敏、非空 downgrade blocker 兼容 |
| 累计 | M1+M2 asset_registry、迁移、后端全量、OpenAPI、Web/Desktop/SDK、typecheck/build、安全扫描 |

属性测试必须固定种子并在失败时输出最小化输入摘要；证据不得包含 manifest 敏感正文、数据库连接串或凭证。

---

## 7. 集成顺序与失败处理

1. M2-A0 最终全量回归 GREEN，证据文档提交，五分支对齐同一 `m1` SHA。
2. 四 Worker 从该 SHA 开始，仅修改各自文件域；每个提交先专项再提交。
3. 总控逐个审查：契约一致性 → 安全失败关闭 → 数据库原子性 → 测试真实性。
4. 每合并一个 Worker 先跑其专项和既有 M1 Registry 回归；四个全部合并后跑完整累计门。
5. 任一累计门失败，停在 A1 修复，不进入 M2-B；修复先更新本方案或风险说明，再做最小代码改动。

不得用跳过测试、降低断言、Mock PostgreSQL、接受随机 hash、泄漏底层异常或扩大公共导出来换取 GREEN。

---

## 8. A1 退出门

同时满足才可标记 M2-A1 GREEN：

- 上位 M2-A 全部解析、预算、snapshot、lock、store 与 migration 退出门通过；
- A0 五个公共 DTO 与错误/schema 契约无漂移；
- M1 Registry 专项与后端全量无回归；
- Web、Desktop、SDK、OpenAPI、typecheck、build 和安全扫描累计门通过；
- 代码工作树只保留用户既有 `AppShell.tsx` 修改；
- 证据文档记录精确提交、测试数、迁移 head、已知非阻断 warning 与风险；
- `m1` 与四个 worker 再次对齐同一提交。

M2-A1 GREEN 只允许进入 M2-B 方案复核和编码，不代表 M2、M0 或电商领域能力已经完成。
