# 112 · W-L8 画布↔生产契约只读投影

> 状态：`GREEN` · 2026-08-20  
> 证据：`.evidence/aip/2026-08-20-w-l8-logic-contract-projection/` · profile `ecommerce-standard`  
> 清单：`59` §8.5 **W-L8** · 验收：Logic 画布只读展示可引用的 Stage/Responsibility Profile；空表诚实

## 1. 目标

在 `/aip/logic` 编辑区（L5 禁旁路旁）增加只读投影：

1. 读取 `GET /v1/aip/production-contracts/stage-templates` 与 `responsibility-plans`
2. 按 `profile` 去重列出「可被哪类生产 Profile 引用」
3. 空表：诚实空态，不伪造 ecommerce 演示行
4. 可点「生产契约」互跳；本投影不提供 start/freeze 写操作

## 2. 不做

- 不在画布侧编译/冻结 Stage（属 E 轨）
- 不做 TaskGraph materialize（L17）
- 不改 w2

## 3. 最小改动

- `LogicCanvasPage.tsx`（或小模块 `logicProductionProjection.ts`）拉取并渲染
- 单测：空表文案；有表显示 profile
- 证据：`.evidence/aip/2026-08-20-w-l8-logic-contract-projection/`

## 4. 风险

- API 失败：显示错误并保持空列表，不降级 mock
