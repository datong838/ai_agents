# 114 · W-L1 分栏就绪语义（installed ≠ 可派发）

> 状态：`GREEN` · 2026-08-20  
> 清单：`59` §8.5 **W-L1** · 验收：同事卡不再把「已安装」当「可派发」  
> 证据：`aos-platform-w1-aip/.evidence/aip/2026-08-20-w-l1-readiness-ladder/`

## 1. 目标

在 AIP 目录 / 列表 / 画布旁路投影六段就绪：

`published → installed → binding → evaluated → operational → runnable(可派发)`

1. API 已有 `installedCount` / `runnableCount` 与 `runtimeReadiness`：**本波未改 runnable 公式**
2. UI 显式分栏；「可运行」改为「可派发」
3. 已安装但 `runnable=0` 不呈现为绿门/可试跑
4. 画布旁路区只读挂 agent 就绪摘要

## 2. 落地

- `aipChineseLabels.ts`：`deriveAgentReadinessLadder` / summary
- `CanonicalAgentRegistryPage.tsx`：阶梯条 + 「已安装 ≠ 可派发」说明
- `CanonicalAgentsPage.tsx`：可派发文案与试跑门
- `LogicCanvasPage.tsx`：旁路只读摘要
- `aip.tsx`：工具条去掉缺省「已安装」当可派发
- 测试 + 浏览器证据如上目录

## 3. 风险

- 画布 History Tab 交互测需显式切 Tab（本波同步修好）
