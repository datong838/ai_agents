# 63 · W-G2 `/aip/agents` 蓝图同级密度

> 状态：`GREEN` · 2026-08-19（清单对账 2026-08-20）  
> 上位：59 D2/D9、蓝图 `foundry/html/agents.html`（左列表 + 右详情 Tab）  
> 边界：只改 `CanonicalAgentsPage`；权威仍为 AgentInstance + runtime-readiness；**禁止**注入蓝图演示 Agent / 假工具数  
> 证据：平台 `f9cf221` · `.evidence/aip/2026-08-19-w-g2-agents-density/`

## 1. 复习结论

现页为单列简卡，低于 D2「与 agents.html 同级密度」。蓝图核心是：**左侧 Agent 列表 + 右侧配置壳 Tab**（提示词/工具/试运行/发布）。

## 2. 文件级清单

| 路径 | 动作 |
|---|---|
| `apps/web/src/pages/s2/CanonicalAgentsPage.tsx` | 主从布局；筛选；右侧 Tab；诚实空态 |
| `apps/web/src/pages/s2/CanonicalAgentsPage.test.tsx` | 选中切换、无可运行时试运行禁用、无 MOCK |
| `.evidence/aip/2026-08-19-w-g2-agents-density/` | vitest + 无头 Chrome 验收 |

## 3. UI 规则

1. 顶栏统计：实例数 / 可运行 / 链目录与市场。  
2. 左栏：可搜索列表；选中高亮；徽章=实例状态 + 目录 runnable/blocked。  
3. 右栏 Tab：`概览`（职责/阻断）· `工具`（深链 `/aip/tools`，注明 Overlay 见工具面板轨）· `试运行`（非 runnable 禁用）· `发布`（链目录/导入，无假发布）。  
4. 无实例：诚实空态 + 去目录安装。  
5. 遵守 52：不用方案编号作文案。

## 4. 非目标

不实现完整 Chatbot Studio 写路径；不造假工具清单 GREEN（属 W-T）。
