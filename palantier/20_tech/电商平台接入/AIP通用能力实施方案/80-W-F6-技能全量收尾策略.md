# 80 · W-F6 技能全量收尾策略

> 状态：`APPROVED_STRATEGY` · 2026-08-20  
> 前置：W-F4 GREEN（8 published / 10 bindings）；W-F5 `延后`  
> 目标：锁定 D5——**全量 publish + 可绑定**；删除「仅核心包」出口

## 裁决

| 项 | 口径 |
|---|---|
| 终态 | ~37 技能全部 `published` 且可挂 SkillBinding（失败则精确 blocker，不隐式跳过） |
| 禁止 | 「核心包足够」关单；无 `aip_logic_graph_revision` 时伪造 publish / ReleaseGate |
| 分批 | 每进库一批 Logic 图 → 跑 Eval 门 → publish-evaluated → Binding；审计脚本扩展 F4 门 |
| 门禁 | 任一剩余技能仍 `LOGIC_GRAPH_MISSING` / `LOGIC_NOT_PUBLISHED` → **W-F6 不得标已完成** |

## 当前事实（F4 证据）

- published=8 · bindings=10 · logicPublication=8  
- 其余 evaluated 诚实阻断 `LOGIC_GRAPH_MISSING`  
- 发布台 batch strip 已 fail-closed

## 本波交付

- 本文件为策略锁；**不改代码、不造 Logic、不假 publish**  
- 清单 **W-F6** → `外部等待`（等 Logic 权威进库）  
- D5 裁决列保持目标，进度改 `外部等待`（策略已锁）

## 退出条件（未来编码波）

1. `aip_logic_graph_revision` 覆盖全部目标 skill 的 `canonicalLogicId`  
2. 对应 LogicPublication + Eval/ReleaseGate 齐备  
3. 审计：`publishedCount == skillCount` 且 bindings 覆盖运营矩阵要求  
4. 方案本文件升 `GREEN`，清单 W-F6 → `已完成`
