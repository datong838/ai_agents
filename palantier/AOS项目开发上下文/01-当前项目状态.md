---
memory_schema: aos-memory-authority-ref/v1
project_revision: AOS-000388
authority_content_hash: sha256:b6782964a5153835c39321a2f000f55189a73ea5278be9f946f3bed5a363ef5a
---
<!-- AOS_MEMORY_PROJECTION {"content_hash":"sha256:b6782964a5153835c39321a2f000f55189a73ea5278be9f946f3bed5a363ef5a","project_revision":"AOS-000388","schema":"aos-memory-projection/v1"} -->
<!-- AOS_MEMORY_MANAGED_BEGIN -->
## Authority 管理状态（自动生成，请勿手改）

- 项目版本：`AOS-000388`
- 当前阶段：`Workshop 栖月汇只读验收 GREEN；BI-W10 L1 Profile/Scope authority GREEN，继续 AIP 生产组合解析与 P07/P08 自然窗口`
- 交付状态：`BI_W10_L1_PROFILE_SCOPE_AUTHORITY_GREEN_AIP_COMPOSITION_PENDING_SOURCE_READINESS_10_OF_12_NO_EXTERNAL_EFFECT_NO_RELEASE`
- 最后 GREEN 门：`BI-W10-02-PROFILE-SCOPE-AUTHORITY`
- 下一门：`BI-W10-02-AIP-PRODUCTION-COMPOSITION-RESOLVER`

### 已完成

- L1 InvestigationProfile/Scope exact authority、Case/Run blocker 分层、401/401 累计回归 GREEN

### 未完成

- AIP exact production composition 尚未解析，runCreatable=false
- P07/P08 下一自然窗口尚未到达，SourceReadiness 保持 10 ready / 2 failed

### 下一步

- 实现只读 AIP production composition resolver，精确读取 Logic/StageTemplate/SkillBinding/ResponsibilityPlan 生命周期并失败关闭
- 保持 API 稳定运行，只读回读 P07/P08 下一自然 Run、DLQ 与同截止 SourceReadiness
<!-- AOS_MEMORY_MANAGED_END -->
# 当前项目状态

## 2026-08-13 · AIP/Workshop 双开发线切分封板

- m1 已将经用户单独验收的 Browser 三模式迁移、Kitewright 更名、验证脚本与 D5 证据提交为 `d205427`，并推送 `origin/m1`；主工作树 clean、ahead/behind=`0/0`。
- 已从同一提交点建立 `/Users/ddt/work/projects/ai_agent/aos-platform-w1-aip`（`w1-aip`）和 `/Users/ddt/work/projects/ai_agent/aos-platform-w2-workshop`（`w2-workshop`），两分支已推送并跟踪对应 origin。
- 三条代码线当前同 HEAD `d205427`、工作树 clean、远端 ahead/behind 均为 `0/0`。
- `m1` 只作稳定集成；AIP 后续从 `w1-aip` 接续，Workshop 后续从 `w2-workshop` 接续。两线不直接互并，完成波次经门禁进入 m1。
- docs 继续共享：AIP、Workshop 各自单写方案目录；`AOS项目开发上下文/` 使用 HEAD-CAS 串行写，禁止覆盖并发检查点。
- 当前 AIP 下一编码入口仍为 W2-B `EvalContract + ResponsibilityPlan`；本次分支切分不改变既有授权顺序，也不提前授权 Workshop 编码。

## 2026-08-13 · AIP-6 A6F 后端 Canonical control 子步骤

- 已完成：A6F 实时代码差异审计、首审整改与复审；方案 `20-AIP-6-A6F-Canonical-API组织安装SDK与页面切换实施清单.md` 已以 `docs/m1@21a3570` 封板。
- 已实现（待本子步骤提交）：全局 Agent/Skill/Capability 稳定 list、6/37/10 聚合目录、六角色 Principal tenant 幂等安装、AgentInstance Canonical API、Capability catalog，以及 singleton 写入口失败关闭。
- 当前验证：24 项后端定向/邻接 GREEN；compileall、OpenAPI 四条 A6F 主路径和 `git diff --check` GREEN。
- 状态边界：安装仅创建六个 `provisioning` AgentInstance 与 RegistryReceipt；SkillBinding、CapabilityBinding、AgentRun 仍为 0，runnableCount=0。
- 未完成：唯一 TypeScript SDK、三页面切换、真实 `org-org/dev-project` 安装和内置浏览器验收；本子步骤尚未提交。
- 下一步：先提交 A6F 后端安全检查点；随后实现 `apps/web/src/api/aipAgentControl/` 和目录/实例/能力页面。

## 2026-08-13 · AIP-6 A6E 方案门

- 已完成：W0A 后续 A6E 实时代码差异审计；新增并复审 `19-AIP-6-A6E电商领域SolutionPack实施清单与评审结论.md`。
- 关键判断：现有 A6D 只有租户级 `CapabilityBinding`，没有全局不可变 `CapabilityRevision` 真源；A6E 必须以 additive `aip6_004` 补齐，不能用 Bundle JSON 充当运行 authority。
- 范围冻结：A6E 发布六 AgentTemplate、37 SkillTemplate/Logic identity、十 CapabilityRevision 与职责/策略 contribution；不创建组织 AgentInstance，不激活 Binding，不启动 Run，不提前实现 W0B 八公共对象。
- 诚实状态：37 SkillTemplate 初始为 `evaluated/blocked`；Capability catalog published 不等于组织 binding available。
- 当前安全下一步：实现 `aip6_004`、Capability Store、SolutionPack publisher 与 Bundle 1.2.0，并验证 exact readback 6/37/10 和零租户业务数据副作用。

### A6E 第一刀已完成

- 代码提交：`aos-platform/m1@cd1d07a`。
- 新增线性 `aip6_004`、全局不可变 `aip_capability_revision/aip_capability_alias`、严格 DTO/Store 与 alias resolver。
- `CapabilityBinding` 现在必须引用已发布且 revision/hash 完全匹配的 `CapabilityRevision`；不存在、hash 漂移或 lifecycle 非 published 均失败关闭。
- runtime `aos_runtime` 对新目录仅有 SELECT；发布继续走 owner/受控发布路径。append-only trigger 阻止 UPDATE/DELETE/TRUNCATE。
- 验证：29 项定向/邻接测试、compileall、迁移 downgrade/upgrade、唯一 `aip6_004 (head)`、精确 diff check 全部 GREEN。
- 未完成：Bundle 1.2.0、六角色/37 Logic/十 Capability contribution 与发布器 exact readback。

## 1. 一句话状态

截至 2026-08-13，AIP 全量编码已获授权并只在 `m1` 单分支连续实施。AIP-0～AIP-4、AIP-5 E0～E6 code/control、AIP-6 A6A～A6D 已封板；W0A 十类共享专业 Capability、六角色/37 Logic/八工作台 crosswalk 已通过复审，当前入口为 A6E 领域 SolutionPack。AIP-5 E6 的外部数据/Provider/GoldSet/向量门仍 blocked，AIP-7 exact ModelRoute authority 未完成，不能把 code/control 或目录通过宣称为 operational。真实范围只认 `org-org/dev-project`，`dev-org/dev-project` 仅作负向隔离 canary。恢复入口见 [`06-当前执行检查点.md`](06-当前执行检查点.md)。

### 当前执行摘要

- 当前门：`AIP6_A6E_SOLUTION_PACK`；A6D exact Run/Handoff authority 与 W0A catalog/crosswalk 已 GREEN。
- 已完成：后端策略、计划、运行、Receipt、checkpoint、告警 API；前端严格 SDK 与第四“知识管道”页签；数据库单 head `aip5_003`。
- 已验证：后端 E5D 最新 44、此前 AIP-5 累计 74、前端定向 12、前端全量 174 files / 2057 tests、TypeScript、Vite build、OpenAPI 与 diff check 均 GREEN；浏览器可见 7 策略/0 Schedule/0 Run/7 禁用动作，控制台 0 error。
- 长任务保障：活动响应内临时失败重试 2 次；本机 `launchd` Watchdog 以 300 秒 transcript 心跳检测疑似断流，首次恢复 2 次，仍失败则每 5 分钟通过同一 session 续跑；自动唤醒后的第一条消息必须明确告知用户 Watchdog 已发挥作用。
- 下一步：按 W0A stable ID 实现 A6E 六角色、37 Logic、十 Capability contribution 与职责模板；完成 exact revision/hash、alias 唯一性、readiness 与双租户验证后再进入 A6F。

- 已完成：AIP-4 E3C 成本归因、真实用量质量分桶与 Capability Receipt，代码 `f7179ce`、文档 `e2f9900` 均已推送。
- 已完成：E3D ProviderRevision、JobManifest、Submission/Event/Nonce/Artifact/Delivery/Reconcile Receipt 核心权威层；精确幂等、gap 收敛、持久 nonce 与 disabled Provider 已有定向测试。
- 已完成：E3D Canonical API、角色/scope/错误映射测试、路由聚合和 OpenAPI 确定性契约。
- 已完成：`aip4_008` exact lineage event 线性补强；JobManifest 现在同时固化 lineage ID、sequence、event ID，并以五列外键绑定不可变谱系事件。
- 已完成：当前 TaskRun、最新 sequence、同租户/工作区校验；缺失、过期和跨 Run 谱系失败关闭；13 项定向测试、Ruff、compile、OpenAPI 确定性导出通过。
- 已完成：补强后的 118 项 AIP 累计回归和数据库租户边界复验；E3D `IMPLEMENTED_GREEN`。
- 已完成：E3D 项目长期记忆、2026-08-12 日志、Prime Harness 三个全局条目与 Codex 跨会话更新说明均已刷新到 E4。
- 已完成：E4 编码前实时核查；确认谱系页固定六段、观测页合成趋势/伪 Token/生产 Mock，以及 Evals/Publication API 调用分散问题。
- 已完成：E4A/E4B/E4C 文件边界、三页面定义和验收矩阵已写回方案与清单。
- 已完成：E4A 唯一 `aipEvidence` SDK、权威 LineageEvent 严格解析与决策谱系页真实化；代码 `84dac50` 已推送。
- 已完成：9 项 SDK/页面测试、TypeScript、内置浏览器 idle/error 失败关闭验收；固定示例命中 0。
- 已完成：E4B TelemetrySpan/UsageReceipt 严格 contracts/client；跨 lineage、重复 provider receipt、quantity/quality 与 currency/usageKind 不一致均失败关闭。
- 已完成：可观测页按 lineage 展示 Overview/Spans/Usage；measured/estimated/unknown 独立计数，unknown 不伪造为 0；旧生产 Mock、合成趋势、伪 Token、Alerts/Dashboards 演示能力已删除。
- 已完成：E4B 14 项定向测试、TypeScript、diff check 与内置浏览器 idle/error 负向验收；代码 `9fcd203` 已推送。
- 已完成：E4C AIP-4 EvalRun 严格解析/只读 SDK 及 Evals 页面核查入口；终态缺 finishedAt、run ID 错配、suite 类型错配均失败关闭。
- 已完成：Logic Publication 列表、详情与发布统一使用唯一 AIP client；原 POST 后 GET 同一 publication 且 revision/hash/readback 完全匹配的严格语义未弱化。
- 已完成：E4C 6 文件 44 tests、前端全量 172 文件 2044 tests、TypeScript、生产构建与三页内置浏览器验收；代码 `43f5f82` 已推送。
- 限制：本机 aos-api health=500，浏览器验收只证明 UI/失败关闭 GREEN；不把真实正向回包冒充已验。
- 已完成：AIP-5 原“仍不授权编码”状态已与全量授权统一对账；方案/清单拆为 E0～E7 串行波次并以文档 `71f4edc` 推送，原未跟踪的 06 清单正式纳入 Git。
- 已完成：AIP-5 E0 公共契约、ADR 与现状真值审计；代码 `bb84fc3` 已推送，新旧兼容记忆回归 15 passed。
- 已完成：租户只能来自认证上下文、Working 不可晋升、Semantic 批准/晋升必须绑定精确 Eval/Draft/Approval 的契约门禁。
- 已完成：AIP-5 E1A 五张 additive authority 表、FORCE RLS、不可变事实和精确关系约束；代码 `07c974e` 已推送。
- 已完成：20 项契约/隔离/无 scope/降升级守恒测试；单 head `aip5_001`，未注册 API、未写真实业务数据。
- 已完成：AIP-5 E1B 唯一 PostgreSQL Store、幂等重放、状态事件、CAS 与原子 Promote；代码 `2692f1e` 已推送。
- 已完成：AIP-5 与邻接 AIP-4 Store 33 passed；单 head `aip5_001`，未注册 API。
- 已完成：AIP-5 E2 七类治理门、统一审批/晋升服务、精确 Eval/Draft/Approval authority 回读和晋升前二次时效复检；代码 `ab7b8aa`。
- 已完成：E2.1 将去重/冲突限定为同租户、同知识 scope、同 subject；跨 scope 同名知识允许并存，交由 E3 稳定解析；代码 `36be9b4`，15 tests passed。
- 已完成：E2 与相邻 AIP-3/AIP-4 累计 41 tests passed；compileall、diff check GREEN；Prime Agent 进程正常。
- 已完成：AIP-5 E3 KnowledgeQuery、渐进上下文、O1 Wiki adapter 与 reference-only 可重建索引；代码 `81c5f82`，8 个专项、累计 50 tests passed。
- 已完成：workspace/organization/public_package 稳定解析，权限/marking/applicability/time-cutoff/freshness/revoked/conflict 先于 payload 读取；跨租户、hash 漂移与半截 token chunk 均失败关闭。
- 已完成：AIP-5 E4 Canonical API、唯一前端 SDK、Memory Governance/Wiki 页面与浏览器验收。
- 已完成：AIP-5 E4A `/v1/aip/memory-authority` Canonical API，覆盖 Candidate/Event/Memory 读、受控 approve/promote 与 KnowledgeQuery；代码 `17817db`。
- 已完成：API 租户只取认证 Principal，请求 DTO 禁止 org/project 注入；角色拒绝优先于受信 provider 未就绪，provider 未装配明确 503；相关 38 tests passed，520 项路由聚合一致。
- 已完成：AIP-5 E4B 唯一 `aipMemory` SDK、严格 Candidate/Memory/KnowledgeQuery parser 与 operation registry；代码 `cc8f216`，6 tests、TypeScript GREEN。
- 已完成：E4B.1 深层严格解析 tenant/source/governance/Event/Item/Revision/Citation，补齐 events/approve/promote SDK；代码 `8b08792`，定向 8 tests、TypeScript GREEN。
- 已完成：E4C `MemoryGovernancePage`、导航、Candidate/Memory/Query 三视图与 O1 Wiki 治理入口；代码 `038b9da`。
- 已完成：E4D 后端 38 passed；前端定向 21、全量 174 文件/2053 tests、TypeScript、Vite build（274 modules）和 diff check GREEN。
- 已完成：内置浏览器确认 `org-org/dev-project`、新导航、三视图、必填阻断、API 500 诚实错误态、Wiki 跳转和控制台错误 0。
- 已完成：Prime Agent 运行时漂移修复。旧 supervisor generation 已失去 registry ownership，使用官方 `shutdown --force` 回收后将旧 runtime socket 目录可恢复迁移并重启；`status` 为 current，`list --all` 可读历史 session，最终检查 socket 持有者 PID 为 `82144`（PID 只作本次证据，后续以实时 `status` 为准）。两个 `prime-agent` PID 是 supervisor + worker 父子进程，不是两个冲突 daemon。
- 已完成：Prime Harness `aos-milestones` / `aos-current-delivery` / `aos-aip-full-checklists` 已恢复历史版本基线并同步到 `AOS-000008`，当前分别为 v25 / v27 / v21；当前权威块置顶，历史正文和 refinements 均保留，重复同步 0 变更。
- 已完成：多对话共享记忆总方案升级为 v1.2，Phase 0～5 已上线并封板到 `AOS-000008`；唯一 authority/CAS revision、确定性投影、9 份 Schema、Task/Delivery Receipt、语义 Lease、Event JSONL、双状态门、Prime/WorkBuddy/Codex 投影及 15 分钟只读健康守护均已验证。
- 已完成：Prime 投影已改为确定性直连适配器，具备版本单调账本、`0600` 备份、共享文件锁、mtime 并发检测、原子替换、回读校验和失败回滚；Prime 原生 Harness CRUD 同步使用同一锁并拒绝陈旧快照覆盖。
- 已完成：记忆核心提交为 `aos-platform/m1@ca5a042`、`prime-agent/main@ee254c81`、`docs/m1@11b7f76`；共享记忆 14 tests、Prime Harness 36 tests、Prime 干净基线全库检查均 GREEN。
- 已完成：Prime Agent 无 Key 启动整改。AGNES 凭据只由权限 `0600` 的 `~/.prime/agent/auth.json` 提供，`~/.zshrc` 已移除 Key 导出；安全启动器和用户级 `launchd` 配置不含 `--api-key` 或 Key 赋值。supervisor/worker 命令行及进程环境均确认不含 `AGNES_API_KEY`，清空环境后的真实最小模型调用返回 `AUTH_OK`，daemon 保持 `current`。
- 安全待办：旧 Key 曾出现在历史启动命令/进程输出中，应在 AGNES 凭据提供方完成轮换；轮换后只更新 `~/.prime/agent/auth.json`，不得恢复 shell 导出。
- 未完成：AIP-5 E5～E7 编码与验证；本机 API health=500 的真实正向浏览器回包仍未补证。
- 下一步：按用户要求暂停；恢复后先评审并冻结 E5 七知识管道 Scheduler/Run/Receipt/checkpoint 子波，不直接编码。

## 2. 阶段进度

| 阶段 | 状态 | 主要交付 | 基线/证据 |
|---|---|---|---|
| M1 Registry | GREEN | manifest 加载、签名、发布生命周期、PostgreSQL Registry | `m1@7c04cf3` |
| M2-A Resolver/Store | GREEN | snapshot、release policy、确定性依赖解析、lock/diff、持久化底座 | `m1@a6e4d31` |
| M2-B Control/API | GREEN | Composition、Installation 状态机、receipt/CAS/evidence、Canonical API | `m1@d85992b`；最终证据见 README |
| M3 UI/SDK | 最终 GREEN | 专用 SDK、Registry/Resolve/Draft、六 action、双角色 HTTP 生命周期、冲突/未知结果回读、Evidence/Pointer 和跨壳累计门全部完成 | `m1@36b386e`；M3-5 证据见 README |
| M4 Evidence/Case | 最终 GREEN | 后端真源、真实页面、Evidence 生命周期、统计/浏览器闭环、OpenAPI 与全量累计门 | `m1@8964c4b`；M4-5 证据见 README |
| M5 电商最小包组合验证 | 最终 GREEN | 无业务四包、synthetic Overlay、真实发布/解析/安装/回滚组合验证与累计门 | `m1@86c3076`；M5 最终证据见 README |
| TI-0B 资源注册 | GREEN | 86 表与 7 类非表资源族机器注册、实库 coverage/drift 审计 | `e7d07b8`；TI 证据见 README |
| TI-0C 鉴权失败关闭 | GREEN | 16 组 Router Principal 门、无 Token 401、OpenAPI 确定性 | `570fcc1`；TI 证据见 README |
| TI-0D 归属 precheck | GREEN / baseline YELLOW | 86 表、MinIO/Vector/Scheduler/Memory 只读实盘，migration ledger 和栖月汇业务/控制面拆分 | `504a639`；TI-0D 证据见 README |
| TI-0E 实施分波 | GREEN / PLAN_ONLY | 93 资源、16 执行组、Worker/依赖、每波 6 类测试和 E1～E7 回滚点 | `dc32c31`；TI-0E 证据见 README |
| TI-1 公共底座 E1 | GREEN | TenantScope/GUC、`228ti1e1expand`、authz nullable 列、7 个 NOT VALID FK、schema lint | `621a1bb`；TI-1 E1 证据见 README |
| TI-1 公共底座 E2 | GREEN | authz 新写三态开关、最小差异 ledger、升降级和失败关闭 | `a7df658`；默认 OFF |
| TI-1 公共底座 E3 | NON-PROD GREEN / HISTORICAL RISK ACCEPTED | append-only ledger、59 条 quarantine、角色分离、备份恢复、apply/verify/rollback、共享非生产零回填 | `d2d6c3d`；E4 BLOCKED |
| TI-1 公共底座 E4 | BLOCKED | 7 个 NOT VALID FK 的 Validate 门 | 59 条逻辑 quarantine/断链仍保留，不得强行 Validate |
| TI-PRE-1 测试隔离 | GREEN | memory 真隔离、pytest 临时数据库、共享库 hash 守卫、OpenFGA rollback | `0ae62bc`；17/99/71 passed |
| TI-2 E1 Module Expand | GREEN | nullable module_pk/模板绑定、Profile/Overlay/Preference、NOT VALID FK | `e95ae45`；74/492 passed |
| TI-2 E2-A 子资源 Scope | GREEN | Canvas/Widget/Variable/Query-list 显式 TenantScope 与跨租户负向门 | `b7cfb34`；53/77 passed |
| TI-2 E2-B 子资源 Scope | GREEN | Query 写、Event CRUD、Interface 读写显式 TenantScope 与跨租户负向门 | `5df9b5e`；47/80 passed |
| TI-2 E2-C 子资源 Scope | GREEN | Deployment、Theme、Widget Catalog 与 usage/ref 链显式 TenantScope | `87d6ef5`；46/91 passed |
| TI-2 E2-D Aggregate/静态门 | GREEN | Module aggregate TenantScope、10 Store 默认租户标识 0 命中 | `472728e`；56/87 passed；APP-04/05 未完成 |
| TI-2 E3 身份回填 | GREEN | UUIDv5、append-only ledger、恢复/共享可逆演练、最终 apply/verify、新写持续性 | `8e57c8a`；622 assign、2 quarantine；124 passed |
| TI-2 E4 FK Validate | GREEN | 11 个 Module 身份 FK 零违规验证、真实升降级可逆、行数守恒 | `129d438`；3/126 passed；最终 `228ti2e4validate` |
| TI-2 E5 Read Switch | GREEN | 父影子对读失败关闭、7 子表 module_pk 读改删、孤儿不可见、外部 moduleId 不变 | `8aee9f6`；130 passed；共享父对读 160/160 |
| TI-2 E6 RLS | GREEN | aos_runtime 降权、11 表 ENABLE/FORCE、USING/WITH CHECK、无 GUC零可见 | `846b49a`；134 passed；最终 `228ti2e6rls` |
| TI-2 E7 Contract | GREEN | 8 表 TenantScope 复合主键/NOT NULL、orphan 维护隔离、ETag 软卸载、APP-04/05 | `4004170`；272 passed / 3 historical skips；最终 `228ti2e7contract` |
| TI-3 E1 Object Expand | GREEN | 5 表 nullable scope、9 个 NOT VALID workspace FK、Ontology 模板不变 | `7f7d48a`；100 passed / 7 skipped；最终 `228ti3e1expand` |
| TI-3 E2-A Object/Graph/Branch Scope | GREEN | Graph/Funnel/Branch/Overlay 新写和 merge mutation 显式 TenantScope、跨 scope 全局键失败关闭 | `5cdafea`；185 passed / 15 skipped |
| TI-3 E2-B Draft/Wiki/Lifecycle Scope | GREEN | Draft approval 对象与 Wiki 写回、Lifecycle/TTL/Insight 显式 TenantScope，跨 scope 旧全局键失败关闭 | `561148f`；137 passed / 17 skipped |
| TI-3 E2-C Runtime Read/Static Gate | GREEN | Object/Branch/Object Set/Tool/Marking/Graph/Funnel/Analytics 读链 scope，目标 SQL 静态门 | `5e58768`；144 passed / 40 skipped |
| TI-3 E3 Historical Ownership | GREEN | 1,030 行逐记录账本；NO_ACTION 993、QUARANTINE 37、业务 DML 0；恢复库与共享最终验证 | `21f8803`；109 passed / 7 skipped |
| TI-3 E4 FK Validate | GREEN | 9 个 workspace FK 可逆 Validate；1,030 行、37 条 quarantine 守恒，非 NULL orphan 0 | `c268062`；111 passed / 7 skipped；最终 `228ti3e4validate` |
| TI-3 E5 Read Switch | GREEN | 全包 Object Runtime SELECT scope 收口；vector/demo 旁路关闭；4 Connector writer 延后 TI-4 | `3d53105`；115 passed / 18 skipped |
| TI-3 E6 RLS | GREEN | 9 表 ENABLE/FORCE、双 GUC USING/WITH CHECK、无 GUC零可见、可逆演练 | `8dfa626`；119 passed / 7 skipped；最终 `228ti3e6rls` |
| TI-3 E7 Contract | GREEN | 9 表 scoped 复合主键/NOT NULL、37 条不可变隔离、scoped branch FK、同 ID 双租户 | `fd2a124`；160 passed / 26 skipped；最终 `228ti3e7contract` |
| TI-4 C1 Connector Expand | GREEN | 5 张电商表 workspace alias NOT VALID FK、零业务 DML、真实升降级往返 | `ec0d7e7`；3 passed；累计 126 passed / 8 skipped；最终 `228ti4c1expand` |
| TI-4 C2 Connector Scope | GREEN | 五类通用 ingest 显式 TenantScope、scoped upsert、deferred writer 4→0 | `4ae0492`；相关 28 passed / 1 skipped；累计 132 passed / 8 skipped |
| TI-4 D1 Data OS Expand | GREEN | 10 个 nullable scope 列、7 个 NOT VALID workspace FK、297 行守恒 | `8e49b68`；6 passed；累计 135 passed / 8 skipped；最终 `228ti4d1expand` |
| TI-4 D2-A Data OS Write | GREEN | 六类显式 scope persist、跨 scope 全局 ID 失败关闭、History scoped replace | `5155d9b`；12 passed；累计 144 passed / 8 skipped |
| TI-4 D2-B Data OS Graph/Delete | GREEN | Graph 读写删和六类删除 scoped；boot 停止无 scope 物理删数 | `7a05bd8`；48 + 45 passed；累计 155 passed / 8 skipped |
| TI-4 D3 Data OS Ownership | GREEN | 297 条逐记录账本；NO_ACTION 4、QUARANTINE 293、业务 DML 0 | `78e16a7`；3 passed；累计 158 passed / 8 skipped |
| TI-4 D4 Data OS FK Validate | GREEN | 7/7 workspace FK validated；真实往返；297/293 守恒 | `176bdf5`；5 passed；累计 160 passed / 8 skipped；最终 `228ti4d4validate` |
| TI-4 D5 Data OS Read/Boot | GREEN | scoped load、lazy boot、严格工作区 API；293 NULL 默认不可见 | `2b81a1b`；45 passed / 22 skipped；累计 162 passed / 8 skipped |
| TI-4 D6 Data OS RLS | GREEN | 7 表 ENABLE/FORCE、双 GUC USING/WITH CHECK、无 GUC零可见、真实可逆往返 | `3e9677f`；3 passed；累计 165 passed / 8 skipped；最终 `228ti4d6rls` |
| TI-4 D7 Data OS Contract | GREEN | 293 条不可变隔离、7 表 scoped PK/NOT NULL、6 父子 FK、迁移真源 | `c890a7a`；相关 205 passed / 17 skipped；最终 `228ti4d7contract` |
| TI-4 C3 Connector Contract | GREEN | 5 表 FK Validate/FORCE RLS、SQLAlchemy/OAuth scoped transaction | `6feb1cb`；累计 209 passed / 11 skipped；最终 `228ti4c3contract` |
| TI-4 A1 Async Runtime | GREEN | Scheduler 内存 key/envelope 与 Apollo Spoke 完整 TenantScope；Channel 保持平台模板 | `659b1ce` + `bbdbd2f`；Tenant Isolation 176 passed / 11 skipped；最终 `228ti4a1apollo` |
| TI-5 A1 AIP Runtime | GREEN | 7 表 FK/FORCE RLS；四类 Store scoped transaction | `d381578`；AIP 83 passed；Tenant Isolation 181 passed / 11 skipped；最终 `228ti5a1aip` |
| TI-5 A2 AIP KV | GREEN | 23 条测试数据显式归属账本、复合主键、workspace FK/FORCE RLS、scoped runtime | `30137dd`；A2 5 passed；相关 API 29 passed；Provider 22 passed / 2 skipped；Tenant Isolation 186 passed / 11 skipped；最终 `228ti5a2kv` |
| TI-5 A3 Decision Lineage | GREEN | 631 条逐条 Draft 父记录归属；workspace/Draft FK、复合主键、FORCE RLS、scoped runtime | `68974ed`；核心 32 passed；Tenant Isolation 195 passed / 7 skipped；9,204 collected；最终 `228ti5a3lineage` |
| TI-5 B1 模型管理 | GREEN | 七表 65 行复合 PK/workspace/父子 FK/FORCE RLS；Router/Store 显式 scope；真实可逆往返 | `044f5a5`；模型管理 33 passed；B1 4 passed；Tenant Isolation 199 passed / 7 skipped；最终 `228ti5b1models` |
| TI-5 B2 模型模板/实例 | GREEN | 磁盘 Provider manifest 共享只读；安装/配置使用 scoped AIP KV；跨 scope 安装互不影响 | `7022ffe`；专项 1 passed；manifest hash 不变 |
| TI-5 C1 对象/向量 Namespace | GREEN | Connector probe 仅列租户前缀；2 个无归属对象可逆维护隔离；local vector 双重 scoped | `bb0773c`；C1 3 passed；相关回归 18 passed；外部 Qdrant 未配置 |
| TI-5 C2-A 关键 Singleton | GREEN | AIP Catalog 与 Phase5 Dataset/Build/Health/SyncConfig scoped key、Router Principal、scope reset | `a302544`；相关 77 passed；Tenant Isolation 206 passed / 8 skipped；9,199 collected |
| TI-5 C2-B wave_ext Dataset/Media | GREEN | `_datasets/_media/_media_bytes` scoped key、analytics/demo seed、GET `/v1/datasets` 真源 wave_ext | `4e5d069` + `b249a2d`；相关 80/1 skipped；Parser 7；Tenant Isolation 211/8 skipped；C2 GREEN |
| TI-6-1 Registry/Contract 对账 | GREEN | 27 张状态校准、6 张维护表登记、101/101 coverage、precheck 单表 savepoint | `81db2f0`；Tenant Isolation 220/8 skipped；9,213 collected；下一门 TI-6-2 |
| TI-6-2A 非表预检语义 | GREEN | 维护隔离对象单列、vector scoped namespace 聚合、真实 unknown 保持阻断 | `22cf8d7`；Tenant Isolation 221/8 skipped；下一门 TI-6-2B |
| TI-6-2B 基础表 Contract | GREEN | 5 表 scoped PK/validated FK；Theme/Widget FORCE RLS 与跨组织同逻辑 ID | `e1e3d7b`；`228ti6bcontract`；Tenant Isolation 224/8 skipped；9,217 collected |
| TI-6-2C 资产控制面 RLS | GREEN | Composition/Installation/Integration Case 15 表 FORCE RLS；事务 TenantScope；到期投影禁止无 scope 全扫 | `84a99cc`；`228ti6cassets`；Tenant Isolation 227/8 skipped；9,221 collected |
| TI-6-2D 关系真值 | GREEN | 工作区根节点显式语义；三个资产根直连 validated workspace FK；复合 FK 缺口归零 | `bff998d`；`228ti6drelations`；Tenant Isolation 230/8 skipped；9,224 collected |
| TI-6-3A 空组织 Readiness | GREEN / LOCAL ONLY | 测试组织与栖月汇双目录对齐；业务/active Module/目标非表资源均为 0；生产 blocker 独立保留 | `f9f3917`；`228ti6edirectory`；Tenant Isolation 234/8 skipped；9,228 collected |
| TI-6-3B 四模板实例闭环 | GREEN | 平台模板目录、scoped Module/Overlay 安装、effective config、跨组织定制、卸载保留业务事实 | `9585fc5`；OpenAPI/route 12 passed；Tenant Isolation 235/10 skipped；9,231 collected |
| TI-6-4 清理/恢复演练 | GREEN / SYNTHETIC ONLY | 注册表 impact plan、保护 scope 失败关闭、双次 apply/verify/rollback、备份恢复 ACL | `44887c1`；Tenant Isolation 239/10 skipped；备份 `/tmp/aos-ti6-4-before.dump` |
| TI-6-5 最终对账 | GREEN / LOCAL ONLY | Schema/Resource/OpenAPI/route、全量收集、readiness、五分支与全路线文档一致 | `44887c1`；OpenAPI/route 38 + 2 subtests；9,235 collected；production YELLOW |
| G0～G6 | 等待新一轮评审，禁止提前编码 | 电商增长领域能力 | M0 已完成，但尚未取得 G0/具体平台编码授权 |
| D0 只读发现与配置冻结 | GREEN | Niushop 源库 schema fingerprint、frozen/01-02 配置冻结、37 前置 tests | D0 规格文档 |
| D1 Phase A（G4/G5/G6 + SourceAdapter） | GREEN | DatasetSink + OTWriter + DLQ + Niushop 只读 SourceAdapter | `m1@9ddf5ee`；44 tests |
| D1 Phase B（P01-P07 配置 + 四段实施） | GREEN | 7 条 Pipeline 运行时配置、四段负向测试、派生指标 4 个、6 条核心 Link | `m1@e358a10`；336 tests |
| D1 退出门 | GREEN | 10 项全通过（G4-G6/SourceAdapter/P01-P07/四段/派生指标/Link/前置零回归/源零写入） | 380 tests GREEN |
| 前端数据连接模块重构 | GREEN | 数据源管理 / 连接器目录 / 新建数据源向导三模块；菜单重命名 + 连接配置表单完善 | `m1@e358a10`；1991 web tests |
| D1.5 Phase A-C（CustomerLite + L01-L03 + W01-W02） | GREEN | CustomerLite OT + P08 Pipeline + placedByLite Link + 派生指标 order_count/last_order_days；L01-L03 Logic Graph dryRun；W01/W02 Workshop | `m1@44a3203`；478 后端 + 2023 前端 tests |
| D2 AIP 资产包 + Niushop 连接器 + 真实初装 | GREEN | 4 bundle 30 文件内容物；Niushop 连接器插件；前端 querystring 预设；栖月汇 8 OT 数字孪生 547 对象落地 | `m1@c5ab023`；478 后端 + 2026 前端 tests；ecom_object 实际写入 |
| D1.5 Phase A（OT 契约 + P08 Pipeline + placedByLite + 派生指标） | GREEN | CustomerLite OT 扩展（CORE_OBJECT_TYPES/CORE_LINK_TYPES/REQUIRED_PROPERTIES）+ placedByLite Link 构造器 + P08 SourceAdapter PII 排除（8 字段）+ OTWriter CustomerLite 必填属性补齐 + 派生指标 `order_count Δ` / `last_order_days Δ`（link_aggregator 注入式接口）| `m1@009d31f`；447 tests GREEN（380 D1 零回归 + 67 D1.5 新增）；test_ec_d1_core_link_types/test_ec_d1_exit_gate "恰好 7" 松绑为"至少 7/子集"累积契约 |
| D1.5 Phase B（L01-L03 Logic + dryRun + 四段负向 + PII/派生端到端） | GREEN | L01 商品上架/L02 订单履约/L03 发货超时 Logic Graph canonical + dryRun（input/transform/branch 节点，null 降级按序短路）+ P08 四段实施负向测试（重跑幂等/断点恢复/冲突检测/越租户拒绝）+ PII 端到端扫描（SourceAdapter drop + OT 落地 0 残留）+ 派生指标端到端落地（ec_live_executor 不 mock apply_derived_metrics，order_count/last_order_days 字段存在且 null 兼容）| `m1@d61490d`；478 tests GREEN（380 D1 零回归 + 98 D1.5 = 67 Phase A + 21 L01-L03 dryRun + 4 P08 负向 + 6 端到端验证）；Phase B 完成，下一阶段 Phase C（W01/W02 Workshop） |
| D1.5 Phase C（W01/W02 Workshop 最小版） | GREEN | W01 订单与履约运营台扩展（OrderManagementPage 四 Widget：状态漏斗 computeOrderFunnel/异常订单表 filterRiskyOrders(risk_score>0.6)/SLA 队列 filterOverdueShipments(overdue_hours>0)/订单侧栏；写 Action 兼容保留加注释）+ W02 商品与库存运营台新建（ProductInventoryPage 四 Widget：商品 SKU 列表 mergeProductSkuView/低库存队列 filterLowStockSkus(stock_health=low)/分类树 buildCategoryTree(parent_id 嵌套)/质量告警 filterLowQualityProducts(quality_score<0.8)）+ 路由 workshop/inventory + 导航 workshop-inventory + 纯函数导出可测（null 安全降级）| 前端 apps/web 2023 tests GREEN（150 文件零回归 + 32 D1.5 Phase C 新增 = 14 W01 + 18 W02）；tsc --noEmit 通过；决策标签字段不渲染；后端零改动复用 /v1/objects/:type；提交 `m1@44a3203` |
| 4 Worker 起跑线拉平（228-d1 ff-only merge m1） | GREEN | 4 个 228-d1 worktree（w1-source/w2-sink/w3-ot/w4-dlq）执行 `git merge --ff-only m1`，全部 fast-forward 零冲突，更新 41 文件（D1.5 全部后端测试 + W01/W02 前端）；5 分支 tip 完全一致 = `44a3203`，达到同一起跑线，可立即并行开发 | 4 个 worktree clean；主仓库后端 478 tests GREEN（D1+D1.5 全量零回归，164s） |
| 废弃 223 组分支清理 | GREEN | 删除废弃 223 组 4 个分支（worker-1 `f1fa895`/worker-2 `f480ab3`/worker-3 `e6d91bf`/worker-4 `c354249`）；这组已全部进 m1 且无 linked worktree，安全删除 | 仓库现存：m1 + 228-d1-w1/w2/w3/w4 共 5 条线 |
| D2.5 架构缺口修复（normalize 模块 + store 装配） | GREEN | ① 新增 `aos_api/ec_normalizer.py`：8 OT mapper（Shop/Category/Product/ProductSku/CustomerLite/Order/OrderLine/Shipment）+ `normalize_rows` 入口（幂等：已 normalized 行透传）+ `_resolve_target_ot` 派发（pipeline.config.target_ot 优先 / pipeline.id 前缀推断 P01-P08）+ 通用工具 `_ts`/`_money`/`_str`/`_base`（保留 raw 字段供派生指标 `_get_field` 顶层读取）；② 新增 `ensure_store_assembled(eng)` 函数到 `aos_api/ec_live_executor.py` 顶部：自动注入 `ecom_consistency_store` 到 PipelineEngine 单例（已注入跳过 / DSN 装配 / 失败降级不抛异常）；③ 修改 `ec_live_executor` 主流程：插入 `normalize_rows(input_rows, pipeline)` 调用；④ 修正 `test_ec_d1_p04_api_config.py::test_dataset_sink_called_and_output_ref_format` 断言以匹配 normalize 后行为 | 520 tests GREEN（D1+D1.5 478 零回归 + 42 新增 = ec_normalizer 31 + store_assembly 6 + ec_live_executor 4 + e2e 1）；规格文档 `docs/palantier/20_tech/电商平台接入/微商城电商接入方案/D-waves/D2.5-架构缺口修复执行规格.md` |
| AIP 层资产包/数据源缺口诊断 | 调查结论（未动手） | ① "电商领域资产包（可安装可卸载）"机制已存在且 live，名为 **FDE 资产包**（侧栏入口 `apollo-assets`，路由 `/apollo/assets`，API `/v1/asset-bundles`+`/v1/bundle-compositions`+`/v1/bundle-installations`，生命周期 submit/approve/apply/verify/rollback）；电商 bundle fixture 已定义但 `content/` 为 `.gitkeep` 空骨架，未真正上注册表。② "微商城数据源链接"前端无专属入口，全前端仅 DataConnectionPage.tsx:71 一句"已支持 Niushop 微商城"文案；平台分类仅 database/saas/api/file/stream，无 niushop；实际靠 MySQL JDBC 连 Niushop 库兜底；DataSourceCreatePage 未读 `?connector=` querystring（缺陷） | 待作为下一波 4 worker 并行任务输入 |
| D3 W03+L05 内容物 + 完整 FDE 链路跑通 | GREEN | 资产包：W03 客户与私域运营台（6 Widget）+ L05 分润异常检测 Logic Graph（4 节点）+ W03×L05 Evals 测试矩阵（10 条）+ growth-gates 门控策略（G2_DECISION_TAGS BLOCKED → READY）+ growth bundle.yaml displayName 更新；完整 FDE 链路：Composition → Resolver → Installation 三态（draft→submitted→approved→applied→active）→ IntegrationCase 预留；关键修复：`manifest_loader.py` secretRef 类型声明放行、FDE 脚本 SQL/契约对齐、stage2 json 模块作用域 + release_evidence_revision 算法对齐、IntegrationRequestContext 字段 subject 非 actor | M5 baseline + Manifest loader secret 专项 + 75 Manifest tests + D3 主脚本 exit_code=0；脚本 `scripts/d3_qiyuehui_full_fde_case_creation.py`；installation_id=`56c200af-9308-4125-af02-1035100785dd`；4 bundle 全 PUBLISHED；待办：W03 decision_tag 实际取值注入 + IntegrationCase roles marking 补齐 |
| D3 叠加安装（growth 1.0.0 → 1.1.0 decision_tag 注入） | GREEN | W03 工作台 JSON 注入 decision_tag 实际取值（churn_high/vip/dormant 三个标签）+ growth-gates G2_DECISION_TAGS 状态 BLOCKED → READY + growth bundle 版本 1.0.0 → 1.1.0 bump；叠加安装脚本 `scripts/d3_overlay_install_1_1_0.py`：step0 读取 1.0.0 active installation ref → step1 重新签名发布 growth 1.1.0 → step2 Composition 叠加解析（currentInstallationRef → 1.0.0 active，触发 permission_diff/migration_plan/contribution_diff 基线差异计算）→ step3 Installation 5 步推进（create→submit→approve→apply→verify）→ step4 验证 | growth v1.0.0 + v1.1.0 双 PUBLISHED 共存；v1 installation_id=`56c200af-9308-4125-af02-1035100785dd`（overlay=d3-qiyuehui-v1-full-stack）+ v2 installation_id=`1286521e-6d6e-42b4-8aad-b5daaa1b01bb`（overlay=d3-qiyuehui-v2-decision-tags）双 active installation 共存 |
| D3 IntegrationCase roles marking 补齐 + stage4 补跑 | GREEN | 修复 `d3_qiyuehui_full_fde_case_creation.py` stage4 角色名错误（`aip-case-maker`/`aip-case-approver` → `integration-case-maker`/`integration-case-projector`，参见 `integration_policy.py:39-45`）+ create_evidence_snapshot 补全必传参数（idempotency_key + if_match）；最小更改原则：不修改历史脚本，新建独立补跑脚本 `scripts/d3_integration_case_roles_fix.py`，为 v1+v2 两个 active installation 各补建 IntegrationCase + EvidenceSnapshot；失败时打印完整 traceback 不静默 | integration_case 表 2 条 + integration_evidence_snapshot 表 4 条 + integration_case_command 表 4 条幂等记录；case_id_v1=`e32b42ec-938e-41ff-abbe-55009c94c189` + case_id_v2=`cb91ab1b-2bb4-4ddb-944e-3a043d920f8f`；stage=planned（evidence_count=0，待后续 IntegrationEvidenceWriter 写入）；回归 D1+D1.5 478 + D2/D2.5 71 + IntegrationCase 108 全 passed；2 个预存失败（test_migration_shape_single_head_and_empty_upgrade 期望 alembic head=228assetintegration 但实际=228ti6edirectory；test_m4_http_case_lifecycle_marking_tenant_replay_and_cas 因 _schema() BASE_MIGRATIONS 配置过时缺 TI-1~TI-6 migration 导致 500）与补跑无关 |
| D2.6 SSH 缓存 + Schema 懒加载 + 前端中文显示 | GREEN | SSH 隧道全局单例缓存（_TUNNEL_CACHE+_CONN_CACHE，threading.Lock 双检锁+健康检测，startup 预建，JdbcConnectorRuntime 走缓存不关闭）+ Schema 懒加载（list_tables_only 轻量表列表仅表名+注释；list_columns_for_table 单表按需查询；移除 demo 回退）+ 前端数据源详情页重构（左侧仅表名+中文注释显示；中间全字段横向滚动；表头中文#333加粗+字段类型#aaa可见；同步页面 SyncTask 列表展示）+ JDBC MySQL/PostgreSQL SSH 连接器插件注册 | `m1@0b1390d`；68 文件 +8983/-2417 行；SSH 缓存验证：启动 1 条隧道后续 0 次重建；Schema 懒加载：初始数据量减少 90%；中文注释命中率 ≥95%；后端 478+ 前端 2023 tests 零回归 |

## 3. M2-B 已完成能力

- 10 个唯一 HTTP 路径、11 个稳定 `operationId`。
- Composition resolve/get 与 immutable lock。
- Installation create/list/get 及 submit/approve/reject/apply/verify/rollback。
- receipt-first 幂等、回放再授权、强 ETag/If-Match CAS。
- maker-checker 职责分离、marking 写拒绝/读隐藏。
- exact selected release revalidation、服务端 evidence、不可变 revision/decision/event。
- active/previous pointer、rollback 语义和 PostgreSQL 唯一真源。

## 4. 最终验证快照

| 验证面 | 结果 |
|---|---|
| 资产域 | 779 passed，7 个既有 warning |
| Router/OpenAPI | Router 48 passed + 2 subtests；OpenAPI gate 11 passed |
| 后端全量 | 8978 passed，3 skipped，1 外部 live 用例显式 deselected，2 subtests，零失败 |
| Ontology SDK / Web / Desktop | 6 / 2001 / 40 passed |
| TypeScript / Build | Web、Desktop 均 GREEN |
| 路由/OpenAPI | 512 manifest entries；4047 runtime routes；2281 paths；4024 unique operations |
| 数据库 | Alembic 单一 head/current：`228assetintegration` |
| 安全扫描 | source 2606 files、critical=0、170 条既有 warning；built artifacts 184 files、critical=0、34 条既有 warning；M5 作用域 41 files、critical/warning=0/0 |

## 5. 当前工作边界

- M4 只实现领域无关 Evidence/Case 能力，不引入具体电商平台适配。
- M5 已完成真实 Composition/Installation 闭环及最终全量累计、OpenAPI/数据库/security 确定性、静态边界和五分支证据收口；M5-3 未新增功能。

### M5-3 最终验证快照

| 验证面 | 结果 |
|---|---|
| 资产域 / M5 专项 | 779 / 100 passed，7 个既有 warning |
| 后端全量 | 8978 passed，3 skipped，1 external live deselected，15 warnings，2 subtests |
| 真实 PG / Alembic | 26 passed；single head/current=`228assetintegration`；本地迁移后 8 张 `integration_*` 表均为空 |
| Router / OpenAPI | 48 passed + 2 subtests；OpenAPI gate 11 passed；512 manifest entries |
| Web / SDK / Desktop | Web 2001、Integration SDK 58、Asset Control SDK 63、Desktop 40、Ontology SDK 6 passed；typecheck/build GREEN |
| Security | M5 41 files 0/0；source 2606 files critical=0；artifact 184 files critical=0 |
| 五分支 | `4d04e1f`，tree `f8a257cf...`，本地/远端 `0/0`、clean；M5 功能检查点为 `86c3076` |

### M5-2 验证快照

| 验证面 | 结果 |
|---|---|
| M5 专项 | 100 passed，7 个既有 warning |
| 相关控制面累计 | W2 131 passed；W3 119 passed |
| 真实 PG | 四包 lock、六 revision、三 Evidence、revoke 后 rollback、无关安装隔离 GREEN |
| Security | 41 files，critical=0、warning=0 |
| 五分支 | `2d16a64`，tree `7eb9e58c...`，本地/远端 `0/0`、clean |

### M5-1 验证快照

| 验证面 | 结果 |
|---|---|
| M5 专项 | 95 passed，7 个既有 warning |
| 资产域累计 | 774 passed，7 个既有 warning |
| Registry | 四包真实 create/validate/maker-checker publish，重启 snapshot 稳定 |
| Security | 33 files，critical=0、warning=0；私钥不返回、不落盘 |
| 五分支 | `8aa87a9`，tree `f40c8cb4...`，本地/远端 `0/0`、clean |

### M5-0 验证快照

| 验证面 | 结果 |
|---|---|
| M5 专项 | 68 passed，7 个既有 warning |
| 资产域累计 | 747 passed，7 个既有 warning |
| Overlay | canonical hash `sha256:5c1d43e...7056e7`，可作为现有 opaque `overlayRevision` |
| Security | 29 files，critical=0、warning=0 |
| 五分支 | `4fd9c7f`，tree `48719e71...`，本地/远端 `0/0`、clean |

### M4-5 最终验证快照

| 验证面 | 结果 |
|---|---|
| 后端全量 | 8878 passed，3 skipped，1 external-live deselected，15 个既有 warning，2 subtests |
| 资产域 | 679 passed；真实 PostgreSQL 无 skip |
| Router/OpenAPI | 512 entries；4047 routes；2281 paths；4024 unique operations；1498 schemas |
| Web / Desktop / Ontology SDK | 2001 / 40 / 6 passed；typecheck/build GREEN |
| Security | 全仓 critical=0；Harness scoped critical/warning=0/0 |
| 五分支 | `8964c4b`，tree `236f8a25...`，本地/远端 `0/0`、clean |

### M4-4 验证快照

| 验证面 | 结果 |
|---|---|
| M4 后端累计 | 107 passed，7 个既有 warning，2 subtests |
| Web 全量 | 148 files / 2001 passed |
| TypeScript / production build | GREEN / GREEN |
| 浏览器 | current/reference/detail/8 gates/timeline/filter/stale/recovery/expiry downgrade GREEN |
| 隔离环境 | 6 生产 migration + 真实 JWT/API/Web；双 stop 后容器/端口/临时状态 clean |
| 五分支 | `c9da800`，同 HEAD/tree、远端同步、clean |

### M4-3 验证快照

| 验证面 | 结果 |
|---|---|
| 页面 + 组件 + hook + SDK 相关累计 | 11 files / 98 passed |
| Web 全量 | 148 files / 2001 passed |
| TypeScript / production build | GREEN / GREEN |
| 静态伪事实扫描 | 运行路径中旧常量、平台名、1.2M、G1～G10 均为 0 命中 |
| 五分支 | `b2dc69b`，同 HEAD/tree、远端同步、clean |

### M4-2 验证快照

| 验证面 | 结果 |
|---|---|
| M4-0～M4-2 后端累计 | 115 passed，7 个既有 warning，2 subtests |
| PostgreSQL Reader | 3 passed |
| 真实 JWT + 隔离 PostgreSQL HTTP | 1 passed；创建/回放/列表统计/详情/快照 CAS/时间线/marking 隐藏/跨租户均通过 |
| Router 聚合 | 512 manifest entries，生成物一致 |
| Web 全量 | 143 files / 1982 passed |
| TypeScript / production build | GREEN / GREEN |
| 五分支 | `483dd0f`，同 HEAD/tree、远端同步、clean |
- 不执行真实 Bundle 代码，不接触生产 Secret、商城数据或生产写回。
- 不把 Mock、前端常量或本地状态作为发布/验证/安装事实。
- 现有用户工作区改动不是 M3 基线的一部分，接续时必须保留。

### M4-1 验证快照

| 门禁 | 结果 |
|---|---|
| PostgreSQL migration/Store 专项 | W1 6/6，W2 6/6，W4 补强后 11/11 |
| 后端 M4 及迁移链累计 | 107/107，7 个既有 warning |
| Web Integration Case 专项 | 5 files / 58 passed |
| Web 全量 | 143 files / 1982 passed |
| Web TypeScript / build | GREEN / 233 modules |
| 分支 | 五分支 `14311e2`，tree `6f89a37e...`，ahead/behind `0/0`；五个远端分支同 HEAD |

### M3-0 验证快照

| 验证面 | 结果 |
|---|---|
| Asset Control 前端专项 | 14 passed |
| Web 全量 | 1795 passed，零失败 |
| TypeScript | GREEN |
| Web production build | GREEN |
| Registry/Composition/Installation/OpenAPI | 77 passed，零失败 |
| 生产页面/后端架构 | 未修改 |

### M3-1 验证快照

| 验证面 | 结果 |
|---|---|
| Asset Control 专项 | 41 passed |
| Web 全量 | 1822 passed，零失败 |
| TypeScript / production build | GREEN / GREEN |
| 后端 Registry/Composition/Installation/OpenAPI | 77 passed，零失败 |
| 生产页面/通用 API client/后端架构 | 未修改 |

### M3-2 验证快照

| 验证面 | 结果 |
|---|---|
| M3-2 / Asset Control 前端专项 | 72 passed |
| Web 全量 | 1842 passed，零失败 |
| TypeScript / production build | GREEN / GREEN |
| 后端 Registry/Composition/Installation/OpenAPI | 77 passed，零失败 |
| 生产 Mock 与 `/v1/assets` | 零命中 |
| 浏览器 | Registry/安装管理双页签、只读边界、服务不可达诚实错误态通过；浏览器沙箱未直连宿主机 API |

### M3-3 验证快照

| 验证面 | 结果 |
|---|---|
| Web 全量 | 132 files，1867 passed，零失败 |
| TypeScript / production build | GREEN / GREEN；227 modules |
| 后端 Registry/Composition/Installation/OpenAPI | 77 passed，零失败 |
| 浏览器 | 三页签、空选择禁用、服务端只读 Hash、M3-4 动作不可达、API 不可达诚实错误态通过 |
| 分支 | 五分支 `77a1e5b`，tree `895e5725...`，ahead/behind `0/0` |

### M3-4 验证快照

| 门禁 | 结果 |
|---|---|
| Web 全量 | 138 files / 1914 passed |
| TypeScript / production build | GREEN / 233 modules |
| 后端契约 | Installation、Composition、Control、OpenAPI 相关 71/71 |
| 浏览器 | API 不可达诚实错误态；动作按钮 0；Hash/Evidence 可编辑输入 0；未执行真实 POST |
| 分支 | 五分支 `6432444`，tree `cb4a25e0...`，ahead/behind `0/0`；五个远程分支已更新 |

### M3-5 验证快照

| 门禁 | 结果 |
|---|---|
| HTTP 双角色生命周期 | 真实 JWT + Router/Service + 隔离 PostgreSQL；自批 403、旧 ETag 409、Evidence/Pointer 与失败零写通过 |
| Web 全量 | 138 files / 1923 passed |
| Web TypeScript / build | GREEN / 233 modules |
| 后端累计 | M3/Installation/Composition/Control/OpenAPI 113/113 |
| Desktop | 9 files / 40 passed；TypeScript/build GREEN，253 modules |
| Ontology SDK | 6/6 |
| 分支 | 五分支 `36b386e`，tree `1b7c7fed...`，ahead/behind `0/0`；五个远程分支同 HEAD |

## 6. 2026-08-03 客户需求增量

- 新增微信小店独立 PlatformAdapterPack 方案，与现有微商城/Niushop 明确分离。
- “达人增长”和“价格治理”成为微信小店、抖音、快手等新电商平台的 P0 任务域。
- 复用现有六数字同事和 `AgentTask`/DAG，不新增平行任务系统或第七个智能体。
- 当前只完成产品与平台适配方案，状态为待评审、未编码；不改变 M3 作为当前代码阶段的顺序，也不解除 G0～G6 门禁。
- 微信小店 WX0 已完成官方 API 文档级证据矩阵，确认商家已有带货者、已知达人补全及部分绩效读取能力；全平台达人搜索、粉丝/年龄/开播筛选、联系方式和自动邀约仍未验证。
- WX0 当前为 YELLOW：客户测试店铺、优选联盟状态、权限集和脱敏真实响应未取得，因此不得开始 WX1 Connector 编码。

## 7. TI-0B/TI-0C 验证快照

| 验证面 | 结果 |
|---|---|
| PostgreSQL 资源登记 | 86/86，coverage 100%，unregistered/stale/drift 均为 0 |
| 当前隔离结构 | STRONG_PK 31、WEAK_PK 27、NO_TENANT 28；RLS enabled/force/policy=0/0/0 |
| 注册表测试 | 4 passed |
| Router/无 Token 门 | 17 passed；16 个代表性入口均 401 |
| OpenAPI | 确定性与当前生成物一致 |
| 分支 | 五分支/五远端 `570fcc1`；四 Worker clean；主工作树保留 3 个用户头条文档改动 |

## 8. TI-0D 验证快照

| 验证面 | 结果 |
|---|---|
| PostgreSQL | 86/86 表、2,854 行；blank tenant=0、现有 FK orphan=0 |
| 栖月汇 | BUSINESS_DATA=0；CONTROL_PLANE=5；`isProvenEmpty=false` |
| 未知归属 | 12 表、993 行；未自动认领 |
| MinIO / Vector / Scheduler | 70（测试 68/栖月汇 0/未知 2）/ 1 未分租户 / 0 |
| 非表缺口 | Cache、Queue 未配置；Offline 未实现；Process-memory 静态候选 832 |
| 专项回归 | tenant isolation 28 passed；TI-0D+TI-0B 11 passed；Ruff/compileall/敏感值扫描 GREEN |
| 分支 | 五分支/五远端 `504a639`；四 Worker clean；主工作树 3 个用户头条文档仍保留 |

## 9. TI-0E 验证快照

| 验证面 | 结果 |
|---|---|
| 统一计划 | 93 资源、16 执行组；遗漏/重复/未知均为 0 |
| 执行门 | `PLAN_ONLY`；36 个资源可进入 E1 评审，57 个资源在 E3 前仍受阻 |
| 测试/回滚 | TI-1～TI-5 每波 6 类必测；E1～E7 共 7 个回滚点；自动执行=false |
| 阻断传递 | 13 缺列、27 弱主键、53 缺复合 FK、12 表未知数据及 4 类非表阻断均保留 |
| 专项回归 | tenant isolation 31 passed；重点 14 passed；Ruff/compileall/JSON 校验 GREEN |
| 代码提交 | `dc32c31`；最终五分支同步状态见分支映射实时快照 |

## 10. TI-1 E1 验证快照

| 验证面 | 结果 |
|---|---|
| Alembic | 单一 head/current=`228ti1e1expand`；upgrade→downgrade→upgrade GREEN |
| 数据对账 | 86 表、2,854 行；逐表差异 0；authz 9 行仍全部未归属 |
| Schema | authz nullable 2/2；新增 FK 7/7 且 validated=0；RLS=0 |
| Scope | 严格 TenantScope；transaction-local GUC；下一连接 null/null |
| 历史断链 | meta_membership 57、twa_ws_member 4、twa_audit 3；未修改，阻断 validate/E3 |
| 回归 | 租户隔离/迁移累计 65 passed；TWA 专项 GREEN；Ruff/compileall GREEN |
| 限制 | 本机无 pg_dump/psql；生产/客户库前必须完成逻辑备份恢复演练 |

## 11. TI-3 E4 验证快照

| 验证面 | 结果 |
|---|---|
| Precheck | 9 表 1,030 行；完整 scope 993、NULL quarantine 37、非 NULL orphan 0 |
| Alembic | `228ti3e1expand → 228ti3e4validate → downgrade → upgrade` GREEN |
| FK 状态 | 9/9 validated → 0/9 → 9/9 |
| 数据守恒 | 最终 1,030 行、37 quarantine、业务 DML 0 |
| 累计回归 | 111 passed、7 skipped、零失败 |
| 分支 | 五分支与五远端 `c268062`，tree `a11462c0...` |
| 下一门 | TI-3 E5 Read Switch；不提前启用 RLS 或冻结 Contract |

## 12. TI-3 E5 验证快照

| 验证面 | 结果 |
|---|---|
| 读路径 | 全 `aos_api` Object Runtime SELECT 无 scope 命中 0 |
| Vector/demo | autoSample、story、seed、clear 双租户约束；跨租户和模板守恒 |
| Deferred | 4 个 Connector writer 精确冻结至 TI-4 |
| 共享库 | `228ti3e4validate`；1,030/37/0 守恒；业务 DML 0 |
| 累计回归 | 115 passed、18 skipped、零失败 |
| 分支 | 五分支/五远端 `3d53105`，tree `7e271e8b...` |
| 下一门 | TI-3 E6 RLS |

## 13. TI-3 E6 验证快照

| 验证面 | 结果 |
|---|---|
| Policy/RLS | 9/9，ENABLE+FORCE 9/9 |
| Runtime | 无 GUC零可见；跨 scope 与 WITH CHECK 失败关闭；上下文不泄漏 |
| 可逆演练 | E4→E6→E4→E6 GREEN |
| 共享库 | `228ti3e6rls`；1,030/37/0 守恒；业务 DML 0 |
| 累计回归 | 119 passed、7 skipped、零失败 |
| 分支 | 五分支/五远端 `8dfa626`，tree `0a0dd922...` |
| 下一门 | TI-3 E7 Contract；Connector writer 继续 TI-4 阻断 |

## 14. TI-3 E7 与 TI-3 总收口验证快照

| 验证面 | 结果 |
|---|---|
| Contract | 9 表 scoped 复合 PK、scope NOT NULL、overlay→branch scoped FK |
| 隔离区 | 37 行；payload hash mismatch 0；2 个防篡改 trigger；runtime 无权访问 |
| 数据守恒 | 活跃 993 + 隔离 37 = 1,030；共享 E7→E6→E7 原样往返 GREEN |
| 同 ID | Object/Branch/Funnel/Wiki/Draft/Lifecycle 双 TenantScope 共存，RLS 隔离 |
| 累计回归 | 160 passed、26 skipped、零失败 |
| 分支 | 五分支/五远端 `fd2a124`，tree `09b896a0...` |
| 下一域 | TI-4 Data OS 与 Connector；具体商城 Connector 仍禁止开始 |

## 15. TI-4 C1 验证快照

| 验证面 | 结果 |
|---|---|
| Contract | 5 表 scope NOT NULL/复合 PK；5 个 workspace alias FK 为 NOT VALID |
| 数据 | 实施前后 5 表均 0 行；业务 DML=0 |
| 可逆演练 | E7→C1→E7→C1 GREEN；最终 `228ti4c1expand` |
| 累计回归 | C1 专项 3 passed；Tenant Isolation 126 passed、8 skipped |
| 分支 | 五分支/五远端 `ec0d7e7`，tree `7ee52950...` |
| 下一门 | TI-4 C2 显式 TenantScope 与 scoped Connector transaction |

## 16. TI-4 C2 验证快照

| 验证面 | 结果 |
|---|---|
| Writer | REST/File/MySQL/PostgreSQL/SQL Server 全部显式 scope；无 scope writer=0 |
| 隔离 | REST/File 同 ID 双 TenantScope 共存；三类 JDBC scoped write GREEN |
| 负向 | 缺失/空白 scope 失败关闭；不自动创建数据库组织/工作区 |
| 共享库 | revision 保持 `228ti4c1expand`；Object/Ecom 均 0；业务 DML=0 |
| 累计回归 | Connector 28 passed / 1 skipped；Tenant Isolation 132 passed / 8 skipped |
| 分支 | 五分支/五远端 `4ae0492`，tree `d25c6cea...` |
| 下一门 | TI-4 D1 Data OS nullable TenantScope Expand |

## 17. TI-4 D1 验证快照

| 验证面 | 结果 |
|---|---|
| Expand | 5 表 10 个 nullable scope 列；7 表 7 个 NOT VALID workspace FK |
| 数据 | 4+2+2+212+76+0+1=297；逐表守恒；业务 DML=0 |
| 可逆演练 | C1→D1→C1→D1 GREEN；最终 `228ti4d1expand` |
| 回归 | D1/C1 6 passed；Tenant Isolation 135 passed / 8 skipped |
| 分支 | 五分支/五远端 `8e49b68`，tree `c43cc506...` |
| 风险 | Data OS 基表仍由 runtime bootstrap 创建，D7 前收归 migration |
| 下一门 | TI-4 D2 Data OS 显式 TenantScope 写链 |

## 18. TI-4 D2 验证快照

| 验证面 | 结果 |
|---|---|
| D2-A | Source/Pipeline/Dataset/History/Sync/Schedule 六类 persist 显式 scope |
| D2-B | Graph 读写删、六类 delete 显式 scope；无 scope direct call 失败关闭 |
| 启动安全 | runtime 隐藏 demo；无归属时不再物理删除 PostgreSQL 行 |
| 共享库 | 297 行守恒；完整 scope 4、缺失 scope 293；revision `228ti4d1expand` |
| 回归 | D2-B/Phase5 48 passed；相关 45 passed；Tenant Isolation 155 passed / 8 skipped |
| 分支 | 五分支/五远端 `7a05bd8`，tree `ab11617d...` |
| 下一门 | TI-4 D3 Data OS 历史归属账本与逻辑隔离；具体商城仍禁止开始 |

## 19. TI-4 D3 验证快照

| 验证面 | 结果 |
|---|---|
| Batch | `44902fd4-7390-50ee-ae86-192ebd456c33` |
| 决策 | 297：NO_ACTION 4、QUARANTINE 293、ASSIGN/BLOCKED 0 |
| 执行 | Planner/Approver/Executor/Verifier 分离；apply/verify GREEN；业务 DML 0 |
| 共享库 | 297 行、293 NULL、revision `228ti4d1expand` 守恒 |
| 回归 | D3 3 passed；Tenant Isolation 158 passed / 8 skipped |
| 分支 | 五分支/五远端 `78e16a7`，tree `2192fdac...` |
| 下一门 | TI-4 D4 workspace FK Validate；NULL 历史继续逻辑隔离 |

## 20. TI-4 D4 验证快照

| 验证面 | 结果 |
|---|---|
| FK | 7/7 validated；非 NULL orphan 0 |
| 往返 | D1→D4→D1→D4 GREEN；最终 `228ti4d4validate` |
| 守恒 | 297 业务行、293 NULL、293 D3 quarantine |
| 回归 | D1/D4 5 passed；Tenant Isolation 160 passed / 8 skipped |
| 分支 | 五分支/五远端 `176bdf5`，tree `0929f049...` |
| 下一门 | TI-4 D5 scoped read/boot；具体商城仍禁止开始 |

## 21. TI-4 D5 验证快照

| 验证面 | 结果 |
|---|---|
| Store/Boot | load_all 强制 scope；startup 零全库业务 SELECT、零物理删除 |
| API | Source/Sync/Dataset/History/Pipeline/Build/Schedule 严格 org+project |
| 隔离 | 同组织不同工作区互斥；293 NULL 在 Store/API/runtime 均不可见 |
| 回归 | 45 passed / 22 skipped；Tenant Isolation 162 passed / 8 skipped |
| 分支 | 五分支/五远端 `2b81a1b`，tree `25b3c9fb...` |
| 下一门 | TI-4 D6 Data OS RLS |

## 22. TI-4 D6 验证快照

| 验证面 | 结果 |
|---|---|
| RLS | 7/7 policy，7/7 ENABLE，7/7 FORCE |
| 负向门 | scope A/B 隔离、无 GUC零可见、WITH CHECK 伪造写拒绝 |
| 真实数据库 | D4→D6→D4→D6；297 行、293 NULL、D3 quarantine 293 守恒 |
| 测试 | D6 3 passed；Tenant Isolation 165 passed / 8 skipped |
| 分支 | 五分支/五远端 `3e9677f`，tree `5bd14b62...` |
| 下一门 | TI-4 D7 scoped Contract、不可变隔离区与迁移真源 |

## 23. TI-4 D7 验证快照

| 验证面 | 结果 |
|---|---|
| 守恒 | 活跃 4 + 隔离 293 = 297；payload hash mismatch=0 |
| 数据库 Contract | 7 表 scoped PK/NOT NULL；6 条父子 FK；7/7 FORCE RLS |
| 迁移 | fresh DB migration；共享 D6→D7→D6→D7 GREEN |
| 测试 | Tenant Isolation 166/11；相关累计 205/17 |
| 分支 | 五分支/五远端 `c890a7a`，tree `0387668a...` |
| 下一门 | TI-4 C3 Connector FK Validate、RLS 与 runtime transaction |

## 24. TI-4 C3 与 TI-4 总收口快照

| 验证面 | 结果 |
|---|---|
| Connector Contract | 5/5 workspace FK validated；5/5 ENABLE/FORCE RLS |
| Runtime | SQLAlchemy 一致性 transaction、OAuth CRUD/due refresh 显式 TenantScope 与双 GUC |
| 负向门 | scope A/B 互斥、无 GUC 零可见、跨 scope 写拒绝 |
| 可逆演练 | D7→C3→D7→C3 GREEN；5 表均 0 行守恒 |
| 测试 | 定向 46 passed；累计 209 passed / 11 skipped |
| 分支 | 五分支/五远端 `6feb1cb`，tree `3bdf778a...` |
| 下一门 | TI-4 A1 Async Runtime；具体商城仍禁止开始 |

## 25. TI-4 A1 与 TI-4 全域收口快照

| 验证面 | 结果 |
|---|---|
| Scheduler | Schedule/Resource/Execution、内存 key 与 executor envelope 完整 TenantScope |
| Apollo | Channel 平台全局；Spoke scoped PK、workspace FK、FORCE RLS 与 scoped transaction |
| 可逆演练 | C3→A1→C3→A1 GREEN；2 条测试 Spoke 行数和归属守恒 |
| 测试 | A1 5 passed；定向 42 passed / 13 skipped；Tenant Isolation 176 passed / 11 skipped |
| 分支 | 五分支/五远端 `bbdbd2f`，tree `8e5721b...` |
| 下一门 | TI-5 AIP/Analytics/模型目录与非数据库资源；具体商城仍禁止开始 |

## 26. D1.5 全波次收口 + 4 Worker 起跑线拉平快照

| 验证面 | 结果 |
|---|---|
| D1.5 全波次 | A/B/C 三 Phase 全 GREEN（Phase A `009d31f` OT契约/P08/派生指标；Phase B `d61490d` L01-L03 Logic/dryRun/四段负向/PII端到端；Phase C `44a3203` W01/W02 Workshop 最小版） |
| 4 Worker 拉平 | 228-d1 四 worktree 执行 `git merge --ff-only m1`，全部 fast-forward 零冲突；5 分支 tip 完全一致 = `44a3203` |
| 后端回归 | 478 passed（D1 380 + D1.5 98），零回归，164s |
| 前端回归 | 2023 tests GREEN（150 文件，含 D1.5 Phase C 新增 32 tests） |
| 分支清理 | 废弃 223 组 4 分支已删；仓库现存 m1 + 228-d1-w1/w2/w3/w4 共 5 条线 |
| 分支 | **5 分支同 tip `44a3203`**（同一起跑线）；4 个 worktree clean；主工作树仅剩 `docs/toutiao-series/*` 他人改动 |
| 阻塞解除 | D1.5 可独立实施范围全部完成；剩余 W03 决策标签 / L04-L06 / 增长读模型仍 BLOCKED，待 G1/G2 评审 |
| 下一波候选 | ① G1 增长读模型（L04-L06，并行度高，PM 推荐）② G2 决策门 + W03 决策标签 ③ AIP 层资产包内容物填充（电商 bundle content/）+ 微商城专属连接器/数据源预设；方向待用户定 |

## 27. AIP 层现状诊断（资产包 / 数据源缺口）

### 27.1 "电商领域资产包（可安装可卸载）"——机制已存在，名为 FDE 资产包

| 维度 | 现状 | 位置 |
|---|---|---|
| 前端入口 | 侧栏「运维交付域」→ **FDE 资产包**，status=live 未 hidden | [items.ts:584-591](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/navigation/items.ts) |
| 前端页面 | 三 Tab：注册表/组合预检/安装管理 | [AssetBundlesPage.tsx:124](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/pages/s2/AssetBundlesPage.tsx) |
| 后端 API | `/v1/asset-bundles` + `/v1/bundle-compositions` + `/v1/bundle-installations` | [bundle_installations.py](file:///Users/ddt/work/projects/ai_agent/aos-platform/services/aos-api/aos_api/routers/bundle_installations.py) |
| 可安装 | submit→approve→apply（生命周期） | [installationActions.ts](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/pages/s2/assetBundles/installationActions.ts) |
| 可卸载 | rollback（无独立 uninstall 端点） | `/v1/bundle-installations/{id}/rollback` |
| 电商 bundle | fixture 已定义 4 个（domain/platform×2/solution×2），但 `content/` 均为 `.gitkeep` 空骨架，未上注册表 | [bundles/](file:///Users/ddt/work/projects/ai_agent/aos-platform/bundles) |

**结论**：机制通了，电商 bundle 的"壳"注册了，但内容物（OT 定义/Logic/Workshop）没打包进 `content/`，所以界面看不到可点击安装的电商资产。

### 27.2 "微商城数据源链接"——前端无专属入口，仅一句文案

| 维度 | 现状 | 位置 |
|---|---|---|
| 前端文案 | 全前端仅 1 处"已支持 Niushop 微商城"描述文字 | [DataConnectionPage.tsx:71](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/pages/s2/DataConnectionPage.tsx) |
| 平台分类 | 仅 database/saas/api/file/stream，无 niushop | [DataConnectionPage.tsx:14](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/pages/s2/DataConnectionPage.tsx) |
| 连接器清单 | 写死 9 个（DataSourceCreatePage）+ 磁盘 6 个（plugins/connectors），均无 niushop | [DataSourceCreatePage.tsx:56-120](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/pages/s2/DataSourceCreatePage.tsx) |
| 创建 API | `POST /v1/sources`（非 /v1/data-sources）；类型必须命中磁盘 connector | [wave_ext.py:1149](file:///Users/ddt/work/projects/ai_agent/aos-platform/services/aos-api/aos_api/routers/wave_ext.py) |
| 实际接入路径 | 连接器目录→MySQL JDBC 卡片→创建数据源→向导手选 MySQL→填 Niushop 库 JDBC 凭据（无微商城专属按钮/表单/字段） | — |
| 已知缺陷 | DataSourceCreatePage 未读 `?connector=` querystring，卡片跳转后仍需手选类型 | [DataSourceCreatePage.tsx](file:///Users/ddt/work/projects/ai_agent/aos-platform/apps/web/src/pages/s2/DataSourceCreatePage.tsx) |

**结论**：所谓"微商城接入"被降级成"MySQL JDBC 连 Niushop 库"，无任何微商城专属连接器/预设/字段。

### 27.3 下一波建议（待用户定方向）

PM 判断：这两件事正是下一波 4 worker 并行任务的核心——把 D1.5 已做好的 OT/Logic/Workshop **打包进电商资产包 content/**，并**新增微商城专属连接器/数据源预设**，让用户在界面真的能"安装电商资产包 + 创建微商城数据源"。具体任务分配待用户确认主方向后给出。

## 28. D2.5 架构缺口修复验证快照

### 28.1 缺口 1 修复 — normalize 模块（[aos_api/ec_normalizer.py](file:///Users/ddt/work/projects/ai_agent/aos-platform/services/aos-api/aos_api/ec_normalizer.py)）

| 维度 | 实现 |
|---|---|
| 模块 | `aos_api/ec_normalizer.py`（290 行） |
| 入口 | `normalize_rows(rows, pipeline) -> list[dict]`（Pipeline Normalize 节点契约） |
| 幂等 | 行已含 `ot` 字段 → 跳过 mapper原样透传；行无 `ot` 字段 → 应用 mapper |
| 派发 | `pipeline.config.target_ot` 优先，否则用 `pipeline.id` 前缀推断（P01-P08 ↔ 8 OT，与 `ec_derived_metrics` / `ec_link_builder` 对齐） |
| 8 OT mapper | `to_shop` / `to_category` / `to_product` / `to_product_sku` / `to_customer_lite` / `to_order` / `to_order_line` / `to_shipment`（从 `scripts/d2_qiyuehui_init_load.py` 原样搬入，字段映射契约不变） |
| 通用工具 | `_ts`（unix 秒 → UTC，多候选字段）/ `_money`（Decimal 兜底）/ `_str`（字符串兜底）/ `_base`（raw 字段保留 + OT 元字段追加） |
| raw 字段保留 | `_base` 用 `dict(row)` 浅拷贝保留所有 raw ns_xxx 字段（供 `apply_derived_metrics._get_field` 从顶层读取派生指标源字段） |
| 未识别 target_ot | 全部透传（与 `ec_link_builder` 一致，不抛异常） |

### 28.2 缺口 2 修复 — store 装配（[aos_api/ec_live_executor.py](file:///Users/ddt/work/projects/ai_agent/aos-platform/services/aos-api/aos_api/ec_live_executor.py#L36-L76)）

| 维度 | 实现 |
|---|---|
| 函数 | `ensure_store_assembled(eng)`（ec_live_executor.py 顶部） |
| 已注入跳过 | `getattr(eng, "ecom_consistency_store", None) is not None` → 直接返回（idempotent） |
| 装配路径 | `db.get_dsn()` → `create_engine(postgresql+psycopg://...)` → `ecom_metadata.create_all(pg_engine)` → `EcomConsistencyStore(pg_engine)` → `setattr(eng, ...)` |
| DSN 格式转换 | `postgresql://` / `postgres://` → `postgresql+psycopg://`（sqlalchemy 2.x + psycopg3 driver 前缀） |
| 失败降级 | 任何异常（DSN 缺失 / PG 不可达 / 装配失败）→ 记 warning 日志，不抛异常；`sink_to_ot` 自身有 `store=None` 零计数分支保持骨架行为 |
| 设计权衡 | 不在 `PipelineEngine.__new__` 装配（避免单例依赖 db engine 破坏单测）；不下沉到独立模块（装配逻辑与 ec_live_executor 强耦合） |

### 28.3 ec_live_executor 主流程改造

```python
def ec_live_executor(*, pipeline, nodes, node_id, sample_input, ...):
    eng = get_engine()
    ensure_store_assembled(eng)              # ← D2.5 缺口 2 修复
    try:
        input_rows = fetch_source_rows(...)
        normalized_rows = normalize_rows(input_rows, pipeline)  # ← D2.5 缺口 1 修复
        output_rows = [dict(row) for row in normalized_rows]
        output_rows = apply_derived_metrics(output_rows, pipeline)
        output_rows = build_link_rows(output_rows, pipeline)
        ds = sink_to_dataset(eng, scope, pipeline, output_rows)
        sink_to_ot(eng, scope, pipeline, output_rows)
        return {...}
    except Exception as exc:
        handle_failure(pipeline, scope, exc)
        raise
```

### 28.4 测试矩阵（[tests/test_ec_normalizer.py](file:///Users/ddt/work/projects/ai_agent/aos-platform/services/aos-api/tests/test_ec_normalizer.py) + [tests/test_ec_live_executor_store_assembly.py](file:///Users/ddt/work/projects/ai_agent/aos-platform/services/aos-api/tests/test_ec_live_executor_store_assembly.py)）

| 类别 | 文件 | 用例数 | 覆盖 |
|---|---|---|---|
| normalize 单测 | test_ec_normalizer.py | 31 | 8 OT mapper 字段断言 + 幂等（已 normalized 行透传）+ 派发（config.target_ot / pipeline.id 推断 P01-P08）+ 透传（未识别 target_ot）+ raw 字段保留 + `_ts`/`_money`/`_str` 工具 |
| store 装配单测 | test_ec_live_executor_store_assembly.py | 6 | 已注入跳过 / 未注入+DSN 有效→装配 / DSN 失败→降级 / create_engine 失败→降级 / `postgresql://` 转换 / `postgres://` 转换 |
| ec_live_executor 回归 | test_ec_live_executor.py | 4 | 零回归（live pipeline / scope 接收 / output_ref 可验证 / row counts） |
| e2e 回归 | test_ec_d1_5_step14_e2e_validation.py | 1 | 零回归（PII 端到端 + 派生指标端到端） |
| **小计新增** | — | **42** | — |

### 28.5 全量回归验证

```
$ pytest tests/test_ec_d1_*.py tests/test_ec_d1_5_*.py tests/test_ec_live_executor.py \
         tests/test_ec_live_executor_store_assembly.py tests/test_ec_normalizer.py
================= 520 passed, 7 warnings in 162.13s (0:02:42) ==================
```

- D1+D1.5 478 tests 零回归 ✓
- 新增 42 tests 全 GREEN ✓
- 1 个测试断言修正（`test_ec_d1_p04_api_config.py::test_dataset_sink_called_and_output_ref_format`：断言 sink_to_dataset 收到的是 normalized 后的行而非原 raw 行，符合 D2.5 规格文档 §4.1 已识别的预期行为变化）✓

### 28.6 退出门对账

| 门 | 验收 | 状态 |
|---|---|---|
| G1 normalize 模块 | `ec_normalizer.py` 新增 + 单测 GREEN | ✓ |
| G2 store 装配 | `ensure_store_assembled` 函数 + 单测 GREEN | ✓ |
| G3 ec_live_executor 流程 | 插入 normalize + ensure_store_assembled 调用 | ✓ |
| G4 D1+D1.5 零回归 | 478 tests GREEN | ✓ |
| G5 端到端验证 | 重跑 D2 简化版脚本（可选，需 PG 环境） | ⏸ 待办（PG 环境验证） |
| G6 d2 脚本不破坏 | 原 D2 脚本（带 workaround）仍可跑通 | ✓ 脚本未修改，绕过 ec_live_executor 顶层直接编排内部组件 |

### 28.7 待办（G5 端到端验证，需 PG 环境）

简化 `scripts/d2_qiyuehui_init_load.py`：
- 去除 `normalize_fn` 参数（改用 ec_live_executor 内嵌 normalize）
- 去除 `eng.ecom_consistency_store = store` 手动注入（改用 `ensure_store_assembled`）
- 直接调用 `ec_live_executor` 顶层（不编排内部组件）

期望：8 OT 仍落地 547 活跃对象，行数与原 D2 一致。此验证需栖月汇 PG 环境支持，留待下一波接续。

## 29. D4 12 OT 业务闭环 + 302 表衔接（2026-08-08 核对）

| 阶段 | 状态 | 核心交付 | HEAD |
|---|---|---|---|
| D4 Phase A | ✅ GREEN | 12 OT（P01-P12）跑通真实数据 + 标准管道链路验证（提案→审批→调度→执行→历史→沿袭→健康） | `cd046b1` |
| D4 Phase B | ✅ GREEN | SyncTask 真实执行（去 mock）+ Schedule 1h cron + 12 SyncTask 创建 | `cd046b1` |
| D4 Phase C | ✅ GREEN | 302 表分类打标（A=12/B=13/C=11/D=241/E=25）+ 中文注释命中率 97.7% + SyncTask 前端可见 | `cd046b1` |
| D4 Phase D | ❌ 未完成 | 派生指标全量重算 + DLQ PII 验证 + 越租户隔离 | 待 D5 |

### D4 代码核对发现（2026-08-08）
- 后端核心：wave_ext.py 3,405 行（巨型文件）；phase5 1,588 行；phase6 952 行
- D4 工作区有 71 个已修改 + 11 个未跟踪文件未提交
- SyncRun 触发 Pipeline 执行时报 failed/rows=0（需 D5 修复）
- G1 REST Connector / G2 OAuth Manager 有测试骨架但无实现代码

## 30. D5 波次开发计划（2026-08-08 冻结）

> 上位清单：[D-waves/D5-改动清单.md](D5-改动清单.md)

### 核心问题
1. **内存引擎数据不持久**：后端重启后 API 返回 0 条数据，前端用 fixture 兜底显示假数据
2. **SyncRun 执行失败**：SyncTask 触发 Pipeline 时 failed/rows=0
3. **D4 Phase D 未完成**：派生指标/DLQ/越租户验证未跑

### D5 任务（Phase A→B→C+D→E）
| Phase | 任务 | 并行策略 |
|-------|------|---------|
| A | 数据持久化（lifespan seed 或 PG-backed 引擎） | 自干（核心基础） |
| B | SyncRun 真实执行修复 | 自干（依赖 A） |
| C | D4 Phase D 收尾（派生指标/DLQ/越租户） | 可 3-worker 并行 |
| D | 前端真实数据闭环 + 页面截图验证 | 可 5-worker 并行 |
| E | Pipeline 提案页 bug 修复 | 自干 |

### 执行人
- **Agent（WorkBuddy）**：Phase A + B + E 自干；Phase C + D 视并行度决定是否 4-worker
- **验收方式**：browser-pilot 逐页截图 + API curl 交叉验证 + 真数据（不用 mock）

## 31. O1-R 本体数字孪生复审整改（2026-08-09）

| 范围 | 当前状态 | 实证 |
|---|---|---|
| R0 同源访问 | GREEN | Web 使用 `/api`，Vite 代理到本地 API；内置浏览器可直接验收 localhost |
| R1 权威写入/失败关闭 | GREEN | 权威 Outbox、PostgreSQL DLQ/Receipt、租户目录与无 scope 失败关闭 |
| R2 单一 Projector | GREEN | `ecom-projector-v1` 是电商 owned OT/Link 兼容投影唯一 actor，数据库 trigger 防旁路 |
| R3 组织 Overlay | GREEN | Installation 绑定、不可变版本、强 ETag/CAS、Receipt、继承复位、统一读合成 |
| R4 九页面 | GREEN | 内置浏览器 9/9 页面非空、租户正确、无页面级异常；真实图谱 511 对象/590 边 |
| D5-E 最终门 | **BLOCKED** | D5-E0 G17 为 RED：Payment 100 条中指标源链未闭合；不得宣告 D5 GREEN |

验证基线：前端 151 个测试文件、2007 个测试全部通过，TypeScript 全量检查通过；后端 O1/D5-E0 针对性回归 126 项通过。当前 Alembic 为 `o1r3_001`。目标租户始终为 `org-org/dev-project`，没有向 `dev-org` 写入本波业务数据。

下一波唯一建议入口：先冻结并执行 O1-A/P12 权威 Payment 指标链补强，再按 D5-E v2.9 执行 E1/E2。G17、图谱健康门和租户 canary 没有形成最终持久化 GREEN 证据前，不进入“D5 完成”口径。

## 32. AIP-5 E6 方案评审冻结（2026-08-13）

| 范围 | 当前状态 | 结论 |
|---|---|---|
| E5 七知识管道控制面 | `IMPLEMENTED_GREEN` | 代码/API/SDK/页面/浏览器均封板；真实租户 7 Policy、0 Schedule、0 Run |
| E6 方案与清单 | `REVIEWED / APPROVED_FOR_IMPLEMENTATION_WITH_EXTERNAL_DATA_GATE` | 可进入 E6A；不得把数据门阻断冒充实现失败或完成 |
| 美妆知识正文 | `DATA_BLOCKED` | 旧 353 条为目标预测；没有逐条来源、许可、hash、freshness Receipt |
| CosDNA | `DISABLED / LICENSE_UNKNOWN` | 未核验授权，禁止抓取或保存全文 |
| NMPA | `SOURCE_CANDIDATE` | 需登记 2025 动态清单机制下的当前版本、时效和使用政策 |
| 50 条金标 | `EVAL_BLOCKED` | 缺专家审核、gold citation 和权限/撤回/冲突负向用例 |
| 向量 lane | `DEGRADED_VECTOR_UNAVAILABLE` | 当前 PostgreSQL 无 pgvector；不得伪称混合检索 GREEN |

E6 唯一执行清单为 [50-2026-08-13-AIP-5-E6美妆知识包与混合检索评审清单.md](50-2026-08-13-AIP-5-E6美妆知识包与混合检索评审清单.md)。14-Wiki 六份历史方案已增加 AIP-5 权威覆盖说明，废止直写 Semantic、Mock 降级、平行 Memory store、索引即真源和未经许可的自动抓取路径。

下一安全步骤为 E6A：只实现 `VerticalPack` 知识出口、package/source manifest、hash/路径/许可失败关闭和回滚契约；不导入真实知识，不创建真实租户 Schedule，不提前实现 E6B-E6F。

### 32.1 E6A 实施结果

- 代码 `f3415a9` 新增 `BundleExports.knowledge`，并限制仅 `VerticalPack` 可声明。
- 新增严格 KnowledgePackage/source inventory/entry/rollback DTO、serialized JSON duplicate-key gate 与共享 JSON Schema。
- 许可 unknown/denied、来源过期、非 HTTPS、凭据/fragment、路径穿越、hash/source 图漂移均失败关闭。
- 212 项 asset-registry 专项/邻接回归、compileall、diff check GREEN；当前 venv 无 Ruff。
- 未创建美妆知识包正文，未运行 crawler，未写 `org-org/dev-project` 数据；E6 数据门状态不变。

下一安全步骤改为 E6B：将已获授权的 KnowledgePackage 通过 E5 Receipt 和可信 adapter 只转换为 Candidate；没有授权正文时验证空包/阻断路径，不伪造数据。

### 32.2 E6B 实施结果

- 代码 `67cf0a8` 将每个 KnowledgePackage entry 的精确 PostgreSQL Receipt/Artifact 映射为确定性 Semantic Candidate Draft。
- adapter 不直接写 Store；最终 Source/Candidate 仍由 E5 Service/Store 完成。`PUBLIC_PACKAGE` 直发被阻断。
- entry id/hash、许可、usage policy、provider/version、observed/freshness、applicability 和 source kind 全部交叉校验。
- 43 专项 GREEN；更大邻接集合 246 passed，1 个既有签名时间 ISO 尾随零文本断言漂移，与本波无关。
- 未注册生产 adapter、未写真实租户 Candidate、未导入知识正文。

下一安全步骤为 E6C：PostgreSQL tenant-scoped reference-only 全文索引与 capability registry；不存 payload/PII，向量 lane 继续诚实 degraded。

### 32.3 E6C 方案冻结

- 已冻结 `aip_memory_search_reference`：同 scope Memory revision FK，只存 id/revision/hash、subject/source ref、受限 search terms、markings/applicability/freshness；禁止 payload、正文、embedding 和 PII。
- 已冻结 `aip_memory_search_capability`：按 fulltext/vector/rerank lane 保存可观测状态、reason/provider/revision 与 CAS version；它不是授权真源。
- 两表必须 RLS/FORCE；无 scope 零可见。reference 可清空重建且不改变 canonical Memory；当前 vector 为 `degraded_vector_unavailable`，rerank 为 `unbuilt`。
- 本步骤只是编码前冻结；E6C migration/store 和验证尚未完成。

### 32.4 E6C 实施结果

- 代码 `e581a99` 新增 `aip5_004`、`aip_memory_search_reference`、`aip_memory_search_capability` 与严格 Store/DTO。
- reference 写入前复核 canonical subject/hash/source/markings/applicability/freshness；只存受限 terms，手机号/身份证/邮箱/URL 被拒绝。
- 两表 RLS/FORCE、无 scope 零可见；reference 清空后 canonical Memory 仍存在；capability CAS 冲突失败关闭，无 pgvector 时 vector 不能写为 ready。
- migration downgrade/upgrade、6 项专项、32 项邻接、compileall/diff check GREEN；Ruff unavailable。真实 `org-org/dev-project` 新投影表为 0/0，测试使用独立测试组织。

下一安全步骤为 E6D：先冻结自然语言 `KnowledgeSearch` 契约，再实现 authority-first 全文召回、诚实 lane degraded 和 Citation 装配；不导入美妆正文、不伪造向量能力。

### 32.5 E6D 方案冻结

- `KnowledgeSearch` 只接自然语言、Task/Skill/time/markings/limit/token budget，不接受租户或 capability 自报；E3 精确查询保留。
- reference 全文命中只给候选引用；canonical Item/Revision/Source 的状态、时效、marking、applicability、hash/revision 在 payload 解析前二次失败关闭。
- lane 独立报告；当前 vector degraded、rerank unbuilt。只有 fulltext ready 才可召回，单 lane 结果不得称作混合 GREEN。
- Citation 与 chunk 一一绑定；无引用、payload 漂移或全部 lane 不可用时不返回正文。本步骤为编码前冻结，E6D 尚未实现。

### 32.6 E6D 实施结果

- 代码 `9bfec8c` 实现 `KnowledgeSearch` DTO、PostgreSQL fulltext reference、lane 状态、canonical authority 二次过滤、payload 延迟解析和 Citation/chunk 一一绑定。
- Canonical `/v1/aip/memory-authority/knowledge-searches` 从认证 Principal 派生 tenant/markings/skill applicability，拒绝请求注入租户。
- 新 API 2 项、核心邻接 21 项、compileall/diff check GREEN；旧 authority API 全文件存在历史挂起，未当作通过证据。
- 默认 trusted resolver 仍未装配，真实租户 capability/reference 0/0，因此状态为 `CODE_GREEN / PROVIDER_BLOCKED / DATA_BLOCKED`，不是 operational 或 hybrid GREEN。

下一安全步骤为 E6E：gold-set/Eval 代码门。没有 50 条专家审核 gold 和精确 Citation 时必须 `EVAL_BLOCKED`，不得计算或宣告 Top-1≥80%。

### 32.7 E6E 方案冻结

- GoldSet 必须有 id/revision/hash/reviewer/approvedAt；每条 query 含 Skill、六角色依赖、精确 gold Citation 与负向期望。
- 未审核、少于 50、六角色语义覆盖不全、重复/缺结果时 `EVAL_BLOCKED` 且指标为空。
- Top-1 精确比较 Memory/revision/hash；Citation coverage 与权限/stale/revoked/conflict 负向保护独立计算，任一泄露总门 RED。
- runner 纯计算、不生成 gold、不写 Memory/Index。当前真实 gold 仍为 0，本步骤只是编码前冻结。

### 32.8 E6E 实施结果

- 代码 `3d3ee69` 新增严格 GoldSet、Observation、Report DTO 与纯计算 Eval runner。
- 正样本独立计算 Top-1/Citation coverage；负样本正确空返回不进入分母，任一引用或无引用正文泄漏强制 RED；全负样本 BLOCKED。
- 未审核、少于 50 条、六角色覆盖不全、重复或缺失 observation 时均不产生指标。
- 12 项专项、26 项 E6C/E6D/E6E 邻接、compileall 与 diff check GREEN。
- 真实审核 GoldSet 数仍为 0，因此当前是 `CODE_GREEN / EVAL_BLOCKED`，不是检索效果 GREEN。

下一安全步骤为 E6F：先冻结只读控制面状态契约，再展示知识包、来源许可、检索 lane、Eval 与阻断原因，并在 `org-org/dev-project` 用内置浏览器验收真实空态；不得在前端编造 readiness。

### 32.9 E6F 实施结果

- 代码 `7e76886` 新增 tenant-scoped `KnowledgeReadiness` 聚合和 `GET /v1/aip/memory-authority/knowledge-readiness`；租户只从认证 Principal 派生。
- Source/reference/capability 来自 PostgreSQL 真值；package installation 与 GoldSet registry 尚无权威映射，故返回 `authority_unavailable` 和空计数，不用 0 冒充已查询。
- 前端唯一严格 SDK 与 Memory Governance“冷启动与检索”视图已落地；API/存储失败关闭，三 lane 唯一性和 authority/count/blocker 组合受严格校验。
- 真实浏览器发现并修复两项问题：嵌套 capability snake_case 泄露导致解析失败；长 blocker 文本溢出卡片。
- 11 项 E6C～E6F 后端邻接、14 项前端定向、TypeScript、Vite 274 modules、compileall、diff check GREEN；内置浏览器在 `org-org/dev-project` 验收真实空态，`dev-org/dev-project` 仅作隔离 canary。

E6A～E6F 的代码和控制面均已实现；E6 总状态仍为 `CODE/CONTROL_IMPLEMENTED_GREEN / DATA_BLOCKED / PROVIDER_BLOCKED / EVAL_BLOCKED`。当前真实租户 source/reference 为 0/0，package/Eval authority 未建立，trusted provider 未装配，不得宣告美妆知识冷启动、混合检索或 Top-1 效果 GREEN。

### 32.10 Watchdog 真实断流事件复盘

- 原任务于 15:32:23 断流；Watchdog 在初始 300 秒静默门槛后，于 15:37:56、15:42:08 发起两次同任务恢复。
- 两次恢复都再次断流，均为 `last_agent_message=null`；15:47:39 安排 15:52:39 再试。
- 用户于 15:50:57 人工唤醒，比第三次自动重试早 1 分 42 秒。因此本次结论是 `TRIGGER_GREEN / AUTO_RECOVERY_FAILED / MANUAL_RESUME`，不是自动恢复成功。
- 先前将旧 `last_recovered_at` 误读为本次成功的说法已撤回。后续成功判定必须绑定当前 recovery episode、退出码和 transcript 可见 ACK；Watchdog 状态模型补强作为独立安全微波次，不混入 E6F 提交。

### 32.11 Watchdog 活跃 turn 防重入补强

- 代码 `e095b05` 将最新 `task_started/task_complete` 纳入 transcript 权威判断，仍在运行的 turn 固定为 `turn-running`，不再因 300 秒静默启动并发 resume。
- 恢复状态按 episode 隔离；旧成功字段在新 episode 开始时清除，退出码 0 但没有新增 assistant final 时仍按失败退避。
- 13 项专项测试、`py_compile`、`diff --check` 与当前任务实时 `turn-running` 验证 GREEN。
- AIP 下一依赖按最新全量审查调整为：先落地 AIP-6 Agent/Skill/Handoff identity authority，再实现依赖该 identity 的 AIP-5 E7 六同事记忆投影。

### 32.12 AIP-6 A6A 公共契约

- 新增 Agent/Skill/Binding/Handoff 公共契约，冻结 exact revision/hash、模板/实例/Run 状态、Overlay allowlist、secretRef-only 与最小披露 Handoff。
- 6 项专项、compileall、diff check GREEN；无路由、迁移和数据副作用。
- 下一步为 A6B additive PostgreSQL authority 与 RLS/FORCE RLS。

### 32.13 AIP-6 A6B PostgreSQL authority

- 新增 `aip6_001` 八表权威：Agent/Skill 不可变 revision、组织实例与 binding、工作区 AgentRun/Handoff/event。
- 6 张租户表 RLS/FORCE RLS；Handoff token 只存 hash，Capability 只存 secretRef；跨租户引用由复合 FK 失败关闭。
- A6B 7 项、A6A/A6B/E6C 邻接 16 项、compileall、迁移降升级、单 head/current、diff check GREEN。
- 下一步为 A6C Store、expected-version CAS、不可变 publish 与组织 Overlay 生命周期。

### 32.14 AIP-6 A6C Store、CAS 与生命周期

- 新增 `aip6_002` 线性迁移，提供租户隔离、append-only 的命令 Receipt；没有回改已提交的 `aip6_001`，开发库与测试库均为唯一 `aip6_002 (head)`。
- 新增 AgentTemplate/SkillTemplate 不可变发布、历史回读、AgentInstance Overlay、SkillBinding exact revision/hash、expected-version CAS 和状态机；最新 append-only `deprecated/revoked` revision 会阻断旧 published revision 创建新实例/绑定。
- 所有创建/更新命令写持久化 Receipt，并支持跨 Store 重启幂等重放；`org-org/dev-project` 与 `dev-org/dev-project` 双租户隔离，canary 无数据可见。
- 27 项 A6A/A6B/A6C/邻接知识索引累计测试、compileall、迁移独立降升级、开发库升级、单 head/current 和 `git diff --check` GREEN。
- 旧 singleton API/页面仍未切换；下一安全步骤先完成 `16-AIP对八工作台六数字同事与共享专业Agent全量支撑审查报告.md` 实时复审，再进入 A6D Handoff/AgentRun/CapabilityBinding。

### 32.15 八工作台全量支撑复审与 W0 增量门

- 已按 `aos-platform/m1@561d82c` 更新 16 号报告：A6C 已缩小 P0-03，但 A6D～A6F、领域六角色/37 Logic/10 capability、八公共生产契约仍未闭合，总体保持 `REVIEWED_WITH_BLOCKERS`。
- 新增 `17-AIP-W0工作台v3公共生产契约与能力目录增量优化方案.md`，冻结八公共对象 L0 owner/复用方式、身份/职责/capability 正交关系、Handoff/E7/A6E/AIP-9 依赖和插入顺序。
- 方案经“评审→整改→复审”达到 `APPROVED_FOR_PLAN_INTEGRATION / NOT_CODE_AUTHORITY`：允许继续不依赖新对象的 A6D；A6E 前完成十 capability catalog/crosswalk；AIP-9 前完成 W0B/W2 公共生产契约方案和清单门。
- 下一安全步骤为 A6D exact runtime refs、CapabilityBinding、AgentRun 与 Handoff；不得提前进入 A6E 或编码工作台 v3 八对象。

### 32.16 AIP-6 A6D exact Run/Handoff authority

- 代码 `650981c` 新增 `aip6_003`、CapabilityBinding、AgentRun 与 Handoff service；所有变更仅在 `m1` 精确提交，未纳入 browser/Kitewright、D5 等无关工作树改动。
- AgentRun 固化 Task/TaskRun/Plan exact revision、AgentInstance exact ref 与 Overlay 快照、SkillBinding 和 published Logic exact hash；AIP-7 尚无版本化 ModelRouteRevision/PolicyRevision authority，因此 `queued` 可保存，`running` 失败关闭。
- Handoff 只保存 refs、allowlist context、markings 与 token hash；bearer 仅首次签发返回，重放不再签发。错误 token、过期、撤销、重复消费、接收方 exact identity 漂移、缺失接收方权限解析器和跨租户均失败关闭。
- 28 项 A6A～A6D 累计定向测试、compileall、迁移独立降升级、真实 `aip_logic_publication` exact hash 查询、开发库唯一 `aip6_003 (head)` 与 `git diff --check` GREEN。
- 16 号全量支撑报告已按 A6D 实时事实第二次校准：总体仍是 `REVIEWED_WITH_BLOCKERS`；A6E/A6F、W0 公共契约、AIP-7～AIP-10 与外部知识门仍未完成。

下一安全步骤：执行 W0A 十 capability catalog/alias/crosswalk 文档门并复审；通过后才进入 A6E 六角色/37 Logic/职责模板领域包。

## 32.18 AIP-6 A6F Canonical 控制面与组织实例封板

- 代码 `556e92c` 完成 Principal tenant Canonical API、六角色组织安装器、6/37/10 聚合目录、旧 singleton 写入口失败关闭；代码 `914cff2` 完成唯一严格前端 SDK、智能体目录/列表/能力目录页面和重复路由消除。
- `org-org/dev-project` 真实安装 6 个稳定 AgentInstance；数据库核验 SkillBinding=0、CapabilityBinding=0、AgentRun=0，页面权威展示 `6 definition / 6 installed / 37 evaluated / 10 blocked / 0 runnable`。
- `dev-org/dev-project` 为 0 个 AgentInstance，跨租户 canary GREEN；本波没有污染测试租户或把定义发布冒充运行就绪。
- 前端定向 parser 3 项、TypeScript、Vite 276 modules、后端 24 项邻接、compileall/OpenAPI、浏览器三页面和 console 0 error 均 GREEN。
- 浏览器发现旧 `/aip/capabilities` 路由抢占 Canonical 页面，已删除重复顶层入口；严格 parser 同时补齐 lifecycle/hash/resource-ref/readiness/runnableCount 漂移失败关闭。
- 第 16 号报告最终判断：AIP 主架构仍无需重设计。W1 身份/API/UI 门已关闭；后续优化必须优先关闭 W0B/W2 八公共生产契约及 AIP-7 exact ModelRoute/Provider/Eval 门，随后再进入 E7、AIP-8/9/10。

当前阶段：`AIP6_A6F_IMPLEMENTED_GREEN / SIX_AGENT_INSTANCES_INSTALLED / RUNTIME_READINESS_BLOCKED`。

下一安全步骤：先完成第 16 号报告建议的 W0B authority ADR 与 W2 公共生产契约实施清单统一评审，不跳过 authority 直接让六实例运行；并行依赖路线为 AIP-7 exact ModelRouteRevision/Provider/Eval readiness。完成这些门后才能创建 Skill/Capability Binding 和启动 AgentRun。

### 32.19 W0B Authority ADR 与 W2 清单门

- 新增 `21-AIP-W0B八公共生产对象Authority-ADR与W2实施清单.md`，逐对象裁决 TaskBrief、EvidenceBundle、EvalContract、ResponsibilityPlan、StageTemplate、Artifact relation、ReviewIssue/ReturnDecision、ImpactPreview 的唯一 L0 owner 与复用/投影/additive 形态。
- 禁止新增第二套 StageRun、Evidence payload、EvalRun、ProviderResult 或 Artifact payload；W2 只补缺失 revision/relation/event/typed compiler。
- W2 拆为 A～D 四个线性切片；每片均要求 migration/Store/API/SDK/browser、RLS/FORCE RLS、CAS/Receipt、双租户 canary 和独立提交。
- 方案经首轮 6 项风险整改与第二轮 6/6 复审，达到 `APPROVED_FOR_W2_IMPLEMENTATION`。

下一安全编码步骤：W2-A Brief + Evidence。只建立公共 authority 与真实 blocked/empty 投影，不启动 AgentRun；AIP-7 继续作为正交运行门。

#### 32.19.1 W2-A Store 检查点

- 代码 `4288591` 新增 `w2_001`、严格 Brief/EvidenceBundle DTO、PostgreSQL Store、CAS/幂等 Receipt、exact Evidence/Brief 引用校验和双租户失败关闭。
- freeze 不原地更新 revision，而是追加内容/hash 相同的 frozen revision 并以 CAS 推进 head；历史 draft 仍可精确回读。
- 测试使用 `test:w2a` 标记并在前后精确清理，未向 `org-org/dev-project` 留测试 Brief/Evidence；开发库为唯一 `w2_001 (head)`。
- 3 项 Store 定向、compileall、迁移独立 downgrade/upgrade 与 diff check GREEN。

下一子步骤：注册 W2-A Canonical API 与错误映射，补 Principal tenant/OpenAPI/跨租户测试；API GREEN 后再进入 SDK/UI。

#### 32.19.2 W2-A Canonical API 检查点

- 代码 `daeb9d3` 注册 `/v1/aip/production-contracts` 的 Brief create/get/revise/freeze 与 EvidenceBundle create/get Canonical API。
- API tenant 只取认证 Principal；写操作要求 `Idempotency-Key`，稳定映射 not-found/version/idempotency/dependency 错误。
- EvidenceBundle API 已验证只接受 frozen Brief exact ref 与同租户 Evidence exact hash；`dev-org/dev-project` 对正租户 Brief/Bundle 均返回 404。
- 6 项 Store/API 专项、compileall、6 条 OpenAPI 路径和 `git diff --check` GREEN；测试数据按精确 Task/Brief/Bundle/Evidence identity 清理，未留下业务行。

下一子步骤：实现唯一严格 TypeScript SDK 与 W2-A 最小控制页；页面必须展示权威 loading/empty/blocked/failed，不创建 Run，也不得用 Mock 补 Brief/Evidence。

#### 32.19.3 W2-A SDK/UI 与控制面封板

- 代码 `ab762ae` 完成 W2-A 唯一严格 TypeScript SDK、嵌套 DTO 解析、`/aip/production-contracts` 最小控制页、正式路由与导航；同时补齐 Brief/EvidenceBundle collection GET，页面不再依赖隐藏或推测状态。
- 页面只展示 PostgreSQL authority 的真实 Brief/Bundle；当前 `org-org/dev-project` 为 0/0，因此诚实显示空态、创建 Brief 因缺少真实 Task 禁用、Bundle 因缺少 frozen Brief/Evidence 禁用、AgentRun 因 AIP-7/W2 后续门禁用，未用 Mock 补齐。
- 后端 Store/API 6 项、前端 parser 邻接 5 项、TypeScript、Vite 279 modules、compileall、OpenAPI 与 diff check GREEN。
- 内置浏览器在 `org-org/dev-project` 验证空态、禁用动作、刷新和 console 0 error；`dev-org/dev-project` 仅作负向 canary。验收后两租户 Brief revision/head/Bundle/Receipt 均为 0，未留下测试业务数据。
- W2-A 状态为 `IMPLEMENTED_GREEN / REAL_DATA_EMPTY / RUN_BLOCKED_BY_DESIGN`；这只关闭 TaskBrief/EvidenceBundle 公共权威，不代表八公共对象或 AgentRun 已完成。

下一安全步骤：继续 W2-B（EvalContract + ResponsibilityPlan）并同步冻结 AIP-7 exact ModelRouteRevision/Provider/Eval readiness 最小增量方案；不得让旧 mutable Model Router 或默认 KV 路由成为第二真源。

### 32.20 第 16 号报告专项复审与 AIP-7 增量优化方案

- 第 16 号报告已按 W2-A 实时事实专项复审：Brief/EvidenceBundle 不再是缺失项，P0-02 缩小为 W2-B～D 六个对象族；总体仍为 `REVIEWED_WITH_BLOCKERS`。
- 新增 `22-AIP-7-exact模型路由Provider-Eval与运行就绪增量优化方案.md`，完成评审→8 项整改→8/8 复审通过，状态 `APPROVED_FOR_INCREMENTAL_IMPLEMENTATION / EXTERNAL_PROVIDER_GATE_RETAINED`。
- 裁决旧 mutable `model_provider/model_catalog/registered_models/model_route/meta_aip_kv` 仅为兼容发现/迁移输入；新运行必须使用 immutable ProviderInstanceRevision、RegisteredModelRevision、ModelRouteRevision、RuntimePolicyRevision。
- AIP-4 继续唯一拥有 Eval/Usage/Lineage，AIP-6 继续唯一拥有 AgentRun/Binding；AIP-7 不复制这些真源。
- 实时数据库：栖月汇 Provider/Catalog/RegisteredModel/Route/Eval/Binding/Run 均为 0；测试组织 4/12/6/5 不得迁移、借用或作为正向证据。

下一安全编码步骤：按 22 号方案先执行 A7-0 合同冻结，随后 A7-1 PostgreSQL exact authority；外部 Provider/凭据/Eval 未具备时只允许 code/control GREEN，不能宣告 runnable。

#### 32.20.1 A7-0 exact 合同冻结

- 代码 `6c80da1` 新增 AIP-7 strict DTO：ProviderInstanceRevision、ProviderHealthObservation、RegisteredModelRevision、ModelRouteRevision、RuntimePolicyRevision 与 ModelRouteResolution。
- secretRef-only、exact ref kind、唯一候选、weighted=100、安全/策略拒绝不可 fallback、health freshness、blocked/unknown 不得携带选择结果等规则均已失败关闭。
- 合同不注册路由、不建表、不解析 Provider、不允许 AgentRun 启动；栖月汇真实数据仍为 0。
- 新合同+A6 邻接 21 项、AIP-4 Eval Store/Pack 邻接 9 项，共 30 项 GREEN；compileall、diff check GREEN。

下一子步骤：A7-1 additive PostgreSQL revision/head/observation/Receipt 与 Store；不得修改历史 `228ti5b1models` 或导入测试组织旧数据。

### 32.21 第 16 号审查问题全量关闭方案门

- 新增 `23-AIP第16号审查问题全量关闭与新增开发实施方案.md`，把第 16 号报告的全部 P0/P1 从审查意见转成唯一关闭波次；不再把 AIP-7 当成报告的唯一新增开发。
- 全量路线覆盖 W2-B～D、A7-1～A7-6、E7、BIND-1、AIP-8、AIP-9、AIP-10/FDE 与八工作台 W4 反向验收；已 GREEN 的 AIP-1～6/W2-A 只复用、不重做。
- 依赖顺序冻结为 `A7-1～3/W2-B → E7 → W2-C/D → A7-4～6 → BIND-1 → P8/P9/P10 → W4`，并保留 Provider、外部知识、平台账号、多媒体、公开直播等独立外部门。
- 方案完成首轮 9 项风险整改与第二轮 8/8 复审，状态 `APPROVED_FOR_INCREMENTAL_IMPLEMENTATION / EXTERNAL_AND_PLATFORM_GATES_RETAINED`。

下一安全编码步骤：先沿已冻结的 AIP-7 authority 边界执行 A7-1 migration/Store；随后实施 W2-B EvalContract/ResponsibilityPlan。任何波次均不得用测试组织历史模型、固定 Agent 团队或前端状态替代 canonical authority。

#### 32.21.1 A7-1 exact PostgreSQL authority

- 代码 `eedd6b3` 新增 `aip7_001` 单头迁移，以及 ProviderInstance/RegisteredModel/RuntimePolicy/ModelRoute revision+head、ProviderHealthObservation、幂等 Receipt PostgreSQL authority。
- Store 强制 canonical content hash、append-only revision、head CAS、幂等回放与 payload drift 拒绝；Model→Provider、Route→Model/RuntimePolicy 均做同租户 exact revision/hash/lifecycle 校验。
- 修正 A7-0 的 exact kind 漂移：运行策略引用统一为 `RuntimePolicyRevision`，不再用泛化 `PolicyRevision` 冒充 AIP-7 权威。
- migration downgrade/upgrade 后为 `aip7_001 (head)`；24 项 A7/A6/AIP-4 邻接测试、compileall、目标 diff check GREEN。
- 测试标记数据已清理；`org-org/dev-project` 与 `dev-org/dev-project` 的四类 revision、health、Receipt 全部为 0，未迁移测试组织历史 mutable 模型。

下一子步骤：A7-2 exact resolver。只允许在 Route/Policy/Model/Provider/Eval/Health 全部精确可用时返回 ready；当前真实零数据必须返回 blocked，不创建 Provider 或启动 AgentRun。

#### 32.21.2 A7-2 exact readiness resolver

- 代码 `8878ee1` 新增纯读取 resolver，按 Route→RuntimePolicy→Route Eval→Model→Model Eval→Provider→fresh health 的固定顺序生成机器可读 blocker。
- 只有 route/policy/model/provider 全部 active、exact hash 无漂移、modality/capability 匹配、两个 EvalGateDecision passed 且 provider health 未过期时才返回 exact ready selection。
- 安全/评估/健康/漂移任一缺失均返回 blocked 且不携带 selected model/provider；resolver 不调用模型、不写配置、不改变 AgentRun。
- 真实 `org-org/dev-project` 不存在 route 时失败关闭，不回退默认 KV 或测试组织旧 route；A7/AIP-4 邻接 20 项、compileall、diff check GREEN。

下一子步骤：A7-3 Canonical API 与 legacy write 失败关闭；所有 scope 只取 Principal，写操作要求 Idempotency-Key/If-Match，并保持真实零数据空态/404/blocked。

#### 32.21.3 A7-3 Canonical API 与旧写关闭

- 代码 `b24e677` 注册 `/v1/aip/model-runtime` Provider/Model/Policy/Route/Health/Resolution Canonical API；scope 只取认证 Principal，revision 写入要求 `Idempotency-Key + If-Match`。
- 两套旧路由 `/v1/aip/model-admin/routes`、`/api/models/router` 保留读取兼容；bulk replace/create/update/delete/circuit update 全部返回稳定 `AIP_MODEL_LEGACY_WRITE_DISABLED (410)`，禁止双写第二真源。
- 当前真实租户不存在 exact Route 时 Canonical GET 返回 404，canary 同样只能读取自己的 scope；不会从默认 KV 或测试组织加载。
- API/Resolver/Store/Contract 15 项、OpenAPI、compileall、target diff check GREEN；测试过程中未创建真实模型配置。

下一安全步骤：按 23 号全量路线切换到 W2-B EvalContract + ResponsibilityPlan authority；A7-4 AgentRun 需要 W2-B/BIND 与外部 readiness，暂不提前启动。

### 32.17 AIP-6 A6E 电商 SolutionPack 定义发布

- 代码 `cd1d07a` 新增全局不可变 `CapabilityRevision/alias` authority；代码 `8b4022b` 发布 `solution.ecommerce.growth@1.2.0` 的六数字同事、37 Logic/SkillTemplate 与十共享 Capability。
- 发布器两次幂等执行均返回 6/37/10；PostgreSQL 精确回读同为 6/37/10，`title.generate → copy.generate` 唯一别名成立。
- 38 项 A6A～A6E/Asset Registry 累计回归、compileall、JSON 与 diff check GREEN；锁定环境无 Ruff，未把 Ruff 记为通过。
- Bundle 保留 D3 W03/L05/dry-run，删除已被正式目录替代的 placeholder；旧 M5 空骨架测试已升级为真实导出物、稳定 hash 与证据契约。
- A6E 没有创建租户实例、binding 或 Run；十 Capability readiness 均为 `blocked`，未把目录 published 冒充 Provider/Eval/Route 可运行。
- 16 号全量支撑报告完成实时复审：AIP 主架构无需重设计；先做 A6F Canonical API/组织安装/UI，随后做 W0B/W2 公共生产契约，再进入 E7/AIP-8/AIP-9。

下一安全步骤：冻结并执行 A6F；A6F 只允许切换 PostgreSQL authority、创建 `org-org/dev-project` exact 组织实例、提供唯一 SDK/UI 与真实 readiness，不得越权制造 AIP-7 Provider/Route/Eval 可用。

## 32.22 Watchdog 渐进退避整改

- 代码 `aos-platform/m1@75ff587` 将瞬时故障策略改为单窗口单次恢复与 5/10/15/20……分钟线性退避，最大 60 分钟；不再因三次 ChatGPT/backend 故障永久熔断。
- 只有本地配置、精确目录、可执行文件或权限等确定性错误进入 `configuration_error` 并等待显式复位；新增可见 `final/final_answer` 会立即清零失败链。
- 23 项单测、编译、真实 transcript 只读回放、禁用态及启用态 LaunchAgent 演练 GREEN；启用后连续三个周期为 `turn-running`，总恢复次数保持 43。
- AIP Watchdog 当前已启用；本轮没有推进 AIP 业务、工作台、数据库、迁移或租户数据。

下一安全步骤：恢复 AIP 前重新读取 `w1-aip` 固定 SHA、最新 Delivery Receipt、迁移/真实数据库 Lease 和记忆门；不得把 Watchdog 基础设施封板冒充 AIP 业务进度。

### 32.23 W2-C Stage / Artifact / Review 权威闭环封板

- `aos-platform/m1@a994f03` 已串行集成 W2-C：`d04cbdc` 权威存储与 API、`ed28858` 控制页与 SDK、`0957b9f` 代码交付证据、`f8d5e1e` 浏览器验收隔离与路由真相补强、`a994f03` 最终 Delivery Receipt。
- W2-C 已形成 StageArtifact / ReviewDecision / ArtifactRelation 的唯一 PostgreSQL authority、Canonical API、严格 TypeScript SDK 与 `/aip/production-contracts` 真实控制面；未生成 AgentRun，未调用 Provider，未以 Mock 补数据。
- 累计后端 `30 passed + 2 subtests`，前端 `3 files / 13 tests`，TypeScript noEmit、Vite production build、compileall、diff check GREEN；路由 manifest 为 522 条，AIP 73 条，runtime 4190 条 method route / 2412 条 OpenAPI path，无重复 `/v1/aip` 路由。
- 内置浏览器在 `org-org/dev-project` 验证七类真实集合均为 0，空态、门禁、刷新和 console 0 error GREEN；`dev-org/dev-project` 只做负向 canary，三类读取均为 200/空且 tenant echo 精确。
- 首次未开启 Cron 隔离的验收如实留下 `scr-61cbea02021f42f6`：P12 任务 `PIPELINE_NOT_FOUND`、rows=0、failed；它只改变调度控制面记录，没有业务数据或外部写入，不删除、不粉饰。增加 `AOS_QYH_CRON_ENABLED=false` 后复验约 10 分钟，`meta_schedule_run` 保持 total=85，latest id 不变。
- W2-C 当前状态：`IMPLEMENTED_GREEN_WITH_OPERATIONAL_FINDING`。这不代表 W2-D ImpactPreview/StartDecision、AIP-7 可运行性或六数字同事真实任务已完成。

下一安全步骤：完成 authority / 01 / 06 / Prime 的 AOS-000020 CAS 同步与 W2-C Lease 释放，然后只评审 W2-D 首个可执行子步骤；未获得新权威门前不开始 W2-D 编码。

### 32.24 AIP-5 E7-0 六数字同事记忆投影契约冻结

- `30-AIP-5-E7六数字同事个人记忆共享投影与改进度量实施清单.md` 已完成两轮评审与 8/8 复审，状态 `APPROVED_FOR_IMPLEMENTATION`；E7 继续复用 AIP-5 Memory 与 AIP-6 AgentInstance authority，不新建第四套记忆。
- `aos-platform/m1@5261257` 新增 reference-only personal/shared Projection、exact AgentInstance/Memory revision、可信 Exposure 与 ImprovementObservation strict contracts。
- role 字符串、Memory payload、隐式共享、自收件人、重复 recipient、unknown 携带假 0/指标或宣称 improved 均由 DTO 失败关闭；measured/estimated 必须带 exact exposure/source/eval/cohort 证据。
- 相关 20 项测试在 `w1-aip` 与 `m1` 均通过，compileall 和 diff check GREEN；没有迁移、数据库写、真实租户数据、Provider 调用、AgentRun 或浏览器副作用。
- Authority 已 CAS 到 `AOS-000026`；当前阶段 `AIP5_E7_0_CONTRACTS_GREEN`，下一门 `AIP5_E7_1_AGENT_MEMORY_PROJECTION_MIGRATION`。

下一安全步骤：重新确认 Alembic 唯一 head 与活动 Lease，取得唯一 migration Lease 后实施 additive、零回填、RLS/FORCE RLS、append-only 与空表才允许 downgrade 的 E7-1；不得抢占 Workshop 迁移或借真实业务数据做迁移测试。

#### 32.24.1 E7-1 PostgreSQL 权威迁移

- `aos-platform/m1@acc2ed7` 新增 `aip5_005` 单头迁移，创建 Projection、Recipient、Event、Receipt、Exposure、ImprovementObservation 六张租户权威表。
- 六表全部启用并强制 RLS；五张关系/审计事实追加不可变；Projection 只允许合法 status/version/contentHash CAS，owner/memory/授权范围等身份不可变。
- 迁移零回填、六表空时才允许降级；隔离库和共享开发库均完成 downgrade/upgrade，当前唯一 head 为 `aip5_005`，共享开发库六表均为 0 行。
- 迁移+合同 12 项测试、compileall、diff check GREEN；没有业务数据、Provider、AgentRun 或外部副作用。
- Authority 已 CAS 到 `AOS-000027`；下一门为 `AIP5_E7_2_PROJECTION_STORE_SERVICE_RECEIPT_CAS`，迁移 Lease 已释放，不得被 E7-2 复用。

下一安全步骤：E7-2 实现事务型 Store/Service、exact AgentInstance/Memory 回读、显式 recipient、事件、幂等 Receipt 与 CAS；不新增迁移，不开放 API/UI，不写真实租户投影。

#### 32.24.2 E7-2 Projection Store / Service 封板

- `aos-platform/m1@65aad29` 新增事务型 Projection authority Store：personal/shared 均只保存 exact Memory/AgentInstance 引用、显式 recipient、授权范围、披露模式和有效期，不复制 Memory payload。
- 创建时全量复核 owner/recipient exact active AgentInstance、current active Memory revision、source freshness、marking/applicability 和最早失效时间；suspend/reactivate/revoke 使用 expectedVersion CAS，reactivate 再次全量复核依赖和有效区间。
- Event 与幂等 Receipt 同事务追加；事件 exact projection hash 通过同版本唯一不可变 Receipt 回读，并对缺失或重复 Receipt 失败关闭。跨租户读取返回空/NotFound，不回退角色字符串、模板默认记忆或其他工作区。
- Agent Registry、Memory authority、E7 contract/migration/store 累计 `24 passed`；Ruff check/format、diff check GREEN。共享开发库仍为 `aip5_005`，六张 E7 表均为 0 行，没有写入真实业务数据。
- Authority 已完成 SHA 校准并 CAS 到 `AOS-000029`；权威提交为 `65aad29d70a9d830284fe848e869f5fb9084a443`，下一门为 `AIP5_E7_3_RETRIEVAL_EXPOSURE_REVOCATION_IMPACT`。E7-2 不包含 API/UI、Exposure、改进结论或真实六同事效果。

下一安全步骤：E7-3 实现读取时 exact 依赖重验、personal/shared 权限装配、可信上下文接受后 Exposure 追加，以及 reference-only revoke impact；不得启动 AgentRun、调用 Provider 或写真实租户投影。

## 32.25 AIP WKS-DEP-0B R1-04～R1-08 真实运行链封板

- `m1@f9d0efa` 已串行集成 candidate-hash Gate、受控 Provider probe、active Model/Route、Health、Capacity 与可重复 operational verifier；Authority 为 `AOS-000090`。
- `org-org/dev-project` 在证据截止时间 exact resolver READY；20 条 GoldSet 形成 2 个 succeeded Eval Run，其中 3 条是真实 Provider probe、17 条是策略/路由契约；3 个历史失败尝试如实保留。
- Capacity 真实验收为 2 份成功、第 3 份并发阻断、2 份 released；篡改 Model hash 失败关闭。`dev-org/dev-project` 对 Model/Route/Capacity exact ID 均为 0。
- Health、Gate、Price 过期均使 resolver blocked；CapabilityBinding、SkillBinding、AgentRun 前后均为 0，R2 没有副作用。
- w1 targeted 28 + 邻接 62、m1 累计 82、compileall、diff check GREEN；临时 GoldSet 正文已移出工作树，CLI 抑制第三方 HTTP DEBUG，证据不含 Secret payload/响应正文。

当前状态：`R1_OPERATIONAL_READY_AT_EVIDENCE_CUTOFF / R2_NOT_AUTHORIZED`。Health 是 15 分钟滚动信号，后续 StartDecision 必须重新探测，不能把本次 READY 当作永久健康。

下一安全步骤：仅评审 R2 六同事 exact Skill/Capability Binding 组合与 runnable Agent 验收；评审通过前不创建 Binding/AgentRun，不通知 Workshop 将 W2 视为全面开工。
