# 分支与工作树映射

> 快照：2026-08-04。接续前必须用实时 Git 状态复核。

## 1. 集成真源

| 仓库/工作树 | 分支 | HEAD | 用途 |
|---|---|---|---|
| `/Users/ddt/work/projects/ai_agent/aos-platform` | `m1` | `5e58768` | TI-3 E2 总收口 GREEN；下一门 E3 |
| `/Users/ddt/work/projects/ai_agent/docs` | `m1` | 当前 TI-3 E2 收口提交 | 方案、证据与上下文真源 |

## 2. M2-B Worker 来源

| 工作树 | 分支 | HEAD | 已交付内容 | 合入 m1 |
|---|---|---|---|---|
| `aos-platform-w1` | `feature/228-ec-w1-contracts` | `764ad05` | Composition 控制 | `192f81a` |
| `aos-platform-w2` | `feature/228-ec-w2-rest-oauth` | `5372caf` | Installation 控制 | `4abe385` |
| `aos-platform-w3` | `feature/228-ec-w3-consistency` | `1a490b3` | Canonical API | `414bad7` |
| `aos-platform-w4` | `feature/228-ec-w4-pipeline` | `9a6818a` | 对抗/并发回归 | `4ff1f18` |

总控在 `d85992b` 完成 Router aggregate、OpenAPI/CORS、签名门和最终回归。Worker 分支是来源证据，不是 M3 的继续开发基线；M3 必须从 `m1@d85992b` 或其后经确认的 m1 基线切出。

## 3. 工作区保护

M3 开工前曾存在以下用户改动，已按用户要求单独提交并进入共同历史；后续仍不得覆盖或回退：

- `apps/web/src/shell/AppShell.tsx`（modified）
- `docs/juejin-228-public-series-plan.md`（untracked）
- `docs/juejin-series-228/`（untracked）

上述内容不得被覆盖、清理、暂存或夹带提交。docs 仓库也存在大量与 228/M3 无关的修改、删除和未跟踪文件；提交时只能显式暂存本任务文件。

## 4. M3-0 Worker 交付

| 工作树 | M3 分支 | Worker 原始提交 | 等价内容提交 | 同步后 HEAD | 内容 |
|---|---|---|---|---|---|
| `aos-platform-w1` | `feature/228-m3-w1-contract-types` | `39b90d8` | `d460c29` | `dff51c1` | Composition/Installation TypeScript 类型 |
| `aos-platform-w2` | `feature/228-m3-w2-registry-contract` | `269ef77` | `e839e8c` | `dff51c1` | Registry 真实响应、parser 与 fixture |
| `aos-platform-w3` | `feature/228-m3-w3-openapi-contract` | `e408fe1` | `435de34` | `dff51c1` | 11 operation OpenAPI/header/error 契约测试 |
| `aos-platform-w4` | `feature/228-m3-w4-operation-map` | `381a898` | `f7937b0` | `dff51c1` | 前端 operation/header/error map |

四个旧 M2-B 分支均保留历史，未 reset、rebase 或改写。M3-0 的四个 Worker 原始提交已通过真实 merge 纳入共同祖先链；M3-0 收口时五分支均指向 `dff51c1`，相互 ahead/behind 为 `0/0`。当时最终 tree 为 `9b68f970...`，与已完成 GREEN 验证的 `435de34` 完全一致。

## 5. M3 后续分支规则

- 总控以已核验的 `m1@36b386e` 作为 M4 方案审计起点。
- Worker 按 M3 方案的文件所有权分波；每波开工前必须确认五个分支同 HEAD、相互 ahead/behind 为 `0/0`。
- 每波完成后先提交总控的必要收口，再由各 Worker 将自己的已审查提交真实 merge 到当前 `m1`，`m1` 依次 fast-forward 接收；四路接收完成后，各 Worker 再 fast-forward 到最终 `m1`。
- 禁止用 reset、rebase 或强推伪造齐平；存在冲突时先停下审查，不能覆盖用户工作区改动。
- 每波退出门同时要求：五分支 HEAD 一致、tree 一致、四个 Worker 工作区干净；`m1` 的受保护用户改动可保留但不得夹带提交。

TI-2 E7 收口后，m1 与四个历史 M3 命名 Worker 分支均同步至 `4004170`；这些分支名只保留历史，不改变当前 TI-3 工作范围。主工作树仍有用户头条/掘金文档改动，未夹带提交。

TI-3 E1 收口后五分支进一步同步至 `7f7d48a`，远端已推送；主工作树用户文档仍保持未暂存、未夹带。

TI-3 E2-A 收口后五分支同步至 `5cdafea`，远端已推送；用户头条/掘金改动仍未暂存。

TI-3 E2-B 收口后五分支同步至 `561148f`，远端已推送；用户头条/掘金改动仍未暂存、未夹带。

TI-3 E2-C 与 E2 总收口后五分支同步至 `5e58768`，远端已推送；用户头条/掘金改动仍未暂存、未夹带。

## 6. M3-1 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `0457cf0` | 命令级幂等键与重试复用 | `89bbb98` |
| W2 | `0110021`、`cb0ee4e`、`a4b61d5` | 错误归一、500 结果未知、离线主动阻断语义 | `89bbb98` |
| W3 | `cbaf5b9`、`fd30057` | Registry/Composition/Installation SDK 与离线写禁用 | `89bbb98` |
| W4/总控 | `89bbb98` | SDK 黑盒契约测试与累计回归 | `89bbb98` |

M3-1 收口时 `m1` 与四个 Worker 全部指向 `89bbb98`，相互 ahead/behind 为 `0/0`，tree 为 `1cf65e7c...`。

## 7. M3-2 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控前置/W4 | `3fd3c6d`、`7f6e80a` | 只读页面模型、资产页集成和既有页面测试替换 | `7f6e80a` |
| W1 | `0faa72d` | Installation 运行时失败关闭 parser 与 SDK 只读接入 | `7f6e80a` |
| W2 | `bf9b285` | 五个只读 hook、乱序保护和 stale/error 语义 | `7f6e80a` |
| W3 | `544660c`、`f6e5d6e` | Registry、Installation 列表与详情纯展示组件 | `7f6e80a` |

M3-2 收口时，`m1` 与四个 Worker 全部指向 `7f6e80a`，相互 ahead/behind 为 `0/0`，tree 为 `7548aba9...`；该行仅为历史快照，当前基线以第 8 节 M3-3 记录为准。

## 8. M3-3 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `eebd152` | Composition parser、fixture 和 exact serializer | `77a1e5b` |
| W2 | `aa8765e` | 命令状态、幂等恢复、resolve 后回读和 create gate | `77a1e5b` |
| W3 | `3681857` | Lock、依赖、Diff 与受控预检纯展示组件 | `77a1e5b` |
| W4/总控 | `ab6d4fa`、`0813044`、`77a1e5b` | 422 策略、SDK parser 接线、draft fixture、根页面与累计回归 | `77a1e5b` |

M3-3 收口时，`m1` 与四个 Worker 全部指向 `77a1e5b`，相互 ahead/behind 为 `0/0`，tree 为 `895e5725...`；五个代码工作树均干净，远程 `origin/m1` 同 HEAD。该行仅为历史快照，当前基线以第 9 节 M3-4 记录为准。

## 9. M3-4 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `5093455` | action exact serializer、完整生命周期 fixture、Runtime Parser、503 失败关闭兼容 | `6432444` |
| W2 | `22f8209` | 单一 action controller、角色/状态门、冲突与未知结果恢复 | `6432444` |
| W3 | `f292319` | 动作矩阵、确认对话框、四 hash 只读、reason 与 Evidence/Pointer 展示 | `6432444` |
| W4/总控 | `3d0e2df`、`6432444` | 唯一 Builder 接线、会话/离线状态、资产页集成、安全复审与文案校正 | `6432444` |

M3-4 收口时，`m1` 与四个 Worker 全部指向 `6432444`，相互 ahead/behind 为 `0/0`，tree 为 `cb4a25e0...`；该行仅为历史快照，当前基线以第 10 节 M3-5 记录为准。

## 10. M3-5 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `4c66711` | 真实 JWT、Router/Service、隔离 PostgreSQL 的完整 HTTP 安装生命周期测试 | `36b386e` |
| W2 | `a3d9459` | Controller 连续状态、409/412、unknown、角色/离线零 POST 累计测试 | `36b386e` |
| W3 | `03efef2` | 页面完整 dry lifecycle、reject、冲突回读和 UI/Controller 防漂移矩阵 | `36b386e` |
| W4/总控 | `54f4c1c`、`36b386e` | Web/Desktop 共享 SDK 低 lib 兼容修复、全量累计门、用户掘金文档独立提交与五分支同步 | `36b386e` |

`m1` 与四个 Worker 当前全部指向 `36b386e`，相互 ahead/behind 为 `0/0`，tree 为 `1b7c7fed...`；五个代码工作树均干净，五个远程分支同 HEAD。

## 11. M4-0 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `84f8952`、`67e4132` | strict Evidence/Case/Snapshot/Timeline DTO | `c10d5ee` |
| W2 | `99e2495` | 8 级连续 Stage Policy、expiry/revoke/negative | `c10d5ee` |
| W3 | `aec8d59`、`9db7449` | Web types/parser/fixture/五端点 operation map | `c10d5ee` |
| W4 | `4fcb1e1` | maker/projector、scope、marking 安全门 | `c10d5ee` |
| 总控 | `7d3210e`、`c10d5ee` | canonical 对拍、错误/统计/UTC/阻塞收口；用户掘金文档独立提交 | `c10d5ee` |

五分支与五远端当前全部指向 `c10d5ee`，相互 ahead/behind 为 `0/0`，tree 为 `2d2b6784...`；五个代码工作树均干净。

## 12. M4-1 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `7e6a367` | 唯一 PostgreSQL migration、8 表、canonical/hash/trigger、真实 PG 迁移测试 | `14311e2` |
| W2 | `da93ab5`、`f409cb3` | Store、Evidence revision、Snapshot/Event/Projection/Receipt 原子事务与真实 PG 测试 | `14311e2` |
| W3 | `6eef14b` | 五端点 Web SDK、幂等/ETag/离线/错误适配 | `14311e2` |
| W4/总控 | `38b105b`、`14311e2` | 重启完整性、marking/reference 与整体回滚对抗，累计门与五分支同步 | `14311e2` |

五分支与五远端全部指向 `14311e2`，相互 ahead/behind 为 `0/0`，tree 为 `6f89a37e...`；五个代码工作树均干净。

## 13. M4-2 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `1d17466` | expiry projector、统一 cutoff、租户批处理、失败隔离与幂等 | `483dd0f` |
| W2 | `c52a5e5` | 五用例 Service、receipt-first、Snapshot CAS、内部 EvidenceWriter | `483dd0f` |
| W3 | `0418e5e` | 五个 Canonical Router、严格请求/header、role/marking/error | `483dd0f` |
| W4/总控 | `483dd0f` | PostgreSQL Reader、Marking Resolver、真实 HTTP E2E、路由/OpenAPI 收口 | `483dd0f` |

五分支与五远端全部指向 `483dd0f`，相互 ahead/behind 为 `0/0`，tree 为 `b07a4755...`；五个代码工作树均干净。

## 14. M4-3 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `b9ec6fb` | 单一 read model、九态、三路竞态门、stale 与租户/scope/filter 清理 | `b2dc69b` |
| W2 | `7d9938b` | 服务端统计与案例目录、`null`/`0`、current/reference 披露 | `b2dc69b` |
| W3 | `2f218c6` | 详情、8 门、Blocker、Evidence 摘要与 Stage Event 时间线 | `b2dc69b` |
| W4/总控 | `b2dc69b` | 薄页面、租户事件、scope/选择/刷新/错误集成与静态事实清零 | `b2dc69b` |

五分支与五远端全部指向 `b2dc69b`，相互 ahead/behind 为 `0/0`，tree 为 `5b3cf4b4...`；五个代码工作树均干净。

## 15. M4-4 Worker 交付

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `108fd52` | 真实 PG Evidence 升降级、续期、撤销、幂等与回滚证明 | `c9da800` |
| W2 | `d66a3d7` | marking/reference/去重/null/0/cutoff/分页统计对抗 | `c9da800` |
| W3 | `e6b10f1` | 隔离 PostgreSQL + 真实 API/JWT/Web 浏览器支撑与精确清理 | `c9da800` |
| W4/总控 | `c9da800` | 浏览器 current/reference/detail/timeline/stale/recovery/expiry 闭环 | `c9da800` |

五分支与五远端全部指向 `c9da800`，相互 ahead/behind 为 `0/0`，tree 为 `4df22172...`；五个代码工作树及隔离运行环境均干净。

## 16. M4-5 最终收口

| 路线 | 结果 | 同步后 HEAD |
|---|---|---|
| W1 | 资产域 679 passed；登记既有 Ruff 格式债务，无代码修改 | `8964c4b` |
| W2 | Web 2001、Integration SDK 58、Asset SDK 63，typecheck/build GREEN | `8964c4b` |
| W3 | Desktop 40、Ontology SDK 6、Harness 语法/清理 GREEN | `8964c4b` |
| W4/总控 | 后端 8878、Router/OpenAPI/security、生成物与 Harness 扫描收口 | `8964c4b` |

`m1` 与四个历史命名 Worker 分支、本地与远端全部指向 `8964c4bf09575ef3df3bba732ddf9482ef65a25a`，tree 为 `236f8a25b96e953e320a3cab38cba2486dc0c4de`，ahead/behind `0/0`，五工作树 clean。M5-0 必须从该共同起跑线开始。

## 17. M5-0 契约与无业务骨架收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `e6eb365`、`64205b4` | 四包 source 骨架、SBOM/Evals、0-byte 空 export 与 strict 契约 | `4fd9c7f` |
| W2 | `3143923` | strict InstanceOverlay Schema、synthetic fixture、canonical hash/负向门 | `4fd9c7f` |
| W3 | `a86d363`、`c205697` | 内核反向依赖、客户/秘密/平台能力偷渡和 scoped scanner 门 | `4fd9c7f` |
| W4/总控 | `4e2b086`、`4fd9c7f` | 三叶/四包依赖图、Overlay→Installation DTO 对拍、集成纠偏与累计门 | `4fd9c7f` |

`m1` 与四个 Worker 本地/远端全部指向 `4fd9c7f9f0e4`，tree 为 `48719e7141404fae8909d32d7bd81b1eae4ddac0`，ahead/behind `0/0`，五工作树 clean。M5-1 必须从该共同起跑线开始。

## 18. M5-1 运行时签名与 Registry 发布收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `59edb69` | 临时四包复制、运行时 Ed25519、Frozen trust root、canonical 签名 helper 与 Loader 证明 | `8aa87a9` |
| W2 | `77b8208`、`a850e6b` | 四包真实 PG 发布、职责分离、五类 Evidence、重启 snapshot | `8aa87a9` |
| W3 | `42b9209` | 受控文件/签名篡改、SBOM/Evals、过期/撤销/revision 变化失败关闭 | `8aa87a9` |
| W4/总控 | `0947084`、`8aa87a9` | helper/manifest 真源映射、真实 Ruff 门纠偏、累计门与五分支收口 | `8aa87a9` |

`m1` 与四个 Worker 本地/远端全部指向 `8aa87a9748f8`，tree 为 `f40c8cb4a4a8a44f725f4859aced0b9452307df7`，ahead/behind `0/0`，五工作树 clean。M5-2 必须从该共同起跑线开始。

## 19. M5-2 Composition 与 Installation 闭环收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `81d97d4` | 三叶请求/core 自动闭包、四包/三边、乱序与 lock/diff hash 稳定 | `2d16a64` |
| W2 | `1e73ce3` | 真实 PG 控制面 support、resolve/lock、六态 Installation、CAS/职责分离/Evidence | `2d16a64` |
| W3 | `573adbd` | active 后真实 release revoke、revalidation stale、安全 rollback 和无关安装隔离 | `2d16a64` |
| W4/总控 | `78bea22`、`2d16a64` | synthetic scope/五迁移边界、真实 Ruff 门、专项累计与五分支收口 | `2d16a64` |

`m1` 与四个 Worker 本地/远端全部指向 `2d16a64af13e`，tree 为 `7eb9e58cb850c5622880e4bef9ff3292448ac368`，ahead/behind `0/0`，五工作树 clean。M5-3 必须从该共同起跑线开始。

## 20. M5-3 最终累计门与 M5 收口

M5-3 为只读累计门，没有生产或测试代码增量。W1 完成资产域、M5 专项和真实 PostgreSQL 门；W2 完成 Web、Integration SDK、Asset Control SDK、typecheck/build；W3 完成 Desktop、Ontology SDK、边界测试与安全扫描；W4/总控完成后端全量、Router/OpenAPI、Alembic 与总对账。主工作树同期发现的掘金文档由 `1bdef79`、`b222083`、`4d04e1f` 独立保存，M5 最终功能检查点为 `86c3076`。

`m1` 与四个 Worker 本地/远端全部指向 `4d04e1f68679179207e19db5610eac9386d28a89`，tree 为 `f8a257cf99c36d8f9fc82ad91e6e8ec5da032a1d`，ahead/behind `0/0`，五工作树 clean。该提交相对 M5 功能检查点只包含用户掘金文档变化；下一步不得直接开始具体平台编码。

## 21. TI-0B/TI-0C 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1 | `e7d07b8` | 86 表/7 类非表资源注册表、只读实库 coverage、漂移失败门、4 项专项测试 | `570fcc1` |
| W3 | `570fcc1` | 16 组业务 Router Principal 门、无 Token 负向测试、OpenAPI/route inventory | `570fcc1` |
| 总控 | `570fcc1` | 两波依次合入 `m1`，每波后四 Worker fast-forward 并推送 | `570fcc1` |

`m1` 与四个 Worker 本地/远端全部指向 `570fcc1b96782e0833619653390e638c4418528b`，tree 为 `773b883c4775b6d64295c034bd1d45e24e7fa4ce`；四个 Worker 工作树 clean。主工作树保留 `docs/toutiao-series/` 下 2 个修改和 1 个未跟踪用户文件，未纳入租户隔离提交，因此不能描述为工作树 clean。

## 22. TI-0D 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W2 | `504a639` | 只读 PostgreSQL 归属/FK/JSON precheck、非表资源盘点、migration ledger、栖月汇 baseline、专项测试 | `504a639` |
| W4/总控 | `504a639` | 本地 PostgreSQL/MinIO 实扫、四份脱敏 JSON、方案与上下文对账 | `504a639` |

`m1` 与四个 Worker 本地/远端全部指向 `504a639`。四个 Worker 工作树 clean；主工作树继续保留 `docs/toutiao-series/` 下 2 个修改和 1 个未跟踪用户文件，未纳入 TI-0D 提交。

## 23. TI-0E 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W1 | `dc32c31` | 注册表 v2、93 资源唯一执行分组、计划失败关闭校验 | `dc32c31` |
| 总控/W4 | `dc32c31` | migration order、每波六类测试、E1～E7 回滚点编译器与 evidence | `dc32c31` |

`m1` 与四个 Worker 本地/远端全部指向 `dc32c31`。四个 Worker 工作树 clean；主工作树保留 `docs/toutiao-series/` 下 4 个修改和 1 个未跟踪用户文件，未纳入 TI-0E 提交。下一步是 TI-1 E1 评审，不得把五分支齐平解读为数据库迁移授权。

## 24. TI-1 E1 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| W1/总控 | `621a1bb` | TenantScope/GUC、nullable expand、7 个 NOT VALID FK、schema lint、迁移与累计测试 | `621a1bb` |

`m1` 与四个 Worker 本地/远端全部指向 `621a1bb`。四个 Worker 工作树 clean；主工作树保留 `docs/toutiao-series/` 下 6 个修改和 1 个未跟踪用户文件，未纳入 TI-1 E1 提交。下一步是 TI-1 E2 评审，五分支齐平不代表双写或回填授权。

## 25. TI-1 E2 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W1 | `a7df658` | authz 三态双写、最小差异 ledger、`228ti1e2dual`、schema lint 和测试 | `a7df658` |

`m1` 与四个 Worker 本地/远端全部指向 `a7df658`。四个 Worker 工作树 clean；主工作树保留 `docs/toutiao-series/` 用户改动，未纳入 E2 提交。下一步是 TI-1 E3 历史归属/隔离区方案评审；五分支齐平不代表 E3 回填授权。

## 26. TI-1 E3-0 只读重基线收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W1 | `26102d4` | 只读 source snapshot、E1 守恒对账、脱敏指纹、CLI、环境指纹安全补强和失败关闭测试 | `26102d4` |

`m1` 与四个 Worker 本地/远端全部指向 `26102d4`，tree 一致。四个 Worker 工作树 clean；主工作树保留 `docs/toutiao-series/` 用户改动，未纳入 E3-0 提交。E3-0 门禁 BLOCKED，五分支齐平不代表 E3-1 或历史 DML 授权。

## 27. TI-1 E3 最终收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W1 | `0ab3c5e`～`d2d6c3d` | append-only ledger、59 条 quarantine、角色分离、可逆执行、恢复库演练与共享非生产零回填 | `d2d6c3d` |

`m1` 与四个 Worker 本地/远端全部指向 `d2d6c3d`，tree 一致。四个 Worker 工作树 clean；主工作树继续保留 `docs/toutiao-series/` 用户改动，未纳入 E3 提交。E3 当前执行集合与非生产可逆性 GREEN；历史连续性风险接受，E4 仍 BLOCKED。

## 28. TI-2 E2-B 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `5df9b5e` | Query 写、Event CRUD、Interface 读写显式 TenantScope，跨租户失败关闭和累计门 | `5df9b5e` |

`m1` 与四个 Worker 本地/远端全部指向 `5df9b5e`，tree 为 `1f784ca6b0951a4d537ac59f3f1fc809c513c582`。四个 Worker 工作树 clean；主工作树继续保留 `docs/toutiao-series/` 用户改动，未纳入 E2-B 提交。下一步 E2-C 处理 Deployment、Theme、Widget Catalog 与 usage 链；五分支齐平不代表同 module_id 双租户复用或 E3 回填已完成。

## 29. TI-2 E2-C 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `87d6ef5` | Deployment、Theme、Widget Catalog 与 usage/ref 链显式 TenantScope、累计门 | `87d6ef5` |

`m1` 与四个 Worker 本地/远端全部指向 `87d6ef5`，tree 为 `d2c91ef101dd20ebf4fbab3842e27910c15d7ea3`。四个 Worker 工作树 clean；主工作树继续保留 `docs/toutiao-series/` 用户改动。下一步 E2-D 清理 Module aggregate 运行路径中的默认租户并建立静态门。

## 30. TI-2 E2-D 与 E2 总收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `472728e` | Module aggregate TenantScope、默认租户静态门、APP-01～03 基础与累计门 | `472728e` |

`m1` 与四个 Worker 本地/远端全部指向 `472728e`，tree 为 `70121cef1c39055082f2bda12268a93f0c33e4d4`。四个 Worker 工作树 clean；主工作树继续保留用户掘金文档。E2 总体 GREEN，但 APP-04 同 ID 双租户共存和 APP-05 卸载仍未完成；下一步是 E3 Module 身份可逆回填，不允许直接跳到 Contract。

## 31. TI-2 E3 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `21ad0b2`、`9502acf`、`8e57c8a` | 确定性身份、append-only ledger、子先父后回滚、恢复/共享演练、新写持续性 | `8e57c8a` |

`m1` 与四个 Worker 本地/远端全部指向 `8e57c8a`，tree 为 `210c312617ddf267d5739b9eae8ce57aad41c466`。四个 Worker 工作树 clean；主工作树继续保留用户掘金文档。共享非生产最终 batch 已 verify 622，2 个孤儿 Event 保持 quarantine；下一步 E4 Validate，不得跳过约束逐项验证。

## 32. TI-2 E4 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `129d438` | 11 个 Module 身份 FK Validate、schema lint、真实 upgrade/downgrade/upgrade 与累计门 | `129d438` |

`m1` 与四个 Worker 本地/远端全部指向 `129d438`。四个 Worker 工作树 clean；主工作树继续保留用户头条/掘金文档，未纳入 E4 提交。共享非生产最终 Alembic 为 `228ti2e4validate`，11/11 FK validated，下一步 E5 Read Switch。

## 33. TI-2 E5 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `8aee9f6` | Module 父影子对读、7 子表 module_pk 读改删、测试身份夹具、孤儿隐藏与累计门 | `8aee9f6` |

`m1` 与四个 Worker 本地/远端全部指向 `8aee9f6`。四个 Worker 工作树 clean；主工作树继续保留用户头条/掘金文档，未纳入 E5 提交。共享库只读核对 160 个父对读一致、子错配 0；下一步 E6 RLS。

## 34. TI-2 E6 收口

| 路线 | 关键提交 | 交付 | 同步后 HEAD |
|---|---|---|---|
| 总控/W2 | `846b49a` | 降权运行角色、11 表 RLS、policy/schema lint、真实升降级和累计门 | `846b49a` |

`m1` 与四个 Worker 本地/远端全部指向 `846b49a`。四个 Worker 工作树 clean；主工作树继续保留用户头条/掘金文档。共享库最终 `228ti2e6rls`，无 GUC运行角色可见 0；下一步 E7 Contract。
