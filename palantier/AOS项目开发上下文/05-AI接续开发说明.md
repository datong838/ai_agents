# AI 接续开发说明

## 1. 开始前必读

按顺序阅读本目录、M0 总方案、M2 公共契约、M2-B 方案、M2-B 最终证据和当前阶段方案。不要仅凭聊天摘要或文件名推断实现。

## 2. 每次接续检查

1. 核验 `aos-platform` 与 `docs` 的分支、HEAD、工作树和 worktree 映射。
2. 识别并保护用户未提交改动；禁止 reset、checkout 覆盖或宽泛暂存。
3. 将方案中的 API、DTO、状态机、权限、错误码映射到真实代码。
4. 若当前基线已前移，先检查新增提交是否改变本目录快照，再更新上下文。
5. 修改代码前先完善当前阶段方案，明确文件所有权、退出门和回滚。

## 3. TI-6 最终收口后的接续指令

- 当前起点：`aos-platform m1@44887c1`；Alembic `228ti6edirectory`；TI-6-5 最终对账完成，本地微商城 Connector 开发前置 GREEN。
- 清理 apply 只允许 `aos_test_*/aos_ti6_*` 数据库与 `ti6-synthetic-*` 组织/工作区同时满足；不得放开这个双重硬门或给共享/生产增加删除入口。
- `workspace_id` 只是 Connector adapter 边界的 `project_id` 别名；`module_organization_profile` 是组织级共享数据，单工作区清理必须保留。不可变历史和未探测外部后端不得冒充已清理。
- 当前只能宣告“本地微商城 Connector 开发前置 GREEN”；严格生产门仍保留 TI-1 E4、未知对象、空/未归属 scope 与外部后端 blocker。
- 下一次开发必须取得首平台与 G0/D0 新授权、先冻结新的 `228-` 实施波方案；不得因本地 GREEN 自动读取 Niushop 凭据、连接真实库、写 Connector 或导入业务数据。
- 四模板是平台只读目录；组织定制必须追加 `module_instance_overlay` 并由服务端校验 hash。不得把模板复制成客户源码分支或让卸载删除业务事实。
- `ti6-microshop-readiness.json` 的 local 与 production 结论必须分开；禁止把 LOCAL READY 写成真实平台或生产部署 GREEN。
- `meta_workspace/twa_workspace` 是显式工作区根；资产 Composition/Command/Case 根已直连 `meta_workspace`。不得给普通业务表滥加 `workspaceRoot` 绕过关系门。
- Composition、Installation、Integration Case 15 表已 FORCE RLS；五类真实 PostgreSQL 访问链必须继续绑定 canonical TenantScope，到期投影禁止恢复无 scope 全租户扫描。
- Theme/Widget 已允许同逻辑 ID 跨组织独立存在；不要恢复全局 `id` 冲突语义。TWA 三表保留 legacy ID 唯一约束仅用于兼容。
- 对象维护隔离 2 项、测试组织 vector 1 项已被正确识别；剩余 3 个未知对象必须继续显式处理，禁止为得到 GREEN 从报告中排除。
- TI-6-1 已把机器注册表校准为实库 101/101、零 drift，并为每表 precheck 增加 savepoint。不要重新把已迁移表回退成旧状态；TI-6-2 只处理重扫确认的真实结构/历史/非表阻断。
- TI-5 D 已证明 A1/A2/A3/B1 迁移链可逆与共享库 fingerprint；不得重复执行共享库 downgrade。后续 TI-6 只读重基线后再决定是否存在新的结构动作。
- C3-B 已将 Phase5 Pipeline/Node/Edge/Proposal/History/Schedule/ScheduleRun 统一为 scoped key，Schedule Router 已鉴权；不得恢复全局 ID 或无 Principal 调用。
- C3-C 分类真源为 `tenant_non_postgres_classification.yaml`；Redis/broker/offline 未配置，Object/Qdrant 外部后端未验证。不得将本地合同 GREEN 写成生产后端 GREEN。
- C3-A 已将 capability Job 与 DLQ 绑定完整 TenantScope；不得恢复全局 job/DLQ id、跨 scope list/retry 或无归属 docintel failure。`_capabilities` 仍是平台模板，不得误改为租户业务实例。
- C2-B：`wave_ext` Dataset/Media/History 已 scoped；`GET /v1/datasets` 真源为 wave_ext；Phase5 同名 GET 为 `NOT_REACHABLE_DUPLICATE`，不得把 Phase5 GET 当绕过路径，也不得本波改 route manifest。
- 不得恢复全局 RID 容器、无 scope demo seed，或 hydrate 后把未落库 demo 当全局可见。
- AIP Catalog 与 Phase5 Dataset/Build/Health/SyncConfig 已使用完整 scope key；不得恢复全局 ID 容器、无 scope engine 调用或全租户 reset。
- C1 已将 `file-object-store` probe 限定为认证 TenantScope 前缀；不得恢复空 prefix 全 bucket list。两个无归属 probe 位于 `_maintenance/quarantine/unowned`，禁止租户 API 可见或默认认领。
- local vector 已由 scoped KV 与 scoped collection name 双重隔离；Qdrant 未配置，只能保持 `EXTERNAL_BACKEND_UNVERIFIED`。
- B2 已证明磁盘 Provider manifest 平台模板 + `meta_aip_kv` scoped 安装/配置实例边界，不得重做。
- TI-5 C3：未配置 Redis/队列/离线后端时只做 canonical key/envelope 契约、本地 fake 负向与 `NOT_CONFIGURED`；不得宣称生产后端 GREEN。完成 mutable finding 分类表后方可进 TI-5 D。
- `decision_lineage` 631 条已逐条以唯一 Draft 父记录归属 `dev-org/dev-project`，不是默认认领；必须保持 Draft 复合 FK、FORCE RLS 和 runtime scoped transaction。
- A3 quarantine 安全网本次为 0 行，但不得删除；任何无唯一 Draft/字段不一致的历史只能进隔离区。
- Action/Draft 的 bootstrap DDL 已移出请求期；不得让 `aos_runtime` 在 API 请求中 CREATE/ALTER 表。
- `meta_aip_kv` 23 条历史仅因“本地共享非生产库 + 用户明确测试数据 + 冻结 key 清单”归属 `dev-org/dev-project`；不得将这个例外推广到 lineage 或其他无证据数据。
- `meta_aip_kv` 已使用 scoped 复合主键、workspace FK、FORCE RLS 和 request ContextVar；无 request/无 task envelope 必须继续失败关闭。
- 7 张 AIP 运行表已 FORCE RLS；Graph/Run/Eval/Publication Store 必须继续使用 scoped transaction，禁止只靠 SQL WHERE 过滤。
- Scheduler 的 Schedule/Resource/Execution、内存 key 和 executor envelope 已绑定完整 TenantScope；不得恢复全局 ID key 或 Router 只鉴权不传 scope。
- Apollo Channel 是平台共享模板；Spoke 是组织/工作区实例，已使用 scoped 主键、workspace FK、FORCE RLS 和 scoped transaction，禁止恢复只按 org 或请求期 DDL。
- TI-5 必须按 `228-TI-5-AIP分析模型与非数据库资源隔离实施方案.md` 先收口 AIP/Analytics/模型目录，再处理对象前缀、向量、缓存、队列、离线和进程内状态。
- 5 张电商 Connector 表已完成 FK Validate、ENABLE/FORCE RLS；SQLAlchemy 一致性 Store 与 PostgreSQL OAuth Store/到期刷新必须继续显式绑定 TenantScope，禁止恢复 owner transaction 或全表 due scan。
- 293 条 Data OS 未知历史已物理进入不可变维护隔离区，禁止恢复到活跃区或默认认领；活跃表 scope 非空且使用 scoped 主键。
- 5 张电商 Connector 表仍是空表，C3 已完成 FK Validate、RLS 与 runtime transaction；这只代表通用底座就绪，不代表任何具体平台已经接入。
- 7 张 Data OS 表已严格 scoped 读写并启用 FORCE RLS；293 条未知归属记录已物理进入不可变维护隔离区，不得恢复到活跃表或默认认领。
- D7 必须以 D3 batch `44902fd4-7390-50ee-ae86-192ebd456c33` 的 293 条 QUARANTINE 为唯一迁移名单，原样进入不可变维护隔离区，禁止默认归测试组织或栖月汇。
- D7 同波冻结 7 表 TenantScope NOT NULL、scoped 主键、父子复合 FK，并把 Data OS 建表真源收归 Alembic；现有 API 业务 ID 不变。
- 5 张电商 Connector 真源表已具备 TenantScope 复合主键、scope NOT NULL 和 NOT VALID workspace alias FK；C1 未启用 RLS、未写业务数据，不能宣告 Connector 已完成。
- REST/File/MySQL/PostgreSQL/MSSQL 的 Object ingest 已插入 scope 并使用 scoped conflict target；不得恢复无 scope direct call、旧全局 conflict target或把 props 标记当成隔离。
- D1 已冻结 7 表 297 行并新增 10 列/7 FK；不得把 `meta_source` 默认 dev scope、props scope 或 ID 命名直接认定为历史归属。
- D2 只改 Data OS 新写路径：所有 Store mutation 必须显式 TenantScope、`connect(scope)` 并写 scope 列；不切换全局 load/delete，不回填 297 行。
- `ensure_data_os_schema()` 仍是基表 bootstrap 的过渡事实；不得静默新增第二份建表定义，D7 Contract 前统一收归 migration。
- Object Runtime 历史决策固定为 NO_ACTION=993、QUARANTINE=37、ASSIGN=0；共享最终 batch `7c82803d-1b18-5df0-9a52-46b8f831f376`，业务 DML=0。不得把 quarantine 改写为已归测试组织。
- Object/Branch/Object Set/Tool/Marking/Graph/Funnel/Analytics 的目标读链已显式 scope；不得恢复 Branch Store 的 optional scope、请求期 DDL 或无 org/project 的目标 SQL。
- Draft approval 的 Object/Wiki/Draft 状态更新和 Lifecycle/TTL/Insight 已显式绑定 Principal scope；E7 后同业务 ID 跨 scope 必须共存且互不可见，不得恢复旧全局键冲突或覆盖 org/project 的逻辑。
- Retention CLI 必须显式传 `--org-id/--project-id`；后台任务不得重新引入“无 scope 扫全库”。
- `decision_lineage` 631 条历史归属仍留给 TI-5；E2-C 不通过默认测试组织或时间推断消除该风险。
- Object/Graph/Draft/Wiki 共 9 张租户资源已登记，scope 全部 NOT NULL，9 个 workspace FK 已 Validate，主键全部包含 TenantScope。37 条未知归属行已原样进入 `object_runtime_orphan_quarantine`，仍禁止默认回填。
- Object Runtime 全包 SELECT 已显式 scope；不得恢复 vector autoSample、demo story/seed/clear 的无 scope 旁路。
- 4 个 Connector writer 已在 TI-4 C2 收口为显式 scope；不得恢复无 scope direct call，也不能通过 owner/BYPASSRLS 绕过。
- 9 张 Object Runtime 表已启用并 FORCE RLS；不得恢复 owner 作为带 scope 业务 Store 的执行角色或移除 WITH CHECK。
- E7 已完成 37 条 NULL quarantine 的维护隔离、scope NOT NULL、TenantScope 复合主键和 scoped branch FK；不得删除/默认认领 quarantine，也不得宣告 Connector 已完成。
- Module 8 张活跃父子表已冻结 TenantScope 复合主键和 NOT NULL；不得恢复全局 ID 唯一假设。两条 orphan Event 位于维护隔离表，runtime 禁止访问。
- APP-04/05 已完成；卸载必须保持 admin/owner + If-Match、只软删当前 scope、停止事件且不删除独立业务对象。
- TI-2 E1 只完成结构扩展；644 行旧 Module 尚无 module_pk/模板绑定，子 Store 默认租户与全局 ID 风险仍在。下一步必须按 TI-2 E2 先传 TenantScope，再做任何回填或读切换。
- pytest 默认创建、迁移并销毁临时 `aos_test_*` 数据库；`AOS_TWA_STORE=memory` 不得读写共享 `meta_*`。无 `CREATEDB` 权限时失败关闭，不得设置 `AOS_TEST_USE_SHARED_DATABASE=1` 规避 CI 门。
- E1 逐记录身份不可恢复，最终状态为 `RISK_ACCEPTED_DISCONTINUITY`；不得将此改写为历史证据已找回。当前 59 条记录均逻辑 QUARANTINE，E4 Validate 仍 BLOCKED。
- E3 备份恢复、四角色 apply/verify/rollback 和共享非生产零回填已完成；客户/生产、物理删除和真实微商城连接未执行。
- E2 的 `AOS_TENANT_DUAL_WRITE_AUTHZ` 必须保持默认 `off`；未完成灰度审批不得把 `shadow/enforce` 写入共享环境默认配置。
- E1 冻结基线中 7 个 FK 均为 NOT VALID，并记录 `meta_membership=57`、`twa_ws_member=4`、`twa_audit=3` 共 64 行历史断链；禁止通过补假父记录、删除或默认归测试组织来消除。
- E1 冻结基线是 `authz_tuple=9`、未知总量 993；E3-0 真库快照为 `authz_tuple=0`、未归属 PostgreSQL 行 986，并精确测得 `meta_membership/twa_ws_member/twa_audit` 孤儿为 53/0/0。这些是未解释漂移，不是已完成归属；栖月汇 baseline 仍为 YELLOW。
- 已通过 PostgreSQL 16.14 容器内匹配版本 `pg_dump/pg_restore` 完成隔离恢复演练；备份保持 Git 外。客户/生产仍需其目标环境独立备份与双审批。
- TI-0E 的唯一执行顺序来自 `tenant_resources.yaml.executionPlan`；每资源旧 `migrationWave` 只是 TI-0D 历史发现提示，不得再作为实施波次。
- TI-0E 三份机器产物为 migration order、test matrix 和 rollback points；计划授权固定为 `PLAN_ONLY`，不得把 GREEN 解读为已执行迁移。
- TI-0D 历史快照为栖月汇 BUSINESS_DATA=0、CONTROL_PLANE=5；TI-6-5 当前复扫为 BUSINESS_DATA=0、CONTROL_PLANE=3。两者的严格全资源 `isProvenEmpty` 都因显式 blocker 不是生产 GREEN；不得把“业务 0”写成“全域已证明空”。
- TI-0D 的 993 行未知归属、2 个未知 MinIO 对象、1 个未分租户 Vector 和四类非表探测缺口必须进入 TI-0E 阻断矩阵。
- TI-0B 的 86 表 coverage 100% 只表示“登记完整”；27 张 WEAK_PK、28 张 NO_TENANT 和 RLS=0 仍是生产阻断项。
- TI-0C 的 401 只表示入口鉴权失败关闭；跨租户 ID、Store TenantScope、复合键/FK、RLS 必须由 TI-1～TI-5 完成。
- M5 专项方案已经冻结；严格按 `228-M5-最小电商包组合验证实施方案.md` 的 M5-0～M5-3、文件所有权与退出门实施。
- M5 只交付四个无业务 Bundle 骨架、synthetic Overlay Schema/fixture、运行时测试签名和真实发布/解析/安装/回滚证明。
- `platform.ecommerce.niushop` 只是 test-only 坐标；不得写 SQL、Connector、schema mapping、JDBC、Pipeline 或平台业务。
- 不提交私钥/固定签名/真实客户引用；InstanceOverlay 只证明文件契约和外部 canonical hash，不冒充生产 Store/API。
- 不虚构独立 diff API，不把 rollback 冒充 uninstall，不把 test published/active 冒充平台上线。
- M5 已最终 GREEN，但这不自动授权 G0～G6 或具体平台编码；任何具体平台代码开始前必须暂停汇报并取得新的明确授权。
- 下一步只允许评审 G0 范围、首个平台选择、真实数据/凭据/写回边界与验收门。不得自行读取真实商城 schema、调用平台 API 或编写 Connector。

## 4. 证据要求

每波记录：基线提交、修改文件、专项测试、前序累计测试、Web typecheck/build、浏览器场景、风险与未解决项。阶段 GREEN 后更新本目录状态和分支映射。

每波分支收口固定执行：提交总控必要改动 → 逐个 Worker merge 当前 `m1` → `m1` 逐个 fast-forward 接收 → 四个 Worker fast-forward 最终 `m1`。退出前验证五分支同 HEAD、ahead/behind 为 `0/0`、tree 一致，并保护所有用户未提交改动。

每波 GREEN 后还必须执行计划对账：核对两份 `00-开发计划*` 的长期路线说明、`228-电商增长参谋长实施文档索引与分波路线图.md`、`228-M0-*`、当前阶段 `228-M*`、本目录状态和证据。旧排期中的“平台 M3（抖音上线）”与 228-AOS 的“M3（FDE UI/SDK）”必须始终显式区分。

## 5. 禁止事项

- 不提前开发 G0～G6 或具体电商平台。
- 不伪造发布、验证、审批、evidence 或 active 状态。
- 不在前端复刻服务端状态机、Resolver 或 hash 算法。
- 不以本地 Mock、localStorage 或离线缓存完成安全敏感 mutation。
- 不将真实客户数据、Secret、生产凭据或商城写回引入测试夹具。
