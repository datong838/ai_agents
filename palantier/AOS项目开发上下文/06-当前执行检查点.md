# 当前执行检查点

> 更新时间：2026-08-12
> 状态：**AIP-5 E0～E3 IMPLEMENTED_GREEN / AIP-5 E4 IN_PROGRESS**
> 唯一真实范围：`org-org / dev-project`
> 负向隔离 canary：`dev-org / dev-project`

## 阶段

AIP-5 E0～E3 已封板；当前阶段为 E4 Canonical API、唯一前端 SDK、Memory Governance/Wiki 页面与内置浏览器验收。

## 已完成

- E4A 新增唯一 `apps/web/src/api/aipEvidence/` SDK 基座和 canonical operations。
- LineageEvent 对 root、连续 sequence、source tuple、hash 与 quality 严格解析，异常响应失败关闭。
- 决策谱系页切换 root type + root id 权威查询，删除固定 Trace ID、固定六段、固定标题和失效治理探针。
- E4B 扩展 `aipEvidence`，严格解析同一 lineage 的 TelemetrySpan 与 UsageReceipt。
- E4B 可观测页面只展示权威 Overview/Spans/Usage；生产 Mock、合成趋势、伪 Token 和无权威后端的 Dashboard 已删除。
- E4C Evals 增加 AIP-4 权威 EvalRun 只读入口，严格展示 suite/target/dataset/judge revision/hash。
- E4C Logic Publication 统一使用唯一 AIP client；严格 POST→GET readback 与既有状态机不变。
- 代码 `43f5f82` 已提交并推送。
- AIP-5 历史“不授权编码”已与用户全量授权统一；E0～E7 分波与安全提交边界已冻结，文档 `71f4edc` 已推送。
- AIP-5 E0 新增 governed memory DTO 与 ADR；三层运行记忆、Candidate/Source/Governance/Citation 边界已机器冻结。
- Working 不可晋升；请求 DTO 不接受 `org_id/project_id`；批准/晋升必须绑定精确 Eval report、Draft 和 ApprovalEvent。
- 旧 `aip_long_memory.py`、`ontology_wiki_engine.py` singleton 只作兼容层；代码 `bb84fc3` 已推送。
- E1A 新增五张 additive authority 表并 FORCE RLS；Candidate 精确绑定 Task/Run，Item current revision 精确绑定 Revision。
- E1A 正向 `org-org/dev-project`、负向 `dev-org/dev-project`、无 scope 失败关闭和 downgrade/upgrade 行数守恒通过；代码 `07c974e` 已推送。
- E1B 唯一 PostgreSQL Store、精确幂等、事件时间线、CAS、治理批准与 Candidate→Item+Revision 原子晋升完成；代码 `2692f1e` 已推送。
- E2 冻结 15 个 fail-closed reason codes，统一 Governance Service 是生产审批/晋升决策入口。
- E2 必须从受信 Inspector/License Resolver 和 PostgreSQL Eval/Draft/Approval authority 取证；不信任客户端自述。
- E2 七门、精确脱敏 Receipt、跨租户不可见、重复/冲突和晋升前二次复检完成；代码 `ab7b8aa`。
- E2.1 复审补强：去重/冲突只在同知识 scope 内判定；workspace/organization/public_package 同名条目可并存；代码 `36be9b4`。
- E3 权威 KnowledgeQuery、渐进上下文、O1 Wiki 只读 adapter 与 reference-only 可重建索引完成；代码 `81c5f82`。
- E3 请求 marking 不得扩大服务端授权；workspace/organization/public_package 稳定解析；stale/revoked/future/conflict/applicability/cross-tenant/payload drift 均失败关闭。
- E3 token 预算仅装配完整 chunk；citation 与 chunk 一一对应；索引 unavailable 显式回源并标记 degraded，canonical Memory 不变。

## 最后验证

- E4A Vitest：3 文件、9 tests passed。
- E4B Vitest：3 文件、14 tests passed。
- E4C Vitest：6 文件、44 tests passed；前端全量 172 文件、2044 tests passed。
- TypeScript：`tsc --noEmit` GREEN；`git diff --check` GREEN。
- 生产构建：Vite build GREEN（270 modules transformed）。
- 内置浏览器：`org-org/dev-project` 组织/工作区正确；可观测页 idle 与 API 失败态诚实；`2.84M`、Dashboard、固定示例命中 0。
- 三页浏览器：Evals 权威 Run 入口、Lineage 无示例、Observability 无 Mock，API 失败均明确 fail-closed。
- AIP-5 E0：`tests/aip/test_aip_memory_contracts.py` + `tests/test_aip_long_memory.py` 共 15 passed；compileall 与 `git diff --check` GREEN。
- AIP-5 E1A：20 passed；Alembic 单 head `aip5_001`；compileall 与 diff check GREEN。
- AIP-5 E1B：AIP-5 与邻接 AIP-4 Store 33 passed；compileall、diff check、单 head GREEN。
- AIP-5 E2：14 个新增 PostgreSQL 集成用例；包含 E0/E1A/E1B 和相邻 AIP-3/AIP-4 的累计 41 tests passed；compileall 与 `git diff --check` GREEN。
- AIP-5 E2.1：15 个 Governance 集成用例 passed；compileall 与 diff check GREEN。
- AIP-5 E3：8 个 Retrieval/O1 Wiki 集成场景 passed；AIP-5 全链及相邻 AIP-3/AIP-4 累计 50 tests passed；compileall 与 diff check GREEN。
- 限制：本机 API health=500，未把真实事件正向回包声明为浏览器 GREEN；E4C 恢复服务后补验。

## 当前工作树

代码仓库 `m1` 已安全提交到 `81c5f82`，只允许保留四项明确排除的未跟踪内容：

- `scripts/browser-pilot-verification/`
- `services/aos-api/tests/d5e/evidence/D5-E0_G17_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G18_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G19_20260809T141944Z.json`

文档仓库大量他人改动继续排除；只精确暂存本波四份文档。

## 未完成

- 本机 aos-api health=500；E4 三页真实正向浏览器回包尚未补验，当前只声明失败关闭 GREEN。
- AIP-5 E4～E7 尚未完成。
- 本机虚拟环境没有 Ruff 模块；E0 已以 compileall、测试和 diff check 代替，不伪称 Ruff GREEN。

## 下一步

1. 核查现有 Memory/Wiki router、OpenAPI、前端 API 与页面真值，冻结 E4 文件边界和权限矩阵。
2. 注册 Canonical Memory API 并建立唯一 `apps/web/src/api/aipMemory/` SDK；服务端租户上下文和授权 markings 不接受客户端伪造。
3. 让 Governance/Wiki 页面覆盖 idle/loading/empty/error/blocked/degraded/complete，使用真实 API，并以内置浏览器逐项点击验收。

## 最后安全提交

- 代码：`aos-platform/m1@81c5f82`
- 文档：`docs/palantier/m1@c863428`；本检查点提交完成后更新。

## 断流恢复

```bash
cd /Users/ddt/work/projects/ai_agent/aos-platform && git log -8 --oneline --decorate && git status --short
cd /Users/ddt/work/projects/ai_agent/docs && git log -8 --oneline --decorate && git status --short
```

恢复时直接执行“下一步”；只有 `01/06` 与实时 Git 不一致时才追溯更早对账。
