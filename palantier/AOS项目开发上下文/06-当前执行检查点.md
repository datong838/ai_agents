# 当前执行检查点

> 更新时间：2026-08-12
> 状态：**AIP-4 E3D API IMPLEMENTING / NOT GREEN**
> 唯一真实范围：`org-org / dev-project`
> 负向隔离 canary：`dev-org / dev-project`

## 阶段

AIP-4 E3D：外部 ResearchJob Provider、Artifact、Delivery 与 Reconcile 权威事实链。

## 最后完成

- AIP-4 E3C 成本归因、真实用量质量分桶与 Capability Receipt 已封板。
- E3D 已复核 v1.2 TAOR ResearchJob 方案和现有代码，确定复用 `aip_task_run/aip_plan_revision` 与 `aip_artifact`，不建立第二套 Task 或 Artifact 真源。
- E3D 文件边界已经写回实施方案和开发清单。
- E3D 核心事实层已实现并提交：ProviderRevision、JobManifest、Submission/Event/CallbackNonce、Artifact、Delivery/Reconcile Receipt；提交 `849f40d` 已推送。

## 最后验证

- E3C：20 项 E3C/路由定向测试通过；105 项 AIP 测试收集并完成；ruff、compile、OpenAPI 与真实迁移验证通过。
- E3D 核心：6 项 migration/store/service 定向测试通过；ruff 修正通过。
- E3D 整体：API、累计回归、OpenAPI、真实迁移与双租户零伪造尚未验证，因此不得宣称完成。

## 当前工作树

代码仓库 `/Users/ddt/work/projects/ai_agent/aos-platform`，分支 `m1`：

- 修改：`services/aos-api/aos_api/routers/domain_manifest.json`
- 新增：`services/aos-api/aos_api/routers/aip_research_jobs.py`

明确排除且不得随 E3D 提交：

- `scripts/browser-pilot-verification/`
- `services/aos-api/tests/d5e/evidence/D5-E0_G17_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G18_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G19_20260809T141944Z.json`

## 未完成

- 补 Canonical API 测试与错误映射、角色和租户作用域负向用例。
- 生成路由聚合和 OpenAPI，核对 operationId/route 唯一性。
- 验证 Alembic 单 head、真实范围与 canary 新表零伪造事实。
- 完成 E3A～E3D 累计回归、代码评审、方案一致性对账和 E3D GREEN 提交。

## 下一步

1. 新增 E3D API 测试，验证 scope、role、path/body 一致性和失败关闭错误映射。
2. 生成路由聚合与 OpenAPI，更新确定性契约计数。
3. 运行 AIP 累计回归、ruff 和 compile。
4. 执行真实迁移及 `org-org/dev-project`、`dev-org/dev-project` 只读零伪造核验。
5. 形成 E3D API/聚合提交、最终对账文档提交并推送，然后进入下一检查点。

## 最后安全提交

- 代码：`aos-platform/m1@849f40d`（E3D 核心事实层，已推送 `origin/m1`）
- 文档：断流检查点机制提交 `docs/m1@639b527`；当前文件自身所在提交以 `git log -1 --oneline` 为准。

## 断流恢复命令

```bash
cd /Users/ddt/work/projects/ai_agent/aos-platform
git branch --show-current
git log -8 --oneline --decorate
git status --short

cd /Users/ddt/work/projects/ai_agent/docs
git log -8 --oneline --decorate
git status --short
```

恢复时先读本文件和 `01-当前项目状态.md`；只有两者与实时 Git 不一致时，才向更早对账文档追溯。
