---
memory_schema: aos-memory-authority-ref/v1
project_revision: AOS-000339
authority_content_hash: sha256:b5f50d127b2a013a7671def4d1af88f350b89c17fffd6463f627651f5ca3e803
---
<!-- AOS_MEMORY_PROJECTION {"content_hash":"sha256:b5f50d127b2a013a7671def4d1af88f350b89c17fffd6463f627651f5ca3e803","project_revision":"AOS-000339","schema":"aos-memory-projection/v1"} -->
<!-- AOS_MEMORY_MANAGED_BEGIN -->
## Authority 管理状态（自动生成，请勿手改）

- 项目版本：`AOS-000339`
- 当前阶段：`BI-W8 Workshop controlled commands schedules review and handoff implementation`
- 交付状态：`BI_W8_01_CONTROLLED_COMMAND_CODE_BROWSER_GREEN_VISUAL_DELTA_ROUTED_TO_FINAL_S8_NO_EXTERNAL_EFFECT_NO_RELEASE`
- 最后 GREEN 门：`BI-W8-01 backend213 frontend2284 TypeScript Vite browser1280/1440/1920 default-command-closed exact-readback-green`
- 下一门：`BI-W8-02`

### 已完成

- BI-W8-01

### 未完成

- BI-W8-02

### 下一步

- Implement BI-W8-02 periodic plan projection and controlled schedule contract without real scheduling or external effect; retain final S8 eight-menu visual plus backend hard gate
<!-- AOS_MEMORY_MANAGED_END -->
# 当前执行检查点

## 最新执行检查点（2026-08-13 · 双开发线共同起跑线）

### 阶段

`AIP_WORKSHOP_DUAL_WORKTREE_BASELINE_GREEN`

### 已完成

- 用户已单独验收的 Browser/Kitewright/D5 改动已提交并推送：`aos-platform/m1@d205427`。
- 已创建并推送 `w1-aip`、`w2-workshop`，物理工作树分别为 `aos-platform-w1-aip`、`aos-platform-w2-workshop`。
- `m1`、`w1-aip`、`w2-workshop` 同 HEAD `d205427`；三工作树 clean；三远端 ahead/behind=`0/0`。
- ADR-010 已冻结代码集成线、docs 目录单写和上下文 HEAD-CAS 协议。

### 未完成

- AIP W2-B `EvalContract + ResponsibilityPlan` 尚未在新 `w1-aip` 工作树开始编码。
- Workshop 仍只有已冻结技术基线；没有因本次切分自动获得实现授权。
- AIP/Workshop 后续首个合流波次尚未实际演练新的 m1 集成门。

### 下一步

1. 在 `/Users/ddt/work/projects/ai_agent/aos-platform-w1-aip` / `w1-aip` 恢复 AIP W2-B，编码前复核方案、Authority 与当前测试基线。
2. W2-B 每个安全子步骤更新本检查点；达到波次门后先推送 `w1-aip`，经验证合入并推送 `m1`。
3. `w2-workshop` 只在无未提交工作的安全点同步新 m1；未获明确授权前不编码工作台。

### 最后安全提交

- 代码共同起跑线：`aos-platform/{m1,w1-aip,w2-workshop}@d205427`。
- 文档：本次 ADR/状态/检查点待 CAS 提交。

## 最新执行检查点（2026-08-13 · AIP-6 A6F 后端 Canonical control）

### 阶段

`AIP6_A6F_BACKEND_CODE_GREEN_PENDING_COMMIT`

### 已完成

- A6F 方案评审—整改—复审通过，文档 `docs/m1@21a3570`。
- 新增 PostgreSQL 6/37/10 目录聚合、六角色组织安装服务、AgentInstance/Capability Catalog Canonical API。
- `/v1/aip/agents` 与 `/v1/aip/agent-registry` 不再读取 singleton；旧 agent/capability 写入口稳定失败关闭。
- 24 项后端测试、compileall、OpenAPI 与 diff check GREEN。

### 未完成

- 后端本子步骤尚待安全提交。
- TypeScript SDK、Agent Registry/Agents/Capability 三页面尚未切换。
- 尚未在真实 `org-org/dev-project` 执行六实例安装；浏览器验收尚未开始。

### 下一步

1. 精确 stage A6F 后端文件并形成安全提交，不纳入 browser/Kitewright/D5 用户改动。
2. 实现唯一严格 SDK 和三页面定义/实例/readiness 分层展示。
3. 前后端回归后启动服务，真实安装 `org-org/dev-project`，以 `dev-org/dev-project` 验证 0 instance canary，并用内置浏览器逐页验收。

### 最后安全提交

- 代码：`aos-platform/m1@8b4022b`（A6E）；A6F 后端待提交。
- 文档：`docs/m1@21a3570`（A6F 方案门）。

## 最新执行检查点（2026-08-13 · AIP-6 A6E 方案门）

### 阶段

`AIP6_A6E_IMPLEMENTATION_APPROVED`

### 已完成

- W0A 十 Capability 目录与六角色/37 Logic crosswalk 已封板。
- A6E 实时代码审计确认 Agent/Skill revision authority 可复用，Capability revision authority 缺失。
- `19-AIP-6-A6E电商领域SolutionPack实施清单与评审结论.md` 已完成评审、6 项整改和第二轮复审，结论 `APPROVED_FOR_IMPLEMENTATION`。

### 未完成

- `aip6_004` CapabilityRevision/alias authority、Store 与 binding exact 校验尚未编码。
- `solution.ecommerce.growth` 仍为 1.1.0 三同事 placeholder；六角色/37 Logic/十 Capability contribution 尚未发布。
- A6F Canonical API/SDK/UI、组织实例安装、AIP-7 Route authority 和运行 readiness 尚未完成。

### 下一步

1. additive 实现 `aip6_004` 与 Capability Store，验证迁移、append-only、runtime 只读和 exact ref。
2. 实现通用 SolutionPack publisher 与 Bundle 1.2.0 六角色/37 Logic/十 Capability contribution。
3. 在真实开发库只发布全局定义并 exact readback 6/37/10；不得写 `org-org/dev-project` 业务数据或冒充运行可用。

### 最后安全提交

- 代码：`aos-platform/m1@650981c`（A6D GREEN）。
- 文档：W0A `c559e7a`；A6E 方案门待本步骤提交。

## 子步骤检查点（2026-08-13 · A6E Capability authority GREEN）

### 已完成

- `aos-platform/m1@cd1d07a`：`aip6_004`、Capability revision/alias DTO、Store、alias resolver、Binding exact revision/hash gate。
- `aos_runtime` 已用 `SET ROLE` 实测为目录 SELECT-only；直接 INSERT 被拒。
- 29 项测试、compileall、迁移降升级、唯一 head 与 diff check GREEN。

### 未完成

- `solution.ecommerce.growth` 1.2.0 contribution 与通用发布器尚未实现。
- 六 AgentTemplate、37 SkillTemplate、十 CapabilityRevision 尚未由真实开发库 exact readback。
- A6F API/SDK/UI、组织实例和运行 readiness 仍未开始。

### 下一步

1. 以 apply_patch 增加 Bundle 1.2.0 六角色、37 Logic、十 Capability、Schema/Policy/Responsibility artifacts，并保留 D3 W03/L05。
2. 实现严格 SolutionPack parser/publisher；同内容幂等，漂移/引用缺失/alias 碰撞失败关闭。
3. 跑 Bundle/发布器测试，随后在真实开发库发布全局定义并 exact readback 6/37/10；确认零 AgentInstance/Binding/Run 副作用。

### 最后安全提交

- 代码：`aos-platform/m1@cd1d07a`。
- 文档：A6E 方案门 `docs/m1@eb4c040`；本结果待提交。

> 最新更新时间：2026-08-13  
> 最新状态：**AIP-6 A6D IMPLEMENTED_GREEN / W0A APPROVED / A6E READY**
> 唯一真实范围：`org-org / dev-project`  
> 负向隔离 canary：`dev-org / dev-project`

## 最新检查点（优先于下方历史快照）

### 最后完成

- E5D 后端知识管道 Canonical API 已完成，代码 `db27919`。
- E5D 前端严格 SDK 与知识管道控制面已完成，代码 `3492970`。
- 方案与清单状态已同步，文档提交 `5bba868`。
- 数据库单 head `aip5_003`；`org-org/dev-project` 策略 API 和空 schedule API 真实可读。
- 内置浏览器第四“知识管道”页签、7 条权威策略、0 Schedule/Run、7 个禁用动作、刷新和控制台已完成验收；证据见 `evidence/2026-08-13-AIP5-E5D-knowledge-pipelines-org-org.png`。

### 最后验证

- 后端：E5D 六组最新 44 passed；此前 E5 专项 36、AIP-5 累计 74 passed。
- 前端：定向 12 passed；全量 174 files / 2057 tests passed。
- TypeScript、Vite production build、OpenAPI 六路由与 `git diff --check`：GREEN。
- 内置浏览器：`org-org/dev-project` 页签点击、空态、禁用动作、刷新、viewport 截图与控制台 0 error 均 GREEN；full-page 截图空白记录为工具限制。
- HTTP：正向 `org-org/dev-project` 为 7/0/0；`dev-org/dev-project` 负向 canary 为 7/0/0；未注册工作区三路 403 `AUTH_TENANT_UNKNOWN`。

### 未完成

- E6/E7 仍处门禁；E6 方案复审、实时差异审计和清单冻结尚未完成。

### 下一步

1. 更新本文件、`01`、方案/清单和共享记忆 authority，形成 E5D 安全封板提交。
2. 阅读 E6 既有方案与清单，核对实时代码、路由、数据库和上下游依赖。
3. 评审并冻结 E6 精确任务/测试/浏览器验收矩阵；未过评审门不编码 E6。

### 断流恢复保障

- 活动响应中的临时工具/连接错误最多即时重试 2 次。
- 断流终止后的续跑由本机 `launchd` Watchdog 检查：首次即时恢复 2 次；仍失败则每 5 分钟恢复一次。
- Watchdog 自动唤醒后的第一条用户可见消息必须先声明：`外部 Watchdog 检测到任务中断并已恢复本任务。`
- Watchdog 发现 transcript 活跃、Lease 冲突、暂停/评审/授权门或完成状态时必须 no-op，禁止重复副作用。
- 完整规则见 [`49-2026-08-13-AIP-5-E5D与长任务断流续跑保障对账.md`](49-2026-08-13-AIP-5-E5D与长任务断流续跑保障对账.md)。

## 当前执行检查点（2026-08-13 · AIP-5 E6A 入口）

### 阶段

`AIP5_E6F_CONTRACT_REVIEW`

### 最后完成

- E6 上位方案、14-Wiki 六文档、现有 E3/E5 代码、数据库 capability、bundle schema 与外部来源边界复审完成。
- 新增 `50-2026-08-13-AIP-5-E6美妆知识包与混合检索评审清单.md`，评审结论为 `APPROVED_FOR_IMPLEMENTATION_WITH_EXTERNAL_DATA_GATE`。
- 主方案、全量清单和 14-Wiki 六份历史方案已统一权威覆盖；E5 过期的 browser-pending 状态已更正为 `IMPLEMENTED_GREEN`。
- E6 拆分为 E6A-E6F；只允许逐门实施，不得跨门用 Mock 或预测数据凑数。
- E6A 代码 `f3415a9` 已实现 `VerticalPack` knowledge export、KnowledgePackage/source inventory/entry/rollback 严格契约和 JSON Schema；212 回归、compileall、diff check GREEN。
- E6B 代码 `67cf0a8` 已实现 per-entry Receipt adapter 与确定性 Candidate Draft；43 专项 GREEN，未写真实租户数据。
- E6C 已冻结 PostgreSQL `aip_memory_search_reference` 与 `aip_memory_search_capability` 两个 tenant-scoped 投影、RLS/FORCE、revision FK、reference 可重建及 capability CAS；不存 payload/正文/embedding/PII。
- E6C 代码 `e581a99` 已落地线性 `aip5_004`、严格 reference/PII gate、authority 交叉校验、清空重建、capability CAS 及 vector capability 硬门。
- migration downgrade/upgrade、6 项专项、32 项邻接、compileall/diff check GREEN；Ruff unavailable。真实租户新表为 0 reference/0 capability。
- E6D 已冻结自然语言请求/结果、lane 状态、authority-first 二次过滤、payload 延迟解析与 Citation 一一绑定；真实 capability 空态不得产出正文。
- E6D 代码 `9bfec8c` 已实现 DTO、fulltext reference、lane 状态、canonical 二次过滤、payload 延迟解析、Citation 一一绑定及 Canonical 路由；2 API/21 邻接 GREEN。
- E6E 已冻结审核 GoldSet、≥50、六角色语义覆盖、Top-1 精确 Citation、负向泄露 RED 和 blocked 无指标规则。
- E6E 代码 `3d3ee69` 已实现纯计算 runner；12 专项、26 邻接、compileall/diff check GREEN。

### 未完成

- E6F 尚未编码；E6A-E6C GREEN，E6D `CODE_GREEN / PROVIDER_BLOCKED / DATA_BLOCKED`，E6E `CODE_GREEN / EVAL_BLOCKED`。
- E6D 默认 trusted resolver 尚未装配，真实 capability/reference 为 0/0；旧 authority API 全文件回归存在历史挂起，未作为 GREEN 证据。
- 美妆真实知识正文、审核制 50 条金标、pgvector/本地 embedding capability 均未具备；真实 GoldSet 数为 0。
- CosDNA 许可未知保持 disabled；NMPA 仍需逐项 source/version/usage-policy 登记。

### 当前工作树

- `aos-platform/m1` E6A/E6B 已安全提交 `f3415a9`、`67cf0a8`；保留 browser/Kitewright、D5 历史证据等非本波工作树改动。
- 文档仓库本步骤只修改 E6 评审清单、AIP-5 主方案/全量清单、14-Wiki 六份覆盖声明与本 `01/06`。

### 下一步

1. 复核现有 Memory Governance 页面、SDK 与可复用只读 API，冻结 E6F 状态聚合契约。
2. 展示知识包/source/license、fulltext/vector/rerank capability、GoldSet/Eval 和稳定阻断原因；真实空态不得补 Mock。
3. 跑前后端测试、TypeScript/build，并以 `org-org/dev-project` 内置浏览器完成正向空态与 `dev-org/dev-project` 负向隔离 canary。

E6B 映射已复核：每个 entry 独立 Receipt/Artifact/Candidate，批量不共享 payload hash；adapter 只生成 draft，E5 Service/Store 继续作为唯一写入口。

### 最后安全提交

- 代码：`aos-platform/m1@3d3ee69`（E6E CODE_GREEN；真实 Eval data blocked）。
- 文档：E6E 方案冻结 `321b7b9`；本结果待提交。

## 当前执行检查点（2026-08-13 · AIP-5 E6F 封板）

### 阶段

`AIP5_E6_CODE_CONTROL_GREEN_EXTERNAL_GATES_BLOCKED`

### 最后完成

- E6F 代码 `7e76886`：tenant-scoped `KnowledgeReadiness` API、严格 SDK、Memory Governance“冷启动与检索”视图。
- 修复真实浏览器暴露的嵌套 snake_case HTTP 契约漂移和长 blocker 卡片溢出。
- E6A～E6F 代码/控制面均已完成；未导入外部知识、未伪造 provider/vector/GoldSet。

### 最后验证

- 后端 E6C～E6F 邻接：11 passed；compileall GREEN。
- 前端定向：14 passed；TypeScript GREEN；Vite production build 274 modules GREEN。
- HTTP：`org-org/dev-project` 与 `dev-org/dev-project` 分别返回自身 tenant，source/reference 均为真实 0/0；后者仅为隔离 canary。
- 内置浏览器：`org-org/dev-project`、package/Eval authority unavailable、source/reference 0/0、provider 未装配、fulltext/vector/rerank unbuilt 和 blocker 全部可见；布局复验 GREEN。
- `git diff --check` GREEN。

### 未完成

- 美妆 KnowledgePackage 安装权威和经许可的真实正文未进入租户。
- trusted search provider、vector/rerank capability 未装配。
- 专家审核 GoldSet 仍为 0，E6 Eval 保持 BLOCKED。
- 不得将当前状态表述为 operational hybrid search、检索效果 GREEN 或 E6 全量业务完成。

### 下一步

1. 先评审 E7 六同事共享记忆投影，或等待外部数据/provider/GoldSet 门条件满足；不得自行跨门。
2. 若进入 E7，先冻结读模型、角色隔离、共享/私有记忆边界和浏览器验收矩阵，再编码。
3. Watchdog 断流日志时间线复盘已完成：本次为“触发成功、两次恢复均失败、人工唤醒早于第三次计划重试”，不是恢复成功；实现补强另列安全微波次。

### 最后安全提交

- 代码：`aos-platform/m1@7e76886`。
- 文档：本检查点所在待提交变更。
- 工作树继续保留 browser/Kitewright、D5 历史证据等非本波用户改动，未纳入 E6F 提交。

### Watchdog 本次复盘

- 原任务 15:32:23 断流；15:37:56、15:42:08 两次自动恢复均再次断流且没有助手可见消息。
- 15:47:39 进入 5 分钟退避，计划 15:52:39 再试；用户 15:50:57 人工唤醒，提前 1 分 42 秒。
- 先前“第二次恢复成功”的回答已纠正；旧 `last_recovered_at` 不再作为当前 episode 成功证据。
- 当前只冻结补强建议，不在 E6F 已封板后夹带修改 Watchdog 代码。

## 当前执行检查点（2026-08-13 · AIP-6 入口与 Watchdog 修复封板）

### 阶段

`AIP6_AGENT_SKILL_HANDOFF_REVIEW_FREEZE`

### 最后完成

- Watchdog 活跃 turn 防重入、episode 状态隔离和可见 final 成功判定已提交 `e095b05`。
- AIP-5 E6A～E6F 代码/控制面已封板；外部 KnowledgePackage、provider/vector、GoldSet 仍按真实门阻断。
- 最新全量审查确认 E7 依赖 AIP-6 Agent identity authority，旧“E6 后直接 E7”顺序已失效。

### 最后验证

- Watchdog 13 tests GREEN；`py_compile`、`diff --check` GREEN。
- 当前真实长 turn 连续判定 `turn-running`，没有再次启动恢复副本。
- shared memory validate GREEN，gate 可变更，Lease 空。

### 未完成

- AIP-6 AgentTemplate/Instance、SkillTemplate/Binding、CapabilityBinding、HandoffEnvelope、AgentRun 权威尚未编码。
- 六数字同事与共享专业 Agent 仍只能处于 disabled/blocked，不得宣告 operational。
- AIP-5 E7、AIP-7～AIP-10 和六同事真实场景仍待实施。

### 下一步

1. 复核 `03-AIP智能体SkillRegistry与Handoff开发清单.md` 与实时 singleton/API/页面，冻结 AIP-6 分波实施清单。
2. 先做 additive PostgreSQL authority 与 RLS，再做 Store/Service/API，最后做唯一 SDK、三页面和浏览器验收。
3. AIP-6 identity GREEN 后回到 AIP-5 E7；不得由前端或 Workshop 建立第二 Agent 真源。

### A6A 进展

- 公共契约和 6 项测试已 GREEN；文件仅限 `aip_agent_registry_contracts.py` 与对应测试。
- 未注册路由、未写数据库，旧 singleton 行为未改变。
- 当前首个未完成安全步骤：A6B additive migration、双租户 RLS/FORCE RLS 和降升级验证。

### A6B 进展

- `aip6_001` 八表 additive authority 已 GREEN，数据库最终为唯一 `aip6_001 (head)`。
- 真实开发库双租户隔离、无 scope 零可见、secretRef/check/FK、append-only 和降升级守恒均通过。
- 当前首个未完成安全步骤：A6C Store/Service；不得在 Store 前切换旧 singleton API。

### A6C 进展

- `aip6_002` durable Receipt authority、Agent/Skill Store、实例服务、exact revision/hash、Overlay、CAS 与 append-only revoke/deprecate 阻断均已实现。
- 27 项累计测试、compileall、迁移独立降升级、开发库 `aip6_002 (head)`、双租户隔离和 diff check GREEN。
- 旧 singleton API/页面未切换，A6C 无浏览器侧行为变化。
- 当前首个未完成安全步骤：详细复审 16 号八工作台/六同事/共享专业 Agent 支撑报告，形成实时差异判断；随后执行 A6D。

### W0 增量复审进展

- 16 号报告已按 A6C 实时实现纠偏；总体仍为 `REVIEWED_WITH_BLOCKERS`，不把通用 Store 等同端到端 AIP-6。
- 17 号 W0 增量方案已通过计划集成复审，明确八公共对象唯一 L0 owner、十 capability/Coordinator 裁决和 A6D→A6E→W2→E7/AIP-8/9/10 顺序。
- 当前首个未完成安全步骤：A6D exact refs 与 CapabilityBinding/AgentRun/Handoff；A6E 前必须先完成 W0A catalog/crosswalk。

### A6D 进展

- `aos-platform/m1@650981c` 已实现 `aip6_003`、CapabilityBinding、AgentRun、Handoff 与 exact Task/TaskRun/Plan/Instance refs。
- AgentInstance Overlay 以 canonical JSON + SHA-256 形成 exact ref；AgentRun 同时保存不可变实例快照，历史运行不依赖当前实例 Overlay。
- Handoff bearer 仅首次 issue 返回，PostgreSQL 仅存 hash；重放不重签。接收方按当前 exact identity 和权限解析 refs，不继承发送方权限。
- 28 项定向累计、compileall、迁移降升级、真实 Logic publication exact 查询、唯一 `aip6_003 (head)`、diff check 均 GREEN。
- 诚实阻断：AIP-7 尚无 ModelRouteRevision/PolicyRevision version/hash authority，AgentRun 只可 `queued`，进入 `running` 固定失败关闭；A6D 未注册 API/页面。
- 16 号报告已按实时事实更新；总体仍为 `REVIEWED_WITH_BLOCKERS`。

当前首个未完成安全步骤：先完成 W0A 十 capability catalog/alias/crosswalk 文档门与复审，再进入 A6E；不得跳过目录裁决硬编码六角色或 10 capability。

### 最后安全提交

- 代码：`aos-platform/m1@650981c`。
- 文档：A6C `c037c21`；W0 `4e04e06`；A6D 结果待提交。

## 当前执行检查点（2026-08-13 · W0A 封板 / A6E 入口）

### 已完成

- 新增 `18-AIP-W0A十类共享专业Capability目录别名与六角色职责Crosswalk.md`，经“评审→整改→复审”达到 `APPROVED_FOR_A6E_INPUT`。
- DS-01～DS-06 已纳入 229、工作台产品 v3、产品吸收矩阵、技术 22/23 和正式开发清单增量来源；旧 38 份仅保留历史基线身份。
- 十类共享专业 Capability 已冻结 stable ID、Schema 名、risk、父子/profile/alias、readiness；`title.generate ⊂ copy.generate`。
- 六角色 6/6、37 Logic 37/37、十 Capability 10/10、八工作台 8/8 的 crosswalk 可逐项定位。
- Coordinator 被裁决为内容官的 `production.coordination` responsibility profile；不是第七数字同事或第十一 Capability。
- 素材采集复用 Object/Evidence/Knowledge/ResearchJob/Media/License exact refs，不建立第二数据真源。
- AIP-11 已统一使用 `HandoffEnvelope`；`HandoffContext` 仅是历史输入 alias。
- 第 16 号审查报告已按 W0A 校准：P0-01/P0-04/P0-05/P0-06 方案差异归零，总体仍为 `REVIEWED_WITH_BLOCKERS`。

### 最后验证

- W0A 首轮 7 个 blocker 全部有整改映射，第二轮 7/7 退出门 PASS。
- `git diff --check` GREEN；稳定 ID、别名、职责和依赖顺序的定向检索一致。
- 本步骤只改方案/清单/检查点，无代码、数据库、API 或页面行为变化，因此不需要浏览器验收。

### 未完成

- A6E 尚未把六角色、37 Logic、十 Capability contribution 和职责模板发布为 exact revision。
- A6F Canonical API/Principal/唯一 SDK/Registry 与页面切换尚未完成。
- AIP-7 exact ModelRouteRevision/PolicyRevision authority 尚未完成，AgentRun 进入 `running` 继续失败关闭。
- W0B 八公共对象 authority ADR、W2 编码、AIP-5 E7、AIP-8～AIP-10 和八工作台运行验收未完成。
- E6 外部知识、trusted provider、专家 GoldSet 和向量/rerank 门仍 blocked。

### 下一步

1. 复习 AIP-6 主方案、03 清单、W0A 和现有 A6A～A6D Store/DTO，冻结 A6E 具体文件、manifest、发布顺序和测试矩阵。
2. 在 `aos-platform/m1` 最小实现 `solution.ecommerce.growth`：六角色、37 Logic、十 Capability contribution、兼容 alias 和职责模板，全部 exact revision/hash 可回读。
3. 验证目录唯一、alias 不重复发布、缺 Schema/Eval/Tool/Data/License/Route 时 blocked、双租户隔离和历史可解析；完成后更新 01/06 并独立提交。
4. A6E GREEN 后进入 A6F；不得提前实现 W0B/W2、E7 或 AIP-9。

### 当前工作树

- `aos-platform/m1`：A6D 功能提交已封板；browser/Kitewright、D5 证据等用户/其他波次改动仍保留未纳入。
- `docs/m1`：存在大量用户/其他开发改动；W0A 只精确暂存本文所列 AIP 方案、清单和 01/06。

### 最后安全提交

- 代码：`aos-platform/m1@650981c`。
- 文档：A6D `b571d34`；W0A 文档提交在本段封板后回填 Git log，不以暂存前哈希推断。

## 历史检查点（2026-08-12，保留审计）

> 更新时间：2026-08-12
> 状态：**AIP-5 E0～E4 IMPLEMENTED_GREEN / PAUSED_BEFORE_E5**
> 唯一真实范围：`org-org / dev-project`
> 负向隔离 canary：`dev-org / dev-project`

## 阶段

AIP-5 E4 Canonical API、唯一严格 SDK、Memory Governance/Wiki 页面与浏览器验收已封板。按用户要求，本波完成后暂停，不进入 E5。

## 已完成

- E4A 独立 `/v1/aip/memory-authority`：Candidate/Event/Memory 读、受控 approve/promote、KnowledgeQuery；代码 `17817db`。
- E4B 唯一 `apps/web/src/api/aipMemory/` SDK；代码 `cc8f216`。
- E4B.1 深层严格 tenant/source/governance/Event/Item/Revision/Citation 解析和 events/approve/promote 方法；代码 `8b08792`。
- E4C `MemoryGovernancePage`、AIP 导航、Candidate/正式 Memory/Knowledge Query 三视图、O1 Wiki 治理与 Citation 入口；代码 `038b9da`。
- O1 Wiki Draft 写链保持不变；无静态 Candidate、Memory 或知识结果回填。

## 最后验证

- 后端 AIP-5 contracts/store/governance/retrieval/API：38 passed。
- 前端 E4 定向：4 文件、21 tests passed。
- 前端全量：174 文件、2053 tests passed。
- TypeScript：GREEN。
- Vite production build：274 modules transformed，GREEN。
- `git diff --check`：GREEN。
- 内置浏览器：`org-org/dev-project` 正确；导航、三视图、必填阻断、API 500 错误态、Wiki 治理入口通过；控制台错误 0。
- 限制：本机 `aos-api health HTTP 500`，未把真实正向 API 回包声明为浏览器 GREEN。
- Prime Agent：旧 supervisor registry ownership 漂移已安全恢复；现由用户级 `launchd` 调用安全启动器，凭据仅从 `~/.prime/agent/auth.json` 读取。supervisor/worker 命令行无 `AGNES_API_KEY` 或 `--api-key`，认证文件权限 `0600`；全局记忆 `aos-milestones/current-delivery/aip-full-checklists` 已同步到 `AOS-000008`（v25/v27/v21）并回读 revision/hash/content hash 一致。
- 共享记忆：`docs/多对话共享记忆机制.md` v1.2 与 Phase 0～5 已实现；authority=`AOS-000008`，14 tests、9 Schema、DRIFTED 失败关闭、Delivery Receipt、Lease 释放、Prime/WorkBuddy/01/06 强一致投影及 900 秒只读健康守护均 GREEN。Codex 原生吸收是异步边界，诚实报告 `PENDING_ASYNC`，不冒充同步完成。
- Prime 写入闭环：AOS 直连适配器和 Prime 原生 Harness CRUD 共用 `.aos-memory-sync.lock`；具备版本单调账本、`0600` 备份、mtime 并发检测、原子替换、陈旧快照拒绝和写后回读/回滚。实际运行时已重装并通过 `launchd` 重启。

## 当前工作树

代码仓库 `m1` 的 AIP-5 E4 变更已安全提交到 `038b9da`。最终复核时工作树另有下列非本波、未纳入提交的 browser/Kitewright 与历史证据改动；后续接续必须先重新读取 `git status`，不得把它们误并入 AIP：

- `plugins/browser/browser-pilot/` 删除项
- `plugins/browser/control-existing-chrome/`
- `plugins/browser/kitewright/`
- `scripts/d5e2_browser_verify.py`
- `docs/228-浏览器控制双通道与Kitewright更名方案.md`
- `scripts/browser-pilot-verification/`
- `services/aos-api/tests/d5e/evidence/D5-E0_G17_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G18_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G19_20260809T141944Z.json`

文档仓库存在大量他人改动；本波只精确暂存四份 AIP-5/上下文文档，不触碰其他文件。

## 未完成

- AIP-5 E5～E7 编码与验证。
- 本机 API health=500 的真实正向浏览器回包补证。
- E5 七知识管道的 Scheduler/Run/Receipt/checkpoint 实施边界尚待下一轮复审冻结。
- AGNES 旧 Key 轮换需要在凭据提供方完成；由于它曾进入历史启动命令/输出，不能仅靠本地移除导出视为已轮换。

## 下一步

当前暂停。用户处理新事项后恢复时：

1. 先读取本检查点、Git log/status 与 AIP-5 方案实时状态。
2. 评审并细化 E5 七知识管道清单，冻结 Scheduler/Run/Receipt/checkpoint、幂等/暂停/恢复/告警和外部 Harness 只进 Candidate 边界。
3. 取得当轮继续指令后再编码；不得跳过方案门直接进入 E5。

记忆体系已独立封板，不再作为下一开发波次。新对话先运行 `scripts/memory/memory-validate --json`、`memory-status --json` 与 `memory-gate --json`；若强一致投影 RED，先修复记忆健康再进行依赖该事实的状态变更。

## 最后安全提交

- 代码：AIP 功能基线 `aos-platform/m1@038b9da`；Prime Agent 无 Key 安全启动 `b17e169`；共享记忆最终实现 `ca5a042`；Prime 原生 Harness 并发写保护 `ee254c81`。
- 文档：共享记忆 v1.2、Phase 0～5、Prime 确定性适配器和 `AOS-000008` authority/Receipt 封板见 `11b7f76` 及本文件所在最新提交。

## 断流恢复

```bash
cd /Users/ddt/work/projects/ai_agent/aos-platform && git log -8 --oneline --decorate && git status --short
cd /Users/ddt/work/projects/ai_agent/docs && git log -8 --oneline --decorate && git status --short
```

恢复时直接执行“下一步”；只有 `01/06` 与实时 Git 不一致时才追溯更早对账。

## 当前执行检查点（2026-08-13 · AIP-6 A6E GREEN）

### 阶段

`AIP6_A6E_IMPLEMENTED_GREEN / PAUSED_BEFORE_A6F_PLAN_FREEZE`

### 已完成

- CapabilityRevision/alias 全局不可变 authority：`aos-platform/m1@cd1d07a`。
- `solution.ecommerce.growth@1.2.0` 六角色/37 Logic/十 Capability 严格发布：`aos-platform/m1@8b4022b`。
- 数据库精确回读 6/37/10；alias 唯一；重复发布幂等；租户 runtime 表零副作用。
- 16 号报告已按 A6E 事实复审：定义门关闭，运行门仍受 A6F/AIP-7/W0B 阻断。

### 最后验证

- A6A～A6E + Asset Registry：38 passed。
- 发布脚本连续两次：6/37/10。
- PostgreSQL：agents=6、skills=37、capabilities=10、available_capabilities=0、title_alias=1。
- compileall、JSON、`git diff --check` GREEN；Ruff 在当前锁定环境不可用。

### 未完成

- A6F Canonical API/Principal、组织安装/实例化、唯一 SDK/UI、内置浏览器与双租户验收。
- AIP-7 ModelRouteRevision/Provider/Eval readiness；不得把 blocked 目录变成在线状态。
- W0B/W2 八公共生产契约、AIP-5 E7、AIP-8/AIP-9/AIP-10。

### 下一步

1. 生成并评审 A6F 实施清单，先核对现有 singleton 路由、前端页面和 Principal 机制。
2. 只通过 Canonical API 安装 `org-org/dev-project` 六个 exact AgentInstance；`dev-org/dev-project` 只作隔离 canary。
3. SDK/UI 分层显示 template/instance/binding/readiness；所有 Provider/Eval/Route unknown 保持 blocked。

### 最后安全提交

- 代码：`aos-platform/m1@8b4022b`。
- 文档：本检查点及 16/17/19 A6E 实时复审待精确提交。
- 无关 browser/Kitewright、D5 与文档仓库既有改动继续保留，未纳入 A6E 提交。

## 当前执行检查点（2026-08-13 · AIP-6 A6F GREEN）

### 阶段

`AIP6_A6F_IMPLEMENTED_GREEN / SIX_AGENT_INSTANCES_INSTALLED / RUNTIME_READINESS_BLOCKED`

### 最后完成

- Canonical 后端：`aos-platform/m1@556e92c`。
- Canonical SDK/UI 与路由收口：`aos-platform/m1@914cff2`。
- `org-org/dev-project` 已安装 6 个六数字同事 AgentInstance；`dev-org/dev-project` 为 0。
- 目录/实例/能力页面全部读取 PostgreSQL authority；旧 capability singleton 页面退出正式路由。

### 最后验证

- 后端 24 项邻接测试、compileall、OpenAPI：GREEN。
- 前端 parser 3 项、TypeScript、Vite production build 276 modules：GREEN。
- 浏览器：6/6 实例、37 evaluated skill definition、10 blocked capability、0 runnable；三页面 console error=0。
- 数据库：`org-org/dev-project` AgentInstance=6、SkillBinding=0、CapabilityBinding=0、AgentRun=0；`dev-org/dev-project` AgentInstance=0。
- 旧 `CapabilityPage.test.ts` 脱离项目 jsdom 配置单独执行时有 1 项 `localStorage is not defined`；它不是本波新页面回归，也未被误报为 GREEN。

### 未完成

- AIP-7 exact ModelRouteRevision/PolicyRevision、Provider、EvalPack 与 operational Capability readiness。
- W0B/W2：TaskBrief、EvidenceBundle、EvalContract、ResponsibilityPlan、Stage、Artifact family、ReviewIssue/ReturnDecision、ImpactPreview 的唯一 L0 authority/typed projection。
- AIP-5 E7 六同事个人记忆/共享投影；AIP-8/9/10 与八工作台反向验收。
- E6 真实知识包、授权正文、trusted provider、专家 GoldSet 与 vector/rerank 外部门。

### 下一步

1. 深审第 16 号报告后，冻结 W0B authority ADR 和 W2 全量实施清单；先方案评审，不直接编码。
2. 同步复核 AIP-7 方案与现有 Model Router/Provider/Usage 代码，冻结 exact route authority 的最小增量。
3. 两个门均通过后，才允许创建 Skill/Capability Binding、把实例由待配置推进到可运行，并以真实 Task/Run/Receipt/浏览器验收。

### 当前工作树

- `aos-platform/m1`：A6F 已提交；browser/Kitewright、D5 证据等既有无关改动仍保留未纳入。
- `docs/m1`：本检查点、01、16、20 精确更新；大量用户/其他开发改动继续保留。

### 最后安全提交

- 代码：`aos-platform/m1@914cff2`（后端前序 `556e92c`）。
- 文档：本检查点封板提交完成后回填 Git log；不得以提交前哈希推断。

## 当前执行检查点（2026-08-13 · W0B 方案门通过）

### 阶段

`W0B_APPROVED_FOR_W2_IMPLEMENTATION / PAUSED_BEFORE_W2_A_CODE`

### 已完成

- 第 16 号报告已基于 A6F 实时状态深审并校准，结论为 AIP 架构无需重设计。
- 21 号 W0B ADR 已冻结八公共对象 owner、复用/投影/additive 边界、四组迁移、Canonical API 和 W2-A～D 顺序。
- 首轮 6 项 blocker/high 均有整改，第二轮 6/6 退出门 PASS。

### 未完成

- W2-A～D 尚未编码。
- AIP-7 exact route/provider/eval 与 operational binding 尚未实现。

### 下一步

1. W2-A 只实现 TaskBriefRevision 与 EvidenceBundleRevision authority；不启动 Run。
2. 完成迁移/Store/API/测试后，再加唯一严格 SDK 与最小控制面浏览器验收。
3. 每个子步骤更新 01/06 并提交；`dev-org/dev-project` 保持 0 canary。

### W2-A Store 子步骤

- 最后完成：`aos-platform/m1@4288591`，包含 `w2_001`、DTO、Store 与 3 项测试。
- 最后验证：compileall、3 passed、独立 downgrade/upgrade、`w2_001 (head)`、测试数据清理、diff check GREEN。
- 未完成：Canonical API/错误映射/OpenAPI、唯一 SDK/UI、真实浏览器证据。
- 下一步：实现 `/v1/aip/production-contracts` Brief/EvidenceBundle 路由；不得创建 Run 或测试业务数据。

### W2-A Canonical API 子步骤

- 阶段：`W2A_API_CODE_GREEN / PENDING_SDK_UI`。
- 最后完成：`aos-platform/m1@daeb9d3`，新增 6 条 Brief/EvidenceBundle Canonical API、Principal tenant、幂等/CAS/依赖错误映射和 API 回归。
- 最后验证：Store/API 6 passed；compileall、OpenAPI 6 paths、跨租户 Brief/Bundle 404、exact Evidence hash、测试数据精确清理与 diff check GREEN。
- 未完成：TypeScript strict SDK、W2-A 控制页、浏览器验收和真实最小 Brief/Bundle 业务证据。
- 下一步：先审计现有前端 API/路由/导航范式，实施 SDK 和最小控制页；真实租户如无合适 Task/Evidence 则诚实展示 empty/blocked，不造业务 Mock。
- 当前工作树：只提交 W2-A API；browser/Kitewright、D5 证据与脚本等用户/其他开发改动继续保留。

## 当前执行检查点（2026-08-13 · W2-A 全链路封板）

### 阶段

`W2A_IMPLEMENTED_GREEN / REAL_DATA_EMPTY / W2B_AND_AIP7_NEXT`

### 已完成

- `aos-platform/m1@ab762ae`：W2-A migration/Store/API/strict SDK/UI/browser 全链路完成。
- Canonical collection/read/write API、exact Brief/Evidence 引用、CAS/Receipt、RLS/FORCE RLS 与跨租户失败关闭已经闭合。
- `org-org/dev-project` 控制页读取 PostgreSQL 真值；真实 Task/Brief/Evidence 缺失时显示 empty/blocked，不创建 Run、不生成 Mock。

### 最后验证

- 后端 6 passed；前端邻接 5 passed；compileall、OpenAPI、TypeScript、Vite 279 modules、diff check GREEN。
- 内置浏览器：栖月汇组织/默认工作区、0 Brief/0 Bundle、动作禁用、刷新成功、console 0 error。
- 数据库：`org-org/dev-project` 与 `dev-org/dev-project` 的 W2-A revision/head/bundle/receipt 均为 0；canary 未被污染。

### 未完成

- W2-B EvalContract/ResponsibilityPlan、W2-C Stage/Artifact relation、W2-D Review/Return/ImpactPreview 尚未编码。
- AIP-7 尚无 immutable exact ModelRouteRevision/PolicyRevision、Provider/Eval readiness authority；AgentRun 仍不得进入 `running`。
- E7、AIP-8/9/10 与外部知识/provider/GoldSet 门继续 blocked。

### 下一步

1. 深审第 16 号全量支撑报告并按 W2-A 实时事实校准 P0-02。
2. 基于现有 mutable Model Router/Provider 代码冻结 AIP-7 additive exact authority 优化方案，禁止重设计 AIP 或建立第二真源。
3. 方案复审通过后继续 W2-B；每个子步骤仍独立测试、浏览器、上下文和提交。

### 当前工作树与最后安全提交

- 代码安全提交：`aos-platform/m1@ab762ae`；browser/Kitewright、D5 历史证据等无关改动继续保留。
- 文档仅精确修改本文件和 `01-当前项目状态.md`；其余大量用户/其他开发改动不纳入。

## 当前执行检查点（2026-08-13 · 第 16 号复审与 AIP-7 方案门）

### 阶段

`AIP7_INCREMENTAL_PLAN_APPROVED / A7_0_NEXT / EXTERNAL_PROVIDER_BLOCKED`

### 已完成

- 第 16 号全量支撑报告按 W2-A 校准，P0-02 不再误报 Brief/EvidenceBundle 缺失。
- 22 号 AIP-7 增量方案经 8 项风险整改与第二轮 8/8 复审通过。
- ownership 冻结：AIP-7 exact Provider/Model/Route/RuntimePolicy；复用 AIP-4 Eval/Usage 与 AIP-6 Run/Binding。
- 栖月汇模型链真实 0；测试组织历史 4/12/6/5 明确禁止自动回填。

### 未完成

- A7-0～A7-6 尚未编码；AgentRun queued→running 继续由现有失败关闭保护。
- W2-B～D、E7、AIP-8/9/10 仍未完成。
- 外部 Provider 插件、secretRef、网络/数据出境、真实 Eval、价格和预算尚未授权或就绪。

### 下一步

1. A7-0：新增 strict exact DTO/错误码及纯合同测试，不注册路由、不写数据库。
2. A7-1：additive migration/Store/Receipt/RLS/FORCE RLS，禁止修改历史 `228ti5b1models`。
3. 逐门继续 resolver/API/AgentRun/capacity/UI；没有外部门时保持 blocked。

### A7-0 子步骤

- 阶段：`A7_0_CONTRACT_CODE_GREEN / A7_1_NEXT`。
- 最后安全提交：`aos-platform/m1@6c80da1`。
- 最后验证：A7-0+A6 21 passed，AIP-4 Eval 邻接 9 passed；compileall/diff check GREEN。
- 未完成：migration/Store/API/resolver/Run/capacity/UI；外部 Provider/Eval 门未解除。
- 下一步：只做 additive `aip7_001` 与 Store/Receipt/RLS/FORCE RLS，不碰旧 mutable 表或真实业务数据。

## 当前执行检查点（2026-08-13 · 第 16 号全量关闭方案门）

### 阶段

`REPORT16_FULL_CLOSURE_PLAN_APPROVED / A7_1_NEXT`

### 已完成

- 新增 23 号统一实施方案，将第 16 号报告的 P0/P1 全部绑定到具体代码波次和证据门。
- 确认新增开发不止 AIP-7，还包括 W2-B～D、E7、BIND-1、AIP-8/9/10 与八工作台反向验收。
- 首轮 9 项风险已整改，第二轮 8/8 复审通过；外部 Provider/知识/平台/多媒体门未被代码授权解除。

### 未完成

- A7-1～A7-6、W2-B～D、E7、BIND-1、P8/P9/P10、W4 尚待逐波编码与验收。
- `org-org/dev-project` 仍无可运行 Provider/Route/Eval/Binding；AgentRun 必须继续失败关闭。

### 下一步

1. A7-1：additive revision/head/health observation/Receipt migration 与 PostgreSQL Store。
2. Store 通过后进入 resolver/API，再实施 W2-B；每个子步骤独立测试、上下文和提交。
3. 最终按 23 号方案逐项回写第 16 号报告，不能以文档通过替代代码完成。

### 当前工作树与最后安全提交

- 代码最后安全提交：`aos-platform/m1@6c80da1`。
- 文档将精确提交 16/23/01/06；仓库其余大量用户/其他开发改动继续保留。

### A7-1 子步骤

- 阶段：`A7_1_AUTHORITY_STORE_GREEN / A7_2_RESOLVER_NEXT`。
- 最后安全提交：`aos-platform/m1@eedd6b3`；开发库迁移 `aip7_001 (head)`。
- 已完成：四类 immutable revision/head、health observation、Receipt、RLS/FORCE RLS、canonical hash、CAS、幂等与 exact 依赖链。
- 最后验证：24 passed；迁移独立 downgrade/upgrade、compileall、diff check、双租户零污染 GREEN。
- 未完成：resolver/API、AgentRun transition/capacity/UI；外部 Provider/Eval/secret/network/budget 门仍 blocked。
- 下一步：实现纯读取 A7-2 resolver 与 blocked/unknown/ready 失败关闭测试；不得写真实模型配置或借用测试组织旧数据。

### A7-2 子步骤

- 阶段：`A7_2_RESOLVER_GREEN / A7_3_API_NEXT`。
- 最后安全提交：`aos-platform/m1@8878ee1`。
- 已完成：Route/RuntimePolicy/Eval/Model/Provider/Health exact readiness 解析；无任意默认回退或 Provider 调用。
- 最后验证：20 passed；compileall、target diff check、真实租户 missing route 失败关闭 GREEN。
- 未完成：Canonical API、旧写关闭、AgentRun/capacity/UI；真实 Provider/Route/Eval 仍为 0。
- 下一步：注册 A7-3 Principal-scoped API 和稳定错误码；随后才进入 W2-B 或 A7-4，不能绕过外部门启动 Run。

### A7-3 子步骤

- 阶段：`A7_3_CANONICAL_API_GREEN / W2_B_NEXT / A7_4_BLOCKED_BY_DEPENDENCIES`。
- 最后安全提交：`aos-platform/m1@b24e677`。
- 已完成：Canonical exact API、Principal scope、Idempotency-Key/If-Match、stable errors、两套 legacy write 410 关闭、旧读兼容。
- 最后验证：15 passed；OpenAPI/compileall/diff check，org/dev 双 scope missing-route 失败关闭 GREEN。
- 未完成：W2-B～D、A7-4～6、E7/BIND/P8/P9/P10/W4；外部 Provider/Eval/secret/network/budget 仍 blocked。
- 下一步：执行 W2-B EvalContract/ResponsibilityPlan migration/Store/API，再回到 AgentRun/BIND 组合门；不以 A7-3 API 完成冒充 runnable。

## 当前执行检查点（2026-08-14 · Watchdog 渐进退避封板）

### 阶段

`WATCHDOG_PROGRESSIVE_BACKOFF_CODE_AND_CONTROLLED_CANARY_GREEN / AIP_BUSINESS_PAUSED`

### 已完成

- `aos-platform/m1@75ff587`：单窗口单次恢复；瞬时故障按 5/10/15/20……分钟线性退避，最大 60 分钟；确定性本地配置错误才停止。
- 生产配置已移除三次失败熔断参数，显式复位旧状态并重新加载 `com.aos.codex-long-task-watchdog`。

### 最后验证

- Watchdog 单测 23 passed；`py_compile`、真实 transcript 只读回放、命令/精确目录预检、`git diff --check` GREEN。
- 禁用态 `total_attempts=43→43`；启用后连续三个巡检周期为 `turn-running`，`total_attempts=43`、失败计数和退避均为 0。
- 没有修改 AIP/工作台业务代码、数据库、迁移和 `org-org/dev-project` 真实数据。

### 未完成

- 真实 ChatGPT 服务端长故障尚未发生；5/10/15……分钟序列和恢复后新增可见 final 仍需下一真实 episode 验证。
- AIP 业务开发本轮暂停，不能由 Watchdog 封板推导为完成或授权。

### 下一步

1. 恢复 AIP 前核对 `w1-aip` 当前 SHA、Git 状态、最近 Delivery Receipt、迁移/真实数据库 Lease 和记忆门。
2. 若事实分类为 `CONTINUE`，从首个未完成安全步骤继续；否则按门禁 no-op 并报告。
3. 下一次真实断流后按当前 episode 证据复盘，不读取历史 `last_recovered_at` 冒充成功。

## 当前执行检查点（2026-08-14 · W2-C 串行集成封板）

### 阶段

`W2C_IMPLEMENTED_GREEN_WITH_OPERATIONAL_FINDING / W2D_REVIEW_NEXT`

### 已完成

- W2-C StageArtifact / ReviewDecision / ArtifactRelation 权威 Store、Canonical API、SDK 和真实控制页已完成。
- 路由 manifest 已补齐 AIP W2-C/AIP-7 新路由，生成器和运行时双向校验 GREEN。
- 增加本地浏览器验收的 Cron 显式关闭开关，仅在验收进程使用，生产默认仍为真 Cron 开启。
- 安全代码提交 `f8d5e1e`，最终证据提交 `a994f03`。

### 最后验证

- 后端 `30 passed + 2 subtests`，前端 `3 files / 13 tests`，TypeScript、production build、compileall、diff check GREEN。
- 内置浏览器：`org-org/dev-project` 七类真实空态、禁用门、刷新、console 0 error GREEN；`dev-org/dev-project` 负向 canary 三类 200/空。
- 首次验收产生的 P12 失败调度记录已如实保留；Cron 隔离后复验约 10 分钟，total=85 且 latest id 不变。

### 未完成

- W2-D ImpactPreview / StartDecision authority、API、SDK、UI 与真实启动门尚未评审/编码。
- 真实 Provider / secretRef / Eval / health / budget 仍未就绪，AgentRun 继续失败关闭。
- P12 `PIPELINE_NOT_FOUND` 是运维线待排查项，不影响 W2-C 权威合同封板，但不得从证据中移除。

### 当前工作树

- `aos-platform/m1@a994f03`：W2-C 代码与 Receipt 已提交，待 CAS 同步和远端推送。
- `aos-platform-w1-aip/w1-aip@f8d5e1e`：仅保留受保护的未跟踪 Watchdog recovery-blocked Receipt；待 m1 封板后 fast-forward。
- `aos-platform-w2-workshop`：未读取、未修改、未同步，由工作台对话独占维护。

### 下一步

1. 以 `AOS-000019` 为 expected revision 完成 `AOS-000020` authority CAS，同步 01/06/Prime。
2. 释放 W2-C 四个 Lease，再检查 memory gate/status/validate。
3. 将 `w1-aip` fast-forward 到 m1 最终提交并推送 m1/w1-aip；不同步、不覆盖 `w2-workshop` 未提交工作。
4. 新一波只先复审 W2-D 实施清单和外部门，未授权前不编码。

## 当前执行检查点（2026-08-14 · AIP-5 E7-0 契约冻结）

### 阶段

`AIP5_E7_0_CONTRACTS_GREEN / E7_1_MIGRATION_NEXT`

### 已完成

- E7 全量清单两轮评审 8/8 PASS；personal/shared 都是 exact Memory revision 的 reference-only 投影。
- `aos-platform/m1@5261257` 冻结 Projection/Event/Receipt、可信 Exposure 与 ImprovementObservation DTO。
- exact AgentInstance、显式 shared recipient、最小披露、unknown 不伪造指标和 measured/estimated exact evidence 门均由合同失败关闭。

### 最后验证

- `20 passed`：E7-0、AIP-6 Agent Registry 与 AIP-5 Memory pipeline 邻接合同；`w1-aip`、`m1` 双线结果一致。
- `compileall`、`git diff --check` GREEN；本波无迁移、数据库写、Provider、AgentRun、真实数据或浏览器副作用。
- Delivery Receipt：`.evidence/aip/2026-08-14-e7-0-agent-memory-projection-contracts-delivery-receipt.json`。

### 当前工作树与权威

- `m1@5261257754af07093adf93de7a58f00060adf9c7` 已串行快进并复测；`w1-aip` 同提交，仅保留受保护的 Watchdog 未跟踪 Receipt。
- Authority：`AOS-000026`；`last_green_gate=AIP5_E7_0_AGENT_MEMORY_PROJECTION_CONTRACTS_GREEN`。
- Workshop 当前活动 Lease 只覆盖其证据文档并排除 migrations/真实数据，与 E7-0 无冲突；未读取或覆盖 `w2-workshop` 未提交工作区。

### 未完成

- E7-1 migration/RLS/append-only；E7-2～E7-5 Store、retrieval、evaluator、API、SDK/UI、浏览器封板。
- “越用越聪明”真实业务效果仍无足量 exposure 与可比 cohort，禁止由合同 GREEN 推导为效果 GREEN。

### 下一步

1. 只读确认当前 Alembic 唯一 head 和 migration Lease。
2. 取得唯一 E7-1 migration Lease，实施 additive 六表、零回填、RLS/FORCE RLS、不可变审计与空表 downgrade 门。
3. 完成 upgrade/downgrade、双租户、无 scope、append-only 测试和 Receipt，再进入 E7-2。

## 当前执行检查点（2026-08-14 · AIP-5 E7-1 迁移封板）

### 阶段

`AIP5_E7_1_MIGRATION_GREEN / E7_2_STORE_SERVICE_NEXT`

### 已完成

- `m1@acc2ed75660b4818e81a00a3f2f91153e6497c63`：`aip5_005` 六表 authority、RLS/FORCE RLS、append-only、Projection CAS 和空表降级门。
- 隔离库 12 项迁移/合同验证 GREEN；共享开发库完成空表降级/升级并保持六表 0 行。
- Authority `AOS-000027`；E7-1 migration Lease 已完成并释放。

### 未完成

- E7-2 Store/Service/Receipt/CAS；E7-3～E7-5 retrieval/evaluator/API/SDK/UI/browser。
- 当前 schema 不等于功能可用，更不等于六同事已经产生真实改进。

### 下一步

1. 取得非迁移 E7-2 精确 Lease，复用 `aip5_005`，不得修改 Alembic。
2. 实现 exact instance/memory 校验、reference-only 投影事务写入、recipient/event/receipt 和 CAS 状态机。
3. 用隔离测试数据验证幂等重放、hash 漂移、重启回读、跨租户与依赖撤回失败关闭。

## 当前执行检查点（2026-08-14 · AIP-5 E7-2 Store/Service 封板）

### 阶段

`AIP5_E7_2_STORE_SERVICE_GREEN / E7_3_RETRIEVAL_EXPOSURE_NEXT`

### 已完成

- `m1@65aad29d70a9d830284fe848e869f5fb9084a443`：reference-only personal/shared Projection Store、显式 recipient、exact AgentInstance/Memory 校验、Event/Receipt 和状态 CAS。
- create 同键重放、payload drift 拒绝、重启回读、共享 recipient 保留、suspend/reactivate、过期/实例漂移失败关闭和跨租户负向 canary 已验证。
- Authority 已由 `AOS-000027` 完成两次 CAS：`AOS-000028` 记录实现事实，`AOS-000029` 校准完整 Git SHA；E7-2 Lease 已完成并释放。

### 最后验证

- 累计 `24 passed`；Ruff check/format、Python compile、`git diff --check` GREEN。
- 共享开发库 current=`aip5_005`；Projection/Recipient/Event/Receipt/Exposure/Observation 六表仍为 `0/0/0/0/0/0`。
- Delivery Receipt：`.evidence/aip/2026-08-14-e7-2-projection-store-service-delivery-receipt.json`。

### 当前工作树

- `aos-platform/m1` 与 `aos-platform-w1-aip/w1-aip` 代码目标均为 `65aad29`；w1 只保留受保护的未跟踪 Watchdog Receipt。
- Workshop 当前活动 Lease 仅覆盖其方案/证据文档并排除业务代码、迁移和 m1，不读取、不覆盖其未提交工作区。

### 未完成

- E7-3 Retrieval Adapter、可信 Exposure、撤回影响；E7-4/E7-5 evaluator/API/SDK/UI/browser。
- 当前只有代码与权威链 GREEN；真实六同事“越用越聪明”仍无足量 Exposure 和可比 cohort，禁止宣告效果完成。

### 下一步

1. 取得非迁移 E7-3 精确 Lease，保持与 Workshop scope 隔离。
2. 读取时重新验证 owner/recipient exact AgentInstance、Memory/source freshness、marking/applicability；失败即返回稳定 blocker。
3. 仅在 AgentRun 接受 exact citation context 后追加 Exposure，并提供不含 payload 的 revoke impact。

## 当前执行检查点（2026-08-17 · R1-04～R1-08 运行链封板）

### 阶段

`WKS_DEP_0B_R1_OPERATIONAL_CHAIN_GREEN_AT_CUTOFF / R2_REVIEW_NEXT`

### 已完成

- `w1-aip` 三个安全提交 `4a1158b`、`a3876f3`、`f9d0efa` 已 fast-forward 到 `m1@f9d0efa`。
- R1-01～08 exact authority、20-case Eval、双 Gate、3 次 Provider probe、Health、Model、Route、Price/Budget/Quota、Capacity 均完成真实回读。
- Capacity 两成功/第三阻断/两释放、hash drift、跨租户 canary、Health/Gate/Price expiry 均完成反证验证。
- Authority CAS 到 `AOS-000090`；本波 Lease 已释放。

### 最后验证

- w1-aip：targeted `28 passed`，邻接 `62 passed`；m1 累计 `82 passed`。
- compileall、diff check GREEN；`org-org/dev-project` READY at cutoff；`dev-org/dev-project` exact assets 0/0/0。
- Binding/SkillBinding/AgentRun 前后 0/0/0；GoldSet 正文未进入 Git。

### 未完成

- R2 Binding 与 runnable Agent 组合评审及实现仍未授权。
- Provider Health 15 分钟后自动失效；任何后续启动都必须先刷新 3 次探测并重新 resolve。
- 当前证据是时间点运行就绪，不等于长期 SLA 或生产可用。

### 下一步

1. 复审 R2 exact SkillBinding/CapabilityBinding 组合与六 Agent readiness；
2. 通过独立审批和 Lease 后刷新 Health，再创建最小 Binding；
3. 仅在全链重新 READY 时执行一个有界 AgentRun canary，并把结果交付 Workshop 依赖门。

### 最后安全提交

- `aos-platform/m1@f9d0efaacceebbc29c16b5ad2be96c82e23462a0`
- 证据：`.evidence/aip/2026-08-17-r1-04-r1-08-runtime-ready.json`
- Receipt：`memory/receipts/deliveries/AIP-WKS-DEP-0B-R1-EVAL-RUNTIME-OPERATIONAL-CLOSE.json`
