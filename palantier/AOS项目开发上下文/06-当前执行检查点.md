# 当前执行检查点

> 更新时间：2026-08-12
> 状态：**AIP-4 E4B IMPLEMENTED_GREEN / E4C READY_TO_IMPLEMENT**
> 唯一真实范围：`org-org / dev-project`
> 负向隔离 canary：`dev-org / dev-project`

## 阶段

AIP-4 E4 页面真实化；当前子步 E4C：Evals/Publication 严格 SDK 收口与最终封板。

## 已完成

- E4A 新增唯一 `apps/web/src/api/aipEvidence/` SDK 基座和 canonical operations。
- LineageEvent 对 root、连续 sequence、source tuple、hash 与 quality 严格解析，异常响应失败关闭。
- 决策谱系页切换 root type + root id 权威查询，删除固定 Trace ID、固定六段、固定标题和失效治理探针。
- E4B 扩展 `aipEvidence`，严格解析同一 lineage 的 TelemetrySpan 与 UsageReceipt。
- E4B 可观测页面只展示权威 Overview/Spans/Usage；生产 Mock、合成趋势、伪 Token 和无权威后端的 Dashboard 已删除。
- 代码 `9fcd203` 已提交并推送。

## 最后验证

- E4A Vitest：3 文件、9 tests passed。
- E4B Vitest：3 文件、14 tests passed。
- TypeScript：`tsc --noEmit` GREEN；`git diff --check` GREEN。
- 内置浏览器：`org-org/dev-project` 组织/工作区正确；可观测页 idle 与 API 失败态诚实；`2.84M`、Dashboard、固定示例命中 0。
- 限制：本机 API health=500，未把真实事件正向回包声明为浏览器 GREEN；E4C 恢复服务后补验。

## 当前工作树

代码仓库 `m1` 已推送到 `9fcd203`，只允许保留四项明确排除的未跟踪内容：

- `scripts/browser-pilot-verification/`
- `services/aos-api/tests/d5e/evidence/D5-E0_G17_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G18_20260809T141944Z.json`
- `services/aos-api/tests/d5e/evidence/D5-E0_G19_20260809T141944Z.json`

文档仓库大量他人改动继续排除；只精确暂存本波四份文档。

## 未完成

- E4C：Evals/Publication SDK 收口，不新增平行发布状态机。
- E4C：全量前端回归/构建、API 恢复后的三页内置浏览器正向/负向验收与封板。

## 下一步

1. 核查 Evals/Publication 页面与 canonical authority API 的实时调用关系。
2. 以最小更改收口 `aipEvidence`，保持现有 Publication 严格写链不变。
3. 跑定向/全量测试、TypeScript、构建和内置浏览器验收；更新 `01/06` 并封板 E4。

## 最后安全提交

- 代码：`aos-platform/m1@9fcd203`
- 文档：`docs/m1@cfa054c`；本检查点提交完成后更新。

## 断流恢复

```bash
cd /Users/ddt/work/projects/ai_agent/aos-platform && git log -8 --oneline --decorate && git status --short
cd /Users/ddt/work/projects/ai_agent/docs && git log -8 --oneline --decorate && git status --short
```

恢复时直接执行“下一步”；只有 `01/06` 与实时 Git 不一致时才追溯更早对账。
