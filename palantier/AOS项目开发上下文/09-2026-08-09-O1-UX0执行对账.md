# 2026-08-09 O1-UX0 执行对账

## 当前结论

- O1-UX 总方案已进入实施，O1-UX0 为 **GREEN**。
- AOS 代码基线：`m1@71f97ea`，已推送 `origin/m1`。
- 唯一真实目标：`org-org` · `dev-project`（栖月汇商贸有限公司 · 默认工作区）。
- 下一波：O1-UX1 专用全宽只读探索布局；不得提前宣称保存、共享、对象集或统一知识图谱画布完成。

## 本波完成

1. 冻结后端与前端 Exploration、ObjectSet、GraphQuery/GraphSnapshot DTO 和 13 个公共错误码。
2. DTO 采用 `extra=forbid`，客户端写入契约不含 `org_id/workspace_id`，租户范围仍由 Principal 推导。
3. 修复图节点丢失邻居类型问题，节点键改为 `type + id + rel`。
4. 增加 `niushop:<site>:<source_pk>` 权威引用解析；真实浏览器验证 `Order/1 → OrderLine/2`。
5. 删除对象探索中未实现的多标签、新建对象集、高级筛选、列设置、注释、展开、更多，以及地址栏 Toast 冒充的分享/保存。
6. 九个本体数字孪生页面全部登记到交互诚实性 Manifest，总清单由 36 扩展为 43 页。

## 验证证据

- 前端全量：`154 files / 2012 tests` 全通过。
- 后端对象与契约：`15 passed`。
- TypeScript：`tsc --noEmit` 通过。
- Manifest CI：`INTERACTION_HONESTY: PASS (43 pages)`。
- 浏览器：租户标题为栖月汇商贸有限公司、工作区为默认工作区；跳转后 URL 为 `/workshop/graph?type=OrderLine&id=2`，选择器为 `OrderLine`，详情为 `OrderLine/2`；刷新后保持一致；禁用主按钮和六类假功能入口均为 0。

## 分支与工作树

- 当前只使用 `m1`，本波逻辑高度耦合，没有为并行而强行创建 w1～w4。
- `m1` 已与 `origin/m1` 对齐。
- `services/aos-api/uv.lock` 为波次前既有未跟踪文件，本波未修改、未提交。

## 风险与边界

- 当前页面仍采用 `BpSplit` 窄主画布，这是 O1-UX1 的明确目标，不属于 UX0 漏项。
- 当前图仍是 1-hop、最多 6 个节点的旧表现层；权威 GraphSnapshot 查询与统一画布分别属于 UX3/UX4。
- UX0 只冻结未来保存契约，没有新增数据库表、路由或真实保存能力。

## 下一波建议：O1-UX1

1. 先生成 `O1-UX1-对象探索全宽只读布局改动清单.md`。
2. 从 `workshop.tsx` 抽出对象探索专用容器，不全局修改 `BpSplit`。
3. 主画布默认全宽；详情改为可关闭抽屉，桌面 400px、窄屏底部 Sheet。
4. 表格增加横向滚动与稳定列头；页面使用纵向滚动，不再挤压内容。
5. 保持现有真实 SDK、URL、租户和脱敏链不变；暂不开放保存、分享、对象集和注释。
6. 用 1280×720、1440×900、768px、390px 浏览器视口验收主任务、刷新和返回上下文。
