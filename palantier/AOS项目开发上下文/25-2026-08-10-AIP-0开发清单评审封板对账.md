# 2026-08-10 AIP-0 开发清单评审封板对账

> 日期：2026-08-10
> 目标租户：`org-org / dev-project`（栖月汇商贸有限公司 / 默认工作区）
> 代码核查基线：`aos-platform/m1@0e5bf33`
> 状态：**AIP-0 开发清单 v1.0 评审通过；未授权编码，代码状态未变**

## 0. 使用的 Rules

1. 本次只生成、评审和封板 AIP-0 开发清单，不修改 `aos-platform`。
2. 当前代码、递归路由、OpenAPI、迁移和浏览器真值共同决定清单，不复述旧完成状态。
3. 公共契约只有一个 authority；复用现有 TaskStatus、Draft 写链和 O1 引用。
4. 真实范围只认 `org-org/dev-project`；`dev-org` 只用于负向 canary。
5. 清单评审通过不等于 AIP-0 代码 GREEN；开工仍需用户明确授权。

## 1. 本轮新增与修改

### 新增

- `AIP通用能力实施方案/14-AIP-0-真值与公共契约冻结开发清单.md`
- `AIP通用能力实施方案/15-AIP-0开发清单评审记录与封板结论.md`
- 本对账文件。

### 修改

- `AIP通用能力实施方案/00-228-AIP通用能力与六数字同事统一实施总方案.md`：补充 AIP-0 清单与评审文件索引。
- `AOS项目开发上下文/README.md`：补充阅读顺序和权威来源。

本轮没有修改业务代码、数据库、页面、真实业务数据或 O1 封板证据。

## 2. 当前代码真值

1. 源码 AIP/模型相关 route rows 为 480，唯一 `method + path` 为 471，存在 9 组重复公共路由。
2. 运行服务筛选操作 498，与源码清单不一致；开工前必须重启并绑定 commit 重新对账。
3. 已提交 OpenAPI 检查当前 RED：实际 4065、旧预期 4069；4 条漂移未归因前禁止直接重生成。
4. Alembic 为单 head `o1ux2_001`；AIP-0 冻结为 schema/data 零变更。
5. Draft 查询/创建/详情/拒绝的 PostgreSQL authority 是 `routers/drafts.py`，批准受控写链是 `routers/runtime_write.py`。
6. `public_contracts.py` 已有 TaskStatus/transition；AIP-0 不创建第二状态机。
7. 部分 Task/TAOR/Agent/Memory/Lineage 仍为内存或单例；AIP-0 只标 legacy/关闭真实写执行，不把它们判为完成。
8. AIP 页面仍存在静态、Mock、fallback 和 estimated 混用；AIP-0 只做最小诚实性补强，不重做页面架构。

## 3. 清单评审 Loop

### 第一轮发现并关闭

- 第二 TaskStatus 模型。
- manifest/aggregate、前端页面和 honesty manifest 的错误路径。
- 不存在的前端 `typecheck/e2e` script。
- `phase3_aip_drafts /transition` 独有接口的兼容风险。
- OpenAPI RED 被快照重生成掩盖的风险。
- AIP-0 越界创建 AIP-1 表/回填业务数据的风险。

### 第二轮结论

路由/OpenAPI、DTO/状态、租户、迁移/回滚、前端/浏览器、测试/EvidencePack 六门均 PASS。通过范围只到“清单可作为开工决策输入”。

## 4. 已冻结执行顺序

```text
真值重采集与 4 条 OpenAPI 漂移归因
  -> authority/DTO/error/compat 冻结
  -> 9 组重复路由收敛与 OpenAPI 唯一化
  -> 前端 AIP SDK 与页面诚实性
  -> 定向测试、org-org 正向、dev-org 负向、19 路由浏览器验收
  -> AIP-0 独立证据封板
```

## 5. 当前门禁与下一波建议

当前门禁是等待用户明确批准“按 AIP-0 清单开工”。批准后第一动作只能是重新采集 HEAD/dirty、源码路由、运行 OpenAPI、提交 OpenAPI 和迁移 head，并解释 4 条漂移。

AIP-0 真正代码 GREEN 后，下一波建议是生成并评审 `AIP-1-Task-Plan-Run持久化内核开发清单`；不得提前进入 AIP-1、微商城工作台或具体平台接入。

## 6. Prime Agent 检查

- `prime-agent` 进程数：2，守护进程仍在运行。
- 日志目录存在：`~/.prime/agent/logs`。
- 最新日志文件时间为 2026-08-09 21:02:18；本轮没有观察到 2026-08-10 的新写入，因此这里只能确认“进程存活”，不能把它表述为“本轮长期记忆已成功写入”。
- 本轮未读取、输出或写入任何 API Key/凭据。
