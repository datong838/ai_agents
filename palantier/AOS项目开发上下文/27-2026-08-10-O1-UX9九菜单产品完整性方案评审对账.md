# O1-UX9 九菜单产品完整性方案评审对账

> 日期：2026-08-10  
> 范围：`org-org / dev-project`（栖月汇商贸有限公司 / 默认工作区）  
> 方案：`O1-UX9-九菜单功能完整性与视觉一致性补强执行清单.md` v1.1  
> 状态：`IMPLEMENTED_GREEN` · 代码、测试、浏览器、租户 canary 与机器证据完成
> 代码提交：`aos-platform m1@68abae6` · 已推送 `origin/m1`

## 当前结论

1. O1 Waves 1—10、D4/D5 与 O1-UX1～UX8 继续保持原封板结论；O1-UX9 不修改 canonical identity、GraphSnapshot、租户隔离或历史证据。
2. O1-UX5 的 GREEN 表示当时合同和主任务门通过，不等于九页面产品功能全部成熟。本次真实页面复审发现的缺口进入新波次，不回写旧证据。
3. 新波次统一覆盖对象探索、Funnel、OKF、图谱健康、活知识 Wiki、Wiki 索引、组织定制与 Overlay 以及跨页视觉一致性。
4. 订单表格冻结为：首列继续显示“订单 · 真实订单号”，默认隐藏重复“订单号”列；服务端按真实下单时间 `createdAt DESC`、`objectId DESC` 稳定排序，空时间排末尾。
5. 旧探索若显式保存了 `orderNo` 继续显示；字段本身仍用于搜索、详情、导出、图谱/Wiki 与上层 Action，不做数据或 schema 删除。

## 评审整改摘要

- 第一轮补齐了排序的服务端权威性、旧探索兼容和 active/实际上架统一口径。
- 第二轮纠正了旧分支模型误用：`/ontology/branches` 继续表达 Installation 绑定的组织 Overlay 修订历史，不把 `/v1/ontology/branches`、`meta_branch` 或 `obj_branch_overlay` 重新设为组织定制真源；同时补齐 OKF 旧 key 兼容、覆盖公式和 Graph Health 问题分类/可复算计分。
- 第三轮完成文件级任务、串行依赖、自动化、浏览器、跨租户 canary、证据和回滚检查。

## 实施结果

1. UX9-0/1 已完成：Order 服务端权威倒序、稳定 tie-break、非法 sort fail-closed；默认订单列去重，旧保存探索兼容；Funnel 直接入口不再永久 loading。
2. UX9-2 已完成：OKF 支持电商多 Object Type；真实整体必填覆盖为 `6/59=10%`，12 个真实有源类型逐项呈现，未配置状态诚实可见。
3. UX9-3 已完成：Graph Health 采用 `GH-SCORE-v2` 可复算计分，当前 617 实例、592 边、0 悬空、0 真实属性冲突、176 个必需关系缺失；问题图谱定位成功读取 9 节点/12 边权威快照。
4. UX9-4 已完成：活知识 Wiki 可直选类型与对象；39 个实际上架商品均作为真实知识缺口展示；索引卡片不再用 raw canonical ID 作为主标题。
5. UX9-5 已完成：页面冻结为 Installation 绑定的组织 Overlay current/history/diff/reset-to-inherit 视图；不恢复旧 `/v1/ontology/branches` 写模型。当前组织 0 个定制，正常继承安装模板。
6. UX9-6/7 已完成：内置浏览器九菜单 9/9 无阻断错误；后端 O1 `67 passed`，前端 `8 files / 36 tests passed`，TypeScript 与 production build 通过。
7. 租户 canary：`org-org/dev-project` 可见 61 订单、39 个实际上架商品、617 图实例和 12 个 OKF 类型；`dev-org/dev-project` 分别为 fail-closed/0/0/0，零泄漏、零写入。

## 文件与证据

- 机器证据：`aos-platform/services/aos-api/tests/d5e/evidence/O1-UX9_20260810T093850Z.json`
- 浏览器截图：`aos-platform/services/aos-api/tests/d5e/evidence/o1-ux9/`
- 核心后端：`services/aos-api/aos_api/routers/ontology.py`、`ecom_core_models.py`
- 核心前端：`ObjectExplorerWorkspace.tsx`、`workshop.tsx`、`remainder.tsx`、`ontology.tsx`、`WikiIndexPage.tsx`
- SDK：`packages/ontology-sdk/src/client.ts`
- 新增定向测试：排序、Funnel、OKF 多类型、Graph Health 计分、Wiki scope 与页面交互测试。

## 当前边界与下一波

- O1-UX9 已独立封板，不重开 O1 Waves 1—10、D4/D5 或旧证据。
- 正向租户仍只认 `org-org · dev-project`；`dev-org` 仅作负向隔离 canary。
- 下一波建议回到已评审但尚未授权编码的 AIP 通用能力计划，从 `AIP-0 真值与公共契约冻结开发清单` 开始；不得把 O1 页面补强扩散成新的 AIP 架构设计。
