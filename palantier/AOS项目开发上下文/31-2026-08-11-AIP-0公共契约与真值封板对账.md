# 2026-08-11 AIP-0 公共契约与真值封板对账

## 结论

AIP-0 已完成并达到 `IMPLEMENTED_GREEN`。后续 AIP-1～AIP-10 统一从本次冻结的单一路由 authority、AIP v1 DTO/错误/状态契约和前端专用 SDK 起步，不得恢复旧重复路由或页面 Mock 成功路径。

## 当前代码状态

- 开发分支：仅 `aos-platform/m1`，基线 `f1b9314`，本波待提交。
- AIP 重复公共路由：0。
- OpenAPI：4065 operations / 2311 paths，确定性检查通过。
- 公共契约：`packages/contracts/aip/v1.contract.json` 与 `v1.compatibility.json`。
- 正向租户：只认 `org-org / dev-project`；`dev-org / dev-project` 仅负向 canary。
- AIP 页面：Agent、Registry、Draft、Model Catalog、Capability、Assist 均不再以本地样例或本地成功冒充服务端事实。

## 验证

- 后端 AIP/路由/TTL 定向回归：27 passed，2 subtests passed。
- 前端 AIP 与交互诚实专项：260 passed。
- TypeScript：通过。
- 交互诚实门禁：46 页通过。
- OpenAPI 与 AIP 契约确定性检查：通过。
- 内置浏览器：在“栖月汇商贸有限公司 / 默认工作区”完成 Draft、Agent、Registry、Capability、Assist、Model Catalog 真空态与禁用行为复核。

## 已知递延

1. Agent/Capability 的旧进程内 store 仍不是生产真源；页面已关闭误导性操作，持久化与租户化归 AIP-6。
2. Task/Plan/Run 仅冻结了 DTO，PostgreSQL authority、RLS、CAS、幂等与恢复由 AIP-1 实现。
3. 非 AIP 的 10 组历史重复路由单独留账，不在 AIP-0 扩大整改范围。

## 下一波

立即执行 AIP-1：Task / PlanRevision / TaskRun / StepRun / Checkpoint / Artifact / Evidence PostgreSQL 权威模型、RLS、CAS、幂等、恢复与前端 SDK 接入；编码前以 AIP-1 清单和本次 v1 契约重新对账。
