# AIP-1A Task 权威存储实施对账

> 日期：2026-08-11  
> 状态：`IMPLEMENTED_GREEN`  
> 代码：`aos-platform/m1@0077055`

## 本波结论

AIP Task 已不再由 `phase3_aip_logic.py` 的进程内 `_tasks` 充当真源。Task、PlanRevision、TaskRun 以及后续执行需要的 StepRun、Checkpoint、Artifact、Evidence 已建立 PostgreSQL canonical schema；新 `/v1/aip/tasks` 主链强制认证租户、幂等键、Task version CAS 和精确 Plan content hash 审批。

旧 Logic execute/automation 两条路由暂时由 canonical `aip_tasks` owner 聚合保留，等待 AIP-2 收敛；旧 Task 创建、审批、执行内存路由已删除，AIP 范围没有重复 path/method。

## 验证

- Alembic：唯一 head `aip1_001`，本地开发库已升级。
- 数据边界：7 张新表全部 `ENABLE + FORCE RLS`，组合 workspace FK；跨已注册工作区读取返回同域 404。
- 并发边界：Task/Plan/Run 幂等 key；Task version CAS；Plan exact revision/hash 审批。
- 测试：30 passed，另有 2 个 manifest subtests passed。
- 契约：OpenAPI/route inventory 已确定性再生成；AIP-0 contract exporter 仍 PASS。

## 尚未声称完成

AIP-1A 只封板持久化和基础 API。TaskRun 当前只到 `queued`；TAOR 四段证据、lease、恢复、pause/resume/rollback、ResearchJob、前端 SDK 和浏览器验收分别属于 AIP-1B/AIP-1C，不能以本波 GREEN 替代。

## 下一波

AIP-1B：将 TAOR 改为消费 canonical TaskRun/PlanRevision，成功与失败都持久化 Think/Act/Verify/Observe；增加有期限 lease、heartbeat、checkpoint、pause/resume/retry/rollback 和 unknown/reconcile 失败语义。
