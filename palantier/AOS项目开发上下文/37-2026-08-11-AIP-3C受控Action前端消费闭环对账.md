# AIP-3C 受控 Action 前端消费闭环对账

> 日期：2026-08-11
> 状态：`IMPLEMENTED_GREEN`
> 代码：`aos-platform/m1@b7bcb76`
> 正向租户：`org-org / dev-project`

## 1. 本波结果

- 新增只读 execution view，页面刷新后从服务端恢复 Proposal、Lease 与 immutable Receipt 链。
- 建立 `apps/web/src/api/aipActions/` 唯一 Action SDK；严格解析跨资源引用、sha256、状态枚举与 Receipt 唯一性，异常时失败关闭。
- `/aip/drafts` 已切换到 canonical Proposal/Draft/Approval/Lease/Receipt；不再注入 Mock、不在浏览器维护第二套状态机。
- approved 只显示“已批准”，只有 applied/reconciled Receipt 才表示外部结果；unknown 禁止重复执行，只开放只读 reconcile。
- Logic Run 面板可按 taskId/runId 打开本次 Action；写操作绑定当前 version/hash，完成后重读权威状态。
- 订单管理遗留 `autoApprove=true` 已关闭，兼容入口只创建待审草稿并明确提示“尚未执行”。

## 2. 验证结果

- 后端相关回归：77 passed，2 subtests passed。
- OpenAPI：12 passed；Router inventory：8 passed，2 subtests passed；导出确定性检查通过。
- 前端定向：10 文件 114 passed；补充边界专项：3 文件 20 passed。
- TypeScript typecheck、Vite production build、`git diff --check`：PASS。
- 真实服务只读烟测：`org-org/dev-project` 与 `dev-org/dev-project` Proposal 均为 200/count=0；不存在跨租户可见。
- 内置浏览器：组织显示“栖月汇商贸有限公司”、工作区显示“默认工作区”；Draft 审批台显示真实空态，四类计数均为 0，刷新后一致。
- 本波未向真实租户写入验收 Proposal，未调用真实外部 Adapter。

## 3. 新增文件

- `apps/web/src/api/aipActions/client.ts`
- `apps/web/src/api/aipActions/contracts.ts`
- `apps/web/src/api/aipActions/index.ts`
- `apps/web/src/api/aipActions/client.test.ts`
- `apps/web/src/api/aipActions/contracts.test.ts`
- `apps/web/src/pages/CanonicalDraftInboxPage.tsx`
- `apps/web/src/pages/CanonicalDraftInboxPage.test.tsx`

## 4. 主要修改

- 后端：`aip_action_execution.py`、`routers/aip_actions.py`、OpenAPI 与 route inventory 基线。
- 前端：`DraftInboxPage.tsx`、`CanonicalTaskRunPanel.tsx`、`OrderManagementPage.tsx`、interaction honesty manifest 与测试。
- 方案：AIP Action 主方案及 04 全量开发清单同步为 AIP-3C GREEN。

## 5. 边界与下一波

- AIP-3 已封板，不重开 Proposal/Approval/Lease/Receipt 状态机。
- 浏览器顶部出现的“待同步写操作（1）”是既有全局离线队列提示，本波没有创建 AIP 写操作；后续应在独立队列治理清单中核对来源，不能把它算作 AIP-3C 事实。
- 下一波进入 AIP-4：Evals 发布门控、决策谱系和可观测性权威链；继续沿用 `org-org/dev-project` 只读浏览器验收与 `dev-org` 负向 canary。
