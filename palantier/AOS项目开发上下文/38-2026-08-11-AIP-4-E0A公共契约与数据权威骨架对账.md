# AIP-4 E0A 公共契约与数据权威骨架对账

> 日期：2026-08-11
> 状态：`IMPLEMENTED_GREEN`
> 代码：`aos-platform/m1@2c02d1f`
> 真实租户：`org-org / dev-project`

## 1. 本波结果

- 冻结 Eval/ReleaseGate/Publication/Lineage/Usage/MetricDefinition 的版本化严格契约。
- 新增 Alembic 单 head `aip4_001`，从 `aip3b_002` 线性扩展 9 组权威表。
- 全表强制 RLS；除 EvalRun 可执行合法状态转移外，事件与决策事实均 append-only。
- 保留现有 Eval Suite/Report、Logic Publication 和历史 decision_lineage，未做破坏性替换。

## 2. 验证结果

- 契约与迁移定向：8 passed。
- 扩展回归（含现有 Eval/Publication store）：26 passed。
- Python compile、`git diff --check`：PASS。当前环境未安装 Ruff，未把 Ruff 写成通过。
- downgrade 到 `aip3b_002` 后再 upgrade 到 `aip4_001`：PASS。
- 历史计数前后均为 Suite=1、Report=2、Publication=1、lineage=631。

## 3. 真值边界

- 上述历史数据实际属于 `dev-org/dev-project`，只证明兼容性，不得宣称为栖月汇真实业务 GREEN。
- `org-org/dev-project` 本波没有新增 Eval、Gate、Publication、Lineage 或 Usage 业务记录。
- E0A 未开放 API 和页面，因此不需要浏览器功能验收。

## 4. 新增文件

- `services/aos-api/aos_api/aip_eval_contracts.py`
- `services/aos-api/alembic/versions/aip4_001_eval_lineage_observability_contract.py`
- `services/aos-api/tests/aip/test_aip4_contracts.py`
- `services/aos-api/tests/aip/test_aip4_migration.py`

## 5. 下一波

E0B1 先实现唯一 tenant-scoped authority store，验证双租户隔离、幂等、冲突、append-only 与重启回读；E0B2 再开放最小 API/OpenAPI，不在存储语义未稳定时越层开页面。
