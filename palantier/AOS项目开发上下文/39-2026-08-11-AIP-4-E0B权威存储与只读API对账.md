# AIP-4 E0B 权威存储与只读 API 对账

> 日期：2026-08-11
> 状态：`IMPLEMENTED_GREEN`
> 代码：`aos-platform/m1@4f9f471`、`aos-platform/m1@0996704`
> 真实租户：`org-org / dev-project`

## 1. 本波结果

- E0B1 建立 Dataset revision、EvalRun/event、ReleaseGate、Publication、Lineage、Usage/Adjustment 和 MetricDefinition 的唯一 tenant-scoped authority store。
- E0B2 只开放 Dataset revision、EvalRun snapshot、Lineage events 三类 GET；没有开放可手工改绿 Gate、Publication 或 Usage 的写入口。
- 新路由进入统一 domain manifest 与 OpenAPI 确定性生成链；Publication API 测试 principal 校正为真实存在的测试作用域，没有放宽生产鉴权。

## 2. 验证结果

- E0B1：31 passed，覆盖双租户、幂等、冲突、不可变事实和重启回读。
- E0B2 冻结范围：55 passed、2 subtests passed。
- OpenAPI：2332 paths、1562 schemas、4097 route rows、4087 unique operation pairs；SHA-256 `812cbffa7d30b4cce82f2316b33bf048ecf46a43e94ac34804bb77f2654d68a9`。
- Domain manifest：514 routers、4101 runtime routes；SHA-256 `ac5664acacbb929e1e187adad35b210208116d94648cfd6d34583357fcc3cd03`。
- Python compile、OpenAPI check、router aggregate check、`git diff --check`：PASS；当前环境未安装 Ruff，不声明 Ruff 通过。
- 真实服务只读冒烟：`org-org/dev-project` 与 `dev-org/dev-project` 的不存在 Run 均为 404，不存在 Lineage 均为 200 空列表。

## 3. 诚实边界与已登记 RED

- 本波没有向 `org-org/dev-project` 写入 Eval、Gate、Publication、Lineage 或 Usage 业务记录；只读冒烟不能冒充业务 Eval GREEN。
- `dev-org/dev-project` 仅是隔离 canary，不是栖月汇完成证据。
- 历史 `tests/test_evals_engine.py` API 夹具在 FORCE RLS 下缺 tenant GUC，扩展回归为 7 failed / 65 passed / 1 skipped / 2 subtests passed。新 authority API、OpenAPI 和路由聚合均通过；该历史 RED 明确转入 E1 旧引擎迁移与测试夹具收口，不把全后端误报为 GREEN。
- 本波没有页面，因此不做浏览器 UI 验收；E4 页面波必须使用内置浏览器逐项验收。

## 4. 新增与主要修改文件

- `services/aos-api/aos_api/aip_eval_authority_store.py`
- `services/aos-api/aos_api/routers/aip_eval_authority.py`
- `services/aos-api/tests/aip/test_aip_eval_authority_store.py`
- `services/aos-api/tests/aip/test_aip_eval_authority_api.py`
- `services/aos-api/aos_api/routers/domain_manifest.json`
- `packages/contracts/openapi/v1.generated.json`
- `packages/contracts/openapi/v1.inventory.json`

## 5. 下一波

进入 AIP-4 E1：先复审并冻结六同事 EvalPack Registry、真实 Dataset revision、脱敏规则和 judge version/hash；随后实现 runner，并让 dataset/judge/target 任一漂移自动使旧门失效。同时迁移或修正旧 `evals_engine` FORCE RLS 测试夹具，直到历史 RED 有明确替代或恢复 GREEN。
