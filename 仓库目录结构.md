# 仓库目录结构 — 谛听 × SalesAgent

> **客户端**：谛听（Diting），目录 `ditingclient/`  
> **服务端**：营销智能体（SalesAgent），目录 `salesagent/`  
> **状态**：编码前定稿（技术方案前置）  
> **关联**：[产品设计文档](产品设计文档-微信聊天监控分析.md)

---

## 1. 总览

本仓库为 **Monorepo**：`ditingclient/` 与 `salesagent/` **独立目录、独立部署**；`shared/` 放**双端共用**的配置文件（不是可执行包，见 §2）。

```
wchat/
├── ditingclient/                # 【谛听】Windows 安装包产物
│   ├── README.md
│   ├── config/
│   │   └── client.json          # agent.package_id = com.yanpanji.pcwx
│   └── src/
│       └── com/yanpanji/pcwx/   # 本次 Agent：PC 微信（§4）
│
├── salesagent/                  # 【SalesAgent】CVM 部署单元（多 Agent 共用）
│   ├── README.md
│   ├── config/
│   │   └── server.json          # package_id = com.yanpanji.agents
│   └── src/
│       └── com/yanpanji/agents/ # 服务端根包（§6）
│
├── shared/                      # 双端共用配置（非 npm/pip 包）
│   ├── README.md
│   └── config/
│       ├── categories.json
│       ├── knowledge.json
│       ├── subscription.json
│       ├── product.json
│       └── agents.json            # 采集 Agent 注册表（包路径 · 规划）
│
├── docs/
│   ├── 产品设计文档-微信聊天监控分析.md
│   ├── 技术方案-谛听客户端.md
│   ├── 技术方案-SalesAgent服务端.md
│   ├── 只读接入Niushop商品与内容库说明.md
│   └── 仓库目录结构.md          # 本文
│
└── README.md
```

---

## 2. `shared/` 是什么？

**是双端共用的配置目录，不是独立的代码包。**

| 放什么 | 不放什么 |
|--------|----------|
| 类目、关键词、套餐、双端产品名、**Agent 注册表** 等 JSON | API 密钥、监听模式、`data_root`、端口 |
| 两端读同一份文件，保证类目/规则一致 | 可执行逻辑、依赖库 |

加载方式：各端在 `client.json` / `server.json` 里通过 `shared_config.dir` 指向 `../shared/config`。

---

## 3. 多 Agent 包命名规范 ✅

未来多个**信息采集 Agent** 共用 **SalesAgent 服务端**；源码按反向域名隔离。

| `package_id` | 产品场景 | 物理目录 | 状态 |
|--------------|----------|----------|------|
| **`com.yanpanji.pcwx`** | 谛听 · PC 微信（Electron + UIA） | `ditingclient/src/com/yanpanji/pcwx/` | **POC** |
| `com.yanpanji.wx` | 手机微信 | 独立工程或 `ditingclient` 内兄弟包 | v1.1 规划 |
| `com.yanpanji.xhs` | 小红书 | 远期新目录 | 规划 |
| `com.yanpanji.xy` | 咸鱼 | 远期新目录 | 规划 |
| **`com.yanpanji.agents`** | SalesAgent（analyze / ingest / WS） | `salesagent/src/com/yanpanji/agents/` | **POC** |

| 端 | Python 入口示例 |
|----|-----------------|
| PC 微信监听 | `python -m com.yanpanji.pcwx.listener` |
| SalesAgent API | `uvicorn com.yanpanji.agents.main:app` |
| 夜间 sync | `python -m com.yanpanji.agents.cli sync_niushop` |

客户端 `POST /api/analyze` 携带 `client_meta.agent_package`；服务端 POC 阶段**仅记录**，不做分流。

---

## 4. 配置归属规则

| 配置项 | 路径 | 归属 |
|--------|------|------|
| Agent 包 ID、微信监听、黑名单 | `ditingclient/config/client.json` | **谛听客户端** |
| 服务端包 ID、API、Chroma、Niushop | `salesagent/config/server.json` | **SalesAgent** |
| 服务端密钥与环境变量 | **`salesagent/.env`**（模板 `salesagent/.env.example`） | **SalesAgent** |
| 垂直类目 | `shared/config/categories.json` | **双端共用** |
| Agent 注册表 | `shared/config/agents.json` | **双端共用** |

---

## 5. 客户端 `com.yanpanji.pcwx` 模块映射

| 子目录 | PRD | 职责 |
|--------|-----|------|
| `listener/` | C1 | UIA/OCR、去重、黑名单 |
| `electron/services/` | C7 | analyze 202、WS、RequestTracker |
| `electron/` + `renderer/` | C5 | 需求雷达、设置页 |
| `db/` | C6 | SQLite schema |

---

## 6. 服务端 `com.yanpanji.agents` 模块映射

| 子包 | PRD | 职责 |
|------|-----|------|
| `api/` | S1 | 路由、鉴权、限流、WS |
| `ingest/` | S2 | 资料 ingest、Qwen-VL |
| `niushop/` | S3 | MySQL / HTTP 同步 |
| `vector/` | S4 | BGE-M3 + ChromaDB |
| `analyze/` | S5 | DeepSeek DemandEvent |

---

## 7. 运行数据（仓库外）

**路径命名约定** ✅：运行目录**一律小写**；多词用**下划线**（如 `_staging`、`_shared`）。**禁止** `Diting`、`SalesAgent` 等同路径混用大小写（Linux 上线会踩坑）。

**部署目录约定** ✅：线上 SalesAgent **部署根** = `/www/wwwroot/salesagent/`（= monorepo 内 `salesagent/` 目录发布到此，**不出现** `/www/wwwroot/salesagent/salesagent`）。

| 环境 | 产品 | 代码 / 配置根 | `data_root` | `.env` |
|------|------|---------------|-------------|--------|
| 客户端本地 | 谛听 | `wchat/ditingclient/` | `C:/work/diting/data/` | — |
| 服务端本地 | SalesAgent | `wchat/salesagent/` | `C:/work/salesagent/data/` | `wchat/salesagent/.env` |
| 服务端线上 | SalesAgent | `/www/wwwroot/salesagent/` | `/www/wwwroot/salesagent/data/` | `/www/wwwroot/salesagent/.env` |

类目子目录示例：`beauty/raw`、`beauty/chroma`、`_staging/`（均已小写）。

---

## 8. 部署关系

```
[用户 PC]  com.yanpanji.pcwx 安装包（谛听）
              │ HTTPS /api/analyze  +  WSS /events
              ▼
[独立 CVM]  com.yanpanji.agents（SalesAgent）
              │ 内网 MySQL 只读
              ▼
          Niushop 商城库（商城 CVM）
```

---

## 9. 与技术方案文档的分工

| 文档 | 内容 |
|------|------|
| **[开发计划总览](开发计划-总览.md)** | POC 编码步骤、模块依赖、里程碑 |
| **[环境准备/00-索引](环境准备/00-索引.md)** | 外部资源安装对接（DeepSeek、Chroma、SQLite…） |
| **本文 + PRD** | 目录边界、包命名、配置归属 |
| **[技术方案-谛听客户端](技术方案-谛听客户端.md)** | §4.0 `com.yanpanji.pcwx` 目录树 |
| **[技术方案-SalesAgent服务端](技术方案-SalesAgent服务端.md)** | §3.0 `com.yanpanji.agents` 目录树 |
