# POC 7b 测试计划 — 出口导向

> **文档版本**：v1.4  
> **最后更新**：2026-06-15  
> **适用范围**：POC v0.1 Phase 7b 验收（**AI 主驾驶 + 人工副驾驶** 协作执行）  
> **关联文档**：[开发计划-总览 §12](开发计划-总览.md)、[PRD §10.0](产品设计文档-谛听.md)、[客户端技术方案 §17](技术方案-谛听客户端.md)、[服务端技术方案 §15](技术方案-SalesAgent服务端.md)、[本地启动](../scripts/dev-local.md)

---

## 1. 目的与原则

### 1.1 目的

在 **Phase 7a（增强身份导入）** 已完成、**私聊 + 当前群聊** 主链路已跑通的前提下，用可重复、可留证的步骤验证：

- 微信消息 **采集 → 合并 → 类目网关 → analyze → 需求雷达/列表** 全链路稳定；
- **发言人 / wxid** 可追踪，需求不张冠李戴；
- **广告 / 无意图** 尽量不进需求列表；
- 满足 POC **量化出口**（漏报率、50 条抽检、黑名单、录屏等）。

### 1.2 测试原则


| 原则              | 说明                                         |
| --------------- | ------------------------------------------ |
| **出口导向**        | 每项验收必须有证据：截图、终端日志、录屏或表格                    |
| **先稳主链路，再扩覆盖面** | 当前窗 + 1 私聊 + 1 群测透，再考虑 24h / 双客户端          |
| **UIA 优先**      | 日常 `text_capture_mode=uia_only`，不依赖 OCR/截屏 |
| **不测 MVP 项**    | 多群后台扫、知识库投喂、商城同步等明确标为「本轮不测」                |


### 1.3 POC 7b 出口标准（总表）

与 [开发计划-总览 §12](开发计划-总览.md) 对齐，**1–9 全部有证据** 方可宣告 POC 通过：


| 序号  | 验收项                                      | 本文章节      |
| --- | ---------------------------------------- | --------- |
| 1   | `wechat_uia_map.json` + 微信版本号            | §9        |
| 2   | 24h 跑分（漏报率、黑名单）                          | §8        |
| 3   | 50 条群消息发言人抽检（含 wxid 命中率）                 | §8.2      |
| 4   | 双 `client_id` WS 隔离                      | §8.4      |
| 5   | 异步 analyze 链路（auth + 202 + `request_id`） | §6.3、§8.3 |
| 6   | 图片消息 OCR 链路演示（能力保留，可选单测）                 | §8.5      |
| 7   | Analyze 超时 / WS 断线                       | §6.4      |
| 8   | 通讯录同步 + 身份匹配（7a 全项）                      | §7        |
| 9   | 侧栏中文菜单 + 核心页可访问                          | §6.5      |


### 1.4 本次联调验收约定（产品方在场 · 2026-06）

> **副驾驶 = 产品 + 验收方**。下列约定仅适用于 **本轮 Cursor 联调**；对外正式 POC 交付仍可补录屏。


| 项               | 原标准                  | **本次**                                                                         |
| --------------- | -------------------- | ------------------------------------------------------------------------------ |
| 出口 **#5** 异步链路  | 录屏 `poc7b-e2e-*.mp4` | **免录屏**；以 **终端日志 + `client.db`** 为主证据                                          |
| UI（雷达出卡、不卡死）    | 录屏画面                 | **你现场看界面**，对话回复 `雷达有卡/无卡` 即可                                                   |
| `request_id` 一致 | 录屏含日志                | 主驾驶从主进程日志 + salesagent 日志 **摘录对齐** 写入 `poc7b-evidence/async-chain-evidence.md` |
| 截图              | 多处要求                 | **仅异常时**；正常路径不强制                                                               |


**验收 #5 通过条件（本次）**：§6.3 表 6.3.1～6.3.2 有日志证据；6.3.1 的 UI 由你口头确认；6.3.3～6.3.4 按 §6.4 测，仍不靠录屏。

---

## 2. 主副驾驶分工（AI 驱动测试）

> **执行模式**：**主驾驶 = Cursor AI**（启停进程、读终端日志、查 `client.db`、跑回归、改代码）；**副驾驶 = 你（产品 + 验收）**（环境、微信发句、**现场看雷达**、拍板通过）。  
> **进度看板**：AI 维护 `docs/poc7b-evidence/run-log.md`（每项 ✅/❌/⏸ 与备注）。

### 2.1 角色总览


| 角色          | 负责什么                                                                                           | 不负责什么                        |
| ----------- | ---------------------------------------------------------------------------------------------- | ---------------------------- |
| **主驾驶（AI）** | 启动/重启谛听；读日志与 `**C:/work/diting/data/client.db`**；写 `async-chain-evidence.md`；`verify:phase4/5` | 不能代替你发微信；Chat 里看不见你屏幕（除非你截图） |
| **副驾驶（你）**  | 环境、按口令发微信、**现场看雷达/列表**、回复 `雷达有卡/无卡`、最终验收拍板                                                     | 不必贴终端、**本次不录屏**              |


### 2.2 口令在哪里看 · 单步等待（必读）

#### 口令发布渠道 — **就在本对话（Cursor Chat）**


| 渠道                               | 谁看         | 用途                            |
| -------------------------------- | ---------- | ----------------------------- |
| **Cursor 与 AI 的聊天窗口**            | **副驾驶（你）** | **唯一正式口令台**：每条口令、等待说明、结果都在这里发 |
| `docs/poc7b-evidence/run-log.md` | 你可选看       | AI 写的进度表（✅/❌/⏳）；**不是**发口令的地方  |
| `docs/测试计划-POC-7b.md`            | 测试负责人备查    | 完整标准与分工；**平时不必翻**             |


**你不需要**另开文档、群聊或终端来「等口令」——**盯着当前这条 Cursor 对话即可**。

#### 口令长什么样

AI 每次需要你动手时，会在对话里发 **一块带标题的口令**，格式固定：

```text
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【副驾驶口令】用例 5.4.1 · 采集
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
请你：
  1. 微信打开私聊「友友」（或当前测试私聊）
  2. 发送：夏天要防晒
  3. 发完后在本对话回复：已发：夏天要防晒
（可选）雷达有卡 / 雷达无卡
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⏸ AI 已暂停，等你回复后再继续。
```

不需要你操作时，AI 会写：

```text
【主驾驶】正在执行 §4 verify:phase4 …（无需操作，请稍候）
```

#### 终端输出谁来看？


| 场景                                   | 谁看终端                            | 副驾驶要不要贴输出                    |
| ------------------------------------ | ------------------------------- | ---------------------------- |
| **§4 回归**（`verify:phase4/5/build`）   | **主驾驶** 用 Cursor Shell 自己跑      | **不需要**；AI 直接读退出码和 stdout    |
| **主驾驶** 启 `npm run dev` / salesagent | **主驾驶** 读 Shell 或 terminals 文件夹 | **不需要**                      |
| 谛听跑在 **你自己另开的** PowerShell 里         | AI 可能读不到实时屏                     | 仅当 AI 说「请贴终端最后 50 行」时再贴      |
| **Electron 界面**（雷达、列表）               | 主驾驶查 **DB/日志**；你看界面为辅           | 歧义时 **截图** 或 `雷达有卡/无卡`（§2.8） |


**结论**：§4 回归阶段 **不用你贴完整终端**；要我配合的主要是 **微信发消息** 和 **界面截图**。

#### 单步等待原则（铁律）


| 规则              | 说明                                            |
| --------------- | --------------------------------------------- |
| **一条口令 · 一步操作** | AI **不会**连发多条要你做的步骤；发完一条就 **停住**              |
| **等你回复再继续**     | 你回复 `已发：…` / `截图已贴` / `好了` 后，AI 才查日志、判结果、发下一条 |
| **AI 自做的不叫你**   | 启客户端、跑脚本、改代码、重启 —— 对话里写「请稍候」，**不用你回复**        |
| **超时**          | 若 5 分钟无回复，AI 在对话里问一句「是否遇到问题？」，**仍不自动跳过**      |
| **暂停**          | 你随时发 `暂停`；AI 停在本项；发 `继续` 再往下                  |


#### 副驾驶推荐回复（复制即用）


| 场景      | 你回复                         |
| ------- | --------------------------- |
| 环境就绪    | `环境好了`                      |
| 消息已发    | `已发：夏天要防晒`（原句照抄）            |
| 连发两句    | `已发两句：①… ②…`                |
| 看了雷达    | `雷达有卡` 或 `雷达无卡`（**本次代替录屏**） |
| 贴了图     | `截图已贴`                      |
| 录屏完成    | ~~本次不用~~ 对外交付时再录            |
| 停服务/重启完 | `已停` / `已重启 salesagent`     |
| 临时走开    | `暂停`                        |
| 回来接着测   | `继续`                        |


### 2.3 协作流程（循环）

```text
┌─────────────────────────────────────────────────────────────┐
│ 0. 副驾驶：环境就绪（§2.4 一次性清单）→ 回复「环境好了」        │
└───────────────────────────┬─────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. 主驾驶：health + 启客户端 + 回归脚本（§4）                  │
└───────────────────────────┬─────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 主驾驶：宣布当前用例编号（如 5.4.1）+ 通过标准              │
│    副驾驶：按 §2.5 介入（发消息 / 截图 / 等待）                │
└───────────────────────────┬─────────────────────────────────┘
                            ▼
                    ┌───────┴───────┐
                    ▼               ▼
              日志+证据 OK      失败 / 不确定
                    │               │
                    ▼               ▼
           记入 run-log ✅    主驾驶修代码→重启→重测该项
                    │               │
                    └───────┬───────┘
                            ▼
                   下一用例，直至 §1.3 九项完成
```

**口令约定**


| 你说              | AI 理解              |
| --------------- | ------------------ |
| `环境好了`          | 开始 §4 回归 + 真机矩阵    |
| `已发：夏天要防晒`      | 该条消息已发出，请查日志       |
| `雷达有卡` / `雷达无卡` | GUI 侧结果，用于与日志交叉验证  |
| `截图已贴`          | 按你附图判断 UI          |
| `跳过 8.4`        | 无双 PC，该项标 ⏸ 后补     |
| `暂停`            | 停在本项，不自动下一项        |
| `继续`            | 从 run-log 下一 ⏳ 项继续 |


### 2.4 副驾驶 — 一次性准备（测前只做一次）

在第一次说 **「环境好了」** 之前完成：


| #   | 你做                                                                                          | 完成打勾 |
| --- | ------------------------------------------------------------------------------------------- | ---- |
| P1  | **SalesAgent** 在 `salesagent` 目录已启动；`Invoke-RestMethod http://127.0.0.1:8765/api/health` 正常 | ☐    |
| P2  | `salesagent/.env` 含有效 `DEEPSEEK_API_KEY`；`SALESAGENT_DATA_ROOT` 可写                          | ☐    |
| P3  | PC **微信已登录**；与谛听 **同管理员权限**；测试用 **1 私聊 + 1 群** 已知名称                                         | ☐    |
| P4  | 谛听 **设置页**：License 激活（`hardcoded_test_key`）；**通讯录已同步**                                      | ☐    |
| P5  | 测试时微信 **当前聊天窗保持前台**（POC 只采当前窗）                                                              | ☐    |
| P6  | （可选）小号或家人配合发消息；不能则你用文件传输助手 **自发自收** 部分用例                                                    | ☐    |
| P7  | Cursor **Agent 模式** 打开本仓库；允许 AI 执行终端命令                                                      | ☐    |


**你不需要**：自己 `npm run dev`（除非 AI 起不来时协助）；自己读完整测试表。

### 2.5 副驾驶 — 按用例介入表（何时需要你）


| 阶段 / 编号         | 主驾驶自动做                               | **何时叫你**                  | **你要做什么**                           | 预计耗时     |
| --------------- | ------------------------------------ | ------------------------- | ----------------------------------- | -------- |
| **§4 回归**       | `verify:phase4/5/build`（**AI 自启终端**） | **一般不需要你**                | AI 直接读命令输出；**仅当** AI 让你贴时才贴         | 0～10 min |
| **§5.4.1 采集**   | 盯 `uia read` / `raw_message`         | **每条测试句**                 | 在指定私聊/群 **发送** AI 给的句子；回复「已发：…」     | 30 s     |
| **§5.4.2 网关**   | 查 `category filtered`                | 发纯闲聊句                     | 同上                                  | 30 s     |
| **§5.4.3 承接句**  | 查 analyze 与卡摘要                       | **连续两句**（先防晒语境，再「我也想要」）   | 同一人连发；说明是否同会话                       | 2 min    |
| **§5.4.4 合并**   | 查 merge / 同 demand_id                | 同一人连发 3 条同类               | 发消息；看雷达是否 **一卡更新** → 告诉 AI          | 3 min    |
| **§5.4.5 身份**   | 查 wxid / contact_key 日志              | 私聊发需求句                    | 发一句；**截图雷达卡**（微信号/性别）               | 2 min    |
| **§5.4.6 群发言人** | 对比 sender 字段                         | 群里 **3 人各 1 句**（或你换号发）    | 协调三人或分批；**截图多张卡**                   | 10 min   |
| **§6 广告 A–E**   | 日志 + 列表策略                            | **每条 A–E**                | 按 §6.2 表发送；回复雷达/列表有无                | 15 min   |
| **§6.3 异步链路**   | 日志 + `client.db` + 写 evidence 文件     | 发一条需求句 + `**雷达有卡/无卡`**    | 不录屏；UI 以你现场为准                       | 3 min    |
| **§6.4 断线**     | 停/启 salesagent                       | AI 喊 **「现在停服务」**          | `Ctrl+C` 停 uvicorn；等 AI 喊再 **重启**   | 3 min    |
| **§6.5 UI 菜单**  | 日志无法覆盖菜单点击                           | **逐项**                    | 点雷达/列表/设置/知识库；**仅异常时截图**（见 §2.8）    | 5 min    |
| **§7 身份 I1–I6** | 查 DB / 日志                            | I1 导入、I4 Process Explorer | 设置页点同步；I4 时你打开 PE 看 listener 无 Hook | 15 min   |
| **§8.1 24h**    | 读日志统计漏报                              | **挂机 + 定时发消息**            | 按表记录 ground truth；AI 白天给发句计划        | 24 h     |
| **§8.2 五十条**    | 填表后半                                 | **提供 50 条 truth**         | 群聊记录谁说了啥；AI 对卡片填表                   | 1～2 h    |
| **§8.4 双客户端**   | 两台日志                                 | 有第二台 PC 时                 | 两台同时在线；各发一条                         | 可选       |
| **§8.5 OCR**    | 改配置、重启                               | 发 `[图片]` 气泡               | 按 AI 指示发图                           | 可选       |


**原则**：能由 **日志判定** 的，不麻烦你；必须 **微信输入 / 人眼 UI** 的，才叫你。

### 2.6 主驾驶 — 自动判定规则


| 信号                                                    | 判通过       | 判失败 → 动作                  |
| ----------------------------------------------------- | --------- | ------------------------- |
| `verify:phase4` exit 0                                | 进入 §5     | 读输出，修 E2E，重跑              |
| 终端有 `raw_message` 且无 OCR                              | §5.4.1 通过 | 查 UIA/权限/微信是否前台           |
| `[pipeline] category filtered`                        | §5.4.2 通过 | 查网关关键词配置                  |
| `[pipeline] analyze dispatched` + `[ws] demand_event` | 分析链路通     | 查 salesagent/DeepSeek/超时  |
| 雷达「分析中」后长期无卡 + `ANALYZE_TIMEOUT`                      | §5.4.9 失败 | 优先修超时/心跳（历史问题）            |
| 你报「雷达有卡」但日志无 `demand_event`                           | 标 **待查**  | 查 WS/缓存/UI 刷新             |
| 你报「列表 SQL 报错」                                         | §5.4.8 失败 | 修 SQL/参数（如 lookupContact） |


每项结束后 AI 更新 `docs/poc7b-evidence/run-log.md` 一行。

### 2.7 失败时的分工


| 步骤  | 主驾驶                                    | 副驾驶                          |
| --- | -------------------------------------- | ---------------------------- |
| 1   | 保存失败时 **终端最后 50 行** + 用例编号             | 补 **截图**（若 UI 相关）            |
| 2   | **最小改动**修代码；`npm run build` 或相关 verify | 无需操作                         |
| 3   | **重启**客户端（`Ctrl+C` → `npm run dev`）    | 确认微信仍前台                      |
| 4   | **只重测失败项**，不跳过                         | 按 AI 口令重发消息                  |
| 5   | 连续 3 次同项失败                             | 与你 **拍板**：继续修 / 记已知问题跳过 / 降级 |


### 2.8 谛听界面怎么「看」？（与微信 UIA/OCR 的关系）

#### 先分清两件事


|          | **微信「凝视」**（产品已实现）                              | **Cursor 里 AI 对话**（测试主驾驶） |
| -------- | ---------------------------------------------- | ------------------------- |
| 是什么      | Python 子进程 `uiautomation` + OCR **在你电脑上**读微信窗口 | 我在仓库里跑命令、读日志/数据库          |
| 能否实时看见像素 | 能（通过 UIA 文本树 / 截屏 OCR）                         | **不能**自动连到你的显示器           |
| 用于       | 采集消息、通讯录 UI 同步                                 | POC 7b 判通过/失败、改代码         |


所以：之前说「除非你截图」指的是 **Chat 里的 AI 没有你的屏幕画面**，不是说谛听产品做不到 UI 自动化。

#### 能不能用同样方式「凝视」谛听 Electron 窗口？


| 手段                  | 对谛听 Electron        | POC 7b 现状 | 说明                                                                                            |
| ------------------- | ------------------- | --------- | --------------------------------------------------------------------------------------------- |
| **UIA 读控件树**        | 理论可行，**效果差**        | ❌ 未实现     | Electron/Chromium 窗口类名多为 `Chrome_WidgetWin_1`；Vue 页面若无无障碍标签，树里几乎没有「需求卡」「发言人」                  |
| **Region 截屏 + OCR** | 与微信 OCR 兜底 **同技术栈** | ❌ 未实现     | 可按窗口标题「谛听」截 ROI → PaddleOCR；能读可见文字，但不如结构化数据稳                                                  |
| **读 SQLite / 日志**   | **最适合自动化**          | ✅ 已有      | `demand_events_cache`、`client.db`、主进程日志里的 `demand_event` / `request_id` —— **等价于「看见雷达里有什么卡」** |
| **你发截图**            | 最直观                 | ✅ 备用      | 歧义或 DB 与界面不一致时用                                                                               |


**结论**：

- **不能像微信监听那样**，在 POC 7b 里默认对谛听界面做同等成熟的 UIA/OCR 凝视（**代码里还没有** `diting_uia_map` / 谛听窗口 OCR）。
- **不必因此依赖你每张卡都截图**：雷达/列表多数用例，主驾驶用 **日志 + 本地库** 验证即可。
- **以后可增强**（MVP 后）：`scripts/capture_diting_ui.py`（窗口 OCR 快照）或给雷达卡片加 `data-testid` + 开发态 CDP；**不阻塞当前 7b**。

#### POC 7b 界面相关用例怎么验（减少你截图）


| 要验什么          | 主驾驶优先手段                                          | 何时仍要你        |
| ------------- | ------------------------------------------------ | ------------ |
| 是否出需求卡        | 日志 `[ws] demand_event` + 查 `demand_events_cache` | 要看卡片样式/动画时截图 |
| 发言人 / 摘要      | DB 字段 + LLM 返回 JSON                              | 与微信对照是否张冠李戴  |
| 需求列表有无条目      | SQL 查询 `list_eligible`                           | 列表页空白/报错时截图  |
| 顶栏监听中 / WS 断线 | 主进程日志 + IPC 状态                                   | 顶栏颜色/文案截图    |
| 侧栏菜单能点开       | **§6.5** 仍建议你点一遍或截图                              | 无法仅靠日志代替     |


### 2.9 AI 能力边界（必读）


| 能做                                          | 不能做 / 需你                  |
| ------------------------------------------- | ------------------------- |
| 读终端、跑 Shell、`verify:`*、改代码重启                | 代替你在 **微信** 里发消息          |
| curl health、grep 日志、**查 `client.db` / 缓存表** | Chat 里**实时**看你的显示器（无截图时）  |
| 用数据库 **间接验证雷达内容**                           | §6.5 菜单逐一点击（需你或后续 OCR 脚本） |
| 维护 run-log                                  | 最终验收签字仍要你确认               |


### 2.10 建议会话节奏（单次连续 2～3 小时）


| 时段     | 内容                        | 副驾驶强度        |
| ------ | ------------------------- | ------------ |
| 第 1 小时 | §4 回归 + §5.4.1～4.6        | **高**（频繁发消息） |
| 第 2 小时 | §6 广告 + §6.3～6.5          | **高**        |
| 第 3 小时 | §7 身份抽检                   | **中**        |
| 另排     | §8.2 五十条、§8.1 24h、§8.4 双机 | **高/挂机**     |


说 **「今天先到这」** 时，AI 汇总 run-log 未完成项，下次从 **「继续」** 接着跑。

---

## 3. 测试环境与前置条件

### 3.1 硬件与系统


| 项    | 要求                                                 |
| ---- | -------------------------------------------------- |
| 操作系统 | Windows 10 / 11                                    |
| 微信   | 记录 **完整版本号**（含 `WeChat.exe` / `Weixin.exe` / MMUI） |
| 权限   | 微信与谛听 **同 UAC 级别**（建议均以管理员运行）                      |
| 网络   | 可访问 DeepSeek API；本地 SalesAgent `127.0.0.1:8765`    |


### 3.2 软件与配置检查清单

每次真机测试前约 **5 分钟** 完成：

- **终端 2**：SalesAgent 已从 `salesagent` 目录启动（非 `ditingclient`）
- `Invoke-RestMethod http://127.0.0.1:8765/api/health` 返回正常
- **终端 3**：`ditingclient` 下 `npm run dev`，Electron 窗口已弹出
- 终端日志含 `text_capture_mode=uia_only`（见 `ditingclient/config/client.json`）
- 设置页 License 已激活（如 `hardcoded_test_key`）
- 设置页 **已同步通讯录**（UIA 或 local_db；wxid 识别依赖此项）
- 顶栏显示 **监听中**（绿）
- 微信 **当前聊天窗在前台**（POC 仅采前台当前窗）

### 3.3 启动命令（勿混目录）

详见 [scripts/dev-local.md](../scripts/dev-local.md)。

**SalesAgent（终端 2）**

```powershell
cd C:\work\projects\wchat\salesagent
$env:PYTHONPATH="src"
C:\Python314\python.exe -m uvicorn com.yanpanji.agents.main:app --host 127.0.0.1 --port 8765
```

**谛听客户端（终端 3）**

```powershell
chcp 65001 > $null
[Console]::OutputEncoding = [Text.UTF8Encoding]::UTF8
cd C:\work\projects\wchat\ditingclient
npm run dev
```

> **常见错误**：在 `ditingclient` 目录执行 uvicorn 会报 `No module named 'com.yanpanji.agents'`。该模块在 `salesagent/src` 下。

### 3.4 推荐测试会话


| 类型    | 用途          | 示例                                                      |
| ----- | ----------- | ------------------------------------------------------- |
| 私聊    | 身份、承接句、合并   | 如「友友」                                                   |
| 群聊    | 多发言人、50 条抽检 | 如「谛听测试群」                                                |
| 黑名单对照 | 验证零采集       | 在 `client.json#session_filter.blacklist_groups` 配置 1 个群 |


---

## 4. 第 0 步：自动化回归（约 10 分钟）

**目的**：确认近期改动未破坏 E2E 与构建。

在 `ditingclient` 目录执行（需终端 2 SalesAgent 已启动）：

```powershell
cd C:\work\projects\wchat\ditingclient
npm run verify:phase4   # 合并 → 网关 → analyze → demand_event
npm run verify:phase5   # License + UI 壳
npm run build
```


| 结果   | 处理               |
| ---- | ---------------- |
| 全部通过 | 进入 §5 真机矩阵       |
| 任一失败 | **停止真机测试**，先修回归项 |


可选（OCR 能力保留验证，非日常路径）：

```powershell
npm run verify:phase6
```

---

## 5. 第 1 步：真机功能矩阵（核心，1～2 天）

使用 **1 个私聊 + 1 个群**，按表逐项执行。每项留存：**截图 1 张 + 终端关键日志 3～5 行**。


| 序号        | 测什么    | 怎么测                         | 通过标准                                        |
| --------- | ------ | --------------------------- | ------------------------------------------- |
| **5.4.1** | 采集     | 对方发带 beauty 品类词一句（如「夏天要防晒」） | 终端有 `uia read` / `raw_message`；**无** OCR 调用 |
| **5.4.2** | 类目网关   | 发纯闲聊（无防晒/护肤等词）              | 日志 `[pipeline] category filtered`；雷达**无新卡** |
| **5.4.3** | 承接句    | 先聊防晒，同一人再发「我也想要」            | 进入 analyze；出**本人**需求卡，摘要与上下文一致              |
| **5.4.4** | 合并     | 同一人同天连续多条同类需求               | 雷达 **同卡更新**，非多张重复卡                          |
| **5.4.5** | 身份（私聊） | 私聊好友发需求句                    | 卡片显示 **微信号 / 性别**；非长期「部分识别」                 |
| **5.4.6** | 群多发言人  | 群里 3 人各发 1 句带需求             | 各卡 **发言人正确**，摘要不张冠李戴                        |
| **5.4.7** | 广告过滤   | 见 §6 广告专项表 A–E              | 见 §6 期望列                                    |
| **5.4.8** | 需求列表   | 打开需求列表页并刷新                  | 页面正常；真需求在；广告/无意图不在                          |
| **5.4.9** | 稳定性    | 连续运行 30～60 分钟，正常收发消息        | 无「未响应」；无 `ANALYZE_TIMEOUT`；listener 不反复重启   |


### 5.1 日志关键字速查

```text
uia read / raw_message          ← 采集成功
[pipeline] category filtered    ← 网关拦截（预期：纯闲聊）
[pipeline] analyze dispatched   ← 已送 LLM
[ws] demand_event               ← WS 回推
ANALYZE_TIMEOUT                 ← 异常：分析超时
[supervisor] restart            ← 异常：监听反复重启
```

---

## 6. 广告 / 推荐过滤专项

### 6.1 当前机制（测前必读）


| 层级     | 位置  | 行为                                                   |
| ------ | --- | ---------------------------------------------------- |
| ① 类目网关 | 客户端 | 无 beauty 关键词 → 不进 analyze                            |
| ② LLM  | 服务端 | 判 `demand_type` / `demand_subject` / `list_eligible` |
| ③ 需求列表 | 客户端 | `无意图` / `other` / `none` / `list_eligible=false` 不入库 |
| ④ 需求雷达 | 客户端 | analyze 成功即可能出卡（含「仅雷达」）                              |


**无专用广告拦截器**；带品类词的广告可能仍上雷达，以实测为准。

### 6.2 测试句与期望

让对方（或小号）在私聊或群聊发送：

#### 应过滤 / 不应进需求列表


| ID    | 测试句                      | 期望                                                              |
| ----- | ------------------------ | --------------------------------------------------------------- |
| **A** | `限时促销！加微信领券，错过再等一年`      | `category filtered`；**雷达无卡**                                    |
| **B** | `XX 牌防晒霜厂家直销，批发价私聊，扫码下单` | 可能 analyze；LLM 应 `无意图` 或 `demand_subject=none`；**列表无**；雷达可能短暂有卡 |
| **C** | `给你推荐一款防晒，我家在做代理`        | 理想：`other` 或 `无意图`；**列表无**                                      |


#### 应保留（真需求）


| ID    | 测试句                  | 期望                               |
| ----- | -------------------- | -------------------------------- |
| **D** | `夏天晒黑了，有没有好用的防晒推荐一下` | beauty → analyze → **雷达 + 列表都有** |
| **E** | `我也想要`（上文在聊防晒）       | 承接句 → **本人需求卡**                  |


### 6.3 Analyze 异步链路（验收项 5、7）

对照 [客户端技术方案 §11.5.7](技术方案-谛听客户端.md)：


| #     | 检查项               | 验证方式                 | 通过标准                                                   |
| ----- | ----------------- | -------------------- | ------------------------------------------------------ |
| 6.3.1 | HTTP 202 后 UI 不卡死 | 发需求句观察雷达             | 显示「分析中」，窗口可操作                                          |
| 6.3.2 | `request_id` 一致   | 对比主进程日志与 WS payload  | HTTP 202 的 `request_id` = WS `demand_event.request_id` |
| 6.3.3 | 超时                | 停 WS 或 mock 延迟 >120s | `ANALYZE_TIMEOUT`，UI 提示，不白屏                            |
| 6.3.4 | WS 断线             | 停 salesagent 再启      | pending 清空；顶栏 WS 断线提示；重连后可继续                           |


**验收项 5（本次 · 免录屏）**：

1. 主驾驶保存：`poc7b-evidence/async-chain-evidence.md`（含 auth、202、`request_id`、WS `demand_event` 日志片段）
2. 主驾驶查询：`demand_events_cache` 最新一条与日志一致
3. 副驾驶确认：`雷达有卡` 且界面未卡死（6.3.1）

对外正式交付时仍可补 `poc7b-e2e-*.mp4`（见 §8.3 备选）。

### 6.4 断线恢复操作步骤

1. 保持客户端与微信打开，确认有 pending analyze
2. 在 SalesAgent 终端 `Ctrl+C` 停止服务
3. 观察客户端：顶栏 WS 断线、pending 失败提示
4. 重新启动 SalesAgent，等待 WS 重连
5. 再发一条测试消息，确认可正常出卡

### 6.5 UI 与菜单（验收项 9）


| 页面         | 检查                  |
| ---------- | ------------------- |
| 需求雷达       | 可打开；实时收卡；中文文案正常     |
| 需求列表       | 可打开；刷新无 SQL 报错      |
| 设置         | License、通讯录同步、监听状态  |
| 知识库 / 商城同步 | 可见；未实现项为「即将推出」toast |


---

## 7. Phase 7a — 身份与通讯录（验收项 8）

对照 [客户端技术方案 §6.9.7](技术方案-谛听客户端.md)：


| #   | 检查项   | 通过标准                                             | 证据                      |
| --- | ----- | ------------------------------------------------ | ----------------------- |
| I1  | 通讯录导入 | ≥100 联系人写入 `contacts`，含 wxid                     | 设置页导入结果截图 + DB 条数       |
| I2  | 单聊匹配  | `contact_key` 以 `wxid:` 开头                       | 雷达卡详情 / 日志              |
| I3  | 群聊好友  | 已加好友发言人命中 wxid（50 条抽检子集 ≥80%）                    | 抽检表 §8.2                |
| I4  | 监听隔离  | 同步后 `diting-listener` 无 Hook 模块                  | Process Explorer 抽检 1 次 |
| I5  | 未同步降级 | 未同步时仍可监听，`contact_match_source=display_fallback` | 清空后重测 1 条               |
| I6  | 群陌生人  | 未加好友用 `group:` 前缀，不崩溃                            | 群内外各 1 例                |


**UIA 通讯录同步**：同步时不要传 `db_key`，应走通讯录 UI 自动化（见 `contact_import_service.ts`）。

---

## 8. 第 2 步：POC 量化验收（硬性项）

可与 §5 并行；24h 跑分建议主链路稳定后再开。

### 8.1 24h 跑分（验收项 2）

**准备**：2 个单聊 + 2 个群（其中 1 个群/会话在黑名单）。

**方法**：连续运行 24 小时；平行窗口人工发/转发测试消息，记录 ground truth。


| 指标         | 目标          | 计算公式                      |
| ---------- | ----------- | ------------------------- |
| 单聊 24h 漏报率 | ≤ **5%**    | 漏抓条数 / 应抓条数（非黑名单单聊新消息）    |
| 群聊 24h 漏报率 | ≤ **8%**    | 同上                        |
| 黑名单有效性     | **100%**    | 黑名单内会话 **0 条** 进入 analyze |
| 守护进程内存     | < **500MB** | 任务管理器峰值记录                 |


**可先试跑 4h**，观察漏报趋势再开完整 24h。

#### 24h 跑分记录表（模板）


| 时段     | 会话类型 | 会话名 | 应抓  | 实抓  | 漏抓    | 备注  |
| ------ | ---- | --- | --- | --- | ----- | --- |
| 0–4h   | 单聊   |     |     |     |       |     |
| 0–4h   | 群聊   |     |     |     |       |     |
| …      |      |     |     |     |       |     |
| **合计** | 单聊   |     |     |     | **%** |     |
| **合计** | 群聊   |     |     |     | **%** |     |


### 8.2 50 条群发言人抽检（验收项 3）

**目标**：群发言人识别准确率 ≥ **90%**；7a wxid 命中率在已加好友子集中 ≥ **80%**。

在测试群录制 **50 条** 带发言人的消息，填写下表：


| #   | 时间  | 群名  | 真实发言人（ground truth） | 真实微信号（若已知） | 卡片发言人 | wxid 是否命中 | 摘要是否正确 | 通过 Y/N |
| --- | --- | --- | ------------------- | ---------- | ----- | --------- | ------ | ------ |
| 1   |     |     |                     |            |       |           |        |        |
| 2   |     |     |                     |            |       |           |        |        |
| …   |     |     |                     |            |       |           |        |        |
| 50  |     |     |                     |            |       |           |        |        |


**统计**：

- 发言人准确率 = 通过条数 / 50
- wxid 命中率 = wxid 命中条数 / 已加好友条数

### 8.3 异步链路证据（验收项 5 · 本次用日志+DB）

**本次（产品方联调）**：不录屏。主驾驶交付 `docs/poc7b-evidence/async-chain-evidence.md`，包含：


| 必含                               | 来源                                                                   |
| -------------------------------- | -------------------------------------------------------------------- |
| WS 首帧 `auth`                     | 主进程日志                                                                |
| HTTP **202** + `request_id`      | 主进程 / salesagent 日志                                                  |
| WS `demand_event` 同 `request_id` | 主进程日志                                                                |
| 落库记录                             | `SELECT * FROM demand_events_cache ORDER BY created_at DESC LIMIT 1` |
| UI 不卡死、雷达出卡                      | **副驾驶现场确认**（对话 `雷达有卡`）                                               |


**备选（对外正式 POC 交付）**：`poc7b-e2e-YYYYMMDD.mp4`，1～3 分钟，含微信发消息 → 终端 `request_id` → 雷达出卡。

### 8.4 双客户端 WS 隔离（验收项 4）

**前置**：2 台 PC 或 2 个 Windows 用户会话，各装一份谛听，同一 License。


| 步骤  | 操作            | 期望                            |
| --- | ------------- | ----------------------------- |
| 1   | 两台同时在线，各自连 WS | 均显示已连接                        |
| 2   | A 端触发 analyze | **仅 A** 雷达实时出卡                |
| 3   | B 端刷新列表       | B 可见同 License 历史（若已落库）；WS 不串台 |
| 4   | 同时各发 1 条      | `client_id` 日志互不混淆            |


### 8.5 图片 OCR 链路（验收项 6，可选）

日常 `uia_only` 不跑此路径。需演示时：

1. 临时将 `client.json` 中 `text_capture_mode` 改为 `ocr_enabled`
2. 重启客户端
3. 对方发 `[图片]` 气泡（含可 OCR 文字）
4. 验证：Region 截屏 → OCR 文本 → analyze → 出卡
5. 测毕改回 `uia_only`

---

## 9. 环境与版本记录（验收项 1）

测试报告首页必填：


| 字段                                      | 示例                                              |
| --------------------------------------- | ----------------------------------------------- |
| 测试日期                                    | 2026-06-24                                      |
| Windows 版本                              | 10.0.19045                                      |
| 微信版本                                    | 3.9.x / Weixin.x.x                              |
| 微信安装路径                                  | C:\Program Files (x86)\Tencent\WeChat3_9\WeChat |
| `listen_mode`                           | `full_collection`                               |
| `text_capture_mode`                     | `uia_only`                                      |
| `wechat_uia_map.json` 版本 / `updated_at` | 见文件内字段                                          |
| 谛听 commit / 分支                          | `完成 v1.0后再 开 github空间`                          |
| SalesAgent commit                       | 同上                                              |
| DeepSeek 模型                             | `deepseek-v4-flash`                             |


---

## 10. 已知边界（本轮不测 / 不测完）


| 能力                 | 计划阶段           | 说明                                  |
| ------------------ | -------------- | ----------------------------------- |
| **多群后台自动扫**        | MVP **M7**     | 当前仅 **前台当前聊天窗**                     |
| **未聚焦会话全量 UIA**    | 7b 记录漏报；MVP 加固 | `read_unfocused_sessions` 已开，但以实测为准 |
| **OCR 日常采集**       | 已关             | `uia_only`；能力保留，§8.5 单测             |
| **知识库客户端投喂**       | MVP M3–M5      | 侧栏占位                                |
| **商城 MySQL 同步**    | MVP M1–M2      | HTTP 联调可选，非 7b 阻塞                   |
| **双客户端**           | 7b-4           | 无第二台 PC 可后补                         |
| **hook_once 完整实现** | MVP T15        | local_db 失败时降级桩                     |


---

## 11. 问题闭环优先级

测出问题按此顺序修，避免在体验项上阻塞主链路：

1. **出不了卡 / 未响应 / `ANALYZE_TIMEOUT`**（主链路）
2. **发言人 / wxid 错误**（7a 身份）
3. **广告漏进需求列表**（网关或 LLM prompt）
4. **合并重复卡**（merge key / batch_flush）
5. UI 文案、角标「部分识别」等体验项

未达标时：按 [PRD §4.9.2](产品设计文档-谛听.md) 执行降级梯（扩黑名单 → 白名单），并在报告中记录原因与微信版本。

---

## 12. 建议日程（可交付 7b 草案）


| 天           | 内容                                          |
| ----------- | ------------------------------------------- |
| **Day 1**   | §3 环境 + §4 回归 + §5 的 5.4.1～5.4.6            |
| **Day 2**   | §5 的 5.4.7～5.4.9 + §6 广告 + §6.3 异步链路（日志+DB） |
| **Day 3**   | §8.2 五十条抽检 + §7 身份项 + 启动 24h（或先 4h 试跑）      |
| **Day 4–5** | 补测 §8.4 双客户端、§8.5 图片 OCR；整理报告               |


**最小可交付草案（本次）**：§5 矩阵全绿 + §8.2 抽检表 + **§8.3 日志证据文件** + §9 版本记录；录屏与 24h 可后补。

---

## 13. 测试报告交付物清单

打包目录建议：`docs/poc7b-evidence/YYYYMMDD/`


| #   | 交付物                                   | 对应验收项 |
| --- | ------------------------------------- | ----- |
| 1   | `环境版本记录.md`（§9）                       | 1     |
| 2   | `24h-跑分表.xlsx` 或 §8.1 表格              | 2     |
| 3   | `50条发言人抽检.csv`（§8.2）                  | 3     |
| 4   | 双客户端录屏或文字说明                           | 4     |
| 5   | `async-chain-evidence.md`（§8.3；本次免录屏） | 5     |
| 6   | 图片 OCR 录屏（可选）                         | 6     |
| 7   | 断线/超时截图 + 日志                          | 7     |
| 8   | 通讯录导入截图 + I1–I6 勾选表                   | 8     |
| 9   | 各核心页截图                                | 9     |
| 10  | `verify-phase4/5/build` 终端输出          | 回归    |


---

## 14. 修订记录


| 版本   | 日期         | 说明                                               |
| ---- | ---------- | ------------------------------------------------ |
| v1.0 | 2026-06-15 | 初稿：POC 7b 出口导向测试计划，对齐开发计划 §12 / PRD §10.0        |
| v1.1 | 2026-06-15 | **§2 主副驾驶分工**；AI 驱动测试流程；章节顺延重编号                  |
| v1.2 | 2026-06-15 | **§2.2** 口令在 Cursor 对话发布；单步等待铁律                  |
| v1.3 | 2026-06-15 | **§2.8** 谛听界面：DB/日志为主；与微信 UIA/OCR 能力边界说明         |
| v1.4 | 2026-06-15 | **§1.4** 产品方联调：出口 #5 **免录屏**，日志+`client.db`+现场确认 |


