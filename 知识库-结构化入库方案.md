# 知识库 · 结构化入库方案

> **版本**：v1.9 · 2026-06-15  
> **状态**：M1.5/M1.6/M2-1/M2-2/M2-3 ✅ · L2-a/b ✅ · **L2-c PPR 有向化 + meta_paths** ✅ · **§十四 生物-数字映射** 已定稿  
> **适用**：Niushop 商品/文章/评价、用户上传资料、主动学习产物（`LearningArtifact`）；**各商业大类（beauty / drink / …）与 `ai_agent` 共用同一套图模型**  
> **关联**：[技术方案-SalesAgent服务端](技术方案-SalesAgent服务端.md) §8–§10、[只读接入Niushop商品与内容库说明](环境准备/只读接入Niushop商品与内容库说明.md)、[开发计划-总览](开发计划-总览.md) §12.3 / §14

---

## 一、文档目的

定义谛听 / SalesAgent **知识库如何「尽可能完整地学」**，并支撑未来 **Skills 输出**（按段落回答、按场景配图/推链、关联商品小程序路径）。

**终极目标**：在大类维度建立 **知识网络**（Knowledge Network）——类比大脑里的神经系统：每个知识点是**神经元（节点）**，文档—段落—图片—商品之间的引用是**突触（边）**；检索从任一节点出发，可沿边扩散到相关商品、文章与配图。  
**架构隐喻的完整定义见 §十四「生物-数字双模态映射模型」**；工程实现路线见 §十二。

核心原则：

1. **向量检索层**（Chroma + BGE）存可检索切片；**结构层**（快照 JSON + SQLite 边表）存文档—段落—媒体之间的有机关系与**跨文档联结**。
2. **长文分段**，不用单条人为加长上限（如 1500 字）代替全文覆盖。
3. **图文视分轨**：文本先全量分段入库；图片/视频 URL 与上下文先结构化保存；**语义描述**由 **M2-2 元宝桌面识图**补齐（⚠️ 原 Qwen2-VL 本地路径已废弃）。
4. **媒体全局去重**：同一 CDN URL 在大类内对应**一个** `media_node`，多商品/多文章通过边表 `appears_in` 挂接（§4.4）。
5. 与现有 M1 Niushop 同步、M2 Ingestion、M5 知识库页、客户端 `LearningArtifact` **同一套文档 + 图模型**；**换类目只换 `category` collection 与映射，不换架构**。

---

## 二、背景：M1 现状与差距


| 项                    | M1 已实现（2026-06-24）                             | 差距                           |
| -------------------- | ---------------------------------------------- | ---------------------------- |
| Niushop 商品/文章/评价     | MySQL → BGE → Chroma；39 商品 + 9 文章 + 2 评价 chunk | 一商品 **一条** chunk             |
| 商品详情 `goods_content` | 去 HTML 后写入                                     | **截断约 400 字**；库内原文可达 4000+ 字 |
| 详情内 `<img>` / 视频     | —                                              | **未解析**；链接在 `strip_html` 时丢失 |
| 图内文字 / 场景描述          | —                                              | 服务端 **尚无** VLM（规划 M2）        |
| 检索                   | analyze 前 Chroma top-K                         | 长详情只能命中摘要，难命中具体段落            |


**结论**：M1 证明链路可用；「完整详情页 + 图文结构化」由 **M1.5（文本分段 + 媒体解析）** 与 **M2（VLM 描述）** 承接；「跨文档知识网」由 **M1.6** 承接（§4.4）。

### 2.1 M1.5 已交付（2026-06-25）


| 项                                    | 状态                                        |
| ------------------------------------ | ----------------------------------------- |
| 500/50 文本分段                          | ✅ `text_split.py` + `document_builder.py` |
| HTML img/video 解析                    | ✅ `html_extract.py`                       |
| 多 chunk / 快照 `knowledge_document_v1` | ✅ 39 商品 → 446 chunk                       |
| 文档内 `doc_id` + `media_assets[]`      | ✅ 每商品/文章一棵树                               |
| 增量 `modify_time`                     | ✅ 以 `_meta` chunk 代表整文档                   |


### 2.2 离「知识网」尚差一步（M1.6 目标）


| 能力                               | M1.5              | M1.6 目标                |
| -------------------------------- | ----------------- | ---------------------- |
| 图片作为**文档内**独立 chunk              | ✅                 | ✅ 保持                   |
| 同一 URL 在商品 A 与文章 B **合并为一个媒体节点** | ❌ 各文档各一条          | ✅ `media_node_id` 全局唯一 |
| 显式边：商品 ↔ 图片 ↔ 文章                 | ❌ 仅 `doc_id` 隐式包含 | ✅ `knowledge_edges` 表  |
| 从命中图片反查所有关联文档                    | ❌                 | ✅ 图遍历 API              |
| 大类内主题/品类树（可选）                    | ❌ 扁平 collection   | 🔜 M2+ `concept` 节点    |


---

## 三、设计决策：分段 vs 单条加长


| 方案                           | 「学全」                       | 检索 / Skills                             |
| ---------------------------- | -------------------------- | --------------------------------------- |
| 单 chunk 提到 1500 字            | 仍是一条上限；长详情不够               | 召回块大、噪声多；难引用具体段                         |
| **分段（500 字 + 50 overlap）** ✅ | 长 `goods_content` / 文章可全覆盖 | 命中「成分段」「用法段」；metadata 带 `section_index` |


**已定**：采用技术方案 §8.3 参数 — `chunk_size: 500`，`overlap: 50`，`batch_size: 8`（2GB 机保守值）。

**chunk_id 约定**（Niushop 商品示例）：

```text
niushop_product_{goods_id}_s{section_index}     # 正文段
niushop_product_{goods_id}_media_{asset_index}   # 图片/视频描述段（M2 后）
niushop_product_{goods_id}_meta                  # 可选：标题+价+路径的短摘要段（便于列表召回）
```

---

## 四、结构化文档模型

向量库每条记录是一个 **Chunk**；逻辑上归属一棵 **Document** 树：

```text
Document（文档）
├── doc_id          goods_{id} | article_{id} | upload_{hash} | learning_{entry_id}
├── doc_type        niushop_product | niushop_article | user_pdf | active_learning | …
├── title, category, sub_category
├── entry_url / mini_path（商品小程序路径等）
│
├── sections[]（段落 → 多个 vector chunk）
│     ├── section_index
│     ├── text（去 HTML 纯文本）
│     ├── char_range（在原文中的偏移，可选）
│     └── theme（可选，M2+ LLM 一句中心思想）
│
└── media_assets[]（图 / 视频 → 独立 chunk 或快照内嵌，供 Skills 选用）
      ├── asset_id
      ├── type            image | video
      ├── url             CDN 完整 URL（如 https://yanpanji.com/upload/...）
      ├── alt / html_title
      ├── context_before / context_after（所在段落前后文，各约 200 字）
      ├── ocr_text        图内文字（OCR / VLM 读出）
      ├── vlm_description 「这是什么、适合什么场景」（M2 Qwen-VL）
      └── use_cases       如：成分表 | 使用步骤 | 前后对比 | 包装展示（Skills 匹配）
```

**「有机组合」**：同一 `doc_id` 下所有 section chunk 与 media chunk 在快照 `{category}/snapshots/` 的 JSON 中可还原整篇阅读顺序；Chroma 只负责 **语义检索入口**。

### 4.1 快照 JSON（检查点）

路径：`{data_root}/{category}/snapshots/niushop_{timestamp}.json`（已有）扩展为：

```json
{
  "schema": "knowledge_document_v1",
  "synced_at": "2026-06-24T22:53:42+08:00",
  "documents": [
    {
      "doc_id": "goods_59",
      "doc_type": "niushop_product",
      "goods_id": 59,
      "title": "399白钻逆龄双效王炸套装",
      "mini_path": "/pages_sub/goods/detail?goods_id=59",
      "sections": [
        { "section_index": 0, "text": "…", "chunk_id": "niushop_product_59_s0" }
      ],
      "media_assets": [
        {
          "asset_id": "goods_59_img_0",
          "type": "image",
          "url": "https://yanpanji.com/upload/1/common/images/...",
          "context_before": "…",
          "vlm_description": null
        }
      ]
    }
  ]
}
```

事务屏障不变：**Snapshot → Chroma Upsert → niushop_sync_log**（见服务端 §10.2）。

### 4.2 Chroma metadata 扩展

在现有字段上增加（向后兼容）：

```json
{
  "source": "niushop_product_section",
  "category": "beauty",
  "goods_id": 59,
  "doc_id": "goods_59",
  "chunk_kind": "section | media_image | media_video | doc_summary",
  "section_index": 2,
  "asset_id": "goods_59_img_3",
  "media_url": "https://yanpanji.com/upload/...",
  "niushop_update_time": "2026-06-16T01:55:00+08:00",
  "ingested_at": "2026-06-24T22:53:42+08:00"
}
```

`source` 枚举扩展：


| source                       | 含义                             |
| ---------------------------- | ------------------------------ |
| `niushop_product`            | M1 兼容：单条商品摘要（逐步迁移为 `_section`） |
| `niushop_product_section`    | 商品详情段落                         |
| `niushop_product_media`      | 商品详情内图/视频描述段                   |
| `niushop_article_section`    | 文章段落                           |
| `niushop_article_media`      | 文章内图/视频                        |
| `niushop_evaluate`           | 评价语料（保持）                       |
| `user_*` / `active_learning` | 用户上传与主动学习（§9）                  |
| `knowledge_media`            | **M1.6+** 全局媒体节点（按 URL 去重，跨文档） |


### 4.3 文档内树 vs 跨文档网（两层结构）

```text
【M1.5 已有】文档内树                    【M1.6 新增】跨文档网
category=beauty
  Document goods_59                      media_node_{url_hash}
    ├── section s0, s1…        appears_in ──► goods_59 (slot: detail)
    └── media ref ─────────────appears_in ──► article_3 (slot: inline)
  Document article_3
    ├── section …
    └── media ref ─────────────same_as──────► media_node_{url_hash}
```

- **树（Tree）**：单篇阅读顺序、`sections[]` 与 `media_assets[]` 的父子关系；快照 JSON 承载。  
- **网（Graph）**：节点 = 文档 / 段落 chunk / 全局媒体 /（M2+）概念；边 = `contains` | `appears_in` | `references` | `same_as` | `mentions` | `related_product`。  
- **检索**：Chroma 命中任一 chunk → 用 `doc_id` 或 `media_node_id` **沿边扩展**上下文（商品路径、姊妹文章、共用海报图）。

**双边冗余（设计约定，非 bug）**：同一 `media` 常同时存在  
`document ──contains──▶ media`（文档归属）与 `section ──references──▶ media`（段落语义邻近）。  
入库时两条边均保留；**图谱展示与收拢以 `references` 控制段落—图片关系**，`contains` 保留给 PPR 结构扩散与文档级归属，**勿删其一**。

### 4.4 知识网数据模型（M1.6，`schema: knowledge_graph_v1`）

#### 4.4.1 节点类型（Node）


| `node_type` | 主键              | 说明                      | 向量 chunk                        |
| ----------- | --------------- | ----------------------- | ------------------------------- |
| `document`  | `doc_id`        | 商品/文章/上传物               | `_meta` chunk                   |
| `section`   | `chunk_id`      | 正文段                     | `*_s{n}` chunk                  |
| `media`     | `media_node_id` | **大类内 URL 唯一** 的图/视频知识点 | `knowledge_media_{hash}` **一条** |
| `evaluate`  | `chunk_id`      | 评价语料                    | 保持独立                            |
| `concept`   | `concept_id`    | （M2-3）功效/成分/场景等**语义概念** | `concept_{slug}` chunk（见下）      |


`media_node_id` 生成：`media_{sha256(normalized_url)[:16]}`；`normalized_url` = CDN 绝对路径、去 query 中尺寸参数（可配置）。

`**concept` 节点规范（M2-3-1 开工前按此入库，不新增表）**：


| 项                | 约定                                                                                                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `concept_id`     | `concept_{slug}`；`slug` = 概念短语的规范化 slug（小写、去空格，如 `concept_玻尿酸`）                                                                                   |
| 图节点              | `node_type=concept`，`node_id=concept_id`；**无独立 SQLite 表**，与 `section`/`media` 一样仅靠 `knowledge_edges` 联结                                           |
| 向量               | Chroma 一条 `concept_{slug}` chunk，`source=knowledge_concept`，文本为概念标准名 + 可选同义词                                                                      |
| 典型边              | `section ──mentions──▶ concept`（M2-3 SPO 写入）                                                                                                      |
| 与 `sub_category` | **不同层**：`sub_category` 是运营分组（防晒/护肤，存在 Chroma metadata）；`concept` 是语义突触（跨商品扩散）。二者不要求 slug 一致；UI 分组继续用 `sub_category`，成网靠 `mentions` + `appears_in` |


#### 4.4.2 边类型（Edge）

SQLite 表 `knowledge_edges`（按 `category` 分库逻辑，表内带 `category` 列）：


| `rel_type`        | 起点         | 终点                  | 含义                                               |
| ----------------- | ---------- | ------------------- | ------------------------------------------------ |
| `contains`        | `document` | `section` | `media` | 文档拥有段落/引用媒体                                      |
| `appears_in`      | `media`    | `document`          | 媒体出现在哪篇文档（可多条）                                   |
| `references`      | `section`  | `media`             | 段落上下文邻近的图（带 `context_before/after` 存边上或节点）       |
| `mentions`        | `section`  | `concept`           | 段落提及概念（M2-3 SPO 写入）                              |
| `same_as`         | `media`    | `media`             | 同图不同 URL（**远期可选**，M2+）                           |
| `related_product` | `document` | `document`          | 文章/资料 → 商品（`article_`* / `upload_*` → `goods_*`） |


```sql
-- salesagent/schema.sql 扩展（M1.6）
CREATE TABLE IF NOT EXISTS knowledge_media_nodes (
  media_node_id   TEXT PRIMARY KEY,
  category        TEXT NOT NULL,
  media_url       TEXT NOT NULL,
  media_type      TEXT NOT NULL,  -- image | video
  url_hash        TEXT NOT NULL,
  vlm_description TEXT,
  ocr_text        TEXT,
  use_cases       TEXT,           -- JSON array
  first_seen_at   TEXT,
  updated_at      TEXT,
  UNIQUE(category, url_hash)
);

CREATE TABLE IF NOT EXISTS knowledge_edges (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  category        TEXT NOT NULL,
  src_type        TEXT NOT NULL,
  src_id          TEXT NOT NULL,
  rel_type        TEXT NOT NULL,
  dst_type        TEXT NOT NULL,
  dst_id          TEXT NOT NULL,
  props_json      TEXT,           -- context_before, slot_index, goods_id, ...
  created_at      TEXT NOT NULL,
  UNIQUE(category, src_type, src_id, rel_type, dst_type, dst_id)
);

CREATE INDEX IF NOT EXISTS idx_edges_src ON knowledge_edges(category, src_type, src_id);
CREATE INDEX IF NOT EXISTS idx_edges_dst ON knowledge_edges(category, dst_type, dst_id);
```

#### 4.4.3 全局媒体 chunk（Chroma）

M1.5 每文档一条 `niushop_product_{id}_media_{n}`；M1.6 改为：

```text
向量层：knowledge_media_{url_hash}     # 大类内唯一，source=knowledge_media
文档层：snapshot.media_assets[].media_node_id  → 指向同一节点
边表：  appears_in(media_node, goods_59), appears_in(media_node, article_3)
```

媒体 chunk 文本聚合该 URL 的**所有出现上下文**（多文档 `context` 拼接，上限可配置），VLM 描述只算一次。

#### 4.4.4 图查询（服务端 API，M1.6）


| API                                                        | 用途                        |
| ---------------------------------------------------------- | ------------------------- |
| `GET /api/knowledge/graph/neighbors?node_id=&rel=&depth=1` | 从命中 chunk 扩展邻域            |
| `GET /api/knowledge/media/{media_node_id}`                 | 媒体节点 + 所有 `appears_in` 文档 |
| `GET /api/knowledge/document/{doc_id}/tree`                | 还原文档内树（快照 + 边）            |


Analyze / Skills 消费：检索 top-K 后，对 `chunk_kind=media` 或 `knowledge_media` 执行 **1-hop `appears_in`**，把关联商品 `mini_path` 一并注入 prompt。

#### 4.4.5 多类目复用


| 层级         | 策略                                                                     |
| ---------- | ---------------------------------------------------------------------- |
| Collection | 每大类独立 Chroma collection（`beauty`、`drink`、…）                            |
| 图模型        | **同一套** `knowledge_media_nodes` + `knowledge_edges` 表结构，`category` 列隔离 |
| Niushop 映射 | `server.json#niushop.goods_category_mapping` 决定 doc 落入哪一类目             |
| `ai_agent` | `doc_type=user_* | active_learning`，同样走 `contains` / `appears_in`      |


换类目 = 新 collection + 新映射 + 独立 sync；**不换代码、不换边语义**。

---

## 五、文本分段流水线（M1.5 ✅）

```text
ns_goods.goods_content (HTML)
    → 解析 DOM：保留 img/video/src、alt
    → 正文 strip_html → 按 500/50 切片
    → 每段 build section chunk + metadata
    → 媒体列表写入 snapshot.media_assets（url + 上下文，vlm 字段先 null）
    → BGE embed → Chroma upsert（batch 50，§8.4）
    → knowledge_chunks 索引（preview = 段首 200 字）
```

**增量**：以 `_meta` chunk 的 `niushop_update_time` 代表整文档；未变则跳过（§10.2）。

**文章** `article_content`：同流水线，`doc_type=niushop_article`。

**代码**：`text_split.py`、`html_extract.py`、`document_builder.py`、`mysql_sync.py`。

---

## 五之二、知识网构建流水线（M1.6 ✅）

在 §五 产出 `BuiltDocument` 之后增加 **图写入阶段**（与 Chroma upsert 同事务屏障）：

```text
BuiltDocument + media_assets[]
    → normalize_url → url_hash
    → UPSERT knowledge_media_nodes（大类内按 url_hash 去重）
    → UPSERT knowledge_edges:
         document --contains--> section_chunk
         document --contains--> media_node
         media_node --appears_in--> document
         section    --references--> media_node（可选，邻近关系）
    → 全局媒体 chunk：仅 1 条 knowledge_media_{hash} embed/upsert
    → 文档内 media chunk（M1.5）迁移为「引用边」；可保留轻量 stub 或删除重复 embed
    → 写 snapshot（media_assets[].media_node_id）
    → commit → Chroma batch upsert
```

**迁移策略**（`sync_niushop --force`）：

1. 扫现有 `niushop_*_media_`* chunk，按 `media_url` 合并为 `knowledge_media_*`。
2. 回填 `knowledge_edges`；删除重复 embed（同 URL 只保留全局节点向量）。
3. `verify_m16.py`：同一 URL 在 2 个 doc 出现时 `media_node` 计数为 1、边 ≥ 2。

**代码入口（规划）**：

```text
salesagent/src/com/yanpanji/agents/knowledge/
  graph_store.py       # edges + media_nodes CRUD
  media_registry.py    # url normalize / hash / get-or-create node
  graph_builder.py     # BuiltDocument → edges
  graph_api.py         # GET neighbors / document tree（挂 routes）
salesagent/src/com/yanpanji/agents/niushop/
  mysql_sync.py        # 接入 graph_builder
```

## 六、图片与视频（M2 及以后）

### 6.1 能力矩阵


| 能力            | M1.5        | M2-2（元宝识图）        | 备注                                          |
| ------------- | ----------- | ------------------- | ------------------------------------------- |
| 保存图片/视频 URL   | ✅ 自 HTML 解析 | ✅                   | CDN 基址 `server.json#niushop.image_cdn_base` |
| 保存所在段落上下文     | ✅           | ✅                   | `context_before/after`                      |
| 图内文字          | —           | ✅ OCR 或 VLM 读出      | 非微信 OCR 链路                                  |
| 场景描述 / 何时用这张图 | —           | ✅ `vlm_description` | 供 Skills 选图；**谛听元宝技能**写入（⚠️ Qwen 本地已废弃）   |
| 视频内容理解        | 仅链接+封面      | 抽帧/封面 VLM（可选）       | 完整视频理解后补                                    |


### 6.2 与客户端 OCR 的边界


| 场景                           | 负责端                              |
| ---------------------------- | -------------------------------- |
| 微信聊天 UIA/OCR 兜底              | **谛听客户端** PaddleOCR              |
| Niushop 商品详情图、知识库上传图、主动学习外链图 | **谛听本机元宝技能** → `apply_yuanbao_vlm_result`（⚠️ 服务端 Qwen2-VL 已废弃） |


### 6.3 媒体 chunk 文本模板（入库用）

```text
【商品图】{goods_name}
图片地址：{media_url}
所在上下文：{context_before} … {context_after}
图内文字：{ocr_text}
场景描述：{vlm_description}
适用场景：{use_cases}
```

检索命中媒体 chunk 时，Skills 可将 `media_url` 转为回复中的配图或卡片字段。

---

## 七、检索与 Skills 消费方式

### 7.1 Analyze 主链路（当前 → M1.6）

1. 用户消息 → BGE 查 `{category}` collection top-K。
2. 命中 section / media / `knowledge_media` → 注入 `retrieved_knowledge`。
3. **M1.6**：若命中 `knowledge_media`，`appears_in` 扩展关联 `doc_id` → 补 `mini_path`、标题。
4. LLM 生成 `DemandEvent`；`keywords` 辅助 `matched_products`（§7.3）。

### 7.2 Skills 输出（MVP+）


| Skill 意图    | 依赖结构                                           |
| ----------- | ---------------------------------------------- |
| 按成分/功效细答    | `section_index` + `doc_id`                     |
| 回复带合适主图     | `media_assets[].vlm_description` + `use_cases` |
| 推小程序商品页     | `goods_id` + `mini_path`（快照）                   |
| 引用用户评价      | `source=niushop_evaluate`                      |
| 一张图关联多商品/文章 | **M1.6** `knowledge_edges.appears_in`          |


**不建议**把整篇 HTML 或全部图片 URL 塞进单次 prompt；应 **检索 relevant chunks → 图扩展 1-hop → 按需拉 snapshot**。

---

## 八、实施阶段


| 阶段            | 任务                                                               | 验收                                                                   |
| ------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------- |
| **M1** ✅      | Niushop MySQL 同步、单 chunk、商城同步页                                   | 39 商品入库；analyze 可召回                                                  |
| **M1.5** ✅    | 文本分段；HTML 媒体 URL+上下文；metadata 扩展；全量 sync                         | 446 chunk；snapshot `knowledge_document_v1`；`verify_m15`              |
| **M1.6** ✅    | **知识网**：`knowledge_media_nodes` + `knowledge_edges`；URL 去重；图 API | `verify_m16`；`knowledge_media` + `appears_in`                        |
| **M2-1** ✅    | **轻量扩散**：PPR/RWR 1-hop→k-hop；meta-path 模板；Analyze 图扩展            | `verify_m21`；命中媒体可扩散到多商品；`references` 边                              |
| **M2-2** ✅    | **元宝识图**：写入 **全局** `media_node.vlm_description`（每 URL 一次）      | [M2-2 元宝技能方案](M2-2-媒体图片描述-元宝技能方案.md)；`verify_m22.py` |
| **M2-3** ✅    | **冷启动补边**：LLM 零样本 SPO + 新 chunk 即时 BGE（inductive）                | `spo_enrich`；`verify_m23.py`                                        |
| **M3+**       | Graph RAG 子图摘要（可选，数据量百万级再开）                                      | community 摘要 + 子图喂 LLM                                               |
| **M3–M5**     | 用户上传、知识库页、ingest_progress                                        | 与本文同一 Document + Graph 模型                                            |
| **M5 Skills** | `matched_products` + `linked_media` + `appears_in` 文档            | 端到端选图闭环                                                              |


代码入口：

```text
salesagent/src/com/yanpanji/agents/niushop/     # M1.5 ✅
  text_split.py, html_extract.py, document_builder.py, mysql_sync.py
salesagent/src/com/yanpanji/agents/knowledge/   # M1.6 ✅ / M2-1 ✅
  graph_store.py, media_registry.py, graph_builder.py, graph_api.py
  ppr.py, graph_retrieval.py                    # M2-1 PPR 扩散检索
salesagent/src/com/yanpanji/agents/ingest/
  pipeline.py, vlm.py                           # M2
```

---

## 九、配置项（建议写入 `server.json`）

```json
{
  "ingestion": {
    "chunk_size": 500,
    "chunk_overlap": 50,
    "incremental_by_hash": true
  },
  "niushop": {
    "image_cdn_base": "https://yanpanji.com/",
    "detail_html": {
      "max_section_chars": 500,
      "media_context_chars": 200,
      "parse_video_src": true
    }
  },
  "models": {
    "qwen_vl": {
      "max_images_per_goods": 5,
      "max_images_per_night": 50
    }
  },
  "knowledge_graph": {
    "enabled": true,
    "schema": "knowledge_graph_v1",
    "media_url_normalize": {
      "strip_query_params": ["w", "h", "imageView2"]
    },
    "global_media_chunk": true,
    "max_contexts_per_media": 5,
    "graph_expand_hops_default": 1
  }
}
```

---

## 十、风险与约束


| 风险                         | 缓解                                                       |
| -------------------------- | -------------------------------------------------------- |
| 分段后 chunk 数暴增（39 商品 → 数百段） | 夜间 sync；增量跳过未改 `modify_time`；§8.4 分 batch upsert         |
| 2GB 机 VLM 峰值 OOM           | 仅 `heavy_tasks_window`；Int4；`vlm_daytime_allowed: false` |
| 详情图过多                      | M2 抽样 + `max_images_per_goods`；优先主图与含大段文字的海报图            |
| 视频                         | MVP 仅存链接与封面；不做全片理解                                       |
| 全局媒体合并后上下文过长               | `max_contexts_per_media` 截断；优先保留最新 `appears_in`          |
| 图边暴增                       | `UNIQUE` 约束 + 增量 sync 只增删变更 doc 的边                       |


---

## 十二、技术纵深：五层架构 × 工程路线（v1.2 定稿）

> 与产品愿景对齐：**知识点 = 神经元，边 = 突触，检索 = 沿图游走推理**（生物学对照表见 **§十四**）。  
> 落在 **知识图谱（KG）× 图检索 × 神经符号** 交叉带；**不一开始拍平同质图**，保留节点/边类型。

### 12.1 五层映射：我们现在在哪、下一步是什么


| 层           | 学术/工业概念                     | **当前（M1.6）**                                                                | **下一阶段（M2）**                                            | **远期（M3+，数据量大时）**                                        |
| ----------- | --------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------- |
| **① 建网**    | 异构图 / HIN / 属性图             | ✅ SQLite 属性图：`node_type` + `rel_type` 不拍平；文档→段落→媒体层级 + `appears_in` 侧枝      | 补 `concept` 节点、`related_product`；登记 **meta-path** 配置    | 百万边时迁 Neo4j / 图分区（**远期可选**）；`co_viewed` 协同浏览边（**远期可选**）  |
| **② 表示**    | KG Embedding / GNN / 多模态初始化 | ✅ **文本/商品/段落**：BGE-M3 chunk 向量（Chroma）；**图片**：URL+上下文文本向量，M2 加 CLIP/Qwen-VL | VLM 填 `media_node`；可选 **TransE 边权重**（`rel_strength`）    | RGCN/HAN **仅当** 边>10万且要 meta-path 学习；2GB 机默认 **不训练 GNN** |
| **③ 扩散检索**  | PPR/RWR、Node2Vec、Graph RAG  | ✅ **ANN top-K**（BGE）+ **1-hop 边扩展**（`appears_in`/`contains` API）            | ✅ **PPR/RWR**（`ann_then_ppr`）；`references` 边；Analyze 接入 | Graph RAG：Leiden 社区 + LLM 子图摘要                           |
| **④ 多模态对齐** | CLIP、BLIP、文档级 encoder       | 文本↔图靠 **上下文拼接** + 共享 BGE 空间（弱对齐）                                            | Qwen-VL 描述进 `knowledge_media`；可选 CLIP 向量副索引             | MM-GNN（数据与算力到位再议）                                        |
| **⑤ 工程组合**  | 见图「三条路线」                    | **路线 A：轻量检索型**（见下）                                                          | A 增强 + VLM                                              | 路线 C：Graph RAG                                           |


### 12.2 已定工程路线：**轻量检索型（路线 A）**

```text
属性图（SQLite knowledge_edges）
  + 节点向量（BGE → Chroma ANN）
  + 扩散（PPR/RWR，M2-1 ✅ 已接入）
  + 可解释 meta-path（不靠黑盒 GNN）
```


| 路线                   | 组合                     | 本项目的适用性                                       |
| -------------------- | ---------------------- | --------------------------------------------- |
| **A 轻量检索型** ✅ **当前** | 属性图 + PPR/RWR + 向量 ANN | 节点 **万级**（39 商品→数百段→~百媒体）；要可解释边；**2GB 机不训模型** |
| B GNN 增强型            | RGCN/HAN + PPR         | 多类型节点已具备，但边规模与标注不足；**M3 前不默认**                |
| C Graph RAG 型        | 社区发现 + LLM 摘要 + 子图检索   | 适合大规模 Q&A；**延迟与成本更高**；类目知识破 **10万 chunk** 再评估 |


### 12.3 异构图：节点 / 边 / Meta-path（不拍平）

**节点类型（`node_type`）**


| 类型             | 实例                      | 向量入口                     |
| -------------- | ----------------------- | ------------------------ |
| `document`     | `goods_59`, `article_3` | `_meta` chunk            |
| `section`      | `niushop_product_59_s2` | BGE section chunk        |
| `media`        | `media_{url_hash}`      | `knowledge_media_{hash}` |
| `evaluate`     | `niushop_evaluate_*`    | 独立 chunk                 |
| `concept`（M2+） | `concept_玻尿酸`           | 概念短语 chunk               |


**边类型（`rel_type`）— 决定游走语义，不可合并为单一 `related`**


| 边                 | 语义        | 游走权重（M2 默认可调）   |
| ----------------- | --------- | --------------- |
| `contains`        | 文档拥有段落/媒体 | 1.0（结构边，必走）     |
| `appears_in`      | 媒体出现在文档   | 0.9（跨文档联结）      |
| `references`      | 段落邻近配图    | 0.85            |
| `mentions`        | 段落/评价提及概念 | 0.8（M2+ LLM 抽取） |
| `related_product` | 文章→商品     | 0.95            |
| `same_as`         | 同图异 URL   | 等价跳转            |


**Meta-path 模板（HIN 语义；§12.3.1 为生产白名单与 MP 对照）**

```text
MP1  用户问功效  : concept ─mentions─► section ─contains◄─ document(goods)
MP2  配图推荐    : section ─references─► media ─appears_in─► document(goods)
MP3  文章带货    : document(article) ─related_product─► document(goods)
MP4  社交证明    : document(goods) ─contains─► evaluate
MP5  同图多品    : media ─appears_in─► document(goods₁) … document(goodsₙ)
```

**配置**：`server.json#knowledge_graph.meta_paths` 白名单 `(src_type, rel_type, dst_type)` 序列。  
**实现状态（2026-06-15）**：✅ L2-c 已接入 `graph_retrieval._load_adjacency` + `knowledge_search`；`ppr.directed: true` + **6 条** `meta_paths`（见 §12.3.1、§12.8）。关系图检索页可开关 `ppr_l2c` / `ppr_concept_bridge` 做 A/B，不改向量与边表。

#### 12.3.1 生产 `meta_paths` 六条 ↔ MP 业务对照

> **「6 条」不是 PPR 跳数**，而是 `server.json#meta_paths` 数组里的 **6 个路径模板**。代码将每条拆成若干 `(src_type, rel_type, dst_type)` 三元组，PPR **仅沿白名单建邻接**；当前共展开为 **7 个允许的三元组**（第 1 条链式路径含 2 段）。

| # | `server.json` 配置（`meta_paths` 一项） | 展开的三元组 | 对应 MP | 业务场景 |
|---|----------------------------------------|-------------|---------|----------|
| 1 | `section → references → media → appears_in → document` | `(section, references, media)`、`(media, appears_in, document)` | **MP2** | 段落邻近配图 → 媒体 → 反查所属商品 |
| 2 | `media → appears_in → document` | `(media, appears_in, document)` | **MP5** | 同一张图出现在多个商品详情 |
| 3 | `section → mentions → concept` | `(section, mentions, concept)` | **MP1**（半段） | 段落提到「补水 / 敏感肌」等概念；**默认止于 concept**，不自动跨品（见下） |
| 4 | `document → contains → section` | `(document, contains, section)` | **MP1**（半段） | 商品/文章 → 正文段落（结构边） |
| 5 | `document → contains → media` | `(document, contains, media)` | 结构边 | 商品详情内嵌图片 |
| 6 | `document → related_product → document` | `(document, related_product, document)` | **MP3** | 文章带货、关联推荐跳转到商品 |

**为何用白名单而不是「全图有向」？**  
PPR 若在全部 `rel_type` 上扩散，仍可能走到业务上不该参与检索的边（或未来新增边）。`meta_paths` = **只允许卖货/答问相关的传导通道**；与 `ppr.directed: true` 配合：**方向正确 + 路径语义可控**，比改前「全图无向 ×0.95」噪音更低。

**MP4（社交证明）— 图谱有边，PPR 未纳入**

| 项 | 说明 |
|----|------|
| **MP4 语义** | `document(goods) ─contains─► evaluate`：从商品扩散到用户评价 |
| **beauty 数据** | ✅ 评价节点与 `contains` 边已入库；**图谱 Tab / neighbors API 可浏览** |
| **当前 PPR 白名单** | ❌ **未列入**上述 6 条 → **`ann_then_ppr` 检索不会沿评价边扩散** |
| **若业务需要** | 在 `meta_paths` 增补：`["document", "contains", "evaluate"]`（条数变为 7，非固定常数） |
| **与 §12.7 的关系** | Analyze 打包上下文时仍可 **优先 evaluate 文本**（`pack_nodes` 伪代码）；与 PPR 是否走 MP4 是两层决策 |

**概念跨品（MP1 完整路径）— 默认不写在 6 条里**  
完整 MP1 为 `concept → section ← document`；第 3 条仅 `section → mentions → concept`（有向出边），**不含** `concept → section` 反向。若业务要从品 1 段落经概念 A 联到品 2：关系图检索可开 **`ppr_concept_bridge`**（仅加 `mentions` 反向）；或关闭 L2-c 用改前无向全边对照。见 [开发计划-总览](开发计划-总览.md) P1-c / 关系图检索 A/B 开关。

### 12.4 扩散检索：具体算法选型（M2-1）

**Step 0 — 入口（已有）**  
用户消息 → BGE → Chroma top-K → 得到种子节点集合 `S`（section / media / concept chunk）。

**Step 1 — 1-hop 扩展（M1.6 已有）**  
对每个种子：`appears_in` / `contains` 拉关联 `document`、`media`；注入 `mini_path`、标题。

**Step 2 — PPR/RWR（M2-1 ✅；L2-c ✅）**

```text
对每个种子 s ∈ S：
  在 category 子图上运行 Personalized PageRank(s, α=0.15, max_iter=20)
  邻接 = ppr.directed 出边 + meta_paths 白名单（§12.3.1）
  边权 = rel_type 默认权重（server.json#rel_weights）
合并多源 PPR 分数 → 重排 chunk / document
取 top-M 节点 → 拼 retrieved_knowledge（仍控 token 预算）
```

- 工程：`graph_retrieval.py` + `ppr.py`；**不引入 Neo4j**（万级边足够）。  
- 与 ANN 关系：**ANN 找种子，PPR 做结构扩散**；不是二选一。  
- **L2-c ✅（2026-06-15）**：有向出边 + `meta_paths` 掩码；`verify_m21` 有向邻接 ~2349 边 vs 改前无向 ~4698 边。  
- **业务可调**：关系图检索 `ppr_l2c` / `ppr_concept_bridge` 请求参数 A/B；定稿后再写回 `server.json`。  
- **远期可选**：边权 × TransE 得分；增补 MP4 等 meta-path 项（§12.3.1）。

**Step 3 — Graph RAG（远期可选）**  
`beauty` 全库 chunk > **5万** 且 analyze 需跨文档综合时：离线 Leiden 社区 → LLM 生成 `community_summary` 节点 → 查询时种子→社区→摘要+子图。

### 12.5 节点表示：为何不立刻上 GNN


| 方案              | 本项目判断                                             |
| --------------- | ------------------------------------------------- |
| TransE / RotatE | **远期可选**：边权重/链接预测，不替换 BGE；适合 `related_product` 补全 |
| RGCN / HAN      | 需大量边与训练窗口；**2GB CVM + 无 GPU** → 默认 **跳过**         |
| Node2Vec        | 与 BGE 功能重叠；结构已靠 PPR 保留 → **不优先**                  |
| **BGE + 图扩散**   | ✅ 与现有 Chroma 投资兼容；新节点 **入库即 embed**（inductive）    |


**多模态初始化（M2-2）**

```text
文本节点  → BGE-M3（已有）
图片节点  → 元宝识图 → vlm_description → 并入 knowledge_media chunk（主）
           → 可选 CLIP 向量副索引（检索增强，非必须）
商品节点  → meta chunk（标题价路径）+ 属性拼接
```

### 12.6 冷启动：新知识点无边时怎么办


| 问题                                    | 补法（已定优先级）                                                                |
| ------------------------------------- | ------------------------------------------------------------------------ |
| 新 `section` chunk 刚入库，尚无 `mentions` 边 | **① BGE 立即 embed** → ANN 可检索（inductive，不依赖游走历史）                          |
| 新商品无评价、无概念边                           | **② LLM 零样本 SPO**（M2-3 ✅）：从段落抽 `concept` + `mentions` 写入 `knowledge_edges` |
| 新图片仅 URL                              | **③ 元宝识图** → `vlm_description` + `knowledge_media` 向量；**④** `appears_in` 在 sync 时自动建立 |
| 完全孤立节点                                | ANN 仍可用；PPR 退化为单节点；待 SPO/VLM 补边后结构增强                                     |

**可选增强：概念三级播种**（[方案评估](知识库-结构化入库方案评估.md)；**非阻塞 L2-b**，M2-3 LLM SPO 已交付后可作成本优化）：

| 级别 | 方法 | 成本 | 用途 |
|------|------|------|------|
| **L1** | 从 `sub_category`、品牌、成分词典规则匹配 → `mentions` | 无 LLM | 快速撑起 MP1 骨架 |
| **L2** | section BGE 聚类 + 每簇 1 次 LLM 提炼 concept | 远低于逐段 SPO | 批量补 concept |
| **L3** | M2-3 逐段 LLM SPO（当前主路径） | 按 section 计费 | 最高精度 |

若 MP1（concept → section ← document）在 SPO 全量跑完前需提前可用，可优先跑 L1。


### 12.7 Analyze 消费协议（M2 目标伪代码）

```text
seeds = chroma_ann(query, top_k=8)
subgraph = ppr_expand(seeds, meta_paths=[MP1, MP2], hop_budget=2)
context = pack_nodes(subgraph, max_tokens=2000)
  # 优先：section 文本 > media vlm_description > goods meta > evaluate
llm.analyze(context + user_message)
```

### 12.8 配置扩展（`server.json#knowledge_graph`）

> **说明**：下列为 **beauty 生产配置（L2-c ✅）**；六条 `meta_paths` 与 MP 业务对照见 **§12.3.1**。MP4 未列入 → PPR 不走评价边。

```json
{
  "knowledge_graph": {
    "enabled": true,
    "retrieval_mode": "ann_then_ppr",
    "ppr": { "alpha": 0.15, "max_iter": 20, "top_n": 12, "directed": true },
    "meta_paths": [
      ["section", "references", "media", "appears_in", "document"],
      ["media", "appears_in", "document"],
      ["section", "mentions", "concept"],
      ["document", "contains", "section"],
      ["document", "contains", "media"],
      ["document", "related_product", "document"]
    ],
    "rel_weights": {
      "contains": 1.0,
      "appears_in": 0.9,
      "references": 0.85,
      "mentions": 0.8,
      "related_product": 0.95
    },
    "cold_start": {
      "llm_spo_on_ingest": true,
      "spo_batch_size": 10
    }
  }
}
```

**增补 MP4 示例**（社交证明参与 PPR 时追加一项，非默认）：

```json
["document", "contains", "evaluate"]
```

---

## 十三、落地状态与运维约定（2026-06-25 起）

> Niushop 商品库知识库 + 知识网已按 **M1.5 + M1.6 + M2-1** 交付；**无需清空重建**。本节记录验收规模与后续 M2-2/M2-3 的增量策略。

### 13.1 当前 `beauty` 类目规模（参考快照）

> **说明**：下表为 **2026-06-25 左右验收快照**；经 `sub_category` backfill、VLM 补描述等运维后数字会变。以 `python -m com.yanpanji.agents.cli health` 与边表聚合为准，**非实时 KPI**。


| 层               | 内容                                       | 数量（约）           |
| --------------- | ---------------------------------------- | --------------- |
| **向量库（Chroma）** | BGE-M3 语义检索入口                            | **419** chunks  |
| **商品**          | `_meta` + `section` 分段                   | 39 商品 + 20 段    |
| **文章 / 评价**     | 文章 meta+段、评价                             | 9 + 34 段 + 2    |
| **全局媒体**        | `knowledge_media_{hash}`（URL 去重）         | **303**         |
| **知识网节点**       | `knowledge_media_nodes`                  | **303**         |
| **知识网边**        | `contains` / `appears_in` / `references` | 444 / 342 / 187 |


### 13.2 知识网结构（与 §4、§五之二 一致）

```text
Document（goods_59 …）
  ├─ contains → section（niushop_product_59_s0 …）  → Chroma 向量
  ├─ contains → media（media_{url_hash}）          → 全局 knowledge_media chunk
  ├─ section ─ references ─► media
  └─ media ─ appears_in ─► 其它 document（同图多品联结）

检索：用户问题 → BGE ANN 种子 → PPR 沿边扩散（ann_then_ppr）→ Analyze 上下文
```

### 13.3 日常维护命令

```powershell
cd C:\work\projects\wchat\salesagent
$env:PYTHONPATH="src"
python -m com.yanpanji.agents.cli sync_niushop          # 增量：仅 modify_time 变更的文档
python -m com.yanpanji.agents.cli health              # mysql / chroma / last_sync
python scripts\verify_m21.py                          # PPR 检索 + live analyze 抽测
# M2-2 媒体识图：谛听「元宝识图补描述」技能（非 cli vlm_enrich — Qwen 已硬拦截）
python -m com.yanpanji.agents.cli spo_enrich --category beauty   # M2-3 存量补边
```

`**--force` 仅用于**：schema 迁移、清理 M1 遗留 per-doc media、首次全量重建。会全库重 embed，**正常运营不要用**。

### 13.4 M2-2 / M2-3：增量增强，非清空重同步

**原则**：在现有知识网基础上 **增补** 信息点与边，Analyze 的 PPR 检索会自然吃到增强，**无需推倒重来**。


| 阶段                  | 做什么                                                                                       | 对现有数据的影响                                                                        |
| ------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| **M2-2 媒体描述**       | 对尚无 `vlm_description` 的媒体节点补描述，更新 `knowledge_media_{hash}` chunk                          | **UPDATE**；**当前方案**：[元宝桌面技能](M2-2-媒体图片描述-元宝技能方案.md)；原 Qwen2-VL CPU 已暂停          |
| **M2-3 LLM SPO**    | 从 **Niushop 存量** section 抽 `concept` 节点，写 `mentions` 等新边；新 chunk 入库即 BGE embed（inductive） | **ADD** 节点和边；**P1 基本盘**（见 §14.9）                                                |
| **M3-S1 元宝文档技能**    | 用户上传 PDF/Word 等 → **本机元宝** 产出挂接 JSON → 服务端 BGE 对齐 + 写 `related_product` / `mentions`      | **ADD** 边；**知识增补 · 基本盘验收后**；[M3-1 方案](M3-1-元宝文档挂接知识网技能方案.md)                    |
| **Niushop 增量 sync** | 仅 `modify_time` 变更的商品/文章                                                                  | 只对该 `doc_id` 执行 `delete_edges_for_document` 后重建边；其它文档、媒体节点、VLM 描述、SPO 边**不受影响** |


与 §12.6 冷启动优先级一致（**排期定稿见 §14.9**：先基本盘成网，后 M3-S1 增补）：

1. 新 `section` → 先 **BGE embed**，无边也能 ANN 检索
2. **Niushop 存量 section** → **M2-3 SPO** 补 `concept` / `mentions`（基本盘）
3. **用户上传文档** → **基本盘验收后** 再 **M3-S1 元宝技能** 写 `related_product` / `mentions`（知识增补）
4. 新图片仅 URL → **元宝识图**（⚠️ 非 Qwen CLI）+ sync 时自动 `appears_in`

**仅以下情况会大范围动刀**（与 M2-2/3 无关）：

- 手动 `sync_niushop --force`（全量重 embed + 清遗留 chunk）  
- 商城后台修改某商品 → 仅该商品边/chunk 局部更新  
- 未来改 `chunk_id` 规则或图 schema 版本 → 需一次性迁移脚本

---

## 十四、生物-数字双模态映射模型（v1.3 定稿）

> **定位**：本章是产品、研发、Skills 对外沟通的**统一语言**；§十二给出工程路线，§4.4 给出表结构，本章给出**为何这样设计**以及**传导机制**的生物学对照。  
> **原则**：**先完善文档与基本盘成网，再编码扩展**；M3-S1 上传挂接属于「知识增补」，排在基本盘之后（§14.9）。  
> **关联**：[M3-S2 开发纪要](M3-S2-知识图谱Tab开发纪要-2026-06.md) · [开发计划 §14.3.1](开发计划-总览.md)

### 14.1 为什么需要「双模态」

知识库若只有向量切片列表，运营看到的是**孤立条目**；若只有文档内树，看到的是**孤立森林**。  
产品愿景是 **Skills + LLM 的「千手观音」**：一次感知，多手输出（识图、荐品、答段落、挂资料…）——这要求底层是**可传导、可扩散、可解释**的网络，而非静态文件夹。

**生物学隐喻**不是装饰，而是三套对齐：


| 模态        | 回答的问题                               |
| --------- | ----------------------------------- |
| **生物**    | 神经系统如何感知、整合、传导、可塑？                  |
| **数字（图）** | 节点、边、权重、meta-path 如何存？              |
| **工程**    | SQLite + Chroma + PPR 如何实现？何时上 GNN？ |


---

### 14.2 核心映射：神经元结构 → 知识图谱组件

```text
                    ┌── 树突（入边 / Dendrites）
                    │    contains◄─ document
                    │    references◄─ section
                    │    appears_in◄─ media（其它文档）
                    ▼
              ┌─────────────┐
              │  细胞体      │  ← 节点实体（node_type + node_id）
              │  ┌───────┐  │
              │  │ 细胞核 │  │  ← 属性 + BGE Embedding + VLM 描述
              │  └───────┘  │
              └──────┬──────┘
                     │ 轴突（出边 / Axon）
                     ▼
              突触（Synapse）→ 下游节点
              强度 = props_json.weight / rel_weights
```


| 生物学结构           | 知识图谱组件     | 本项目实例                                                                    |
| --------------- | ---------- | ------------------------------------------------------------------------ |
| **神经元细胞体**      | 节点实体       | `document` / `section` / `media` / `evaluate` / `concept`                |
| **细胞核**         | 核心属性 + 向量  | Chroma `documents`、metadata、`vlm_description`、`use_cases`、`sub_category` |
| **树突（入边）**      | 上游指向本节点的边  | `contains`←文档、`references`←段落、`appears_in`←媒体、`mentions`←概念              |
| **轴突 / 突触（出边）** | 本节点向外传导的边  | 同上，方向相反；`props_json` 可存置信度、上下文                                           |
| **髓鞘 / 兰氏结**    | 远距离高效传导的枢纽 | **共享 `media_node`**（`appears_in` 多文档）、未来 `**concept` Hub**               |
| **动作电位**        | 一次查询激活     | 用户问句 → BGE ANN 种子集合 `S`                                                  |
| **突触可塑性**       | 边权随使用演化    | `rel_weights` + 查询日志反馈（§14.6，L3）                                         |


**子类（`sub_category`）**：当前为 Chroma **元数据分组**，类比「皮层功能分区」，尚未写入 `knowledge_edges`；成网后可通过 `concept` 节点间接联结，不必硬造「子类→文档」边。

---

### 14.3 知识点如何「通信」：消息传递与聚合

图神经网络（GNN）的 **Message Passing** 循环，在本项目中**分阶段落地**，不必一步上全量 GNN。

#### 14.3.1 生物学 / GNN 抽象（目标态）


| 步骤  | GNN 术语         | 生物学类比                       |
| --- | -------------- | --------------------------- |
| 1   | **Message 编码** | 激活节点沿轴突向邻居发送状态向量            |
| 2   | **Aggregate**  | 树突汇聚多路上游信号（可用 Attention 加权） |
| 3   | **Update**     | 细胞体融合自身核信息与邻居信号（MLP）        |
| 4   | **多轮迭代**       | 深度推理                        |
| 5   | **Readout**    | 得分最高的节点/路径 = 答案或荐品          |


#### 14.3.2 工程映射（当前 → 远期）


| GNN 概念        | 本项目实现                              | 状态                                               |
| ------------- | ---------------------------------- | ------------------------------------------------ |
| 种子激活          | `query` → BGE → Chroma top-K       | ✅                                                |
| 1-hop 聚合      | `GET /graph/neighbors`、subtree API | ✅                                                |
| 异构边类型         | `rel_type` + `rel_weights`（§12.3）  | ✅                                                |
| 多跳扩散          | PPR / RWR（`ann_then_ppr`）          | ✅ 已接入 analyze；**全图无向 PPR**，meta-path 掩码 **L2-c** |
| Attention 聚合  | 按 `rel_type` / meta-path 掩码加权      | ⏳ L2-c（配置在 §12.8，代码未接）                           |
| 可学习 Attention | R-GCN / HAN                        | **远期可选**（L3）                                     |
| 多轮 MLP Update | 端到端 GNN 训练                         | **远期可选**（L3）                                     |


**结论**：冷启动阶段 **BGE 种子 + 加权 PPR** 即「无需训练的 Message Passing」；全量 GNN 是 L3 增强，不是基本盘前置。

#### 14.3.3 统一接口契约（Skills / Analyze 消费）

算法可替换，接口保持稳定：

```text
activate(query, category) → seeds[]           # 动作电位：ANN 激活
propagate(seeds, meta_paths[], hop_budget, edge_weights) → subgraph  # 轴突传导
package(subgraph, max_tokens) → prompt_context  # 细胞体群读出
llm.infer(context, user_message) → skill_output
```

实现体可以是 `ppr_expand()`，未来可换 `gnn_forward()`，**Skills 侧不感知**。

---

### 14.4 三层传导架构（L1 / L2 / L3）


| 层          | 生物隐喻         | 工程内容                                                     | 排期属性           |
| ---------- | ------------ | -------------------------------------------------------- | -------------- |
| **L1 解剖层** | 神经元形态、突触连接   | `node_type`、`rel_type`、细胞核向量与属性、图谱 UI 点网可视化              | ✅ 已有           |
| **L2 传导层** | 动作电位、髓鞘跳跃    | `appears_in` 跨文档展示、M2-3 补 `mentions`、meta-path PPR、子类级子图 | **基本盘 · 当前重点** |
| **L3 可塑层** | 赫布学习、突触强化/退化 | 边权随查询反馈更新、低置信剪枝、可选 R-GCN                                 | 基本盘验收后         |


```text
L1 建骨 → L2 通电成网 → L3 学会哪条路更好走
```

---

### 14.5 边类型的生物别名（产品 / UI 图例）


| `rel_type`        | 生物别名   | 传导语义      | 默认权重（§12.3） |
| ----------------- | ------ | --------- | ----------- |
| `contains`        | 胞体-胞突  | 文档拥有段落/媒体 | 1.0         |
| `references`      | 短程突触   | 段落邻近配图    | 0.85        |
| `appears_in`      | 长程髓鞘突触 | 同图跨文档联结   | 0.9         |
| `mentions`        | 语义树突簇  | 段落提及概念    | 0.8         |
| `related_product` | 挂接轴突   | 资料/文章指向商品 | 0.95        |
| `same_as`         | 等价突触   | 同图异 URL   | 等价跳转        |


知识图谱 Tab 图例与关系图检索 UI **应使用此对照**，便于运营理解「为何虚线、为何跨列」。

---

### 14.6 结构优化：突触可塑性（L3）

**赫布法则**：频繁一起激活的突触变强。


| 机制          | 图实现                                          | 数据来源                 |
| ----------- | -------------------------------------------- | -------------------- |
| **强化**      | `props_json.weight ↑`                        | 关系图检索命中路径、analyze 采纳 |
| **退化 / 剪枝** | 权重低于阈值则软隐藏；`staleness` 高则标记待清理              | 长期未命中、低置信 SPO 边、目标节点失效 |
| **髓鞘增厚**    | Hub 节点（高度 `appears_in`、concept 度）提高 PPR 停留概率 | 图统计 + 配置             |

**`props_json` 预留字段**（先写入结构、L3 再消费；与 M2-3-6 低置信复核同一治理链路）：

| 字段 | 含义 |
|------|------|
| `weight` | 边权（已有；`rel_weights` 默认） |
| `last_traversal_at` | 最后一次被 PPR / 检索命中 |
| `traversal_count` | 累计遍历次数 |
| `staleness` | 0–1 新鲜度分（越高越「坏死」） |
| `auto_seeded` | 规则播种边（§12.6 L1） |
| `pruned` | 软删除标记，便于回滚 |

**突触修剪（staleness_score，L3）**：综合「距上次 PPR 命中天数」「边龄」「目标节点是否仍有效（商品下架/文档删除）」计算腐烂分。`staleness > 阈值` 的边在图谱 Tab **红色半透明**展示，提示运营「待清理或需重跑元宝识图」；高于软删阈值则写 `pruned=true`（周批任务，远期）。

存储：沿用 `knowledge_edges.props_json`，无需改 schema；与 M2-3-6「低置信边复核」、未来 `ingest_log` 同一治理链路。

**多模态突触**：除文本边外，媒体节点已有 `vlm_description` / `use_cases`；未来 CLIP 副向量可作为「平行轴突」，带宽高于纯文本（§12.1 ④）。

---

### 14.7 算法优化：异构图与高阶通信（meta-path）

#### 14.7.1 异构感知（Heterogeneous-aware）

节点类型与边类型**不可拍平**为同质 `related`（§12.3 已定）。  
扩散时按 `(src_type, rel_type, dst_type)` 分块转移，等价于「不同类型神经元不同激活函数」——工程上即 **R-GCN 的简化静态版**（权重表而非可学习矩阵）。

#### 14.7.2 元路径（Meta-path）= 高阶突触序列

两节点无直连，但通过中间节点强关联：


| 模板      | 路径                           | 业务语义     | beauty 数据                | PPR `meta_paths` 白名单（§12.3.1） |
| ------- | ---------------------------- | -------- | ------------------------ | ------------------------------ |
| **MP1** | concept → section ← document | 问功效命中多商品 | ✅ M2-3（mentions ~399） | ✅ 部分（`mentions` + `contains`）；概念跨品需 `ppr_concept_bridge` 或关 L2-c |
| **MP2** | section → media → document   | 配图反查商品   | ✅ 部分                     | ✅ 第 1 条 |
| **MP3** | article → goods              | 文章带货     | ✅ M2-3-2（related_product ~9） | ✅ 第 6 条 |
| **MP4** | goods → evaluate             | 社交证明     | ✅ 图谱可浏览                 | ❌ **未列入**；PPR 不沿评价扩散；需时增补 §12.8 |
| **MP5** | media → doc₁ … docₙ          | 同图多品     | ✅ **342 条 `appears_in`** | ✅ 第 2 条 |


**成网基本盘验收**：至少 **MP2 + MP5** 在图谱 UI 可遍历；MP1 在 M2-3 后可见。

配置：`server.json#knowledge_graph.meta_paths`（§12.8）；PPR 仅沿白名单游走；**六条业务含义见 §12.3.1**。

---

### 14.8 与 Skills「千手观音」的关系

```text
        知识网（联合皮层连接组）
              │
    ┌─────────┼─────────┐
    ▼         ▼         ▼
 识图 Skill  检索 Skill  挂接 Skill …   ← 多只「手」
    │         │         │
    └─────────┴─────────┘
              │
         LLM 工作记忆
    （读 propagate 后的子图上下文）
```

- 每只 Skill 是一种**输出模式**，不应各自拼假图。  
- 统一走 §14.3.3 契约：`activate → propagate → package → llm`。  
- **M3-S1 上传挂接** = 向网络**新增突触**（增补），不是**替网**；须在网络已可辨后再做（§14.9）。

---

### 14.9 基本盘 vs 知识增补：实施顺序（2026-06-29 定稿）


| 类别       | 定义                       | 代表工作                                            | 相对顺序       |
| -------- | ------------------------ | ----------------------------------------------- | ---------- |
| **基本盘**  | Niushop 存量成网：可浏览、可扩散、可解释 | `appears_in` UI、M2-3 补 `mentions`、MP 可视化、PPR 接入 | **先做**     |
| **知识增补** | 用户新上传资料挂接到已有网            | M3-S1 `related_product` / 新 `mentions`          | **基本盘验收后** |


**推荐步骤**：

1. **L2-a**：subtree / neighborhood API 展示 `appears_in`；图谱 Tab 跨文档列与开关
2. **L2-b**：M2-3-1 + M2-3-3 夜间存量 SPO → `concept` + `mentions`
3. **L2-c**：M2-3-2 / M2-3-4 文章内链 + 同 concept 跨商品；子类级子图 API
4. **L2 验收**：beauty 上 MP2、MP5（+ MP1）可视觉遍历；analyze 可走 `ann_then_ppr`
5. **L3 / 增补**：突触可塑性日志；**M3-S1** 上传挂接

---

### 14.10 基本盘验收标准（建议 · 分两档）

**L2-a 验收（不依赖 LLM，优先交付）**


| #   | 验收项     | 可观测结果                                                      |
| --- | ------- | ---------------------------------------------------------- |
| A1  | 跨文档边可见  | 任选一 `media`，图谱展示 ≥2 个 `appears_in` 目标文档（MP5）               |
| A2  | UI 非孤立树 | 知识图谱 Tab 可开关「跨文档关联」；点网可收拢；**远期** Hub 默认视图 + 边粗细映射 `rel_weight`（见 §14.13） |
| A3  | 文档一致    | 本章、§十二、§十三.4、[M3-S2 纪要](M3-S2-知识图谱Tab开发纪要-2026-06.md) 排期一致 |


**L2-b 验收（M2-3 完成后）**


| #   | 验收项     | 可观测结果                                              |
| --- | ------- | -------------------------------------------------- |
| B1  | 概念边可见   | 任一商品详情 section 有 ≥3 条 `mentions` 或登记 `skip_reason` |
| B2  | 扩散可执行   | `verify_m21.py` 通过；analyze 上下文含跨文档 `mini_path`     |
| B3  | MP1 可遍历 | 图谱或 neighbors 可沿 concept 触及多商品（M2-3-4）             |


> **说明**：L2-a 不要求 `mentions` 边存在。  
> **2026-07-02 脚本复验**：`verify_m23.py` ✅（mentions=1273，129 section，MP1 pass）；`verify_m21.py` ✅；`verify_m31.py` ✅（M3-S1 已交付）。**L2-b 主体通过**。  
> **下一产品迭代**：**M6-1 美妆客服智能体**（[方案](M6-1-美妆客服智能体-渐进式对话方案.md)）；M5-1 主动学习暂缓。

---

### 14.11 与现有章节索引


| 主题                                         | 见                                          |
| ------------------------------------------ | ------------------------------------------ |
| 表结构、`node_type` / `rel_type`、双边冗余          | §4.3–4.4                                   |
| PPR 公式、`rel_weights`、meta-path 配置与**实现差距** | §12.3–12.4                                 |
| 运维规模、M2-3 / M3-S1 增量策略                     | §13                                        |
| 客户端图谱 Tab 迭代记录                             | [M3-S2 开发纪要](M3-S2-知识图谱Tab开发纪要-2026-06.md) |


### 14.12 文档与代码对齐说明（2026-06-15）


| 项               | 文档描述              | 代码现状                     | 计划       |
| --------------- | ----------------- | ------------------------ | -------- |
| `meta_paths` 掩码 | §12.3.1 / §12.8 六条 | ✅ `graph_retrieval._load_adjacency`；MP4 未列入 | 按需增补 MP4 |
| PPR 有向性         | `appears_in` 单向语义 | ✅ `ppr.directed: true`；`verify_m21` | — |
| `mentions` 边    | §4.4 / MP1        | beauty **~399**             | ✅ M2-3   |
| `related_product` | MP3              | beauty **~9**               | ✅ M2-3-2 |
| M2-2 媒体描述      | 元宝识图              | `apply_yuanbao_vlm_result` ✅ | 当前方案 |
| Qwen2-VL 本地批补  | 历史 Runbook        | `vlm.enabled=false`；`get_vlm_pipeline` 硬拦截 | ⚠️ 废弃 |
| M3-S1 上传挂接      | M3-1 方案           | ✅ E2E + linked_media | **下一迭代：M5-1 主动学习** |
| GNN / TransE    | §12.5、§14.6       | 未实现                      | **远期可选** |
| `co_viewed` 边   | §12.1 远期          | 未定义                      | **远期可选** |
| `staleness` / 突触修剪 | §14.6          | `props_json` 未写入          | L3       |

### 14.13 工程增强路线（评审摘入 · 2026-06-15）

> 来源：[知识库-结构化入库方案评估.md](知识库-结构化入库方案评估.md)。**不阻塞当前 MVP**；与 L2-c / L3 排期对齐。

| 主题 | 要点 | 阶段 |
|------|------|------|
| **PPR 有向化** | 出边邻接表；消除 `appears_in` 反向污染；与 meta-path 同级 | **L2-c** |
| **概念三级播种** | L1 规则 / L2 聚类 / L3 LLM SPO（主路径已交付） | L2-b 可选优化 |
| **`props_json` 预留** | `last_traversal_at`、`traversal_count`、`staleness` | L3 先占位 |
| **图谱 UX** | Hub 默认、1-hop 懒加载、边粗细∝`rel_weight`；收拢展开已有 | M3-S2 P2+ |
| **突触修剪** | `staleness_score` 周批；图谱红色预警「坏死突触」 | L3 |

**联动**：播种（骨架）→ 有向 PPR（干净传导）→ staleness 自检（质量监控）= L2 传导层完整闭环。

---

## 十一、变更记录


| 版本   | 日期         | 说明                                                                                   |
| ---- | ---------- | ------------------------------------------------------------------------------------ |
| **v1.10** | **2026-07-02** | **L2-b 脚本复验**（mentions 1273 / related_product 14）；**M5-1 主动学习**方案；M3-S1 标 ✅ |
| v1.9 | 2026-06-15 | **§12.3.1** 生产六条 `meta_paths` ↔ MP 业务对照；**MP4 未入 PPR 白名单**说明；§12.4/§12.8/§14.7.2/§14.12 同步 L2-c ✅ |
| v1.8 | 2026-06-15 | 评审摘入 §14.13；§12.4/§12.6/§14.6 增强；§14.12 对齐 L2-b 与 Qwen 废弃；Qwen 代码硬拦截 |
| v1.7 | 2026-06-29 | 方案评审修订：M2-3→P1 基本盘；§4.3 双边冗余；concept 规范；PPR/meta-path 实现差距；L2-a/b 验收分档；§14.12 文档代码对齐 |
| v1.6 | 2026-06-29 | **§十四 生物-数字双模态映射模型** 定稿；§一/§十二/§13.4 交叉引用与排期修订                                       |
| v1.4 | 2026-06-27 | M2-2 切换为元宝桌面技能方案；§13.4 更新                                                            |
| v1.5 | 2026-06-28 | §13.4 增补 M3-S1 元宝文档挂接技能与 M2-3 关系                                                     |
| v1.1 | 2026-06-25 | M1.5 已交付；§4.3–4.4 知识网；§五之二 M1.6 流水线                                                  |
| v1.0 | 2026-06-24 | 首版：分段优于加长、Document/Section/Media 模型                                                  |


